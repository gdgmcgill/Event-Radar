-- RECOVERED HISTORY — production version 20260316034955 ("add_banner_url_to_users"). NOT a migration: this file is documentation of what production already ran; it is never replayed, and the .sql.recovered suffix keeps it unreadable to the Supabase CLI even from the top level.

ALTER TABLE public.users ADD COLUMN banner_url text;
