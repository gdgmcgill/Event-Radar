-- RECOVERED HISTORY — production version 20260315103436 ("add_contact_email_to_clubs"). NOT a migration: this file is documentation of what production already ran; it is never replayed, and the .sql.recovered suffix keeps it unreadable to the Supabase CLI even from the top level.

ALTER TABLE clubs ADD COLUMN IF NOT EXISTS contact_email TEXT;
