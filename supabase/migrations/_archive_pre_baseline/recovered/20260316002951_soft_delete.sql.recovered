-- RECOVERED HISTORY — production version 20260316002951 ("soft_delete"). NOT a migration: this file is documentation of what production already ran; it is never replayed, and the .sql.recovered suffix keeps it unreadable to the Supabase CLI even from the top level.

ALTER TABLE events ADD COLUMN IF NOT EXISTS deleted_at TIMESTAMPTZ DEFAULT NULL;
CREATE INDEX IF NOT EXISTS idx_events_not_deleted ON events (id) WHERE deleted_at IS NULL;
