All three Supabase clients (`client.ts`, `server.ts`, `service.ts`) were already clean factory functions with no singletons or dead exports. `service.ts` is correctly limited to API routes only. `admin.ts` and `roles.ts` are distinct (server-side auth vs client-side role helpers). `upload-utils.ts` and `image-upload.ts` are distinct (proxy upload vs direct storage upload). A duplicate `isMcGillEmail` in `auth/callback/route.ts` was removed and replaced with an import from `@/lib/utils`. Unused catch bindings in `utils.ts` were tightened to bare `catch`. Lint: clean.
