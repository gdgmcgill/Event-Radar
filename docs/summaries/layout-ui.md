Deleted four unused shadcn/ui primitives (`carousel`, `select`, `dropdown-menu`, `switch`). Added `console.error` to `ErrorBoundary.componentDidCatch` so errors are never silently swallowed when no `onError` prop is provided. Removed trailing blank lines from `Footer.tsx`. All other layout components (`Header`, `SideNavBar`, `AppBreadcrumb`, `ThemeToggle`, `navItems.ts`) were already clean with no issues.
