---
phase: 01-club-infrastructure-and-team-management
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/clubs/[id]/page.tsx
  - src/components/clubs/FollowButton.tsx
  - src/app/invites/[token]/page.tsx
autonomous: false
requirements:
  - CLUB-01
  - FLLW-01
  - FLLW-02
  - FLLW-03
  - TEAM-04

must_haves:
  truths:
    - "A visitor can open /clubs/[id] and see club logo, name, description, category, Instagram link, follower count, and upcoming/past events"
    - "A logged-in student can click Follow on the public club page and the follower count increments immediately"
    - "A logged-in student can click Unfollow and the follower count decrements immediately"
    - "A user who opens /invites/[token] while logged in with the matching email is added to the club"
    - "An expired or already-used invite token shows an appropriate error"
  artifacts:
    - path: "src/app/clubs/[id]/page.tsx"
      provides: "Public club page with SSR data"
      min_lines: 80
    - path: "src/components/clubs/FollowButton.tsx"
      provides: "Follow/unfollow toggle with optimistic update"
      min_lines: 40
    - path: "src/app/invites/[token]/page.tsx"
      provides: "Invite acceptance flow"
      min_lines: 50
  key_links:
    - from: "src/app/clubs/[id]/page.tsx"
      to: "/api/clubs/[id]"
      via: "Server-side Supabase queries (SSR)"
      pattern: "supabase.*from.*clubs"
    - from: "src/components/clubs/FollowButton.tsx"
      to: "/api/clubs/[id]/follow"
      via: "fetch POST/DELETE with optimistic state"
      pattern: "fetch.*follow"
    - from: "src/app/invites/[token]/page.tsx"
      to: "club_invitations table"
      via: "Server-side token lookup and club_members insert"
      pattern: "club_invitations.*token"
---

<objective>
Build the student/visitor-facing pages: the public club profile page with follow functionality, and the invite acceptance page.

Purpose: Students can discover clubs and follow them; invited users can join clubs via link.
Output: Public club page at /clubs/[id], FollowButton component, invite acceptance at /invites/[token].
</objective>

<execution_context>
@/Users/adyanullah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adyanullah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-club-infrastructure-and-team-management/01-CONTEXT.md
@.planning/phases/01-club-infrastructure-and-team-management/01-RESEARCH.md
@.planning/phases/01-club-infrastructure-and-team-management/01-01-SUMMARY.md

<interfaces>
<!-- API contracts from Plan 01 -->

GET /api/clubs/[id] response:
```typescript
{ club: Club, followerCount: number }
```

GET /api/clubs/[id]/events response:
```typescript
{ events: (Event & { club: { id: string; name: string; logo_url: string | null } })[] }
```

GET /api/clubs/[id]/follow response:
```typescript
{ following: boolean }
```

POST /api/clubs/[id]/follow — returns 201
DELETE /api/clubs/[id]/follow — returns 200

From src/types/index.ts:
```typescript
export interface Club {
  id: string;
  name: string;
  description: string | null;
  category?: string | null;
  instagram_handle: string | null;
  logo_url: string | null;
  status?: "pending" | "approved" | "rejected";
  created_by?: string | null;
  created_at: string;
  updated_at: string;
}

export interface ClubInvitation {
  id: string;
  club_id: string;
  inviter_id: string;
  invitee_email: string;
  token: string;
  expires_at: string;
  accepted_at: string | null;
  created_at: string;
}

export interface Event {
  id: string;
  title: string;
  description: string;
  event_date: string;
  event_time: string;
  location: string;
  club_id: string;
  tags: EventTag[];
  image_url: string | null;
  // ...
}
```

From src/hooks/useClubs.ts:
```typescript
export function useFollowStatus(clubId: string): SWRResponse<{ following: boolean }>
```

From src/components/ui/EmptyState.tsx — available for empty states
From src/components/layout/AppBreadcrumb.tsx — available for navigation breadcrumbs

Existing EventCard component at src/components/events/EventCard.tsx — use for displaying events on public club page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public club page + FollowButton component</name>
  <files>
    src/app/clubs/[id]/page.tsx,
    src/components/clubs/FollowButton.tsx
  </files>
  <action>
  **FollowButton (src/components/clubs/FollowButton.tsx):**
  Client component. Props: `{ clubId: string; initialFollowing: boolean; initialCount: number }`.
  - Local state: `isFollowing` (boolean), `followerCount` (number), `isLoading` (boolean).
  - Initialize from props.
  - `handleToggle`: Set isLoading true. Optimistically update isFollowing and followerCount. Call `fetch(/api/clubs/${clubId}/follow, { method: isFollowing ? "DELETE" : "POST" })`. On error, revert state. Set isLoading false.
  - Render: Heart icon (Lucide `Heart`) — filled red when following, outline when not. Show follower count next to it. Disable button while isLoading.
  - Also show text "Follow" / "Following" next to the icon.
  - If user is not logged in (check via useAuthStore), clicking should open sign-in modal or redirect. Use the existing auth flow pattern.
  - Use `cursor-pointer`, smooth transitions per UI/UX skill guidelines.

  **Public Club Page (src/app/clubs/[id]/page.tsx):**
  Server component with async data fetching. Replace the placeholder from Plan 01.
  - Await params: `const { id } = await params;`
  - Use `createClient()` from `@/lib/supabase/server`.
  - Parallel fetch: club details, follower count, club events (all three queries in Promise.all).
  - Split events into upcoming (event_date >= today) and past (event_date < today). Sort upcoming ascending, past descending.
  - Check if current user is following (if logged in): query club_followers for user's row.
  - If club not found, call `notFound()` from next/navigation.
  - Layout (use Tailwind + shadcn Card):
    - Header section: Club logo (or placeholder icon if no logo), club name (h1), category badge, description paragraph.
    - Stats row: Follower count with FollowButton, Instagram link (external link icon, opens in new tab).
    - Upcoming Events section: Grid of EventCard components. If empty, show EmptyState with "No upcoming events" message.
    - Past Events section: Collapsible or separate section. Grid of EventCard components. If empty, omit section entirely.
  - McGill branding: Use Burgundy (#561c24) for headings, Red (#ED1B2F) for accent/follow button.
  - Responsive: Single column on mobile, wider layout on desktop.
  - Add proper meta title using `export function generateMetadata()` for SEO.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Public club page at /clubs/[id] renders club info, follower count, follow button, upcoming and past events. FollowButton toggles with optimistic updates. Page is server-rendered with proper SEO metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Invite acceptance page</name>
  <files>src/app/invites/[token]/page.tsx</files>
  <action>
  Server component. Replace placeholder from Plan 01.
  - Await params: `const { token } = await params;`
  - Use `createClient()` from `@/lib/supabase/server`.
  - Get current user via `supabase.auth.getUser()`. If not logged in, middleware should have redirected, but as defense-in-depth, redirect to `/?signin=required&next=/invites/${token}`.
  - Look up invitation: `.from("club_invitations").select("*, club:clubs(id, name, logo_url)").eq("token", token).is("accepted_at", null).single()`.
  - Validation checks (show appropriate error UI for each):
    1. Token not found or already accepted: "This invitation is no longer valid"
    2. Token expired (`expires_at < now()`): "This invitation has expired"
    3. User email doesn't match `invitee_email` (case-insensitive compare): "This invitation was sent to a different email address"
    4. User is already a member of this club: "You are already a member of this club"
  - If valid:
    - Show club logo, name, and message "You've been invited to join [club name] as an organizer"
    - Auto-accept: Insert into `club_members` with `user_id: user.id, club_id: invite.club_id, role: "organizer"`. Update `club_invitations` set `accepted_at: new Date().toISOString()` where `id = invite.id`.
    - Show success message with a link to `/my-clubs/${invite.club_id}`.
  - If any step fails, show error with a "Go Home" link.
  - Use shadcn Card for the centered invite card layout. Keep it clean and simple.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Invite acceptance page validates token, checks email match, auto-adds user to club on valid invite, marks invite as accepted, shows success with link to club dashboard. Error states handled for expired, invalid, wrong-email, and already-member cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify public club page and invite flow</name>
  <what-built>Public club page with follow functionality and invite acceptance flow</what-built>
  <how-to-verify>
    1. Navigate to /clubs/[id] for an existing club — verify logo, name, description, category, follower count, and events are displayed
    2. Click Follow — verify count increments and button shows "Following"
    3. Click Unfollow — verify count decrements and button reverts
    4. Check responsive layout on mobile viewport (375px width)
    5. If possible, test invite flow: create an invite via API, then open /invites/[token] while logged in
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` completes without errors
3. Public club page renders with real data
4. Follow/unfollow updates count optimistically
5. Invite acceptance creates club_members row
</verification>

<success_criteria>
- Public club page shows all required info: logo, name, description, category, Instagram, follower count, upcoming/past events
- FollowButton works with optimistic updates and handles auth state
- Invite acceptance validates all edge cases (expired, wrong email, already member)
- All pages are responsive and use McGill branding
</success_criteria>

<output>
After completion, create `.planning/phases/01-club-infrastructure-and-team-management/01-02-SUMMARY.md`
</output>
