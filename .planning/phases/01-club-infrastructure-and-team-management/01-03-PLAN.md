---
phase: 01-club-infrastructure-and-team-management
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/my-clubs/page.tsx
  - src/app/my-clubs/[id]/page.tsx
  - src/components/clubs/ClubDashboard.tsx
  - src/components/clubs/ClubOverviewTab.tsx
  - src/components/clubs/ClubSettingsTab.tsx
  - src/components/clubs/ClubMembersTab.tsx
  - src/components/clubs/ClubQuickSwitch.tsx
  - src/components/clubs/ClubCard.tsx
  - src/components/layout/SideNavBar.tsx
autonomous: false
requirements:
  - CLUB-02
  - CLUB-03
  - CLUB-04
  - TEAM-01
  - TEAM-02
  - TEAM-03

must_haves:
  truths:
    - "Organizer can view My Clubs list showing all clubs they own or organize with name, logo, role, and member count"
    - "Single-club organizer is redirected directly to their club dashboard, skipping the list"
    - "Club owner can edit club name, description, category, Instagram link from a settings tab, and changes persist"
    - "Quick-switch dropdown in dashboard header shows logo + name for each club, switching always lands on Overview tab"
    - "Club owner can view members list with role and joined date"
    - "Club owner can invite a McGill email to join as organizer"
    - "Club owner can remove an organizer (but not themselves)"
  artifacts:
    - path: "src/app/my-clubs/page.tsx"
      provides: "Club list page with single-club redirect"
      min_lines: 40
    - path: "src/app/my-clubs/[id]/page.tsx"
      provides: "Club management dashboard page shell"
      min_lines: 20
    - path: "src/components/clubs/ClubDashboard.tsx"
      provides: "Tabbed dashboard container with quick-switch header"
      min_lines: 60
    - path: "src/components/clubs/ClubOverviewTab.tsx"
      provides: "Club details display"
      min_lines: 40
    - path: "src/components/clubs/ClubSettingsTab.tsx"
      provides: "Club edit form"
      min_lines: 60
    - path: "src/components/clubs/ClubMembersTab.tsx"
      provides: "Member list + invite form + remove action"
      min_lines: 80
    - path: "src/components/clubs/ClubQuickSwitch.tsx"
      provides: "Dropdown for switching between clubs"
      min_lines: 30
    - path: "src/components/clubs/ClubCard.tsx"
      provides: "Card for my-clubs list"
      min_lines: 20
  key_links:
    - from: "src/components/clubs/ClubDashboard.tsx"
      to: "src/hooks/useClubs.ts"
      via: "useClub(clubId) SWR hook"
      pattern: "useClub"
    - from: "src/components/clubs/ClubSettingsTab.tsx"
      to: "/api/clubs/[id]"
      via: "fetch PATCH for club update"
      pattern: "fetch.*PATCH"
    - from: "src/components/clubs/ClubMembersTab.tsx"
      to: "/api/clubs/[id]/members"
      via: "useClubMembers hook + DELETE fetch"
      pattern: "useClubMembers|fetch.*DELETE"
    - from: "src/components/clubs/ClubMembersTab.tsx"
      to: "/api/clubs/[id]/invites"
      via: "fetch POST for invite creation"
      pattern: "fetch.*invites.*POST"
    - from: "src/components/clubs/ClubQuickSwitch.tsx"
      to: "src/hooks/useClubs.ts"
      via: "useMyClubs() SWR hook"
      pattern: "useMyClubs"
    - from: "src/app/my-clubs/page.tsx"
      to: "club_members table"
      via: "Server-side single-club redirect check"
      pattern: "redirect.*my-clubs"
---

<objective>
Build the organizer-facing pages: My Clubs list with single-club redirect, club management dashboard with Overview, Settings, and Members tabs, plus the quick-switch dropdown.

Purpose: Organizers can manage their clubs end-to-end -- view details, edit settings, manage team members, and switch between clubs.
Output: Complete organizer management experience at /my-clubs and /my-clubs/[id].
</objective>

<execution_context>
@/Users/adyanullah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adyanullah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-club-infrastructure-and-team-management/01-CONTEXT.md
@.planning/phases/01-club-infrastructure-and-team-management/01-RESEARCH.md
@.planning/phases/01-club-infrastructure-and-team-management/01-01-SUMMARY.md

<interfaces>
<!-- API contracts from Plan 01 -->

GET /api/my-clubs response:
```typescript
{ clubs: Array<{
  id: string;
  role: "owner" | "organizer";
  created_at: string;
  club: Club;
  memberCount: number;
}> }
```

GET /api/clubs/[id] response:
```typescript
{ club: Club, followerCount: number }
```

PATCH /api/clubs/[id] request body:
```typescript
{ name?: string; description?: string; category?: string; instagram_handle?: string; logo_url?: string; }
```

PATCH /api/clubs/[id] response:
```typescript
{ club: Club }
```

GET /api/clubs/[id]/members response:
```typescript
{ members: Array<{
  id: string;
  user_id: string;
  role: "owner" | "organizer";
  created_at: string;
  users: { id: string; email: string; name: string | null; avatar_url: string | null; }
}> }
```

DELETE /api/clubs/[id]/members request body:
```typescript
{ memberId: string }
```

POST /api/clubs/[id]/invites request body:
```typescript
{ email: string }
```

POST /api/clubs/[id]/invites response:
```typescript
{ invite: { token: string } }
```

From src/hooks/useClubs.ts:
```typescript
export function useClub(clubId: string): SWRResponse
export function useClubMembers(clubId: string): SWRResponse
export function useMyClubs(): SWRResponse
```

From src/lib/utils.ts:
```typescript
export function isMcGillEmail(email: string): boolean
```

Available shadcn/ui components: Card, Badge, Button, Input, Dialog, Tabs, TabsList, TabsTrigger, TabsContent, Skeleton, Avatar, DropdownMenu
Available: EmptyState component, AppBreadcrumb component, Lucide icons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: My Clubs list page + ClubCard + SideNavBar update</name>
  <files>
    src/app/my-clubs/page.tsx,
    src/components/clubs/ClubCard.tsx,
    src/components/layout/SideNavBar.tsx
  </files>
  <action>
  **My Clubs List Page (src/app/my-clubs/page.tsx):**
  Server component. Replace the placeholder from Plan 01.
  - Await `createClient()` from server. Get user via `supabase.auth.getUser()`.
  - Fetch memberships: `.from("club_members").select("club_id").eq("user_id", user.id)`.
  - **Single-club redirect (LOCKED DECISION):** If exactly 1 membership, `redirect(/my-clubs/${memberships[0].club_id})`. User should NEVER see the list page if they have exactly 1 club.
  - If 0 clubs: render EmptyState with message "You're not part of any clubs yet" and possibly a link to browse clubs.
  - If 2+ clubs: render a client component `MyClubsList` that uses the `useMyClubs()` SWR hook to fetch full club data with stats. Display a grid of ClubCard components.
  - Page title: "My Clubs".

  **ClubCard (src/components/clubs/ClubCard.tsx):**
  Client component. Props: `{ club: Club; role: "owner" | "organizer"; memberCount: number }`.
  - Render as a shadcn Card with:
    - Club logo (or Users icon placeholder if no logo) — 48x48 rounded
    - Club name (bold)
    - Role badge: "Owner" (Burgundy) or "Organizer" (Sage)
    - Member count: "{N} members"
  - Entire card is a Link to `/my-clubs/${club.id}`
  - Hover state: subtle shadow elevation, cursor-pointer
  - Responsive: 1 col mobile, 2 cols md, 3 cols lg

  **SideNavBar update (src/components/layout/SideNavBar.tsx):**
  - Add "My Clubs" navigation item. Check if there's a navItems config file (src/components/layout/navItems.ts mentioned in research). Add entry with icon `Users` from Lucide, label "My Clubs", href "/my-clubs". Place it logically near other organizer items.
  - Only show "My Clubs" to authenticated users (check existing auth-gating pattern in the nav).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>My Clubs page redirects single-club organizers, shows list for multi-club organizers with ClubCard grid, shows empty state for non-members. SideNavBar includes "My Clubs" link for authenticated users.</done>
</task>

<task type="auto">
  <name>Task 2: Club dashboard with Overview, Settings, Members tabs + QuickSwitch</name>
  <files>
    src/app/my-clubs/[id]/page.tsx,
    src/components/clubs/ClubDashboard.tsx,
    src/components/clubs/ClubOverviewTab.tsx,
    src/components/clubs/ClubSettingsTab.tsx,
    src/components/clubs/ClubMembersTab.tsx,
    src/components/clubs/ClubQuickSwitch.tsx
  </files>
  <action>
  **Dashboard Page (src/app/my-clubs/[id]/page.tsx):**
  Client component page (needs interactivity for tabs, forms, etc.). Replace placeholder.
  - Extract clubId from URL params.
  - Render `<ClubDashboard clubId={clubId} />`.

  **ClubQuickSwitch (src/components/clubs/ClubQuickSwitch.tsx):**
  Client component. Props: `{ currentClubId: string }`.
  - Uses `useMyClubs()` hook to get all user's clubs.
  - If only 1 club, don't render the dropdown (just show club name).
  - If 2+ clubs: shadcn DropdownMenu with trigger showing current club logo (16x16) + name. Dropdown items show logo + name for each club. Clicking navigates to `/my-clubs/${clubId}` (always Overview tab per LOCKED DECISION). Use `router.push()`.
  - **LOCKED DECISION:** No role badge in dropdown, no "View all clubs" footer link.

  **ClubDashboard (src/components/clubs/ClubDashboard.tsx):**
  Client component. Props: `{ clubId: string }`.
  - Uses `useClub(clubId)` hook. Show Skeleton loading state while fetching.
  - Header area: ClubQuickSwitch on the left, club name as page title.
  - Use AppBreadcrumb: My Clubs > [Club Name].
  - shadcn Tabs with three tabs: "Overview", "Settings", "Members".
  - Tab content renders the corresponding tab component.
  - **Settings and Members tabs only visible to owners** — check the user's role from useMyClubs() data or fetch membership. Organizers see Overview only.
  - Pass club data and mutate functions down to tab components.

  **ClubOverviewTab (src/components/clubs/ClubOverviewTab.tsx):**
  Client component. Props: `{ club: Club; followerCount: number }`.
  - Display club details in a clean card layout:
    - Logo (large, 96x96, rounded) with fallback icon
    - Name, description, category badge, Instagram link (external)
    - Stats row: follower count, member count (if available)
  - Read-only view. "Edit" link/button that switches to Settings tab for owners.

  **ClubSettingsTab (src/components/clubs/ClubSettingsTab.tsx):**
  Client component. Props: `{ club: Club; onUpdate: (club: Club) => void }`.
  - Form with fields: name (required, 3-50 chars), description (textarea, optional), category (select from existing categories or free text), instagram_handle (text, optional, strip @ prefix if provided).
  - Logo upload: Show current logo with "Change" button. Use file input, validate image type and <2MB size. Upload via `POST /api/clubs/logo` with FormData, then include returned URL in the PATCH.
  - Submit: `fetch(PATCH /api/clubs/${club.id}, { body: formData })`. On success, call `onUpdate` with new club data and call `mutate` on the SWR cache. Show success toast (use sonner if available, otherwise a simple success message).
  - On error, show error message near the form. Don't lose form state on error.
  - Disable submit button while saving (loading-buttons UX rule).

  **ClubMembersTab (src/components/clubs/ClubMembersTab.tsx):**
  Client component. Props: `{ clubId: string; isOwner: boolean }`.
  - Uses `useClubMembers(clubId)` hook.
  - **Member List:** Table or card list showing each member:
    - Avatar (or initials fallback), name, email, role badge (Owner/Organizer), joined date formatted with date-fns `format(parseISO(date), "MMM d, yyyy")`.
    - For owner viewing: "Remove" button on organizer rows (never on the owner's own row per TEAM-03). Confirm removal with a Dialog: "Remove [name] from [club]? They will lose organizer access."
    - Remove action: `fetch(DELETE /api/clubs/${clubId}/members, { body: { memberId } })`. On success, mutate SWR cache.
  - **Invite Section** (owner only):
    - Input for email address + "Send Invite" button.
    - Client-side validation: must be McGill email (use isMcGillEmail from utils).
    - On submit: `fetch(POST /api/clubs/${clubId}/invites, { body: { email } })`. On success, show the invite link: `${window.location.origin}/invites/${token}` with a "Copy Link" button that copies to clipboard.
    - Show validation errors from API (already member, pending invite exists, etc.).
  - Empty state if no members (unlikely but handle).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Club dashboard renders with tabs (Overview, Settings for owners, Members for owners). Quick-switch dropdown navigates between clubs. Settings form persists edits via PATCH. Members tab shows list with remove capability and invite form that generates shareable links.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify organizer management experience</name>
  <what-built>Complete organizer club management experience: My Clubs list, dashboard with tabs, quick-switch, settings form, member management, invite flow</what-built>
  <how-to-verify>
    1. Navigate to /my-clubs — if you have 1 club, verify auto-redirect to dashboard; if multiple, verify list with cards
    2. On club dashboard, verify tabs: Overview shows club info, Settings lets you edit (change description, save, reload to confirm persistence)
    3. Test quick-switch dropdown (if you have multiple clubs) — verify it shows logo + name, switching lands on Overview
    4. On Members tab, verify member list shows roles and dates
    5. Try inviting a McGill email — verify invite link is generated and copyable
    6. Check responsive layout on mobile viewport
    7. Verify SideNavBar shows "My Clubs" link
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` completes without errors
3. Single-club redirect works
4. Dashboard tabs switch correctly
5. Settings form saves and data persists
6. Member removal works with confirmation
7. Invite generates valid link
</verification>

<success_criteria>
- My Clubs list page correctly redirects single-club organizers (LOCKED DECISION)
- Dashboard has working Overview, Settings (owner only), Members (owner only) tabs
- Quick-switch shows logo + name only, no role badge, no footer link (LOCKED DECISIONS)
- Settings edits persist via API
- Member list shows role + joined date
- Invite flow generates shareable link with copy-to-clipboard
- Owner cannot remove themselves
- All pages responsive and use McGill branding
</success_criteria>

<output>
After completion, create `.planning/phases/01-club-infrastructure-and-team-management/01-03-SUMMARY.md`
</output>
