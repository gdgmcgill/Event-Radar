# smoke.sh — Tier 2 anonymous HTTP smoke pass
# host        : http://localhost:3000
# label       : b1
# started_at  : 2026-09-15T04:58:39Z
# curl        : curl 8.7.1 (x86_64-apple-darwin25.0) libcurl/8.7.1 (SecureTransport) LibreSSL/3.3.6 zlib/1.2.12 nghttp2/1.68.1
# ring rows   : 5 6 8 10 (request path only — independent of Supabase data)
# data rows   : 1 2 3 4 7 9 (need a reachable Supabase with content)

PASS 1 GET / :: expected 200+marker got 200+marker
FAIL 2 GET /api/events :: expected 200 and >=1 event got 200 and 0 events
PASS 3 GET /api/events?search=test&tags=academic :: expected 200 got 200
PASS 4 GET /clubs :: expected 200 got 200
PASS 5 GET /profile :: expected 307 -> signin=required&next=/profile got 307 /?signin=required&next=/profile
PASS 6 GET /my-events :: expected 307 -> signin=required got 307 /?signin=required&next=/my-events
PASS 7 GET /docs :: expected 200 got 200
PASS 8 POST(x31) /api/events :: expected 429 + Retry-After got 429 Retry-After=60
PASS 9 GET /api/health :: expected 200 got 200
PASS 10 GET /_next/static/chunks/%5Bturbopack%5D_browser_dev_hmr-client_hmr-client_ts_10z625~._.js :: expected 200 no redirect got 200 no-location

--- ring rows (5 6 8 10): 4/4 passed
--- data rows (1 2 3 4 7 9): 5/6 passed
--- total: 9/10 passed, 1 failed
--- NOTE: a failing data row may be environment-limited (unreachable or
---       empty Supabase). The expectation is NOT lowered; name the row
---       and the reason in the run record instead.
# finished_at : 2026-09-15T04:58:41Z

# ==============================================================================
# Run record — appended by plan 02-04 (batch 1).
# Everything ABOVE this block is the verbatim stdout of scripts/smoke.sh.
#   command     : SMOKE_HOST=http://localhost:3000 SMOKE_LABEL=b1 bash scripts/smoke.sh
#   target      : local `npm run dev`, started and stopped by this plan
#   cwd         : /Users/adyan/Documents/GitHub/Event-Radar
#   exit code   : 1  (the script's exit code IS the failing-row count)
#
# RING ROWS 5, 6, 8, 10 — 4/4 PASS. These are the rows the plan requires, and
# they are the rows that are independent of Supabase data:
#   row 5  /profile    307 -> /?signin=required&next=/profile   the page-level auth ring
#   row 6  /my-events  307 -> /?signin=required                 second protected route
#   row 8  POST x31    429 + Retry-After=60                     the rate limiter
#   row 10 /_next/...  200, no Location                         the matcher exclusion
#
# ROW 2 FAILS, AND IT IS THE SAME ENVIRONMENT LIMITATION AS BATCH 0.
#   expected : 200 and >=1 event
#   got      : 200 and 0 events        <- actual value printed, not suppressed
#   reason   : the local Supabase reachable from this machine holds no approved
#              events, so the query is correct and the dataset is empty. The
#              route returns 200 with a well-formed empty array; this is a data
#              condition, not a request-path defect.
#   The expectation is NOT lowered and the row is NOT removed from the script.
#   Identical behaviour was recorded in evidence/smoke.b0.txt at batch 0, before
#   any dependency changed, which is what establishes it as pre-existing rather
#   than introduced by batch 1.
#
# BATCH 0 -> BATCH 1 COMPARISON: byte-for-byte the same verdicts on all ten rows
#   (9/10 passed, ring 4/4, row 2 the only failure). Eleven declarations left the
#   production tree and no smoke row changed. That is the behaviour-preservation
#   claim for this batch.
#
#   Row 7 (/docs -> 200) is worth calling out: /docs is the redoc route, and
#   redoc is what refuted the planned `yaml` removal. This row passing is the
#   runtime confirmation that restoring yaml fixed the route and not merely the
#   build.
# ==============================================================================
