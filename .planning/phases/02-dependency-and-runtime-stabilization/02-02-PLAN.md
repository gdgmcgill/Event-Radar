---
phase: 02-dependency-and-runtime-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middlewareRateLimit.test.ts
  - src/middleware.test.ts
  - scripts/smoke.sh
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs
autonomous: true
requirements: [STAB-06, STAB-09, STAB-13]

must_haves:
  truths:
    - "The rate limiter's 429 budget is asserted by an automated test that passes on today's unmodified code"
    - "The proxy/middleware matcher's included and excluded paths are asserted by an automated test using Next's own matcher compiler"
    - "Any agent can ask, in one command, whether the tree is at or better than the AUDIT-13 baseline"
    - "A smoke pass is a command with captured output, not a claim in a commit message"
  artifacts:
    - path: "src/middlewareRateLimit.test.ts"
      provides: "STAB-06 rate-limit characterization (PRESERVE)"
      min_lines: 40
    - path: "src/middleware.test.ts"
      provides: "STAB-06 matcher characterization (PRESERVE); renamed to src/proxy.test.ts in plan 02-06"
      min_lines: 25
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs"
      provides: "Mechanical STAB-13 comparator against the AUDIT-13 baseline"
      min_lines: 80
    - path: "scripts/smoke.sh"
      provides: "Tier 2 anonymous HTTP smoke pass, 10 rows"
      min_lines: 60
  key_links:
    - from: "src/middleware.test.ts"
      to: "src/middleware.ts"
      via: "imports the exported config object and feeds it to Next's matcher compiler"
      pattern: "unstable_doesMiddlewareMatch"
    - from: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs"
      to: ".planning/audit/baseline/jest.txt"
      via: "parses the baseline pass/skip/suite counts from the Phase 1 capture instead of inlining them"
      pattern: "baseline/jest\\.txt"
---

<objective>
Batch 0b: build the instruments before the measurements. Three artifacts that every later batch depends on and that do not exist today.

Two characterization suites lock the behaviour of the one file in this phase that sits on the request path, so the batch-3 rename can be proven identical rather than asserted identical. One zero-dependency comparator makes STAB-13 mechanical instead of eyeballed. One shell script gives STAB-09's "and a smoke pass" a concrete, capturable meaning before Phase 3's Playwright harness exists.

Purpose: `src/middleware.ts` is this application's only page-level authentication ring, and it also carries the rate limiter, the ban check, and the onboarding guard. Batch 3 renames it. Next 16 hard-throws at build if `middleware.*` and `proxy.*` coexist, so there is no incremental path and no chance to verify halfway. The only honest way to run that change is to write the assertions against today's behaviour first, watch them pass on today's code, and then watch the same assertions pass after.
Output: `src/middlewareRateLimit.test.ts`, `src/middleware.test.ts`, `evidence/tools/check-baseline.mjs`, `scripts/smoke.sh`.

**No CONTEXT.md exists for this phase** — there are no `D-NN` user decisions. Locked constraints come from `.planning/PROJECT.md` § Constraints, `.planning/REQUIREMENTS.md` § Out of Scope, and `02-RESEARCH.md` § Phase Constraints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md
@src/middlewareRateLimit.ts
@src/middleware.ts
@.planning/audit/tools/validate.mjs
</context>

<phase_locked_constraints>
1. `react` / `react-dom` untouched. 2. Forced audit remediation is forbidden; the lockfile is reconciled, never regenerated. <!-- planner-discipline-allow: audit fix --> 3. `.env.local` is never read or written; no secret value enters `.planning/`. 4. No schema file is touched and no database operation is performed anywhere in Phase 2. 5. `shasum -a 256`, not `sha256sum`. 6. **Zero-dependency tooling: nothing this plan creates may be added to `package.json`.** 7. No behaviour changes — this plan adds tests and tools only, and modifies no production source file.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the two STAB-06 characterization suites against today's unmodified code</name>
  <files>src/middlewareRateLimit.test.ts, src/middleware.test.ts</files>
  <read_first>
    - src/middlewareRateLimit.ts — the subject. Read the whole file: `applyApiRateLimit(req: NextRequest): NextResponse | null` is the export, the store hangs off `globalThis` under a module-private key, `WINDOW_MS` is 60_000, there is an admin-prefix bypass and a high-frequency POST prefix list. Take every constant from the source, not from this plan.
    - src/middleware.ts — read lines 145-158 for the exported `config.matcher`; it is a single-entry array with one negative-lookahead pattern. Read lines 110-125 for `PROTECTED_ROUTES` (8 entries) so the matcher test covers the paths that actually matter.
    - src/lib/sanitize.test.ts — the minimal pure-module suite analog: relative import of the subject, bare `describe`/`it`, no setup file, no mocks
    - src/__tests__/api/events/date-validation.test.ts lines 1-19 — the file-level JSDoc header + `// ─── Section ───` divider + SCREAMING_SNAKE module-constant conventions
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Code Examples 1 and 2 (both were executed live in this tree) and § Pitfall 4
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § `src/middlewareRateLimit.test.ts` and `src/proxy.test.ts`
  </read_first>
  <behavior>
Rate limiter (`src/middlewareRateLimit.test.ts`) — every expectation must pass on today's unmodified `src/middlewareRateLimit.ts`:
  - 31+ POSTs to `/api/events` from one IP within the window: the last response status is 429 and carries a truthy `Retry-After` header
  - `/api/admin/*` is exempt: a POST count well past the standard budget from a fresh IP still returns null (no rate-limit response)
  - A path in the high-frequency POST prefix list gets the larger budget: a POST count above the standard budget but below the high-frequency budget still returns null
  - A non-`/api` path returns null regardless of method or count
  - The standard budget boundary: the Nth request inside the budget returns null and the N+1th returns 429, with N read from the source constant rather than hard-coded in the assertion

Matcher (`src/middleware.test.ts`) — asserted through Next's own matcher compiler, not a hand-rolled regex:
  - Runs on every one of the 8 `PROTECTED_ROUTES` entries, plus `/`, `/api/events`, `/docs`, `/banned`, `/onboarding`
  - Skips `/_next/static/<chunk>.js`, `/_next/image`, `/favicon.ico`, `/auth/callback`, and image extensions such as `.png` and `.svg`
  </behavior>
  <action>
Create `src/middlewareRateLimit.test.ts`. Import `applyApiRateLimit` with a relative path (`./middlewareRateLimit`) matching the `sanitize.test.ts` convention, and `NextRequest` from `next/server`. Build requests with a small local helper taking path, method, and IP, setting the client address through the `x-forwarded-for` header.

The store is keyed by IP **and** path and lives on `globalThis`, so it survives across tests within a file. Give every test a distinct IP or a distinct path, and say so in the file-level JSDoc header — a future reader who adds a test with a shared IP will get a confusing cross-test failure otherwise. Jest's default module-registry isolation is per file, not per test.

Read the budget numbers and the prefix lists out of `src/middlewareRateLimit.ts` and reference them as module-level SCREAMING_SNAKE constants at the top of the test file, with a comment naming the source line each came from. Do not transcribe a number this plan states; the plan can be stale and the source cannot.

Title the top-level `describe` so it records intent: this is a PRESERVE characterization suite whose job is to behave identically after the proxy rename.

Create `src/middleware.test.ts`. Import the `config` object from `./middleware` and `unstable_doesMiddlewareMatch` from `next/experimental/testing/server`. **The export is named for middleware, not for proxy.** The Next documentation shows a proxy-named variant of this helper; that export does not exist in either the installed 16.2.1 or in 16.3.5 — both tarballs were checked. Importing the documented name fails at runtime with an is-not-a-function TypeError that reads like a Jest ESM interop problem and sends an executor down the wrong road entirely.

Call the helper with an object carrying `config`, an empty `nextConfig`, and the `url` under test. Use `it.each` over an included-paths array and a second `it.each` over an excluded-paths array so a failure names the offending path.

Both files live under `src/` and are `.ts`, so the current single-project Jest config picks them up in the node environment with no config change. Do not add a setup file, do not add jsdom, do not modify `jest.config.js` — that is batch 5.

Neither file may modify, wrap, or re-export anything from `src/middleware.ts` or `src/middlewareRateLimit.ts`. The subjects stay byte-identical in this plan.
  </action>
  <acceptance_criteria>
    - `npx jest --ci src/middlewareRateLimit.test.ts` exits 0 with at least 4 passing tests and 0 skipped
    - `npx jest --ci src/middleware.test.ts` exits 0 with at least 2 passing tests and 0 skipped
    - `src/middleware.test.ts` contains the string `unstable_doesMiddlewareMatch`
    - `grep -c 'unstable_doesProxyMatch' src/middleware.test.ts` prints 0
    - `src/middlewareRateLimit.test.ts` contains a `429` assertion and a `Retry-After` assertion
    - `src/middleware.test.ts` asserts a true result for all 8 strings in `PROTECTED_ROUTES` as read from `src/middleware.ts`
    - `src/middleware.test.ts` asserts a false result for at least one `/_next/static` path and for `/auth/callback`
    - `git diff --stat -- src/middleware.ts src/middlewareRateLimit.ts` prints nothing — the subjects are unmodified
    - Both files open with a file-level JSDoc block and use `describe`/`it` with double-quoted strings
    - `npm test -- --ci` exits 0 with a passing count strictly greater than the AUDIT-13 baseline of 220
  </acceptance_criteria>
  <verify>
    <automated>npx jest --ci src/middlewareRateLimit.test.ts src/middleware.test.ts &amp;&amp; node -e "const fs=require('fs');const m=fs.readFileSync('src/middleware.test.ts','utf8');const r=fs.readFileSync('src/middlewareRateLimit.test.ts','utf8');const src=fs.readFileSync('src/middleware.ts','utf8');const routes=(src.match(/PROTECTED_ROUTES\s*=\s*\[([^\]]*)\]/)||[])[1]||'';const list=routes.split(',').map(s=>s.trim().replace(/^[\"']|[\"']$/g,'')).filter(Boolean);const missing=list.filter(p=>!m.includes(p));const bad=[];if(!m.includes('unstable_doesMiddlewareMatch'))bad.push('wrong-matcher-export');if(m.includes('unstable_doesProxy'+'Match'))bad.push('nonexistent-export-used');if(missing.length)bad.push('protected-routes-uncovered:'+missing.join('|'));if(!/429/.test(r))bad.push('no-429-assertion');if(!/Retry-After/.test(r))bad.push('no-retry-after-assertion');if(!m.includes('_next/static'))bad.push('no-exclusion-assertion');if(!m.includes('auth/callback'))bad.push('no-callback-exclusion');if(bad.length){console.error('STAB-06 characterization gaps: '+bad.join(', '));process.exit(1)}console.log('STAB-06 characterization OK — '+list.length+' protected routes covered')"</automated>
  </verify>
  <done>Both characterization suites pass against unmodified production source. The rate-limit budgets and the matcher's include/exclude sets are now facts recorded by a command, so the batch-3 rename has a before to be compared against.</done>
</task>

<task type="auto">
  <name>Task 2: Build the zero-dependency STAB-13 baseline comparator</name>
  <files>.planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs</files>
  <read_first>
    - .planning/audit/tools/validate.mjs — the precedent named by RESEARCH. Copy five things from it: the header block stating the zero-dependency contract and CLI surface (lines 1-31), the `emit`/`makeCtx` one-line-per-rule reporter (lines 79-97), the `{ requirement, inputs, run(ctx) }` check-registry shape (lines 336-348), the `main()` + summary line + terminal-error wrapper (lines 957-999), and the load-expected-counts-from-a-file helpers (lines 99-120)
    - .planning/audit/baseline/jest.txt — the AUDIT-13 reference. Its summary lines read `Test Suites: 5 skipped, 16 passed, 16 of 21 total` and `Tests: 36 skipped, 220 passed, 256 total`. Parse these; never inline 220, 36, 16 or 21 as literals
    - .planning/audit/baseline/lint.txt — reference line `✖ 12 problems (0 errors, 12 warnings)`
    - .planning/audit/baseline/tsc.txt — reference is zero diagnostics
    - .planning/audit/baseline/versions.txt — the key=value format and `build_route_row_count=140`
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § `check-baseline.mjs`
  </read_first>
  <action>
Create `.planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs`. It imports only `node:fs`, `node:path`, and `node:child_process`. It is never added to `package.json` — Phase 2 measures the dependency tree, and a measuring tool that enlarges the tree corrupts its own measurement.

CLI surface, mirroring `validate.mjs`: bare invocation runs every check and exits 0 only if none FAILed; `--check <name>` runs exactly one check and treats an absent input as a FAIL; `--list` prints the registry; `--help` prints usage. Output is one line per rule in the form `PASS|FAIL|SKIP <check-name> :: <rule> :: <detail>`, followed by a `--- N passed, N failed, N skipped` summary.

Checks to register, each tagged with its requirement id the way `validate.mjs` tags `AUDIT-01`:

`jest` (STAB-13) — shells out to `npx jest --ci --json`, parses the JSON result, and asserts `numFailedTests === 0`, `numPassedTests >= <baseline passed>`, `numPendingTests <= <baseline skipped>`, and `numTotalTestSuites - numPendingTestSuites >= <baseline executing suites>`. Every baseline number is parsed out of `.planning/audit/baseline/jest.txt` at runtime. If that file is missing or its summary lines do not parse, that is a hard FAIL, never a silent default — the Phase 1 rule is that an absent input is a FAIL under `--check` and a SKIP only under an explicit quick mode.

`tsc` (STAB-13) — runs `npx tsc --noEmit` and asserts exit 0 with empty stdout and stderr.

`lint` (STAB-13) — runs `npm run lint` and asserts 0 errors and a warning count no greater than the baseline parsed from `.planning/audit/baseline/lint.txt`.

`node-pin` (STAB-01) — asserts `package.json` `engines.node`, `.nvmrc`, and the `node-version-file` input in `.github/workflows/ci.yml` all agree on one Node major.

`no-unplanned-majors` (STAB-10) — compares the major of every `dependencies` and `devDependencies` range in `package.json` against a committed snapshot taken at the batch-0 commit, and FAILs on any major change other than `@types/node`. Write the snapshot on first run into `evidence/tools/majors.b0.json` and read it on every subsequent run.

`react-untouched` (STAB-05) — asserts the `react` and `react-dom` ranges in `package.json` are byte-identical to the snapshot above.

`lockfile-discipline` (STAB-11) — asserts no commit message in the phase's commit range references the forced audit-remediation command, and that `lockfileVersion` in `package-lock.json` is unchanged from the snapshot. <!-- planner-discipline-allow: audit fix -->

Keep the secret-scrubbing helper from `validate.mjs` lines 58-77 and route every caught error through it before printing, because `.planning/` is committed and a shelled-out command's stack can carry an environment fragment.

The comparator must be re-runnable from the repository root with no arguments and no environment setup beyond a working `npx`.
  </action>
  <acceptance_criteria>
    - `node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs --list` prints at least 7 registry rows, each carrying a `STAB-` requirement tag
    - `node .../check-baseline.mjs` exits 0 on the current tree and its final line matches `^--- \d+ passed, 0 failed`
    - `node .../check-baseline.mjs --check jest` exits 0 and emits a line matching `^PASS jest :: `
    - `node .../check-baseline.mjs --check does-not-exist` exits 1 and prints a line containing `unknown-check`
    - `grep -cE "\b(220|256|36)\b" .../check-baseline.mjs` prints 0 — no baseline count is inlined as a literal
    - The file contains the string `baseline/jest.txt` — the counts are read from the Phase 1 capture
    - `node -e "const p=require('./package.json');const s=JSON.stringify(p);process.exit(/check-baseline/.test(s)?1:0)"` exits 0 — the tool is not registered in package.json
    - `head -1 .../check-baseline.mjs` is a node shebang and the file imports nothing outside the `node:` namespace
    - Temporarily renaming `.planning/audit/baseline/jest.txt` makes `--check jest` exit 1 with a FAIL line, and restoring it returns exit 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs --list &amp;&amp; node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs &amp;&amp; node -e "const fs=require('fs');const f='.planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs';const t=fs.readFileSync(f,'utf8');const body=t.split('\n').filter(l=>!/^\s*(\*|\/\/|\/\*)/.test(l)).join('\n');const bad=[];if(/\b(220|256|36)\b/.test(body))bad.push('inlined-baseline-literal');if(!t.includes('baseline/jest.txt'))bad.push('does-not-read-baseline-file');if(/from ['\"](?!node:)[^'\"]+['\"]/.test(body))bad.push('non-node-import');if(/check-baseline/.test(JSON.stringify(require('./package.json'))))bad.push('registered-in-package-json');if(bad.length){console.error('comparator gaps: '+bad.join(', '));process.exit(1)}console.log('comparator OK')"</automated>
  </verify>
  <done>One command answers "is this tree at or better than AUDIT-13", reading every reference number out of the Phase 1 captures rather than from a literal, and failing loudly when an input is missing.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Tier 2 anonymous HTTP smoke script</name>
  <files>scripts/smoke.sh</files>
  <read_first>
    - .planning/audit/tools/cache-probe.sh lines 1-46 — the boxed header naming WHAT THIS DOES, the method contract, INPUTS (environment only, no defaults for the target host), and OUTPUTS by path; also its in-pipeline redaction rule
    - .planning/audit/tools/readonly-guard.sh lines 1-53 — the shell-option rationale comment block and the accumulate-don't-abort exit contract (`set -u`, deliberately no `set -e`, never `set -x`)
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Smoke Pass Design — the 10-row Tier 2 table is the specification; row 5 is the single most important line in it
    - src/middleware.ts lines 110-125 — the redirect shape rows 5 and 6 assert (`/?signin=required&next=<path>`)
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § `scripts/smoke.sh`
  </read_first>
  <action>
Create `scripts/smoke.sh`, executable, implementing the 10 Tier 2 rows. It takes its target from a required `SMOKE_HOST` environment variable with **no default** — a fallback could silently smoke the wrong system, which is the reasoning `cache-probe.sh` gives for the same choice. Accept an optional `SMOKE_OUT` for the capture path, defaulting under the phase evidence directory.

Header: copy the boxed `cache-probe.sh` format. State WHAT THIS DOES, the INPUTS (environment only), the OUTPUTS by path, and — this is the honest deviation — state that unlike `cache-probe.sh` this script is **not** GET-only, because row 8 must issue 31 POSTs to prove the rate limiter returns 429. Do not copy a GET-only contract into a script that posts.

<!-- planner-discipline-allow: set -x -->
Shell options: `set -u` to catch unset variables; deliberately no `set -e`, because an early abort suppresses the diagnostic output that makes a failure actionable — accumulate failures in a counter and exit with it; never `set -x`, because the script may one day run against a deployment whose environment carries a token.

The ten rows, each printing one line of the form `PASS|FAIL <n> <method> <path> :: expected <x> got <y>`:
1. `GET /` expects 200 and an HTML body containing an event card marker
2. `GET /api/events?limit=5` expects 200 and a JSON array of length at least 1
3. `GET /api/events?search=test&tags=academic` expects 200
4. `GET /clubs` expects 200
5. `GET /profile` expects a 307 whose `Location` contains `signin=required` and `next=/profile` — **do not follow redirects**. This is the protected-route ring and the proxy rename's entire blast radius; if the rename silently disables the ring this row turns 200 and every protected page becomes public
6. `GET /my-events` expects a 307 whose `Location` contains `signin=required`
7. `GET /docs` expects 200
8. 31 × `POST /api/events` from a single simulated client address expects the last response to be 429 with a `Retry-After` header
9. `GET /api/health` expects 200
10. `GET /_next/static/<a chunk path discovered from the built output>` expects 200 with no redirect — this proves the matcher's negative lookahead survived the rename

On failure a row must print the value it actually saw, not merely that it failed — copy the echo-plus-diff diagnostic style from `readonly-guard.sh`. Write the full capture to the output path and also echo it to stdout so a CI log carries it.

Redact inside the pipeline that writes the file, using the Phase 1 secret patterns (JWT-shaped strings, Supabase secret keys, Postgres URLs with credentials, bearer tokens, Supabase auth cookies), so an unredacted token can never reach disk. `.planning/` is committed.

The script is a shell file under `scripts/`; it is not registered in `package.json` and adds no dependency. Verify it runs end-to-end against a local `npm run dev` before considering the task done, and record which rows passed. Rows 1-4 and 7-9 depend on a reachable Supabase; when the environment has only CI placeholder credentials, a row may legitimately return a non-200 — in that case the script must still print its actual value, and the run record must name which rows were environment-limited rather than silently lowering an expectation.
  </action>
  <acceptance_criteria>
    - `test -x scripts/smoke.sh` exits 0
    - `bash -n scripts/smoke.sh` exits 0 (syntax valid)
    - Running `scripts/smoke.sh` with `SMOKE_HOST` unset exits non-zero and prints a message naming `SMOKE_HOST`
    - `grep -c 'set -x' scripts/smoke.sh` prints 0
    - `grep -q 'set -u' scripts/smoke.sh` exits 0
    - The script contains all ten row paths: `/`, `/api/events`, `/clubs`, `/profile`, `/my-events`, `/docs`, `/api/health`, `/_next/static`
    - The script asserts `signin=required` for both `/profile` and `/my-events` and does not pass a follow-redirects flag on those two requests
    - The script issues at least 31 POST requests in its rate-limit row and checks for `429` and `Retry-After`
    - Against a running `npm run dev` with `SMOKE_HOST=http://localhost:3000`, rows 5, 6, 8 and 10 print PASS — these four depend only on the request-path ring and not on Supabase reachability
    - The capture file written by the run contains no string matching the JWT shape `eyJ[A-Za-z0-9_-]{8,}\.`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/smoke.sh &amp;&amp; test -x scripts/smoke.sh &amp;&amp; node -e "const fs=require('fs');const t=fs.readFileSync('scripts/smoke.sh','utf8');const need=['/api/events','/clubs','/profile','/my-events','/docs','/api/health','/_next/static','signin=required','429','Retry-After','set -u','SMOKE_HOST'];const bad=need.filter(n=>!t.includes(n));if(/set -x/.test(t))bad.push('set-x-present');const posts=(t.match(/POST/g)||[]).length;if(posts<1)bad.push('no-post-row');if(bad.length){console.error('smoke.sh gaps: '+bad.join(', '));process.exit(1)}console.log('smoke.sh structure OK')" &amp;&amp; (SMOKE_HOST= scripts/smoke.sh >/dev/null 2>&amp;1; test $? -ne 0 &amp;&amp; echo "input-gate OK")</automated>
  </verify>
  <done>"A smoke pass" is now a command with captured, redacted output and ten named expectations, with the protected-route ring as its load-bearing row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → running application | The script issues real requests, including 31 POSTs, against whatever `SMOKE_HOST` names |
| captured output → committed `.planning/` | Response headers can carry session cookies and bearer tokens |
| test process → `globalThis` rate-limit store | The characterization suite mutates a process-global bucket map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | `scripts/smoke.sh` capture files under `.planning/` | mitigate | Redact inside the write pipeline using the Phase 1 pattern set (JWT, `sb_secret_`, Postgres URL with credentials, bearer, `sb-*-auth-token` cookie); never `set -x`; capture headers, not bodies, for any authenticated row |
| T-02-02-02 | Denial of Service | Target named by `SMOKE_HOST` during the 31-POST rate-limit row | mitigate | `SMOKE_HOST` is required with no default so the script cannot silently target production; 31 requests is one over the documented per-IP budget and is bounded by construction; row 8 is run against `npm run dev` or a preview deployment, never against production |
| T-02-02-03 | Tampering | Characterization suite weakened to make a later batch pass | mitigate | The suites are committed before any change they guard; `check-baseline.mjs --check jest` asserts the pass count never regresses, so deleting an assertion is visible as a count drop |
| T-02-02-04 | Spoofing | `x-forwarded-for` used to key rate-limit buckets in tests | accept | Test-only; the header is the production key-derivation input already and this plan changes no production behaviour. The spoofability of that header is a real property of the current implementation and belongs to REFAC-18 in Phase 5, not here |
| T-02-02-05 | Repudiation | A green smoke run that proves nothing because rows silently degraded | mitigate | Every row prints the value it actually saw; environment-limited rows must be named in the run record rather than having their expectation lowered |
| T-02-02-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** The zero-dependency rule (constraint 6) forbids registering `check-baseline.mjs` or `smoke.sh` in `package.json`; the acceptance criteria assert their absence from the manifest |
</threat_model>

<verification>
- `npx jest --ci src/middlewareRateLimit.test.ts src/middleware.test.ts` exits 0
- `npm test -- --ci` exits 0 with a passing count above the AUDIT-13 baseline of 220 and 0 failures
- `node .../evidence/tools/check-baseline.mjs` exits 0 with zero FAIL lines
- `bash -n scripts/smoke.sh` exits 0 and the input gate rejects a missing `SMOKE_HOST`
- `git diff --stat -- src/middleware.ts src/middlewareRateLimit.ts` is empty — no production source changed
- `npx tsc --noEmit` exits 0 (test files remain excluded from the tsc program by design)
</verification>

<success_criteria>
The three instruments exist and are green on today's tree. The rate limiter's 429 budget and the matcher's include/exclude sets are recorded as passing assertions against unmodified source, so batch 3 has a real before. STAB-13 is answerable by one command that reads its reference numbers from the Phase 1 captures. The smoke pass is a script with ten named rows and in-pipeline redaction. Nothing was added to `package.json`.
</success_criteria>

<artifacts_this_phase_produces>
Contributed by this plan:

**New files**
- `src/middlewareRateLimit.test.ts` — STAB-06 rate-limit characterization (PRESERVE)
- `src/middleware.test.ts` — STAB-06 matcher characterization (PRESERVE); renamed to `src/proxy.test.ts` inside the single batch-3 commit in plan 02-06
- `.planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs` — zero-dependency STAB-13 comparator with a `--check <name>` CLI
- `.planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/majors.b0.json` — the dependency-major snapshot the `no-unplanned-majors` check compares against
- `scripts/smoke.sh` — Tier 2 anonymous HTTP smoke, 10 rows

**New package.json scripts / fields:** none, deliberately. Both tools are invoked by path.
**Deleted files:** none.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-dependency-and-runtime-stabilization/02-02-SUMMARY.md` when done.
</output>
