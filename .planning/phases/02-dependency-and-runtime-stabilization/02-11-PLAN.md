---
phase: 02-dependency-and-runtime-stabilization
plan: 11
type: execute
wave: 8
depends_on: ["02-09", "02-10"]
files_modified:
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - CLAUDE.md
  - .claude/CLAUDE.md
  - README.md
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-npm-ci.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/STAGE-2-COMPLETION.md
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/lockfile-review-log.md
autonomous: true
requirements: [STAB-10, STAB-11, STAB-12, STAB-13, STAB-17]

must_haves:
  truths:
    - "A fresh clone of the final commit installs from the lockfile alone and builds on the pinned Node"
    - "The finding register agrees with the roadmap about which findings this phase closes"
    - "A reader can verify every clause of the Stage 2 exit gate from committed artifacts without re-running anything"
    - "The documentation no longer asserts facts the working tree contradicts"
  artifacts:
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-npm-ci.txt"
      provides: "STAB-12 reproducible-install evidence from a fresh clone at the final commit"
      min_lines: 30
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/STAGE-2-COMPLETION.md"
      provides: "STAB-17 exit gate note answerable from artifacts alone"
      min_lines: 120
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/lockfile-review-log.md"
      provides: "STAB-11 per-batch lockfile diff line counts and the never-regenerated statement"
      min_lines: 25
  key_links:
    - from: ".planning/audit/FOUNDATION_AUDIT.md"
      to: ".planning/audit/findings.json"
      via: "the markdown register is generated from the machine register; hand-editing it fails the staleness check"
      pattern: "gen-foundation-audit"
    - from: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/STAGE-2-COMPLETION.md"
      to: ".planning/audit/baseline/jest.txt"
      via: "quotes both the pass count and the skip count against the AUDIT-13 baseline"
      pattern: "baseline/jest\\.txt"
---

<objective>
Batch 6c: prove the tree reproduces, reconcile the finding register with the roadmap, correct the documentation this phase made false, and write the note that evidences the Stage 2 exit gate.

Purpose: STAB-17's gate is not "the work is done" — it is "a reader can confirm the work is done from artifacts alone, without re-running anything." That is a higher bar and it is the bar that lets Phase 3 start. The completion note is the artifact the Stage 3 gate is checked against, and everything in it must be quoted from a committed file rather than recalled.

This plan runs last because three of its inputs only exist once every other plan has landed: the final commit the clean room clones, the final census plan 02-09 measured, and the bundle delta plan 02-10 wrote.
Output: the clean-room evidence, a regenerated finding register, corrected documentation, the lockfile review log, and `STAGE-2-COMPLETION.md`.

**No CONTEXT.md exists for this phase** — there are no `D-NN` user decisions. Locked constraints come from `.planning/PROJECT.md` § Constraints, `.planning/REQUIREMENTS.md` § Out of Scope, and `02-RESEARCH.md` § Phase Constraints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md
@.planning/audit/SEVERITY_SLA.md
@.planning/audit/baseline/versions.txt
</context>

<phase_locked_constraints>
1. `react` / `react-dom` untouched. 2. Forced audit remediation is forbidden; the lockfile was reconciled, never regenerated — and this plan is where that is attested. <!-- planner-discipline-allow: audit fix --> 3. `.env.local` is never read, written, or copied — including into the clean room. No secret value enters `.planning/`. 4. No schema file is touched and no database operation is performed anywhere in Phase 2. 5. `shasum -a 256`, not `sha256sum`. 6. Zero-dependency tooling. 7. No behaviour changes. **This plan makes no change to `package.json`, `package-lock.json`, or `src/`.**
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run the clean room at the final commit and write the lockfile review log</name>
  <files>.planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-npm-ci.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/lockfile-review-log.md</files>
  <read_first>
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Clean-Room Protocol — the seven steps, and the reason step 1 insists on a fresh clone rather than the working tree
    - .planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-interim.txt — the interim run from plan 02-07; this run is the same protocol at the final commit, and the two are compared
    - .planning/audit/baseline/lock.sha256 — the Phase 1 lock hash reference format
    - .github/workflows/ci.yml — the two placeholder environment values; the clean-room build uses the same ones so the two runs measure the same thing
    - The six `evidence/lock.b*.before.sha256` files and the batch commit bodies — the per-batch lockfile diff line counts recorded as each batch landed
  </read_first>
  <action>
Run the clean-room protocol properly, at the final commit, and capture everything to `evidence/cleanroom-npm-ci.txt` including the exit code of every step.

Clone the branch into a temporary directory outside this repository. Do not reuse the working tree: a local `node_modules` can resolve a package that was removed from the manifest, which would let a broken tree appear to install.

Inside the clone, first assert the runtime satisfies the `engines` block — print the Node and npm versions and compare them against the declared ranges before installing anything. Proving the pinned toolchain builds the project is the entire point of the exercise, so an install on a different runtime proves something else.

Compare the clone's `package-lock.json` hash against the working tree's using `shasum -a 256`. They must match.

Remove any `node_modules` and build output, then install with `npm ci` — never `npm install`. `npm ci` failing loudly when the manifest and lockfile disagree is the reproducibility property under test; `npm install` would paper over exactly the disagreement you want to detect.

Build with the same two placeholder Supabase environment values the CI workflow sets at workflow level. Then run the test suite in CI mode. Capture stdout, stderr, and an explicit exit code line for each of the four steps, following the Phase 1 capture format. Never create or copy `.env.local` into the clone.

Compare against the interim run from plan 02-07 and note any difference — batch 5 added four packages after that run, so the install tree legitimately differs; what must not differ is that every step still exits 0.

Then write `evidence/lockfile-review-log.md`, which is STAB-11's evidence. One row per batch: the batch identifier, the commit SHA, the `package-lock.json` diff line count as recorded when that batch landed, and a one-line statement of what moved. Then three attestations, each backed by a command with its output rather than asserted:

That the lockfile was never regenerated wholesale — every change came from reconciling against a hand-edited manifest, and `lockfileVersion` never changed across the phase.

That no forced remediation command was ever run — searched across every commit message in the phase range, with the search command and its zero result printed. <!-- planner-discipline-allow: audit fix -->

That every batch's diff was read as a diff before installation, which is why the line counts exist to be quoted at all.

Close with the `*Phase: … *Plan: …` footer.
  </action>
  <acceptance_criteria>
    - `evidence/cleanroom-npm-ci.txt` records four steps — runtime check, `npm ci`, build, tests — each with an explicit exit code line, and all four are 0
    - The capture shows a working directory outside this repository
    - The capture records the Node and npm versions and compares them against the `engines` declaration
    - The capture records a lockfile hash comparison between the clone and the working tree, and they match
    - The capture contains `npm ci` and does not contain a bare `npm install` invocation
    - The capture contains no string matching the JWT shape `eyJ[A-Za-z0-9_-]{8,}\.` and no reference to `.env.local`
    - `evidence/lockfile-review-log.md` has one row per batch with a commit SHA and a diff line count
    - The log contains the never-regenerated attestation, the no-forced-remediation search with a zero result, and the diff-read-before-install statement
    - `git log --format=%B <phase-range>` contains no commit message referencing a forced audit remediation command
    - The log ends with lines matching `^\*Phase:` and `^\*Plan:`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/02-dependency-and-runtime-stabilization/evidence/';const bad=[];const c=fs.readFileSync(d+'cleanroom-npm-ci.txt','utf8');const exits=(c.match(/exit(_code)?[= ]\d+/g)||[]);if(exits.length<4)bad.push('fewer-than-4-exit-codes:'+exits.length);if(exits.some(e=>!/[= ]0$/.test(e)))bad.push('non-zero-step:'+exits.filter(e=>!/[= ]0$/.test(e)).join('|'));if(!/npm ci/.test(c))bad.push('no-npm-ci');if(/npm install(?!\s*--package-lock-only)/.test(c))bad.push('bare-npm-install-used');if(!/engines|node -v|node --version/.test(c))bad.push('no-runtime-check');if(!/[0-9a-f]{64}/.test(c))bad.push('no-lockfile-hash-comparison');if(/eyJ[A-Za-z0-9_-]{8,}\./.test(c))bad.push('SECRET-LEAK-in-cleanroom');if(/\.env\.local/.test(c))bad.push('env-local-referenced');const l=fs.readFileSync(d+'lockfile-review-log.md','utf8');const rows=l.split('\n').filter(x=>/\|/.test(x)&&/[0-9a-f]{7,40}/.test(x));if(rows.length<5)bad.push('fewer-than-5-batch-rows:'+rows.length);if(!/regenerat/i.test(l))bad.push('no-never-regenerated-attestation');if(!/lockfileVersion/.test(l))bad.push('no-lockfile-version-attestation');if(!/^\*Phase:/m.test(l))bad.push('footer-phase');if(!/^\*Plan:/m.test(l))bad.push('footer-plan');if(bad.length){console.error('STAB-12/11 gaps: '+bad.join(', '));process.exit(1)}console.log('STAB-12 clean room OK, STAB-11 log OK — '+rows.length+' batch rows')"</automated>
  </verify>
  <done>A fresh clone of the final commit installs from the lockfile alone and builds and tests green on the pinned runtime, with no secret and no environment file anywhere near the capture. The lockfile's per-batch history is logged with diff line counts and three attestations backed by commands rather than claims.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the finding register with the roadmap and correct the stale documentation</name>
  <files>.planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md, CLAUDE.md, .claude/CLAUDE.md, README.md</files>
  <read_first>
    - .planning/audit/findings.json — the machine register. F-051, F-052 and F-053 record a closing phase of 03 and F-056 and F-057 record null, while F-063 through F-066 correctly record 02
    - .planning/audit/tools/gen-foundation-audit.mjs — read its header. The markdown register is **generated** from the machine register and is never hand-edited; the tool has a staleness check mode that exits non-zero if the document does not match its input
    - .planning/audit/tools/validate.mjs — the findings check, which asserts the document and the register agree on the finding count
    - .planning/ROADMAP.md § Phase 2 success criteria — the phase contract, which names the CLI removal, the framework patch, and the API-documentation disposition explicitly
    - .planning/audit/quality/dead-code.md § 4 — the stale-documentation findings and the instruction that each correction rides along with the change that made the claim false
    - .planning/audit/baseline/versions.txt § UPSTREAM DISCREPANCIES — the documented-versus-actual protected-route count and the two count discrepancies
    - CLAUDE.md, .claude/CLAUDE.md, README.md — the three documents carrying claims this phase falsified
  </read_first>
  <action>
**Resolve the register disagreement in favour of the roadmap.** Five findings record a closing phase that disagrees with where the roadmap assigns their remediation: three say 03 and two say null, while the roadmap's Phase 2 success criteria name the CLI removal, the framework patch, and the API-documentation disposition by name. Whether that was a deliberate deferral or a transcription slip during the Phase 1 register generation is not determinable from the artifacts — but the roadmap is the phase contract, so it wins.

Set the closing phase to 02 for all five, and set each one's status to reflect what actually happened in this phase rather than assuming closure. Two need care: the Windows RCE finding is closed **by version**, which also retires its unverified-operating-system evidence gap — say that in its resolution note. And the shared-cache finding is **not** closed by this phase; if it appears in the set, leave it Open and pointed at Phase 6.

**Edit `findings.json`, then re-run the generator, then run the validator.** Do not hand-edit the markdown register — it is generated, and a hand edit fails the staleness check. Run the generator's staleness check afterwards to confirm the document matches its input, then run the audit validator's findings check.

Then correct the three documents whose claims this phase falsified. Each correction rides along with the change that made the claim false, which is the Phase 1 convention:

The agent-instruction file at `.claude/CLAUDE.md` names Vitest as the test framework and never mentions Jest. Jest is now demonstrably the single runner with two projects and a setup file. Correct the testing section to describe the real harness: the runner, its TypeScript transformer, the two project environments, the setup file, and the actual test commands.

`README.md` states a Next.js major that is two behind and describes state management as plain React hooks when the project uses a Zustand store and a data-fetching library. Correct both.

`CLAUDE.md` documents six protected routes where the source declares eight. Read the array from the source file — which is now `src/proxy.ts`, not `src/middleware.ts` — and list all eight. While in that file, also correct any reference to the middleware file convention, since that file no longer exists.

Re-derive every claim you correct from the working tree rather than from a planning document. The Phase 1 rule holds: counts are re-derived, never transcribed. Where a document states a count, state the command that produces it.

Do not rewrite these documents beyond the false claims. A documentation refactor inside the exit-gate plan is scope the phase did not budget and noise in the diff a reviewer needs to read.
  </action>
  <acceptance_criteria>
    - `node -e` over `.planning/audit/findings.json` confirms F-051, F-052, F-053, F-056 and F-057 all have a closing phase of `02`
    - The shared-cache finding F-025 still has a status of `Open` and does not have a closing phase of `02`
    - `node .planning/audit/tools/gen-foundation-audit.mjs --check` exits 0 — the generated document matches its input
    - `node .planning/audit/tools/validate.mjs --check findings` exits 0
    - `git diff -- .planning/audit/FOUNDATION_AUDIT.md` shows changes consistent with a regeneration, and the generator was run rather than the file hand-edited
    - `.claude/CLAUDE.md` mentions Jest and no longer names Vitest as the test framework
    - `README.md` names the installed Next.js major and no longer describes state management as React hooks alone
    - `CLAUDE.md` lists all eight protected routes and contains no claim of six
    - `grep -rc 'src/middleware.ts' CLAUDE.md .claude/CLAUDE.md README.md` prints 0 for each
    - `git status --porcelain -- package.json package-lock.json src/` prints nothing
  </acceptance_criteria>
  <verify>
    <automated>node .planning/audit/tools/gen-foundation-audit.mjs --check &amp;&amp; node .planning/audit/tools/validate.mjs --check findings &amp;&amp; node -e "const fs=require('fs');const raw=JSON.parse(fs.readFileSync('.planning/audit/findings.json','utf8'));const arr=Array.isArray(raw)?raw:(raw.findings||[]);const by=Object.fromEntries(arr.map(f=>[f.id,f]));const bad=[];for(const id of ['F-051','F-052','F-053','F-056','F-057']){const f=by[id];if(!f){bad.push('missing:'+id);continue}if(String(f.closes_in_phase)!=='02')bad.push(id+':closes_in_phase='+f.closes_in_phase)}const c=by['F-025'];if(c&&(String(c.closes_in_phase)==='02'||c.status!=='Open'))bad.push('F-025-wrongly-closed:'+c.status+'/'+c.closes_in_phase);const cl=fs.readFileSync('.claude/CLAUDE.md','utf8');if(!/Jest/.test(cl))bad.push('agent-doc-omits-jest');if(/Vitest/i.test(cl)&&!/removed|deleted|no longer/i.test(cl))bad.push('agent-doc-still-names-vitest');const rd=fs.readFileSync('README.md','utf8');if(/Next\.js 14/.test(rd))bad.push('readme-stale-next-major');const src=fs.readFileSync('src/proxy.ts','utf8');const routes=((src.match(/PROTECTED_ROUTES\s*=\s*\[([^\]]*)\]/)||[])[1]||'').split(',').map(s=>s.trim().replace(/^[\"']|[\"']$/g,'')).filter(Boolean);const md=fs.readFileSync('CLAUDE.md','utf8');const missing=routes.filter(r=>!md.includes(r));if(missing.length)bad.push('claude-md-missing-routes:'+missing.join('|'));for(const f of ['CLAUDE.md','.claude/CLAUDE.md','README.md'])if(/src\/middleware\.ts/.test(fs.readFileSync(f,'utf8')))bad.push('stale-middleware-path-in:'+f);if(bad.length){console.error('reconciliation gaps: '+bad.join(', '));process.exit(1)}console.log('register reconciled and docs corrected — '+routes.length+' protected routes documented')"</automated>
  </verify>
  <done>The finding register agrees with the roadmap about which findings this phase closes, regenerated through its generator rather than hand-edited, with the shared-cache finding deliberately left Open. The three documents that asserted a dead test runner, a stale framework major, and six of eight protected routes now describe the tree that exists.</done>
</task>

<task type="auto">
  <name>Task 3: Write STAGE-2-COMPLETION.md, the exit gate note</name>
  <files>.planning/phases/02-dependency-and-runtime-stabilization/evidence/STAGE-2-COMPLETION.md</files>
  <read_first>
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Exit Gate Evidence Checklist — the eleven clauses this note must be answerable on, from artifacts alone
    - .planning/ROADMAP.md § Phase 2 success criteria — the five statements the phase is measured against
    - .planning/audit/baseline/jest.txt, lint.txt, tsc.txt, build.txt, versions.txt — the AUDIT-13 baseline every green-checks claim is quoted against
    - Every `evidence/` artifact produced by plans 02-01 through 02-10 — the note quotes them by path and cannot state a figure that is not in one of them
    - .planning/phases/02-dependency-and-runtime-stabilization/evidence/VULNERABILITY-POLICY.md — the policy the gate is judged against, including its filled exception register
    - .planning/audit/SEVERITY_SLA.md — the parent policy
  </read_first>
  <action>
Write `evidence/STAGE-2-COMPLETION.md`. The standard it has to meet: **every clause answerable from committed artifacts alone, without re-running anything.** Each claim cites the artifact path it comes from. A figure that is not in a committed file does not go in the note.

Open with the metadata line and a blockquoted statement of whether the Stage 2 exit gate is met.

The eleven clauses, each with its evidence:

**No unexplained Critical production vulnerabilities** — the critical count from the final census, with the before and after pair cited by path.

**No reachable High without a dated, owner-signed exception** — one row per remaining High from the policy's exception register, each with its named human, ISO date, expiry at most 90 days out, and reachability argument; or a statement that the count is zero.

**Reproducible install** — the clean-room capture showing install and build exiting 0 from a fresh clone on the pinned Node, cited by path, with the commit SHA it ran against.

**Checks green at or better than the AUDIT-13 baseline** — a table with six rows, each quoting the baseline and the measured value: passing tests against 220, skipped tests against 36, executing suites against 16 of 21, type diagnostics against 0, lint warnings and errors against 12 and 0, and build exit against 0. **Quote both the pass count and the skip count.** The baseline's pass count was already satisfied by a tree in which five suites never ran, which is exactly why the requirement names two numbers.

**Reviewed lockfile** — the per-batch diff line counts from the review log, plus the explicit statements that no forced remediation was run and the lockfile was never regenerated wholesale.

**CycloneDX SBOM** — committed, schema-validated, generated from the final lockfile, with the byte-stability check result.

**Update automation configured** — the configuration committed and validated, **and** the note recording whether the bot's GitHub App is actually installed. A configuration file with no bot behind it configures nothing, and the note must not imply otherwise.

**Bundle size before and after** — the two-family table cited by path, with the honest note that the batch-1 removals moved install-tree metrics and not route bundles, and why.

**The npm configuration warning** — the four-scope probe result, the emitter identification, and the CI-side answer.

**The CLI removal decision** — removal rather than relocation, with its reasoning.

**The file-convention migration before and after** — the two characterization captures, the rename diff, the smoke captures either side, the Tier 3 human confirmation, and **the stated ban-check coverage limitation**.

Then a section the checklist does not ask for and the phase needs anyway: **what this phase did not close.** Four items, each stated plainly so nothing downstream reads a silence as a pass.

The tsconfig test-file exclusion remains, so F-066 is **partially closed** — installable skips cleared, whole-program type-check hole remaining — and Phase 3 is its natural home alongside the generated types and the type-drift check.

The contract-drift suite stays skipped and its contract question is handed to Phase 4's event read path slice.

The shared-cache exposure is **not** fixed. The framework advisories that depend on it are closed by version, but Phase 1 proved the precondition is live on this deployment, and Phase 6 is what removes it.

The Supabase SDK is either shipped or deferred — state which, and if deferred, what Phase 3 inherits.

Then a requirements table: all seventeen STAB identifiers, each with its status, the artifact that evidences it, and the plan that delivered it. Any requirement not fully met is marked partially met with what remains, never quietly marked complete. The Phase 1 precedent is explicit here — withhold when the artifacts contradict the claim, not reflexively, but never claim what the artifacts do not support.

Close with the `*Phase: … *Plan: …` footer.

Finally, run the baseline comparator one last time and confirm it exits 0 against the final tree.
  </action>
  <acceptance_criteria>
    - `evidence/STAGE-2-COMPLETION.md` exists, contains `**Plan:**`, `**Phase:**`, `**Recorded:**`, a blockquoted gate statement, and ends with lines matching `^\*Phase:` and `^\*Plan:`
    - The note contains a six-row checks table quoting both a baseline and a measured value per row, including a passing-test row and a separate skipped-test row
    - The measured passing count exceeds 220 and the measured skipped count is below 36
    - The note cites at least 12 distinct artifact paths under the phase evidence directory
    - The note contains a section naming what the phase did **not** close, covering the tsconfig exclusion, the skipped contract-drift suite, the shared-cache exposure, and the Supabase SDK outcome
    - The note contains `F-025`, `F-066`, `REFAC-10`, and `REFAC-19`
    - The note contains a requirements table with all 17 `STAB-` identifiers, each with a status and an evidencing artifact
    - The note records whether the update bot's GitHub App is installed
    - The note contains the ban-check coverage limitation and names `CERT-05` as where that assertion lands
    - `node .../evidence/tools/check-baseline.mjs` exits 0
    - Every artifact path cited in the note exists on disk
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs &amp;&amp; node -e "const fs=require('fs');const d='.planning/phases/02-dependency-and-runtime-stabilization/evidence/';const n=fs.readFileSync(d+'STAGE-2-COMPLETION.md','utf8');const bad=[];for(const s of ['**Plan:**','**Phase:**','**Recorded:**','F-025','F-066','REFAC-10','REFAC-19','CERT-05'])if(!n.includes(s))bad.push('missing:'+s);if(!/^> /m.test(n))bad.push('no-blockquoted-gate-statement');if(!/^\*Phase:/m.test(n))bad.push('footer-phase');if(!/^\*Plan:/m.test(n))bad.push('footer-plan');const ids=new Set((n.match(/STAB-\d{2}/g)||[]));if(ids.size!==17)bad.push('stab-ids-covered:'+ids.size+'/17');const base=fs.readFileSync('.planning/audit/baseline/jest.txt','utf8').match(/Tests:\s+(\d+) skipped, (\d+) passed/);const bsk=Number(base[1]),bps=Number(base[2]);if(!n.includes(String(bps)))bad.push('baseline-pass-count-not-quoted');if(!n.includes(String(bsk)))bad.push('baseline-skip-count-not-quoted');if(!/skip/i.test(n))bad.push('no-skip-row');const cited=[...new Set((n.match(/evidence\/[A-Za-z0-9._-]+/g)||[]))];if(cited.length<12)bad.push('too-few-artifacts-cited:'+cited.length);const missing=cited.filter(p=>!fs.existsSync('.planning/phases/02-dependency-and-runtime-stabilization/'+p));if(missing.length)bad.push('cited-but-absent:'+missing.slice(0,3).join('|'));if(!/did not close|not closed|remaining|out of scope/i.test(n))bad.push('no-what-was-not-closed-section');if(!/App|app/.test(n))bad.push('bot-app-status-not-recorded');if(bad.length){console.error('STAB-17 gaps: '+bad.join(', '));process.exit(1)}console.log('STAB-17 OK — 17 requirements, '+cited.length+' artifacts cited, baseline '+bps+'/'+bsk+' quoted')"</automated>
  </verify>
  <done>The Stage 2 exit gate is evidenced in one note answerable from committed artifacts alone, quoting both the pass count and the skip count against the baseline, citing every artifact by path, covering all seventeen requirements with honest partial-completion where it applies, and naming the four things this phase deliberately did not close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh clone → clean-room build | The clean room is the only place the lockfile's reproducibility claim is actually tested |
| completion note → the Stage 3 gate | Phase 3 starts on the strength of what this note asserts |
| finding register → program-wide closure tracking | `closes_in_phase` drives which findings later phases still consider open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Information Disclosure | Clean-room capture carrying a credential | mitigate | The build uses the two literal placeholder values already committed in the CI workflow; `.env.local` is never created or copied; the acceptance criteria grep the capture for a JWT shape and for any reference to the environment file, and fail on either |
| T-02-11-02 | Repudiation | A completion note asserting figures nobody can verify | mitigate | Every claim cites an artifact path, the verify asserts at least twelve distinct cited paths and that **each one exists on disk**, and the baseline figures are parsed from the Phase 1 capture rather than accepted as written |
| T-02-11-03 | Repudiation | "Tests pass" standing in for the real gate | mitigate | The note is required to quote the pass count **and** the skip count against their separate baselines, and the verify rejects a note that omits either. The baseline pass count was already satisfied by a tree where five suites never ran |
| T-02-11-04 | Tampering | The generated finding register hand-edited into agreement | mitigate | `findings.json` is edited and the document regenerated through its generator; the generator's staleness check and the audit validator's findings check both run as acceptance criteria, so a hand edit fails mechanically |
| T-02-11-05 | Repudiation | Silence read as closure on the four items this phase did not finish | mitigate | A dedicated what-was-not-closed section is mandatory, covering the retained tsconfig exclusion, the skipped contract-drift suite, the live shared-cache exposure, and the SDK outcome — each named, each pointed at its owning phase |
| T-02-11-06 | Repudiation | Update automation reported as configured when no bot is installed | mitigate | The note must record the app-installation status explicitly; the verify asserts the statement is present |
| T-02-11-07 | Tampering | A masked install succeeding because of a stale local `node_modules` | mitigate | The clean room clones to a temporary directory outside the working tree, asserts the runtime against `engines` before installing, compares lockfile hashes, and uses `npm ci` rather than an install that would paper over a manifest disagreement |
| T-02-11-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed and no manifest file is touched.** The clean-room clone installs from the already-reviewed lockfile with `npm ci` in a throwaway directory; the acceptance criteria assert `package.json`, `package-lock.json`, and `src/` are unchanged by this plan |
</threat_model>

<verification>
- Clean-room `npm ci`, build, and test suite all exit 0 from a fresh clone at the final commit on the pinned Node
- The clone's lockfile hash matches the working tree's
- `node .planning/audit/tools/gen-foundation-audit.mjs --check` exits 0 and `validate.mjs --check findings` exits 0
- All five reassigned findings record a closing phase of 02; the shared-cache finding is still Open
- The three documents no longer assert the falsified claims, and every corrected count is re-derived from the tree
- `STAGE-2-COMPLETION.md` covers all 17 requirements, cites at least 12 existing artifacts, and quotes both the pass count and the skip count
- `node .../evidence/tools/check-baseline.mjs` exits 0
- `npm run lint`, `npx tsc --noEmit`, `npm test -- --ci`, `npm run build` all exit 0
- `package.json`, `package-lock.json`, and `src/` unchanged by this plan
</verification>

<success_criteria>
A fresh clone of the final commit installs from the lockfile alone and builds and tests green on the pinned Node. The finding register agrees with the roadmap, regenerated rather than hand-edited, with the shared-cache finding deliberately left open. The documentation describes the tree that exists. And the Stage 2 exit gate is evidenced in one note that a reader can check clause by clause against committed artifacts without re-running anything — including the four things this phase deliberately did not close.
</success_criteria>

<artifacts_this_phase_produces>
Contributed by this plan:

**New files**
- `evidence/cleanroom-npm-ci.txt` — STAB-12 reproducible-install evidence from a fresh clone at the final commit
- `evidence/lockfile-review-log.md` — STAB-11 per-batch diff line counts and the three attestations
- `evidence/STAGE-2-COMPLETION.md` — STAB-17 exit gate note

**Modified files**
- `.planning/audit/findings.json` — closing phase set to 02 for F-051, F-052, F-053, F-056, F-057, with per-finding resolution notes
- `.planning/audit/FOUNDATION_AUDIT.md` — **regenerated** by `gen-foundation-audit.mjs`, never hand-edited
- `.claude/CLAUDE.md` — testing section corrected from Vitest to the real Jest harness
- `README.md` — framework major and state-management description corrected
- `CLAUDE.md` — all eight protected routes listed, stale file-convention paths corrected

**New package.json scripts / fields:** none. **New CI steps:** none. **Deleted files:** none.

**Findings addressed**
- F-063 (documented facts contradicted by the working tree) — closed
- F-051, F-052, F-053, F-056, F-057 — closing phase reconciled to 02 and statuses set
- F-025 — deliberately left Open, pointed at REFAC-19 in Phase 6
- F-066 — recorded as partially closed, pointed at Phase 3

---

## Full Phase 2 artifact manifest

**New repository-root files:** `.nvmrc`, `jest.setup.ts`, `sbom.cyclonedx.json`, `renovate.json`
**New source files:** `src/proxy.ts` (renamed), `src/proxy.test.ts` (renamed), `src/middlewareRateLimit.test.ts`
**New scripts:** `scripts/smoke.sh`
**New phase tooling:** `evidence/tools/check-baseline.mjs`, `evidence/tools/majors.b0.json`
**Deleted files:** `vitest.config.ts`, `vitest.setup.ts`, `test-results/.last-run.json`, `src/components/ui/dropdown-menu.tsx`, `src/middleware.ts`, `src/middleware.test.ts`
**New package.json fields:** `engines.node`, `engines.npm`, `scripts.test`, `scripts.test:ci`
**Removed package.json entries:** `scripts.check:feedback`; nine production dependencies; two devDependencies; `tailwindcss-animate` relocated to devDependencies
**New devDependencies:** `jest-environment-jsdom`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/jest-dom`
**New CI steps:** `Run tests`; `Production vulnerability gate`; `Setup Node.js` switched to `node-version-file`
**Evidence documents:** `VULNERABILITY-POLICY.md`, `STAGE-2-COMPLETION.md`, `devdir-investigation.md`, `vercel-removal-decision.md`, `next-upgrade-note.md`, `proxy-migration-note.md`, `supabase-js-decision.md`, `skipped-suite-disposition.md`, `types-node-major-note.md`, `bundle-size.md`, `lockfile-review-log.md`, `ci-green-run.md`, `sbom-generation.txt`, `renovate-validation.txt`, plus the per-batch capture set and the audit census series
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-dependency-and-runtime-stabilization/02-11-SUMMARY.md` when done.
</output>
