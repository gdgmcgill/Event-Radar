---
phase: 02-dependency-and-runtime-stabilization
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - package.json
  - package-lock.json
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/supabase-js-decision.md
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.before.json
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4a.after.json
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.after.json
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b4.before.sha256
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-lint.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-tsc.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-jest.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-build.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/smoke.b4.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-interim.txt
autonomous: true
requirements: [STAB-07, STAB-09, STAB-11, STAB-12]

must_haves:
  truths:
    - "The API documentation route still renders after the documentation renderer is patched"
    - "No reachable High advisory remains in the production dependency tree, or each survivor has a policy-conformant exception"
    - "A clean checkout of the remediated tree installs and builds from the lockfile alone"
    - "The Supabase SDK change is separable — it is its own commit and can be reverted without unwinding the rest of the batch"
  artifacts:
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.after.json"
      provides: "The post-remediation production advisory census the exit gate is measured against"
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/supabase-js-decision.md"
      provides: "The shipped-or-deferred decision for the 35-minor SDK jump, with the gate evidence behind it"
      min_lines: 35
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-interim.txt"
      provides: "The interim does-the-remediated-tree-still-install check"
  key_links:
    - from: "package.json"
      to: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/VULNERABILITY-POLICY.md"
      via: "every version chosen here is justified against the policy's named-open-items table"
      pattern: "VULNERABILITY-POLICY"
    - from: "scripts/smoke.sh"
      to: "package.json"
      via: "smoke row 7 proves the documentation route still renders after the renderer patch"
      pattern: "/docs"
---

<objective>
Batch 4: the remaining patch and minor remediation, plus one isolated minor whose blast radius is every data call.

Four patch bumps and two transitive updates close what the removals and the framework upgrade did not. Then, as a separate sub-commit with its own gate, the Supabase SDK jumps 35 minors — not because any advisory demands it, but because carrying that much drift into Stage 3's refactor would mean every unexplained behaviour change has two possible causes instead of one.

Purpose: This is the batch that drives the production High count to its exit-gate value. The version targets are ordered after the framework upgrade deliberately: the framework pins a PostCSS version forward, and the root PostCSS devDependency bump has to be reconciled against that pin rather than fighting it. The documentation renderer patch is the second half of STAB-07 — Phase 1's reachability answer kept Redoc because it is reachable from the public documentation route, and removed Swagger UI because nothing imports it.
Output: the remediated tree, the Supabase SDK decision note, the post-remediation census, an interim clean-room check, and the batch-4 gate evidence.

**No CONTEXT.md exists for this phase** — there are no `D-NN` user decisions. Locked constraints come from `.planning/PROJECT.md` § Constraints, `.planning/REQUIREMENTS.md` § Out of Scope, and `02-RESEARCH.md` § Phase Constraints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md
@.planning/phases/02-dependency-and-runtime-stabilization/evidence/VULNERABILITY-POLICY.md
@.planning/audit/quality/dependency-report.md
</context>

<phase_locked_constraints>
1. `react` / `react-dom` untouched. **A broad update command with no package arguments will move them within their range; a bare update invocation is forbidden in this batch — every update names its packages explicitly.** 2. Forced audit remediation is forbidden; the lockfile is reconciled with `npm install --package-lock-only`, never deleted and regenerated. <!-- planner-discipline-allow: audit fix --> 3. `.env.local` is never read or written; no secret value enters `.planning/`. 4. No schema file is touched and no database operation is performed anywhere in Phase 2. 5. `shasum -a 256`, not `sha256sum`. 6. Zero-dependency tooling; one-shot `npx` at pinned versions. 7. No behaviour changes.
</phase_locked_constraints>

<batch_gate>
This plan is **batch 4**, and it is the one batch with two commits: **4a** (the patch and transitive remediation) and **4b** (the Supabase SDK alone). Each gets the full gate independently: `npm run lint` → `npx tsc --noEmit` → `npm test -- --ci` → `npm run build`, then `scripts/smoke.sh`, then the lockfile diff read as a diff. Splitting them is the point — if 4b has to be reverted, 4a survives untouched.
</batch_gate>

<majors_explicitly_excluded>
Not in this phase. Each would need its own change and its own migration note under STAB-10, and none is security-driven: the Supabase SSR helper's 0.7 to 0.12 jump, TypeScript 5 to 7, Tailwind 3 to 4, ESLint 9 to 10, the icon library's 0.344 to 1.46, date-fns 3 to 4, the Tailwind class merger 2 to 3, the image cropper 5 to 6, the OpenAPI doc generator 0.4 to 0.5, and `@types/node` beyond 24. React 18 to 19 is excluded by project constraint. `npm outdated` will list all of these; do not spend the batch on them.
</majors_explicitly_excluded>

<tasks>

<task type="auto">
  <name>Task 1: Commit 4a — patch bumps, transitive updates, and the browserslist refresh</name>
  <files>package.json, package-lock.json, .planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.before.json, .planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4a.after.json, .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b4.before.sha256</files>
  <read_first>
    - package.json — the current ranges for the documentation renderer, the HTML sanitizer, and the PostCSS devDependency
    - .planning/audit/quality/dependency-report.md §§ 1-2 — the API-doc reachability answer (which renderer stays and why) and the remaining High/Critical rows after the removals
    - .planning/phases/02-dependency-and-runtime-stabilization/evidence/VULNERABILITY-POLICY.md § Named open items — the table this task's version choices must satisfy, including the two transitive rows
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Standard Stack (Packages to UPGRADE) and § Pattern 1
    - src/lib/sanitize.ts and src/lib/sanitize.test.ts — the only first-party import path of the HTML sanitizer; the existing suite is the regression net for that bump
  </read_first>
  <action>
Capture the before state first: `shasum -a 256 package-lock.json` into `evidence/lock.b4.before.sha256`, and `npm audit --omit=dev --json --package-lock-only` into `evidence/audit.b4.before.json`.

Then raise four declared ranges, re-verifying each target against the registry immediately before setting it rather than trusting a version this plan names:

- The API documentation renderer (`redoc`) to its current patch release. This closes three High advisories in its subtree plus a moderate in its HTML sanitizer dependency. This renderer **stays** — it is reachable from the public documentation route, which is exactly why it is patched rather than removed, and it is the other half of STAB-07 that batch 1's Swagger UI removal did not cover.
- The HTML sanitizer (`sanitize-html`) to its current patch release. This is the only moderate advisory sitting on a first-party import path.
- The PostCSS devDependency to its current patch release. Reconcile against whatever version the framework upgrade in batch 2 already pinned — the ordering of these two batches exists for this reason. The bump also lifts a nested ID generator past its vulnerable ceiling.
- Update two transitive packages by name only: the WebSocket library and the glob matcher. Both have in-range patched versions available, so neither needs a lockfile override. Name them explicitly on the update command — **a bare update with no package arguments would move React within its range and is forbidden.**

Then refresh the browser-compatibility database with a one-shot pinned `npx` invocation of the browserslist updater. It writes to the lockfile, so review that part of the diff like any other. The build currently prints a stale-database warning; this removes it.

Reconcile once with `npm install --package-lock-only` after the manifest edits, read `git --no-pager diff --stat package-lock.json` and `git --no-pager diff package.json`, then `npm ci`.

Assert React did not move on both the manifest and the lockfile side before proceeding.

Capture the post-4a census into `evidence/audit.b4a.after.json`, then run the full gate. If any production High survives, do not force it — record it for the exception register that plan 02-09 fills. A High with a policy-conformant, dated, owner-signed exception carrying a reachability argument is a passing state; a High that was crushed with a forced remediation command is a failed phase.

Commit 4a on its own.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');process.exit((p.dependencies.redoc!=='^2.5.2'&&p.dependencies['sanitize-html']!=='^2.17.1'&&p.devDependencies.postcss!=='^8.4.35')?0:1)"` exits 0 — all three declared ranges moved
    - `npm ls redoc --omit=dev` resolves to a version at or above 2.5.4
    - `npm ls sanitize-html --omit=dev` resolves to a version at or above 2.17.7
    - `npm ls postcss` resolves to a version at or above 8.5.28
    - `npm ls ws` resolves to a version at or above 8.21.0
    - `npm ls picomatch` resolves every instance to a version at or above 2.3.2
    - `node -e "const p=require('./package.json');process.exit((p.dependencies.react==='^18.3.0'&&p.dependencies['react-dom']==='^18.3.0')?0:1)"` exits 0, and the resolved React versions in the lockfile are unchanged from the batch-3 commit
    - `npx jest --ci src/lib/sanitize.test.ts` exits 0 — the sanitizer bump did not change its observable behaviour
    - `evidence/audit.b4a.after.json` reports 0 critical, and its high count is no greater than the count in `evidence/audit.b4.before.json`
    - `npm ci`, `npx tsc --noEmit`, and `npm run build` all exit 0
    - `git --no-pager diff --stat package-lock.json` reports an unchanged `lockfileVersion`
    - `npm run build` output contains no stale-browser-database warning
  </acceptance_criteria>
  <verify>
    <automated>npm ci &amp;&amp; npx tsc --noEmit &amp;&amp; npx jest --ci src/lib/sanitize.test.ts &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('fs');const cp=require('child_process');const p=require('./package.json');const bad=[];const ver=(n)=>{try{return require(n+'/package.json').version}catch(e){return null}};const ge=(v,t)=>{if(!v)return false;const a=v.split('.').map(Number),b=t.split('.').map(Number);for(let i=0;i<3;i++){if((a[i]||0)>(b[i]||0))return true;if((a[i]||0)<(b[i]||0))return false}return true};const checks={redoc:'2.5.4','sanitize-html':'2.17.7',postcss:'8.5.28',ws:'8.21.0',picomatch:'2.3.2'};for(const[n,t]of Object.entries(checks)){const v=ver(n);if(!ge(v,t))bad.push(n+':'+v+'<'+t)}if(p.dependencies.react!=='^18.3.0')bad.push('react-range-moved');if(p.dependencies['react-dom']!=='^18.3.0')bad.push('react-dom-range-moved');const a=JSON.parse(fs.readFileSync('.planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4a.after.json','utf8')).metadata.vulnerabilities;const b=JSON.parse(fs.readFileSync('.planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.before.json','utf8')).metadata.vulnerabilities;if(a.critical!==0)bad.push('critical-remains:'+a.critical);if(a.high>b.high)bad.push('high-increased:'+b.high+'->'+a.high);if(bad.length){console.error('batch-4a gaps: '+bad.join(', '));process.exit(1)}console.log('batch-4a OK — prod high '+b.high+' -> '+a.high)"</automated>
  </verify>
  <done>Four declared ranges and two transitive packages are on their patched versions, the browser database is current, React is provably unmoved on both sides, the sanitizer's own suite still passes, and the production Critical count is zero with the High count at or below where the batch started.</done>
</task>

<task type="auto">
  <name>Task 2: Commit 4b — the Supabase SDK minor, alone, with an explicit defer rule</name>
  <files>package.json, package-lock.json, .planning/phases/02-dependency-and-runtime-stabilization/evidence/supabase-js-decision.md</files>
  <read_first>
    - package.json — the Supabase SDK range is `^2.49.0` and resolves 35 minors behind the current release; the SSR helper stays on its current major and is not touched here
    - src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/service.ts — the three client factories the whole application routes through; the blast radius of this bump is every data call in the project
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Upgrade Batching Order batch 4b and assumption A6 — this is recorded as the highest residual risk in the phase, and the defer rule is part of the recommendation, not a fallback
    - .planning/audit/baseline/test-runner-decision.md — the decision-note format
  </read_first>
  <action>
This sub-commit is separable by design. It closes no advisory of its own. It is here because 35 minors of drift carried into Stage 3 would mean every unexplained behaviour change in the refactor has two candidate causes instead of one. That is a real benefit and also a weak one, which is why the defer rule below is a genuine option and not a face-saving escape hatch.

Re-verify the current SDK version against the registry, then raise only the `@supabase/supabase-js` range. Do **not** touch `@supabase/ssr` — its 0.7 to 0.12 move is a major and is out of scope for this phase.

Reconcile with `npm install --package-lock-only`, read the diff, then `npm ci`.

Run the full gate: lint, type-check, the whole test suite, build, and `scripts/smoke.sh`. Pay particular attention to the smoke rows that exercise real data — the event list endpoint and the health endpoint — because those are where an SDK behaviour change would first show.

**The defer rule, stated up front so it is a decision and not a retreat:** if the gate is ambiguous in any way — a test that passes but slower and differently, a build warning that was not there before, a smoke row whose response shape changed, a type error that needs a cast to silence — **revert this sub-commit and defer the SDK bump to Phase 3.** It closes no advisory, so deferring costs nothing at the exit gate, and shipping an ambiguous data-layer change into the phase whose entire purpose is provable behaviour preservation costs a great deal. A type error resolved by widening a signature or adding a cast is the specific warning sign: that is a behaviour change smuggled in as a type fix.

Write `evidence/supabase-js-decision.md` either way, and write it with the same care in both branches. Metadata line, blockquoted decision up front — shipped at version X, or deferred to Phase 3 with the ambiguity named. Then: the version before and after; the reason the bump is not security-driven; the gate results item by item; which smoke rows exercised real data and what they returned; and the residual risk statement, that no changelog across the 35 minors was read and the confidence rests entirely on the gate. If deferred, state precisely what was ambiguous and what Phase 3 needs to resolve it. Close with the `*Phase: … *Plan: …` footer.
  </action>
  <acceptance_criteria>
    - `evidence/supabase-js-decision.md` exists, contains `**Plan:**`, `**Phase:**`, `**Recorded:**`, a line beginning `> **Decision:`, and ends with lines matching `^\*Phase:` and `^\*Plan:`
    - The note records the SDK version before and after, or before and deferred
    - The note contains a residual-risk statement naming that no changelog was read across the version span
    - `node -e "const p=require('./package.json');process.exit(p.dependencies['@supabase/ssr']==='^0.7.0'?0:1)"` exits 0 — the SSR helper was not touched
    - If shipped: `npm ls @supabase/supabase-js` resolves to the verified target, and the commit containing that change touches only `package.json` and `package-lock.json`
    - If deferred: `git status --porcelain -- package.json package-lock.json` prints nothing and the note names the specific ambiguity
    - Either way: `npm run lint`, `npx tsc --noEmit`, `npm test -- --ci`, and `npm run build` all exit 0
    - Either way: `git diff -- src/` is empty — no source file was edited to make the SDK bump type-check
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --ci &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('fs');const cp=require('child_process');const p=require('./package.json');const n=fs.readFileSync('.planning/phases/02-dependency-and-runtime-stabilization/evidence/supabase-js-decision.md','utf8');const bad=[];for(const s of ['**Plan:**','**Phase:**','**Recorded:**'])if(!n.includes(s))bad.push('note-missing:'+s);if(!/^> \*\*Decision:/m.test(n))bad.push('no-blockquoted-decision');if(!/^\*Phase:/m.test(n))bad.push('footer-phase');if(!/changelog/i.test(n))bad.push('no-residual-risk-statement');if(p.dependencies['@supabase/ssr']!=='^0.7.0')bad.push('ssr-helper-touched:'+p.dependencies['@supabase/ssr']);const shipped=p.dependencies['@supabase/supabase-js']!=='^2.49.0';const deferred=/defer/i.test(n);if(!shipped&&!deferred)bad.push('neither-shipped-nor-deferred');const srcDiff=cp.execSync('git diff --name-only -- src/ || true',{encoding:'utf8'}).trim();if(srcDiff)bad.push('src-edited-for-sdk:'+srcDiff.split('\n')[0]);if(bad.length){console.error('batch-4b gaps: '+bad.join(', '));process.exit(1)}console.log('batch-4b OK — '+(shipped?'shipped '+p.dependencies['@supabase/supabase-js']:'deferred to Phase 3'))"</automated>
  </verify>
  <done>The SDK jump is either shipped as its own revertible commit behind a full gate, or deferred to Phase 3 with the specific ambiguity named — and in both cases the decision, the gate results, and the unread-changelog residual risk are written down. No source file was edited to make it type-check.</done>
</task>

<task type="auto">
  <name>Task 3: Run the batch-4 gate, capture the post-remediation census, and do the interim clean-room check</name>
  <files>.planning/phases/02-dependency-and-runtime-stabilization/evidence/audit.b4.after.json, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-lint.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-tsc.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-jest.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-04-build.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/smoke.b4.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/cleanroom-interim.txt</files>
  <read_first>
    - .planning/audit/baseline/build.txt — the raw-capture-plus-provenance format
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Clean-Room Protocol — the seven-step protocol; this task runs it the first of its two times, as the does-the-remediated-tree-still-install check
    - .planning/phases/02-dependency-and-runtime-stabilization/evidence/VULNERABILITY-POLICY.md — the policy the surviving advisories are judged against
    - scripts/smoke.sh — row 7 is the documentation route, which is the STAB-07 proof that patching rather than removing the renderer was correct
  </read_first>
  <action>
Capture the four gate commands verbatim into the `batch-04-*.txt` files with provenance blocks and `exit_code=` lines. Delete `.next` before the build capture.

Run `scripts/smoke.sh` against a local development server and capture to `evidence/smoke.b4.txt`. Row 7 is load-bearing in this batch: the documentation route must still render after the renderer patch, which simultaneously confirms that removing Swagger UI in batch 1 changed nothing about it. Rows 5, 6, 8 and 10 must still pass.

Capture the post-remediation census into `evidence/audit.b4.after.json` with `--omit=dev --json --package-lock-only`. This is the census the exit gate is measured against, so it must be taken after both sub-commits have landed or after 4b has been reverted — not in between.

Then run the interim clean-room check and capture everything, including each step's exit code, into `evidence/cleanroom-interim.txt`. Follow the protocol properly: clone the current branch into a temporary directory rather than reusing the working tree, because a local `node_modules` can mask a dependency that was removed from the manifest but is still resolvable on disk. Assert the runtime versions satisfy the `engines` block before installing. Compare the clean clone's lockfile hash against the working tree's using `shasum -a 256`. Install with `npm ci`, never `npm install` — `npm ci` failing loudly when the manifest and lockfile disagree is the reproducibility property being tested. Build with the same two placeholder Supabase environment values the CI workflow already sets, so the clean-room run and the CI run measure the same thing. Then run the test suite. Never copy or create `.env.local`.

This is the interim run. Plan 02-11 runs it again at the final commit as the STAB-12 evidence; both runs exist because a tree that installs after batch 4 can still stop installing after batch 5's four new packages.

Finally, confirm `node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs` exits 0.
  </action>
  <acceptance_criteria>
    - All four `batch-04-*.txt` files exist with provenance blocks and end with `exit_code=0`
    - `evidence/batch-04-jest.txt` reports at least 220 passing and 0 failing
    - `evidence/batch-04-lint.txt` reports 0 errors
    - `evidence/audit.b4.after.json` parses and reports `metadata.vulnerabilities.critical` equal to 0
    - Every remaining High in `evidence/audit.b4.after.json` is listed by name in the task summary, for the exception register plan 02-09 fills
    - `evidence/smoke.b4.txt` shows PASS for the `/docs` row, and for the `/profile`, `/my-events`, rate-limit, and `/_next/static` rows
    - `evidence/cleanroom-interim.txt` records a `npm ci` step with exit 0 and a build step with exit 0, both from a path outside this working tree
    - `evidence/cleanroom-interim.txt` records the runtime version check against `engines` and a lockfile hash comparison that matched
    - `evidence/cleanroom-interim.txt` contains no string matching the JWT shape `eyJ[A-Za-z0-9_-]{8,}\.`
    - `node .../evidence/tools/check-baseline.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs &amp;&amp; node -e "const fs=require('fs');const d='.planning/phases/02-dependency-and-runtime-stabilization/evidence/';const bad=[];for(const f of ['batch-04-lint.txt','batch-04-tsc.txt','batch-04-jest.txt','batch-04-build.txt']){const t=fs.readFileSync(d+f,'utf8');if(!/^exit_code=0$/m.test(t))bad.push(f+':exit')}const j=fs.readFileSync(d+'batch-04-jest.txt','utf8');const m=j.match(/Tests:\s+(?:\d+ skipped, )?(\d+) passed/);if(!m||Number(m[1])<220)bad.push('jest-regression');if(/\d+ failed/.test(j))bad.push('jest-failures');const a=JSON.parse(fs.readFileSync(d+'audit.b4.after.json','utf8')).metadata.vulnerabilities;if(a.critical!==0)bad.push('critical-remains:'+a.critical);const s=fs.readFileSync(d+'smoke.b4.txt','utf8');for(const row of ['/docs','/profile','/my-events','_next/static'])if(!s.includes(row))bad.push('smoke-row-missing:'+row);const c=fs.readFileSync(d+'cleanroom-interim.txt','utf8');if(!/npm ci/.test(c))bad.push('cleanroom-no-npm-ci');if((c.match(/exit(_code)?[= ]0/g)||[]).length<2)bad.push('cleanroom-exit-codes-missing');if(/eyJ[A-Za-z0-9_-]{8,}\./.test(c))bad.push('SECRET-LEAK-in-cleanroom-capture');if(bad.length){console.error('batch-4 gate gaps: '+bad.join(', '));process.exit(1)}console.log('batch-4 gate OK — prod critical 0, high '+a.high)"</automated>
  </verify>
  <done>The batch-4 gate is captured, the documentation route still renders after the renderer patch, the post-remediation census is recorded with zero Criticals and every surviving High named for the exception register, and a fresh clone of the remediated tree installs and builds from the lockfile alone with no secret in the capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | Six package versions move, four declared and two transitive |
| Supabase SDK → every data call | The three client factories route the entire application through this package |
| public documentation route → the renderer | The patched renderer serves an anonymous public route |
| fresh clone → clean-room build | The clean room proves the lockfile alone reproduces the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | React moved by a package-argument-free update command | mitigate | Phase constraint 1 forbids a bare update invocation in this batch; every update names its packages. Both manifest ranges and resolved lockfile versions are asserted after the reconcile |
| T-02-07-02 | Tampering | Supabase SDK behaviour change across 35 unread minors | mitigate | Isolated as its own revertible sub-commit with an independent gate and an explicit defer rule; the residual risk is recorded rather than hidden. A type error resolved by a cast is named as the warning sign, and the acceptance criteria assert no source file was edited |
| T-02-07-03 | Information Disclosure | Cross-site scripting through the patched HTML sanitizer on a first-party import path | mitigate | The sanitizer's existing suite is run as a dedicated acceptance criterion, not just as part of the full run, so a behaviour change on that specific path is visible |
| T-02-07-04 | Information Disclosure | Documentation route breaking or changing what it publishes after the renderer patch | mitigate | Smoke row 7 asserts the route still renders. Note the boundary: gating that route behind authentication is F-054 and belongs to Phase 5 — this batch preserves its current public behaviour and does not change it |
| T-02-07-05 | Tampering | An unreviewable lockfile from six simultaneous version moves | mitigate | One reconcile for 4a's edits, a separate one for 4b, and `git diff --stat` read before each install; the browserslist refresh's lockfile writes are reviewed like any other |
| T-02-07-06 | Repudiation | A High crushed by a forced remediation command rather than fixed or excepted | mitigate | Forbidden by phase constraint 2; the task instruction is explicit that a surviving High goes to the exception register, and plan 02-09 requires a named human, an ISO date, an expiry at most 90 days out, and a reachability argument |
| T-02-07-07 | Information Disclosure | Clean-room capture carrying a credential | mitigate | Build with the two placeholder Supabase values the CI workflow already sets; `.env.local` is never copied or created; the acceptance criteria grep the capture for a JWT shape and fail on a match |
| T-02-07-SC | Tampering | npm/pip/cargo installs | mitigate | Six version moves on packages already in the tree; **no new package name is introduced.** `02-RESEARCH.md` § Package Legitimacy Audit approves the documentation renderer, the sanitizer, PostCSS and the Supabase SDK rows — all already direct dependencies with first-party source repositories, 0 SLOP and 0 genuine SUS — so no blocking human checkpoint is warranted |
</threat_model>

<verification>
- `npm run lint` exits 0 with 0 errors and at most 12 warnings
- `npx tsc --noEmit` exits 0 with no output
- `npm test -- --ci` exits 0 with at least 220 passing and 0 failing
- `npm run build` exits 0 with no stale-browser-database warning
- `scripts/smoke.sh` passes rows 5, 6, 7, 8 and 10
- `evidence/audit.b4.after.json` reports zero production Criticals; every surviving High is named
- A fresh clone installs with `npm ci` and builds, both exit 0, on the pinned runtime
- `react`, `react-dom`, and `@supabase/ssr` are unchanged
- Both lockfile diffs were read as diffs with their line counts recorded in the respective commit bodies
</verification>

<success_criteria>
The remaining patch and transitive remediation has landed, the documentation renderer is patched and its route still renders, the production Critical count is zero and every surviving High is named for the exception register, and the Supabase SDK is either shipped as a separable commit behind a full gate or deferred with its ambiguity written down. A fresh clone of the remediated tree installs and builds from the lockfile alone. No forced remediation was used and React never moved.
</success_criteria>

<artifacts_this_phase_produces>
Contributed by this plan:

**New files**
- `evidence/supabase-js-decision.md` — the shipped-or-deferred record for the SDK minor
- `evidence/audit.b4.before.json`, `evidence/audit.b4a.after.json`, `evidence/audit.b4.after.json`
- `evidence/lock.b4.before.sha256`
- `evidence/batch-04-lint.txt`, `evidence/batch-04-tsc.txt`, `evidence/batch-04-jest.txt`, `evidence/batch-04-build.txt`
- `evidence/smoke.b4.txt`
- `evidence/cleanroom-interim.txt` — the first of the two clean-room runs

**Changed `package.json` entries**
- `dependencies.redoc`, `dependencies.sanitize-html`, `devDependencies.postcss` → current patch releases
- `dependencies.@supabase/supabase-js` → the verified minor, **or unchanged if deferred**
- Transitive-only: the WebSocket library and the glob matcher lifted in-range; the browser-compatibility database refreshed

**Deleted files:** none. **New CI steps:** none.

**Findings addressed**
- F-053's remaining half (API documentation renderer disposition per the AUDIT-12 reachability answer) — closed
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-dependency-and-runtime-stabilization/02-07-SUMMARY.md` when done.
</output>
