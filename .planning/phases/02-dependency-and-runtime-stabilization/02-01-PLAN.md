---
phase: 02-dependency-and-runtime-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - .github/workflows/ci.yml
  - tsconfig.json
  - .gitignore
  - vitest.config.ts
  - vitest.setup.ts
  - test-results/.last-run.json
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/types-node-major-note.md
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-lint.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-tsc.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-jest.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-build.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b0.before.sha256
autonomous: true
requirements: [STAB-01, STAB-02, STAB-08, STAB-10, STAB-13]
user_setup:
  - service: vercel
    why: "STAB-01 — the Vercel project's Node.js Version dashboard setting is separate state from engines.node. RESEARCH assumption A1 records it as unobserved."
    dashboard_config:
      - task: "Read (and optionally set to 24.x) the Node.js Version setting"
        location: "Vercel Dashboard -> Project (uni-verse) -> Settings -> General -> Node.js Version"

must_haves:
  truths:
    - "A developer running `npm test` gets the Jest suite; the command exists and exits 0"
    - "CI runs the Jest suite on every pull request, on the same Node major the repo pins"
    - "The repo declares one Node version and three consumers (engines, .nvmrc, CI) read it without disagreeing"
    - "No Vitest or Playwright residue remains tracked in the repository"
  artifacts:
    - path: ".nvmrc"
      provides: "Single source of truth for the Node major"
      contains: "24"
    - path: "package.json"
      provides: "engines pin, test scripts, @types/node aligned to Node 24"
      contains: "\"engines\""
    - path: ".github/workflows/ci.yml"
      provides: "CI reads .nvmrc and runs the test suite"
      contains: "node-version-file"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".nvmrc"
      via: "actions/setup-node node-version-file input"
      pattern: "node-version-file:\\s*'?\\.nvmrc'?"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "the Run tests step invokes the npm test script"
      pattern: "run:\\s*npm test"
---

<objective>
Batch 0a of the Phase 2 upgrade sequence: lay the toolchain floor so every later batch has an automated net under it.

Pin Node 24 in three places that cannot drift, add the `test` and `test:ci` scripts that do not exist today, make CI actually run the suite, and delete the tracked residue of two test runners this project does not use.

Purpose: Nothing downstream is safe without this. `npm test` errors `Missing script: "test"` today, so CI's lint/type-check/build gate has never once executed the 220 passing tests (F-065). Vercel disables Node 20 on 2026-10-01 and CI is pinned to Node 20. `@testing-library/jest-dom@7` (batch 5) declares `engines.node >= 22`, so the Node pin is a hard prerequisite of the test-harness install, not a cosmetic tidy-up.
Output: `.nvmrc`, `engines`, `test`/`test:ci` scripts, a CI test step, `@types/node@^24`, the deletion of `vitest.config.ts` / `vitest.setup.ts` / `test-results/.last-run.json`, and the batch-0 gate evidence.

**No CONTEXT.md exists for this phase** — `/gsd-discuss-phase` was not run, so there are no `D-NN` user decisions to honour. The locked constraints for every Phase 2 plan are transcribed from `.planning/PROJECT.md` § Constraints, `.planning/REQUIREMENTS.md` § Out of Scope, and `02-RESEARCH.md` § Phase Constraints, and carry the same authority.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md
@.planning/audit/baseline/versions.txt
</context>

<phase_locked_constraints>
These bind every task in every Phase 2 plan. Violating one fails the phase, not the task.

1. `react` and `react-dom` are never touched. React 19 is deferred by project constraint.
2. `npm audit fix --force` is never run. `package-lock.json` is reconciled with `npm install --package-lock-only`, never deleted and regenerated. <!-- planner-discipline-allow: audit fix -->
3. `.env.local` is never read, written, or copied. Secret *values* never enter `.planning/` — names and counts only, per the Phase 1 redaction convention in `.planning/audit/REDACTION.md`.
4. `supabase/migrations/` is never replayed against any environment and no `supabase db push` is run. 41 of 101 live production policies are declared by no migration (STATE.md); a reset would drop them. **This phase touches no schema file and performs no database operation of any kind.**
5. `shasum -a 256` is the hashing command on this toolchain. `sha256sum` does not exist here, despite RESEARCH.md's snippets using it (`02-PATTERNS.md` § Lockfile reconciliation).
6. Zero-dependency tooling rule: no tool written or used by this phase may be added to `package.json`. Phase 2 measures the dependency tree; a tool that enlarges it corrupts its own measurement. Use one-shot `npx --yes <pkg>@<pinned-version>`.
7. No behaviour changes. Every fail-open guard, `getSession()` call, cache header, and missing env var found in Phase 1 stays exactly as it is — those are Phase 5 and Phase 6 work. Changing behaviour here makes "same as before" unprovable, which is the one property this phase exists to establish.
</phase_locked_constraints>

<batch_gate>
This plan is **batch 0a**. The batch gate below runs at the end of this plan and at the end of every subsequent Phase 2 plan. It is STAB-09's "followed by lint, type-check, test, build, and a smoke pass", made concrete.

Commands, in order, all four must exit 0:
`npm run lint` → `npx tsc --noEmit` → `npm test -- --ci` → `npm run build`

Then: `git --no-pager diff --stat package-lock.json` is read by a human before anything is installed, and the line count recorded.

**Commit discipline (STAB-09/STAB-11).** A batch's manifest + lockfile change lands in exactly **one** commit. Evidence-capture commits that touch only `.planning/` do not count against that and may be separate. Label every batch commit body with the batch id, the manifest lines changed, the `package-lock.json` diff line count, and the four gate results.
</batch_gate>

<tasks>

<task type="auto">
  <name>Task 1: Pin Node 24 across engines, .nvmrc, CI, and @types/node</name>
  <files>package.json, package-lock.json, .nvmrc, .github/workflows/ci.yml, .planning/phases/02-dependency-and-runtime-stabilization/evidence/types-node-major-note.md, .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b0.before.sha256</files>
  <read_first>
    - package.json — current manifest; note there is no `engines` block at all today and `@types/node` is `^20.11.0`
    - .github/workflows/ci.yml — current workflow; `actions/setup-node@v4` is pinned to `node-version: 20`
    - .planning/audit/baseline/versions.txt — `node_version=24.16.0`, `npm_version=11.13.0`, `ci_node_major=20`; this file is the only source of expected counts in this program
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Code Examples 4 (Node/npm pinning) and § Recommendation N-1 rationale
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § `.github/workflows/ci.yml` (step naming convention) and § No Analog Found (`.nvmrc`, `engines`)
    - .planning/audit/baseline/test-runner-decision.md — the metadata-line + blockquoted-decision-up-front format the new note must copy
  </read_first>
  <action>
Create `.nvmrc` at the repository root containing exactly the two characters `24` followed by a newline. No `v` prefix, no patch version — fnm is the local version manager and reads this file, and `actions/setup-node` resolves a bare major to the latest 24.x.

Add an `engines` block to `package.json` with `node` set to `24.x` and `npm` set to `>=11`. `24.x` (not `>=24`) is the form Vercel maps to the latest 24.x release line, and `engines.node` overrides the Vercel project dashboard's Node.js Version setting, which makes the repository the single source of truth. `npm` is a floor rather than a pin because npm ships with Node; the local toolchain is 11.13.0.

Raise `@types/node` from `^20.11.0` to `^24.13.4` in `devDependencies`. This is the only major-version bump in the entire phase and it is forced by the Node pin, not chosen — Node 24 runtime types and Node 20 `@types` disagreeing is exactly the drift the pin exists to remove.

Follow the batch protocol for the manifest edit: record `shasum -a 256 package-lock.json` into `evidence/lock.b0.before.sha256` first, edit `package.json` (use `npm pkg set` for the scoped key so the JSON stays canonical), then `npm install --package-lock-only` to reconcile the lockfile without materialising `node_modules`, then read `git --no-pager diff --stat package-lock.json`, then `npm ci`. Never a bare `npm install`.

<!-- planner-discipline-allow: node-version: 20 -->
In `.github/workflows/ci.yml`, replace the `node-version: 20` input of the `Setup Node.js` step with `node-version-file: '.nvmrc'`, keeping `cache: npm` and `actions/setup-node@v4` unchanged. This removes the third place the version can disagree. Change nothing else in the workflow in this task — the test step is Task 2 and the `npm audit` step is deliberately last in the phase.

Write `evidence/types-node-major-note.md` as the STAB-10 migration note for the `@types/node` major. It carries the `**Plan:** · **Phase:** · **Recorded:**` metadata line, a blockquoted decision stated up front, the reason the major is forced rather than elected, the `npx tsc --noEmit` diagnostic count before and after the bump (baseline is 0 — see `.planning/audit/baseline/tsc.txt`), and the statement that this is the only major landing in Phase 2. Close with the `*Phase: … *Plan: …` footer convention.
  </action>
  <acceptance_criteria>
    - `.nvmrc` exists and `cat .nvmrc` prints `24`
    - `node -e "const e=require('./package.json').engines; process.exit(e && e.node === '24.x' && typeof e.npm === 'string' ? 0 : 1)"` exits 0
    - `node -e "const d=require('./package.json').devDependencies; process.exit(d['@types/node'].startsWith('^24') ? 0 : 1)"` exits 0
    - `node -e "const fs=require('fs'); const n=fs.readFileSync('.nvmrc','utf8').trim(); const e=require('./package.json').engines.node; process.exit(e.startsWith(n) ? 0 : 1)"` exits 0 — engines and .nvmrc agree
    - `grep -q "node-version-file: '.nvmrc'" .github/workflows/ci.yml` exits 0
    - `grep -c 'node-version: 20' .github/workflows/ci.yml` prints 0
    - `npx tsc --noEmit` exits 0 with no output, matching the `.planning/audit/baseline/tsc.txt` baseline of zero diagnostics
    - `npm ci` exits 0
    - `git --no-pager diff --stat package-lock.json` reports fewer than 2000 changed lines and `lockfileVersion` is unchanged
    - `git diff -- package.json | grep -cE '^[+-].*"react(-dom)?"'` prints 0 — React untouched
    - `evidence/types-node-major-note.md` contains the strings `**Plan:**`, `**Phase:**`, `**Recorded:**` and a line beginning `> **Decision:`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('./package.json');const n=fs.readFileSync('.nvmrc','utf8').trim();const ci=fs.readFileSync('.github/workflows/ci.yml','utf8');const ok = p.engines && p.engines.node==='24.x' && p.engines.npm && n==='24' && p.engines.node.startsWith(n) && p.devDependencies['@types/node'].startsWith('^24') && /node-version-file:\s*'\.nvmrc'/.test(ci) && !/node-version:\s*20/.test(ci) && p.dependencies.react==='^18.3.0' && p.dependencies['react-dom']==='^18.3.0'; if(!ok){console.error('STAB-01 pin assertions failed');process.exit(1)} console.log('STAB-01 OK')" &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`.nvmrc`, `package.json` `engines`, and `.github/workflows/ci.yml` all name Node 24 and cannot disagree because CI reads `.nvmrc`; `@types/node` is on ^24 with `tsc` still at zero diagnostics; the STAB-10 migration note for that major is written; React is byte-identical in the manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Add the test scripts, wire CI to run them, and delete the two-runner residue</name>
  <files>package.json, .github/workflows/ci.yml, tsconfig.json, .gitignore, vitest.config.ts, vitest.setup.ts, test-results/.last-run.json</files>
  <read_first>
    - package.json — `scripts` today has no `test` entry and has a `check:feedback` entry pointing at `scripts/check-feedback-loop.mjs`
    - vitest.config.ts and vitest.setup.ts — confirm for yourself they reference packages absent from package.json, package-lock.json and node_modules before deleting them
    - tsconfig.json — the `exclude` array currently lists `vitest.config.ts`, which only exists to keep `tsc` green over an orphan
    - .gitignore — `test-results/` is not listed; `test-results/.last-run.json` is tracked
    - jest.config.js — the live config; do not modify it in this plan, it is rewritten as a two-project config in batch 5
    - .planning/audit/baseline/test-runner-decision.md — the recorded Keep-Jest decision and the 14-to-0 mock-call evidence; this task implements it, it does not re-litigate it
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Code Examples 5 (CI workflow final shape and the scripts block)
  </read_first>
  <action>
<!-- planner-discipline-allow: passWithNoTests -->
Add two scripts to `package.json`: `test` set to `jest`, and `test:ci` set to `jest --ci`. Do **not** add `--passWithNoTests`. Jest 30 exits 1 when it finds no tests, and that is the behaviour worth keeping: a config error that makes Jest match nothing would otherwise report green in CI.

Delete the `check:feedback` script. Its target `scripts/check-feedback-loop.mjs` does not exist — `scripts/` holds exactly three `.ts` files. This is half of F-064.

Delete three tracked files: `vitest.config.ts`, `vitest.setup.ts`, and `test-results/.last-run.json`. The two Vitest files reference three packages present in neither `package.json` nor `package-lock.json` nor `node_modules`; the Playwright last-run file asserts `{"status":"failed"}` from an installation that does not exist. This is STAB-08's "Vitest orphans deleted" clause plus the rest of F-064.

Remove the now-dangling `"vitest.config.ts"` entry from the `exclude` array in `tsconfig.json`. Leave every other exclude entry alone — in particular `**/*.test.ts` and `**/*.test.tsx` stay excluded. Removing those is F-066's validation criterion, it produces 86 errors across 10 files, and it is explicitly **out of scope for Phase 2** (RESEARCH Open Question 3). The completion note in plan 02-11 records F-066 as partially closed rather than claimed.

Add a `test-results/` line to `.gitignore` under the existing `# testing` section, next to `/coverage`.

In `.github/workflows/ci.yml`, insert a `Run tests` step that runs `npm test` between the `TypeScript type-check` step and the `Run build` step. Use the existing `- name: <Sentence case>` + `run:` step convention. <!-- planner-discipline-allow: audit-level=high -->
Do **not** add the `npm audit --audit-level=high --omit=dev` step here — it exits 1 on this tree today and would red-light every pull request from now through batch 5; it lands last, in plan 02-09.
  </action>
  <acceptance_criteria>
    - `npm test -- --ci` exits 0 and its summary reports at least 220 passing tests and 0 failing
    - `node -e "const s=require('./package.json').scripts; process.exit(s.test==='jest' && s['test:ci']==='jest --ci' && !('check:feedback' in s) ? 0 : 1)"` exits 0
    - `node -e "process.exit(/--passWithNoTests/.test(JSON.stringify(require('./package.json').scripts)) ? 1 : 0)"` exits 0
    - `test ! -e vitest.config.ts && test ! -e vitest.setup.ts && test ! -e test-results/.last-run.json` exits 0
    - `git ls-files test-results vitest.config.ts vitest.setup.ts` prints nothing
    - `node -e "process.exit(require('./tsconfig.json').exclude.includes('vitest.config.ts') ? 1 : 0)"` exits 0
    - `node -e "const e=require('./tsconfig.json').exclude; process.exit(e.includes('**/*.test.ts') && e.includes('**/*.test.tsx') ? 0 : 1)"` exits 0 — the test-file exclusion is deliberately retained
    - `grep -q 'test-results/' .gitignore` exits 0
    - `grep -A1 'name: Run tests' .github/workflows/ci.yml | grep -q 'run: npm test'` exits 0
    - `grep -c 'audit-level=high' .github/workflows/ci.yml` prints 0 — the vulnerability gate is not added in this batch
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --ci &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs');const s=require('./package.json').scripts;const t=require('./tsconfig.json');const ci=fs.readFileSync('.github/workflows/ci.yml','utf8');const gi=fs.readFileSync('.gitignore','utf8');const gone=['vitest.config.ts','vitest.setup.ts','test-results/.last-run.json'].every(f=>!fs.existsSync(f));const ok = s.test==='jest' &amp;&amp; s['test:ci']==='jest --ci' &amp;&amp; !('check:feedback' in s) &amp;&amp; gone &amp;&amp; !t.exclude.includes('vitest.config.ts') &amp;&amp; t.exclude.includes('**/*.test.ts') &amp;&amp; /test-results\//.test(gi) &amp;&amp; /name: Run tests[\s\S]{0,60}run: npm test/.test(ci) &amp;&amp; !/audit-level=high/.test(ci); if(!ok){console.error('STAB-08 batch-0 assertions failed');process.exit(1)} console.log('STAB-08 batch-0 OK')"</automated>
  </verify>
  <done>`npm test` exists and runs Jest; CI runs it on every pull request on Node 24; the Vitest and Playwright residue is gone from the working tree and from git; the tsconfig test-file exclusion is untouched and its out-of-scope status is deliberate.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the batch-0 gate evidence and confirm the CI npm config surface</name>
  <files>.planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-lint.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-tsc.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-jest.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-00-build.txt</files>
  <read_first>
    - .planning/audit/baseline/build.txt — the raw-capture format: verbatim tool stdout, then an appended `# ===` provenance block naming command, cwd, captured_at, toolchain, and exit code
    - .planning/audit/baseline/jest.txt — same format; its `Test Suites:` / `Tests:` summary lines are the AUDIT-13 reference this batch must meet or beat
    - .planning/audit/baseline/lint.txt — baseline is 12 warnings, 0 errors, exit 0
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § `evidence/*.txt` (Analog A — capture verbatim, do not summarize)
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Open Questions 5 (grep the batch-0 CI run's npm log for an unknown-config warning)
  </read_first>
  <action>
Create the evidence directory `.planning/phases/02-dependency-and-runtime-stabilization/evidence/` if it does not exist.

Run the four batch-gate commands and capture each one's combined stdout+stderr verbatim into `evidence/batch-00-lint.txt`, `batch-00-tsc.txt`, `batch-00-jest.txt`, and `batch-00-build.txt`. Redirect; do not summarize, do not reformat, do not strip the npm banner. Append to each file a provenance block in the `.planning/audit/baseline/build.txt` style — a `# ===` rule, then `command`, `cwd`, `captured_at` as an ISO instant, `toolchain` as node/npm/typescript versions, and a final bare `exit_code=<n>` line.

`npm run build` must be run after `rm -rf .next` so the capture is not reading a stale incremental build.

In `evidence/batch-00-jest.txt`'s provenance block, additionally record the three comparison numbers against the AUDIT-13 baseline as `key=value` lines: passing tests (baseline 220), skipped tests (baseline 36), executing suites of total (baseline 16 of 21). This batch adds no test files, so the expectation is unchanged numbers; a change here is a signal, not noise.

For STAB-02's CI half (RESEARCH Open Question 5): after the batch-0 commit is pushed and the CI run completes, search the `Install dependencies` step log for an npm unknown-config warning and record the result — present or absent, with the scope word npm names if present — as a `ci_npm_unknown_config=<absent|present:scope>` line appended to `evidence/batch-00-jest.txt`'s provenance block. If the CI run is not observable from this session, record `ci_npm_unknown_config=unobserved` with the reason; plan 02-03 owns the local half of STAB-02 and will carry the combined answer.

Do not attempt to "fix" anything about npm configuration in the repository. The warning is machine state, not repository state: there is no `.npmrc` in this tree or in any of npm's four config scopes on this machine, and `npm config get devdir` returns `undefined`. A repository edit here would be inventing a fix for a defect that does not exist in the repository.
  </action>
  <acceptance_criteria>
    - All four files exist under `.planning/phases/02-dependency-and-runtime-stabilization/evidence/` and each ends with a line matching `^exit_code=0$`
    - `evidence/batch-00-jest.txt` contains a line matching `Tests:` with a passing count of at least 220 and a failing count of 0
    - `evidence/batch-00-lint.txt` contains a line matching `0 errors`
    - `evidence/batch-00-tsc.txt` records zero diagnostics, matching `.planning/audit/baseline/tsc.txt`
    - `evidence/batch-00-build.txt` contains the Next.js build banner and reports build success
    - Each of the four files contains the provenance keys `command`, `cwd`, `captured_at`, and `toolchain`
    - `evidence/batch-00-jest.txt` contains a line beginning `ci_npm_unknown_config=`
    - `git status --porcelain` shows no modification to any file under `src/` from this task
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/02-dependency-and-runtime-stabilization/evidence/';const files=['batch-00-lint.txt','batch-00-tsc.txt','batch-00-jest.txt','batch-00-build.txt'];let bad=[];for(const f of files){if(!fs.existsSync(d+f)){bad.push(f+':missing');continue}const t=fs.readFileSync(d+f,'utf8');if(!/^exit_code=0$/m.test(t))bad.push(f+':exit');for(const k of ['command','cwd','captured_at','toolchain'])if(!t.includes(k))bad.push(f+':'+k)}const j=fs.readFileSync(d+'batch-00-jest.txt','utf8');const m=j.match(/Tests:\s+(?:(\d+) skipped, )?(\d+) passed/);if(!m||Number(m[2])<220)bad.push('jest:passcount');if(/\d+ failed/.test(j))bad.push('jest:failures');if(!/ci_npm_unknown_config=/.test(j))bad.push('jest:ci-npm-probe');if(bad.length){console.error('batch-00 evidence gaps: '+bad.join(', '));process.exit(1)}console.log('batch-00 evidence OK')"</automated>
  </verify>
  <done>The batch-0 gate is captured as four verbatim command logs with provenance blocks, the Jest capture quotes pass count and skip count separately against the 220/36 baseline, and the CI-side npm config question is answered in writing rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | `@types/node@^24.13.4` resolution pulls a new tarball into `package-lock.json` |
| repository → CI runner | `.nvmrc` and the workflow file determine what runs against every pull request |
| working tree → `.planning/` evidence | Captured command output is committed and publicly readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `package-lock.json` during the `@types/node` bump | mitigate | `npm install --package-lock-only` then read `git diff --stat` before `npm ci`; forced audit remediation is forbidden by phase constraint 2; `shasum -a 256` recorded before the edit |
| T-02-01-02 | Spoofing | `@types/node` package identity | accept | Already a direct devDependency at `^20.11.0`; a same-name major bump on a first-party DefinitelyTyped package, verified OK in `02-RESEARCH.md` § Package Legitimacy Audit — no new package name enters the tree |
| T-02-01-03 | Elevation of Privilege | CI `Run tests` step running untrusted PR code | accept | `pull_request` on a public repo already runs `npm ci` and `npm run build` with no secrets beyond two placeholder Supabase values that are literal strings in the workflow file; adding `npm test` widens no boundary |
| T-02-01-04 | Information Disclosure | Evidence captures committed to `.planning/` | mitigate | Only lint/tsc/jest/build output is captured; none of these commands receive a credential. `.env.local` is never read. Apply the Phase 1 scrub patterns if any capture is ever extended to a credentialed command |
| T-02-01-05 | Repudiation | Node pin present in the repo but not honoured by the platform | mitigate | `engines.node` overrides the Vercel dashboard value; the dashboard value is recorded as a `user_setup` item so the divergence is written down rather than discovered later |
| T-02-01-SC | Tampering | npm installs in this plan | mitigate | One package changes version and it is already declared. No new package name is introduced, so no legitimacy checkpoint is warranted (see `02-RESEARCH.md` § Package Legitimacy Audit — 0 SLOP, 0 genuine SUS) |
</threat_model>

<verification>
- `npm run lint` exits 0 with 0 errors and no more than 12 warnings
- `npx tsc --noEmit` exits 0 with no output
- `npm test -- --ci` exits 0 with at least 220 passing and 0 failing
- `npm run build` exits 0
- `npm ci` from the reconciled lockfile exits 0
- `git --no-pager diff --stat package-lock.json` was read and its line count recorded in the batch commit body
- No file under `src/`, `supabase/`, or `.env*` was modified
</verification>

<success_criteria>
`npm test` runs Jest and CI runs it on every pull request, on the Node major the repository itself declares in one place. `@types/node` matches the pinned runtime and carries a written migration note as the phase's only major. The Vitest and Playwright residue is deleted from the tree and from git. The batch-0 gate is evidenced as four verbatim captures with provenance, and every later batch has an automated net under it.
</success_criteria>

<artifacts_this_phase_produces>
Contributed by this plan (the full Phase 2 manifest is assembled in `02-11-PLAN.md`'s completion note):

**New files**
- `.nvmrc` — contains `24`
- `.planning/phases/02-dependency-and-runtime-stabilization/evidence/` — the phase evidence directory
- `evidence/types-node-major-note.md` — STAB-10 migration note for the `@types/node` 20 → 24 major
- `evidence/lock.b0.before.sha256`
- `evidence/batch-00-lint.txt`, `evidence/batch-00-tsc.txt`, `evidence/batch-00-jest.txt`, `evidence/batch-00-build.txt`

**New `package.json` fields and scripts**
- `engines.node` = `24.x`, `engines.npm` = `>=11`
- `scripts.test` = `jest`
- `scripts.test:ci` = `jest --ci`

**New CI steps**
- `Run tests` (`npm test`) in `.github/workflows/ci.yml`, between type-check and build
- `Setup Node.js` switched from `node-version: 20` to `node-version-file: '.nvmrc'`

**Deleted files**
- `vitest.config.ts`
- `vitest.setup.ts`
- `test-results/.last-run.json`

**Deleted `package.json` entries**
- `scripts.check:feedback`
- `"vitest.config.ts"` from `tsconfig.json` `exclude`

**Findings addressed**
- F-064 (tracked tooling residue), F-065 (CI never runs the tests, Node pinned to 20) — closed
- F-066 — partially addressed only; the tsconfig test-file exclusion is deliberately retained and recorded as out of scope
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-dependency-and-runtime-stabilization/02-01-SUMMARY.md` when done.
</output>
