---
phase: 02-dependency-and-runtime-stabilization
plan: 08
type: execute
wave: 6
depends_on: ["02-07"]
files_modified:
  - package.json
  - package-lock.json
  - jest.config.js
  - jest.setup.ts
  - src/components/ErrorBoundary.test.tsx
  - src/components/events/EventFilters.test.tsx
  - src/components/events/FilterSidebar.test.tsx
  - src/hooks/useEvents.test.ts
  - src/app/api/events/route.test.ts
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/skipped-suite-disposition.md
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b5.before.sha256
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-lint.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-tsc.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-jest.txt
  - .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-build.txt
autonomous: true
requirements: [STAB-08, STAB-09, STAB-11, STAB-13]

must_haves:
  truths:
    - "The four suites that were skipped only because testing-library was absent now execute and pass"
    - "A component test and a hook test both get a DOM environment, chosen by a rule in one reviewable place rather than by file extension"
    - "The measured passing count is above the AUDIT-13 baseline and the measured skipped count is below it"
    - "The one suite that no install can revive is dispositioned in writing rather than revived against the wrong contract"
  artifacts:
    - path: "jest.config.js"
      provides: "Two-project configuration routing node and jsdom suites"
      contains: "projects"
    - path: "jest.setup.ts"
      provides: "jsdom project setup loading the DOM matchers"
    - path: ".planning/phases/02-dependency-and-runtime-stabilization/evidence/skipped-suite-disposition.md"
      provides: "The per-suite record and the decide-do-not-fix outcome for the contract-drift suite"
      min_lines: 45
  key_links:
    - from: "jest.config.js"
      to: "jest.setup.ts"
      via: "the jsdom project loads the setup file after the environment is created"
      pattern: "setupFilesAfterEnv"
    - from: "jest.config.js"
      to: "src/hooks/useEvents.test.ts"
      via: "the jsdom testMatch includes the hooks directory explicitly, because this .ts file needs a DOM"
      pattern: "src/hooks"
---

<objective>
Batch 5: install the test environment the suites already ask for, and let four of the five skipped suites actually run.

Three `.tsx` component suites and one `.ts` hook suite are skipped today, each with a comment in its own source naming the missing package. Installing four packages and routing the right files to a DOM environment revives all four. The fifth suite is a different problem entirely and gets a written decision, not a fix.

Purpose: STAB-13's gate is a quoted pass count **and** a separately quoted skip count, measured against 220 passing and 36 skipped. That phrasing exists because "tests pass" is satisfied today by suites nobody runs. This batch is where the skip count actually moves. It comes after the Node pin — deliberately, not cosmetically — because the DOM matcher package declares a Node floor of 22 and there is no earlier major that escapes it.
Output: four packages installed, a two-project Jest configuration, a setup file, four suites un-skipped and repaired, one suite dispositioned, and the batch-5 gate evidence.

**No CONTEXT.md exists for this phase** — there are no `D-NN` user decisions. Locked constraints come from `.planning/PROJECT.md` § Constraints, `.planning/REQUIREMENTS.md` § Out of Scope, and `02-RESEARCH.md` § Phase Constraints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md
@.planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md
@.planning/audit/baseline/test-runner-decision.md
@jest.config.js
</context>

<phase_locked_constraints>
1. `react` / `react-dom` untouched — note the React testing library peers React 18 and 19, so its install must not pull React forward. 2. Forced audit remediation is forbidden; the lockfile is reconciled, never regenerated. <!-- planner-discipline-allow: audit fix --> 3. `.env.local` is never read or written. 4. No schema file is touched and no database operation is performed anywhere in Phase 2. 5. `shasum -a 256`, not `sha256sum`. 6. Zero-dependency tooling for the phase's own tools — the four packages installed here are devDependencies of the project's test harness, which is a different thing and is exactly what STAB-08 asks for. 7. **No behaviour changes: no file under `src/app/api/**` or any other production module may be edited to make a test pass.**
</phase_locked_constraints>

<out_of_scope>
**Un-excluding test files from `tsconfig.json` is out of scope for this phase.** F-066's validation criterion asks for it; doing it today produces 86 errors across 10 files — 26 block-scoped redeclarations and 12 duplicate implementations from cross-file scope collisions, 15 unsafe conversions and 14 argument-count errors on route-handler casts, 14 missing-property errors from DOM matchers before their types load, and 5 genuine fixture drifts. `ts-jest` compiles each file in isolation, which is why all executing suites pass today despite those whole-program errors.

The trap to avoid is specific: an executor "fixing" the argument-count errors by widening handler signatures under `src/app/api/**` would be shipping a behaviour change disguised as a type fix, inside the phase whose entire purpose is behaviour preservation.

So: leave `**/*.test.ts` and `**/*.test.tsx` in the `exclude` array. Plan 02-11's completion note records F-066 as **partially closed** — skips cleared, type-check hole remaining — rather than silently claimed. The natural home is Phase 3, alongside the generated types and the type-drift CI step.
</out_of_scope>

<batch_gate>
This plan is **batch 5**. Gate: `npm run lint` → `npx tsc --noEmit` → `npm test -- --ci` → `npm run build`, then the lockfile diff read as a diff. One commit for the manifest and lockfile change.
</batch_gate>

<tasks>

<task type="auto">
  <name>Task 1: Install the four test-harness packages and configure two Jest projects</name>
  <files>package.json, package-lock.json, jest.config.js, jest.setup.ts, .planning/phases/02-dependency-and-runtime-stabilization/evidence/lock.b5.before.sha256</files>
  <read_first>
    - jest.config.js — the live single-project config. Its four settings (the ts-jest preset, the node environment, the path-alias mapper, and the three ignore patterns) must be lifted verbatim into the shared base so both new projects inherit today's exact resolution. Note the file uses single quotes; config files in this repository are single-quoted even though the source formatter specifies double quotes for `src/` — match the file you are editing
    - package.json — the current devDependency set; Jest is on 30.x and ts-jest on 29.x, and the jsdom environment package's major must match Jest's
    - src/hooks/useEvents.test.ts — a `.ts` file that needs a DOM. This is why the routing rule cannot be "by extension"
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Code Examples 6 (the two-project config and the setup file) and § Pitfall 6 (the required peer and the Node floor)
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Package Legitimacy Audit — all four packages are approved; three carry a recency-only SUS reason that the audit explicitly says does not warrant a verification checkpoint
  </read_first>
  <action>
Install four packages as devDependencies, **together in one command**, re-verifying each current version against the registry first:

The jsdom test environment, at a major matching the installed Jest major — Jest extracted jsdom into a separate package and a major mismatch produces a confusing environment-not-found failure. The React testing library. The DOM testing library — this one is the trap: the React testing library lists it under peer dependencies with **no** optional marker (only the React type packages are marked optional there), so installing the other three alone leaves it as an unmet peer. And the DOM matchers package, which the component suites already call by name in their own skip comments.

Confirm before installing that the React testing library's peer range admits React 18 and that the install does not move `react` or `react-dom`. Confirm after installing that the DOM testing library resolves to a real version rather than reporting empty or unmet.

Follow the batch protocol: record the lockfile hash into `evidence/lock.b5.before.sha256`, edit the manifest, reconcile with `npm install --package-lock-only`, read `git --no-pager diff --stat package-lock.json`, then `npm ci`.

Create `jest.setup.ts` at the repository root. One line: a side-effect import of the DOM matchers package. Nothing else belongs in it.

Rewrite `jest.config.js` as a two-project configuration. Lift the current four settings into a shared base object and spread it into both projects so neither can drift from today's resolution.

The node project: display name `node`, the node environment, matching `.ts` test files under `src`, and extending the shared ignore patterns with the hooks directory so hook suites do not run twice.

The jsdom project: display name `jsdom`, the jsdom environment, matching `.tsx` test files under `src` **and** `.ts` test files under the hooks directory, with the setup file loaded after the environment is created.

Put the routing rule in this one reviewable place. A per-file environment docblock would also work, but the hook suite is a `.ts` file that needs a DOM, so the rule is not "by extension" either way — a config-level rule keeps it in one place and tags every result line with the project it ran under.

<!-- planner-discipline-allow: passWithNoTests -->
Do not add a watch-mode flag, a coverage threshold, or `--passWithNoTests` anywhere. Jest exiting non-zero when it matches nothing is the behaviour worth keeping: a config error that made it match nothing would otherwise report green.
  </action>
  <acceptance_criteria>
    - `node -e "const d=require('./package.json').devDependencies;const need=['jest-environment-jsdom','@testing-library/react','@testing-library/dom','@testing-library/jest-dom'];process.exit(need.every(n=>d[n])?0:1)"` exits 0
    - `npm ls @testing-library/dom` resolves to a real version and reports no unmet peer dependency
    - `node -e "const a=require('jest/package.json').version.split('.')[0];const b=require('jest-environment-jsdom/package.json').version.split('.')[0];process.exit(a===b?0:1)"` exits 0 — majors match
    - `node -e "const p=require('./package.json');process.exit((p.dependencies.react==='^18.3.0'&&p.dependencies['react-dom']==='^18.3.0')?0:1)"` exits 0, and the resolved React versions are unchanged from the batch-4 commit
    - `test -f jest.setup.ts` exits 0 and the file is a single side-effect import of the DOM matchers package
    - `node -e "const c=require('./jest.config.js');process.exit(Array.isArray(c.projects)&&c.projects.length===2?0:1)"` exits 0
    - The two projects carry the display names `node` and `jsdom`, and both inherit the ts-jest preset, the path-alias mapper, and the three ignore patterns from the current config
    - The jsdom project's match patterns include both a `.tsx` pattern and a hooks-directory `.ts` pattern; the node project excludes the hooks directory
    - `npx jest --ci --listTests | wc -l` reports the same file count as `.planning/audit/baseline/jest-listtests.txt`, plus the test files added in plan 02-02, minus any file deleted — no suite silently stopped being discovered
    - `grep -c 'passWithNoTests' jest.config.js package.json` prints 0
    - `npm ci` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm ci &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs');const cp=require('child_process');const p=require('./package.json');const c=require('./jest.config.js');const bad=[];const need=['jest-environment-jsdom','@testing-library/react','@testing-library/dom','@testing-library/jest-dom'];for(const n of need)if(!p.devDependencies[n])bad.push('not-installed:'+n);const jm=require('jest/package.json').version.split('.')[0];const em=require('jest-environment-jsdom/package.json').version.split('.')[0];if(jm!==em)bad.push('jsdom-major-skew:'+em+' vs jest '+jm);try{require.resolve('@testing-library/dom/package.json')}catch(e){bad.push('dom-peer-unresolvable')}if(p.dependencies.react!=='^18.3.0'||p.dependencies['react-dom']!=='^18.3.0')bad.push('react-moved');if(!Array.isArray(c.projects)||c.projects.length!==2)bad.push('not-two-projects');else{const names=c.projects.map(x=>x.displayName).sort().join(',');if(names!=='jsdom,node')bad.push('display-names:'+names);const js=c.projects.find(x=>x.displayName==='jsdom');const nd=c.projects.find(x=>x.displayName==='node');if(!js.setupFilesAfterEach&&!(js.setupFilesAfterEnv||[]).length)bad.push('jsdom-no-setup-file');if(!JSON.stringify(js.testMatch).includes('hooks'))bad.push('hooks-not-routed-to-jsdom');if(!JSON.stringify(nd.testPathIgnorePatterns).includes('hooks'))bad.push('hooks-not-excluded-from-node');for(const proj of [js,nd]){if(proj.preset!=='ts-jest')bad.push(proj.displayName+':preset-lost');if(!proj.moduleNameMapper||!proj.moduleNameMapper['^@/(.*)$'])bad.push(proj.displayName+':alias-lost')}}if(!fs.existsSync('jest.setup.ts'))bad.push('setup-file-missing');if(/passWithNoTests/.test(fs.readFileSync('jest.config.js','utf8')))bad.push('passWithNoTests-added');if(bad.length){console.error('batch-5 config gaps: '+bad.join(', '));process.exit(1)}console.log('batch-5 harness config OK')"</automated>
  </verify>
  <done>Four packages are installed together with the required peer resolved and React unmoved, the DOM matchers load from a setup file, and one reviewable config routes component suites and the hook suite to a DOM environment while everything else stays on node — with today's preset, alias mapping, and ignore patterns inherited by both projects.</done>
</task>

<task type="auto">
  <name>Task 2: Un-skip the four revivable suites and repair their fixture drift</name>
  <files>src/components/ErrorBoundary.test.tsx, src/components/events/EventFilters.test.tsx, src/components/events/FilterSidebar.test.tsx, src/hooks/useEvents.test.ts</files>
  <read_first>
    - src/components/ErrorBoundary.test.tsx — stub shape 1. Lines 1-7 carry a destructured empty-object stub cast to any, and a stray client directive on line 1 that has no business in a test file
    - src/components/events/FilterSidebar.test.tsx — stub shape 2. Lines 1-15 also show the child-component mock pattern that must be preserved, and line 52 carries a lowercase tag string literal that must become a member of the tag enum
    - src/components/events/EventFilters.test.tsx — stub shape 2 as well; this one revives on the install alone
    - src/hooks/useEvents.test.ts — stub shape 3. Lines 1-24 carry a rework TODO, a hook-renderer stub, a global fetch mock that must be kept, and a fixture with a capitalised tag string, an incomplete club object missing eight fields, and two date columns that Phase 1 proved do not exist on the events table
    - src/types/index.ts — the tag enum and the `Event` and `Club` interfaces the repaired fixtures must satisfy
    - .planning/audit/baseline/test-runner-decision.md § 5 — the per-suite skip reasons and counts
    - .planning/phases/02-dependency-and-runtime-stabilization/02-PATTERNS.md § The five previously-skipped suites — three distinct stub shapes; do not write one blanket substitution
  </read_first>
  <action>
There are **three distinct stub shapes** across these four files. The edit differs per file. A single find-and-replace across all four will corrupt at least one of them.

For each of the four suites, replace the stub line — a destructured empty object cast to any, standing in for the testing library's exports — with a real named import from the React testing library, importing exactly the helpers that file uses. Remove the accompanying not-installed comment. Change the suite's skipped describe into a plain describe and drop the parenthetical reason from its title, since the reason no longer holds.

File-specific work beyond the stub swap:

The error-boundary suite also carries a stray client directive on its first line. Remove it — a Jest test module is not a client component and the directive is meaningless there.

The filter-sidebar suite needs its lowercase tag string literal replaced with the corresponding member of the tag enum from the shared types module. Preserve its child-component mock exactly as written; that mock is what keeps the suite focused on the sidebar.

The hooks suite needs the most repair. Replace the capitalised tag string with the enum member. Complete the club fixture so it satisfies the `Club` interface — read the interface and fill every required field rather than casting the gap away. Give the destructured filters binding a real type. And remove the two non-existent date columns from the event fixture: Phase 1 established that those columns exist on no production table and that the start and end timestamp columns are authoritative. Keep the global fetch mock. Delete the rework TODO, since the rework is what you are doing.

Repair fixtures to match the types. Do not loosen the types to match the fixtures, and do not reach for a cast to make a shape fit — the fixture drift is the finding, and casting it away preserves the drift while hiding it.

**Do not edit any production module.** If a suite fails because production code behaves differently from what the test asserts, that is a finding to record in the disposition note, not a signal to change the production code. This phase preserves behaviour.
  </action>
  <acceptance_criteria>
    - `grep -rc 'describe.skip' src/components/ErrorBoundary.test.tsx src/components/events/EventFilters.test.tsx src/components/events/FilterSidebar.test.tsx src/hooks/useEvents.test.ts` prints 0 for every file
    - `grep -rc 'not installed' src/components/ErrorBoundary.test.tsx src/components/events/EventFilters.test.tsx src/components/events/FilterSidebar.test.tsx src/hooks/useEvents.test.ts` prints 0 for every file
    - All four files contain a real named import from `@testing-library/react`
    - `head -1 src/components/ErrorBoundary.test.tsx` is not a client directive
    - `grep -c 'event_date\|event_time' src/hooks/useEvents.test.ts` prints 0
    - `grep -cE '"[Aa]cademic"' src/hooks/useEvents.test.ts src/components/events/FilterSidebar.test.tsx` prints 0 for both — tag literals are enum members now
    - `grep -c 'as any' src/hooks/useEvents.test.ts` is no greater than it was before this task — no new cast was added to paper over a fixture gap
    - `npx jest --ci src/components/ErrorBoundary.test.tsx src/components/events/EventFilters.test.tsx src/components/events/FilterSidebar.test.tsx src/hooks/useEvents.test.ts` exits 0 with at least 31 passing tests and 0 skipped
    - `git diff --stat -- src/app src/lib src/components/events/FilterSidebar.tsx src/components/ErrorBoundary.tsx src/hooks/useEvents.ts` prints nothing — no production module was edited
    - `npm test -- --ci` exits 0 with a passing count above 220 and a skipped count below 36
  </acceptance_criteria>
  <verify>
    <automated>npx jest --ci src/components/ErrorBoundary.test.tsx src/components/events/EventFilters.test.tsx src/components/events/FilterSidebar.test.tsx src/hooks/useEvents.test.ts &amp;&amp; node -e "const fs=require('fs');const cp=require('child_process');const files=['src/components/ErrorBoundary.test.tsx','src/components/events/EventFilters.test.tsx','src/components/events/FilterSidebar.test.tsx','src/hooks/useEvents.test.ts'];const bad=[];for(const f of files){const t=fs.readFileSync(f,'utf8');if(/describe\.skip/.test(t))bad.push(f+':still-skipped');if(/not installed/.test(t))bad.push(f+':stub-comment-remains');if(!/from ['\"]@testing-library\/react['\"]/.test(t))bad.push(f+':no-real-import')}const eb=fs.readFileSync(files[0],'utf8').split('\n')[0];if(/use client/.test(eb))bad.push('error-boundary-stray-directive');const h=fs.readFileSync('src/hooks/useEvents.test.ts','utf8');if(/event_date|event_time/.test(h))bad.push('nonexistent-date-columns-remain');if(/[\"'][Aa]cademic[\"']/.test(h))bad.push('tag-string-literal-remains-in-hooks');const fsb=fs.readFileSync(files[2],'utf8');if(/[\"'][Aa]cademic[\"']/.test(fsb))bad.push('tag-string-literal-remains-in-sidebar');const prod=cp.execSync('git diff --name-only -- src/app src/lib src/hooks/useEvents.ts src/components/ErrorBoundary.tsx src/components/events/FilterSidebar.tsx src/components/events/EventFilters.tsx || true',{encoding:'utf8'}).trim();if(prod)bad.push('production-module-edited:'+prod.split('\n')[0]);if(bad.length){console.error('un-skip gaps: '+bad.join(', '));process.exit(1)}console.log('four suites revived, no production module touched')"</automated>
  </verify>
  <done>All four suites execute and pass, each un-skipped according to its own stub shape, with the stray client directive gone, tag literals replaced by enum members, the club fixture completed against its interface, and the two non-existent date columns removed. Not one production module was edited.</done>
</task>

<task type="auto">
  <name>Task 3: Disposition the fifth suite, run the batch-5 gate, and record the measured STAB-13 numbers</name>
  <files>src/app/api/events/route.test.ts, .planning/phases/02-dependency-and-runtime-stabilization/evidence/skipped-suite-disposition.md, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-lint.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-tsc.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-jest.txt, .planning/phases/02-dependency-and-runtime-stabilization/evidence/batch-05-build.txt</files>
  <read_first>
    - src/app/api/events/route.test.ts — the fifth suite. Its assertions describe a cursor-pagination contract
    - src/app/api/events/route.ts — the handler it tests. Search it for any cursor concept; there is none. That mismatch is the whole question
    - src/__tests__/api/events/get-events.test.ts — the live sibling suite for the same route, and the analog anyone rewriting the fifth suite later would follow
    - .planning/audit/baseline/jest.txt — the summary lines giving the 220 passing, 36 skipped, 16 of 21 executing baseline this batch is measured against
    - .planning/phases/02-dependency-and-runtime-stabilization/02-RESEARCH.md § Open Questions 2 — the decide-do-not-fix recommendation and its reasoning
    - .planning/audit/baseline/test-runner-decision.md — the decision-note format
  </read_first>
  <action>
**Decide the fifth suite; do not fix it.** Leave `src/app/api/events/route.test.ts` skipped, with its self-documenting title intact. Do not rewrite it, do not delete it, and do not edit the route handler to satisfy it.

The reasoning belongs in the record, not just in the outcome. No install revives this suite — its problem is contract drift, not a missing package. The suite asserts a cursor-pagination contract and the handler has no cursor concept at all. Which of the two is wrong is genuinely unknown: either pagination regressed out of the route, or the tests are simply obsolete. Reviving the suite against the handler's current behaviour would freeze a possible defect as the contract — the exact trap Phase 1 named when it insisted that a route's expected status records what it *should* return, never what it happens to return today. STAB-08 requires Jest to be the single runner and the *installable* skips to be cleared; it does not require this suite. The contract question belongs to Phase 4's event read path slice (REFAC-10), which owns that route.

Write `evidence/skipped-suite-disposition.md`. Metadata line, blockquoted decision up front. Then a per-suite table covering all five: the suite path, its skipped test count before this batch, whether the install alone revived it, what extra repair each needed, and its status after. Then the reasoning above for the fifth, with the handler-has-no-cursor observation stated as a re-runnable grep with its result. Then the explicit hand-off to REFAC-10 in Phase 4, naming what that slice has to decide.

Also record in this note that the `tsconfig.json` test-file exclusion was deliberately **not** removed, with the error count that removal produces today and the reason it is out of scope — so F-066 reads as partially closed rather than claimed. Plan 02-11 quotes this in the completion note.

Then run the batch-5 gate, capturing the four commands verbatim into the `batch-05-*.txt` files with provenance blocks and `exit_code=` lines. Delete `.next` before the build capture.

In `evidence/batch-05-jest.txt`'s provenance block, record the measured STAB-13 numbers as `key=value` lines against the baseline, both sides quoted: passing tests measured versus the baseline's 220; skipped tests measured versus the baseline's 36; executing suites of total measured versus the baseline's 16 of 21. Quote **both** the pass count and the skip count. "Tests pass" is not the gate — the baseline's pass count was already satisfied by a tree where five suites never ran, which is precisely why the requirement names two numbers.

Confirm `node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs` exits 0.
  </action>
  <acceptance_criteria>
    - `grep -c 'describe.skip\|it.skip' src/app/api/events/route.test.ts` is unchanged from before this batch — the fifth suite is still skipped
    - `git diff --stat -- src/app/api/events/route.ts src/app/api/events/route.test.ts` prints nothing — neither the handler nor the suite was edited
    - `evidence/skipped-suite-disposition.md` contains `**Plan:**`, `**Phase:**`, `**Recorded:**`, a line beginning `> **Decision:`, the string `REFAC-10`, and the string `F-066`
    - The note contains a five-row per-suite table and a re-runnable grep showing the handler has no cursor concept, with its result
    - The note ends with lines matching `^\*Phase:` and `^\*Plan:`
    - All four `batch-05-*.txt` files exist with provenance blocks and end with `exit_code=0`
    - `evidence/batch-05-jest.txt` records `stab13_passed_measured`, `stab13_passed_baseline`, `stab13_skipped_measured`, `stab13_skipped_baseline`, `stab13_suites_measured`, `stab13_suites_baseline`
    - The measured passing count is strictly greater than 220 and the measured skipped count is strictly less than 36
    - The measured executing-suite count is at least 20
    - `npm run build` exits 0 and `npm run lint` reports 0 errors
    - `node .../evidence/tools/check-baseline.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/02-dependency-and-runtime-stabilization/evidence/tools/check-baseline.mjs &amp;&amp; node -e "const fs=require('fs');const d='.planning/phases/02-dependency-and-runtime-stabilization/evidence/';const bad=[];const n=fs.readFileSync(d+'skipped-suite-disposition.md','utf8');for(const s of ['**Plan:**','**Phase:**','**Recorded:**','REFAC-10','F-066'])if(!n.includes(s))bad.push('note-missing:'+s);if(!/^> \*\*Decision:/m.test(n))bad.push('no-blockquoted-decision');if(!/^\*Phase:/m.test(n))bad.push('footer-phase');const five=fs.readFileSync('src/app/api/events/route.test.ts','utf8');if(!/\.skip/.test(five))bad.push('fifth-suite-was-revived');const base=fs.readFileSync('.planning/audit/baseline/jest.txt','utf8');const bm=base.match(/Tests:\s+(\d+) skipped, (\d+) passed/);const bsk=Number(bm[1]),bps=Number(bm[2]);const j=fs.readFileSync(d+'batch-05-jest.txt','utf8');for(const k of ['stab13_passed_measured','stab13_passed_baseline','stab13_skipped_measured','stab13_skipped_baseline','stab13_suites_measured','stab13_suites_baseline'])if(!j.includes(k))bad.push('missing-key:'+k);const gp=(k)=>{const m=j.match(new RegExp('^'+k+'=(\\\\d+)','m'));return m?Number(m[1]):null};const mp=gp('stab13_passed_measured'),ms=gp('stab13_skipped_measured'),mu=gp('stab13_suites_measured');if(mp===null||mp<=bps)bad.push('pass-count-not-improved:'+mp+' vs '+bps);if(ms===null||ms>=bsk)bad.push('skip-count-not-improved:'+ms+' vs '+bsk);if(mu===null||mu<20)bad.push('executing-suites-below-20:'+mu);for(const f of ['batch-05-lint.txt','batch-05-tsc.txt','batch-05-jest.txt','batch-05-build.txt'])if(!/^exit_code=0$/m.test(fs.readFileSync(d+f,'utf8')))bad.push(f+':exit');if(bad.length){console.error('batch-5 gate gaps: '+bad.join(', '));process.exit(1)}console.log('batch-5 gate OK — passed '+bps+'->'+mp+', skipped '+bsk+'->'+ms+', suites '+mu)"</automated>
  </verify>
  <done>The contract-drift suite is dispositioned in writing and handed to Phase 4 rather than revived against a possibly-defective contract, the retained tsconfig exclusion is recorded so F-066 reads as partially closed, and the STAB-13 position is captured as measured numbers on both sides — more passing than the baseline and fewer skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependency tree | Four new package names enter the repository for the first time in this phase |
| test environment → production modules | Reviving suites creates pressure to edit production code to make assertions pass |
| skipped suite → recorded contract | Reviving a drifted suite would write a possible defect into the specification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | Production behaviour changed to satisfy a newly-executing assertion | mitigate | Phase constraint 7 forbids it; the acceptance criteria assert an empty diff across `src/app`, `src/lib`, and the three subject modules, so the verify fails mechanically if a production file moves |
| T-02-08-02 | Tampering | Fixture drift hidden with a cast instead of repaired | mitigate | The task instruction is explicit that fixtures are repaired to match the types rather than the reverse, and the acceptance criteria assert the cast count in the hooks suite did not increase |
| T-02-08-03 | Repudiation | A drifted suite revived against current behaviour, freezing a defect as the contract | mitigate | Decide-do-not-fix, with the reasoning and the handler-has-no-cursor grep recorded, and the contract question handed explicitly to REFAC-10 in Phase 4 |
| T-02-08-04 | Tampering | React pulled forward by the testing library install | mitigate | The library peers React 18 and 19, so an install could in principle resolve differently; the acceptance criteria assert both the manifest ranges and the resolved lockfile versions after `npm ci` |
| T-02-08-05 | Denial of Service | A configuration error making Jest match nothing while CI reports green | mitigate | No permissive no-tests-found flag is added anywhere, and the discovered-test-file count is compared against the Phase 1 listing so a silently-dropped suite is visible |
| T-02-08-06 | Repudiation | The pass count improving while the skip count is not reported | mitigate | Both numbers are recorded as `key=value` pairs against their baselines, and the verify asserts improvement on both independently |
| T-02-08-SC | Tampering | npm/pip/cargo installs — **four new package names** | mitigate | `02-RESEARCH.md` § Package Legitimacy Audit dispositions all four: every one has a first-party source repository, eight-figure weekly downloads, and is named either by this repository's own test files or by Phase 1's test-runner decision. Three carry a recency-only SUS reason, and the audit states explicitly that this reason alone does not warrant a verification checkpoint. **Verdict: no blocking human checkpoint; 0 SLOP, 0 genuine SUS.** Installed together in one command from the reviewed lockfile via `npm ci` |
</threat_model>

<verification>
- `npm run lint` exits 0 with 0 errors and at most 12 warnings
- `npx tsc --noEmit` exits 0 with no output
- `npm test -- --ci` exits 0, passing count above 220, skipped count below 36, at least 20 executing suites, 0 failures
- `npm run build` exits 0
- `npm ci` from the reconciled lockfile exits 0 with the required peer resolved
- `react` and `react-dom` unchanged in manifest and lockfile
- No production module under `src/app`, `src/lib`, or the three test subjects was edited
- `node .../evidence/tools/check-baseline.mjs` exits 0
- The lockfile diff was read as a diff and its line count recorded in the commit body
</verification>

<success_criteria>
Jest is the single runner with a DOM environment for the suites that need one, chosen by a rule in one place rather than by file extension. The four suites that were skipped only for a missing package now execute and pass, with their fixture drift repaired against the real types. The fifth is dispositioned in writing and handed to the phase that owns its route. STAB-13 is satisfied on both numbers — measurably more passing and measurably fewer skipped than the AUDIT-13 baseline — and no production module was touched to get there.
</success_criteria>

<artifacts_this_phase_produces>
Contributed by this plan:

**New files**
- `jest.setup.ts` — loads the DOM matchers into the jsdom project
- `evidence/skipped-suite-disposition.md` — the five-suite record, the decide-do-not-fix outcome, and the retained-tsconfig-exclusion statement
- `evidence/lock.b5.before.sha256`
- `evidence/batch-05-lint.txt`, `evidence/batch-05-tsc.txt`, `evidence/batch-05-jest.txt` (carrying the six measured STAB-13 `key=value` lines), `evidence/batch-05-build.txt`

**New `package.json` entries** (devDependencies)
- `jest-environment-jsdom`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/jest-dom`

**Modified config**
- `jest.config.js` — rewritten as a two-project configuration with `node` and `jsdom` display names

**Modified tests** (four suites un-skipped and repaired)
- `src/components/ErrorBoundary.test.tsx`, `src/components/events/EventFilters.test.tsx`, `src/components/events/FilterSidebar.test.tsx`, `src/hooks/useEvents.test.ts`

**Deliberately unchanged**
- `src/app/api/events/route.test.ts` — still skipped; contract question handed to REFAC-10 in Phase 4
- `tsconfig.json` `exclude` — test files stay excluded; F-066 is partially closed, not claimed

**Deleted files:** none. **New CI steps:** none.

**Findings addressed**
- F-066 — partially closed: installable skips cleared, whole-program type-check hole deliberately retained and recorded
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-dependency-and-runtime-stabilization/02-08-SUMMARY.md` when done.
</output>
