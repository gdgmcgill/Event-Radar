---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/__tests__/api/auth-ring/writeHandlerTable.ts
  - src/__tests__/api/auth-ring/write-handlers-characterization.test.ts
  - src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts
  - src/__tests__/api/events/anonymous-personalized-defect.test.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-characterization-handlers.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-mutation-check-handlers.txt
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "Every state-changing, non-admin, non-cron handler arm under src/app/api is enumerated from the tree, not from memory, and each arm's anonymous response bytes are pinned PRESERVE before slice 3 touches it"
    - "The ten legacy ban callers' 403 {\"error\":\"Account suspended\"} for a banned caller is pinned PRESERVE, so the seam guard must reproduce it byte for byte"
    - "Today's admission of a banned caller (on arms with no ban check), of a caller with no profile row (on every arm), and of an un-onboarded caller (on every non-exempt arm) is pinned DEFECT against F-088 and F-089"
    - "The two onboarding exemptions (POST /api/onboarding/complete, PATCH /api/users/[id] self) and the two anonymous-tolerant writers (POST /api/interactions, POST /api/feedback) are pinned as admitted, before and after"
    - "The four F-028 routes' anonymous 200 bodies are pinned DEFECT F-028"
  artifacts:
    - path: "src/__tests__/api/auth-ring/writeHandlerTable.ts"
      provides: "One descriptor per state-changing arm: id, invoke closure typed through the handler's own export, flags legacyBan / onboardingExempt / anonymousTolerant"
      min_lines: 150
    - path: "src/__tests__/api/auth-ring/write-handlers-characterization.test.ts"
      provides: "PRESERVE: anonymous bytes per arm, legacy ban bytes, exemptions admitted"
      min_lines: 100
    - path: "src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts"
      provides: "DEFECT F-088/F-089/F-062 with a GUARDED set the fix commits edit row by row"
      min_lines: 100
    - path: "src/__tests__/api/events/anonymous-personalized-defect.test.ts"
      provides: "DEFECT F-028 for events/following, events/friends-activity, events/friends-organizing, events/[id]/friends"
      min_lines: 60
  key_links:
    - from: "src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts"
      to: "src/__tests__/api/auth-ring/writeHandlerTable.ts"
      via: "imports the arm descriptors; the GUARDED set lives in the DEFECT file so the PRESERVE file's inputs never change"
      pattern: "GUARDED"
    - from: "src/__tests__/api/auth-ring/writeHandlerTable.ts"
      to: "src/app/api"
      via: "each invoke closure dynamically imports the route and calls its verb with a request and { params: Promise<...> }"
      pattern: "await import\\(\"@/app/api/"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No production source file is modified; every file under src/app, src/lib and src/server is byte-identical at the end of this plan"
    - "No Phase 4 PRESERVE suite is edited"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 3, characterization part 2 of 2: pin the handler ring. Every write arm's anonymous bytes, the legacy ban bytes, and today's admission of banned, profile-less and un-onboarded callers — before 05-06 and 05-07 put `requireActiveUser` and `requireOnboarded` in front of them. Also pin F-028's four anonymous 200s.

Purpose: REFAC-11 says the ban check fails closed and the onboarding guard cannot be bypassed by direct API calls; DEC-34 makes that the contract for roughly forty arms. A table-driven net is the only way to change forty arms without changing their bytes by accident, and the DEFECT half turns "the arm is now guarded" into a one-token change per row that the ledger can record.

Output: a descriptor table, a PRESERVE suite, a DEFECT suite, the F-028 DEFECT suite, and mutation evidence — all against unmodified source.
</objective>

<slice_outcome>
Green when the three suites pass on the unmodified tree, the descriptor table's arm list equals the list a node one-liner derives from `src/app/api` (recorded in evidence), each anonymous expectation was measured before it was pinned, and every mutation cycle shows its named test red.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-CONTEXT.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type, never widened. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the write arms, build the descriptor table, and pin PRESERVE and DEFECT behaviour for each</name>
  <files>src/__tests__/api/auth-ring/writeHandlerTable.ts, src/__tests__/api/auth-ring/write-handlers-characterization.test.ts, src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts</files>
  <read_first>
    - src/__tests__/helpers/fakeSupabase.ts lines 1-145 and 600-660 — createFakeSupabase, the `calls` log (table, operation), throwOn, rpc; the fake has no `storage` member
    - src/__tests__/api/events/save-characterization.test.ts lines 1-120 — the PRESERVE docblock, the mock of `@/lib/supabase/server`, profile() fixture shape
    - src/__tests__/api/events/friends-defect.test.ts lines 1-60 — the DEFECT docblock shape with a Status paragraph
    - src/lib/ban.ts — the legacy helper's reads and its 403 bytes
    - 05-RESEARCH.md § A "checkBanStatus() callers (10)" and "State-changing authenticated handlers without a ban check today", and § Corrections C2 (the two onboarding exemptions, the anonymous-tolerant writers)
    - each route file named in the arm list below, only far enough to see its auth branch, its request-body contract and its first read (grep for `getUser`, `request.json`, `formData`, `params`)
  </read_first>
  <action>
**Enumerate from the tree.** With a node one-liner, list every `export async function POST|PUT|PATCH|DELETE` in `src/app/api/**/route.ts` excluding `src/app/api/admin/`, `src/app/api/cron/` and `src/app/api/recommendations/batch/` (admin-gated), and record it in `evidence/slice-3-characterization-handlers.txt`. The expected result is 39 arms in 30 files: clubs/[id]/appeal POST, clubs/[id]/follow POST and DELETE, clubs/[id]/invites POST, clubs/[id]/members/role PATCH, clubs/[id]/members DELETE, clubs/[id] PATCH and DELETE, clubs/[id]/transfer POST, clubs/banner POST, clubs/logo POST, clubs POST, events/[id]/appeal POST, events/[id]/invite POST, events/[id]/report POST, events/[id]/reviews POST, events/[id] PATCH and DELETE, events/[id]/rsvp POST and DELETE, events/[id]/save POST and DELETE, events/create POST, events/upload-image POST, feedback POST, interactions POST, notifications/[id] PATCH, notifications POST, onboarding/complete POST, organizer-requests POST, profile/avatar POST, profile/banner POST, profile/inferred-tags DELETE, profile/interests PUT, recommendations/feedback POST, user/engagement POST, users/[id]/follow POST and DELETE, users/[id] PATCH. If the tree disagrees, the tree wins and the difference is recorded.

**`writeHandlerTable.ts`** exports `WRITE_ARMS`, one descriptor per arm: `id` (for example `"events/[id]/save DELETE"`), `family` (`events` | `clubs` | `other`), `invoke(req: NextRequest): Promise<Response>` — a closure that dynamically imports its route module and calls the verb with the request and `{ params: Promise.resolve({ ... }) }`, so each handler is called as its own type and no signature is widened — `makeRequest(): NextRequest` building a minimal valid body read from the handler (JSON, or FormData for the four upload arms), and flags `legacyBan` (true for the ten legacy-helper POST arms: clubs POST, clubs/[id]/appeal, user/engagement, recommendations/feedback, users/[id]/follow POST, events/[id]/invite, events/[id]/appeal, events/[id]/rsvp POST, events/create, events/[id]/save POST), `onboardingExempt` (onboarding/complete POST, users/[id] PATCH — whose id param is the caller's own id), `anonymousTolerant` (interactions POST, feedback POST). It also exports persona builders returning `FakeSupabaseInit`: `anonymous`, `active` (onboarding_completed true, banned_at null), `banned` (banned_at set, ban_expires_at null, onboarded), `expired` (banned_at and ban_expires_at in 2020), `unonboarded` (onboarding_completed false), `noProfile` (auth user, no users row). Tables beyond users hold only what the arms read first; the table does not need to make handlers succeed.

Both suites mock `@/lib/supabase/server` (createClient → the fake client), `@/lib/supabase/service` (createServiceClient → the same fake client, so any service write is recorded), and `next/headers` (a cookies() stub with get, getAll, set, delete); add a mock only when an arm needs one, and name it in the docblock.

**PRESERVE** `write-handlers-characterization.test.ts` — docblock `PRESERVE characterization — the write-handler auth ring (REFAC-11)`. Measure each arm first, record the measurement in the evidence file, then pin:
- P1 every arm, anonymous: the exact status and parsed body measured today (an inline `ANONYMOUS_BYTES` map keyed by arm id; for anonymous-tolerant arms, the measured non-401 status).
- P2 the ten legacyBan arms, banned caller: 403 with body exactly `{ "error": "Account suspended" }`, and `calls` holds no insert, update, delete or rpc.
- P3 the two onboardingExempt arms, unonboarded caller: the response is not 403 `{"error":"Onboarding required"}` (admitted), and onboarding/complete answers 200 `{ "success": true }`.
- P4 the ten legacyBan arms, expired-suspension caller: not refused with `Account suspended`.

**DEFECT** `write-handlers-ring-defect.test.ts` — docblock `DEFECT characterization — F-088, F-089 (F-062 related)`, `Status: OPEN — rows flip as 05-06 and 05-07 guard each arm`. An inline `GUARDED: ReadonlySet<string>` (empty now) lives in this file. For each arm, when not in GUARDED assert today's shape, when in GUARDED assert the fixed shape:
- D1 (F-088) arms with legacyBan false, banned caller — today: the response is not (403 and error "Account suspended"); fixed: 403 `{ "error": "Account suspended" }` and `calls` holds exactly one users select and nothing else.
- D2 (F-088) every arm, noProfile caller — today: not (403 and error "Profile not found"); fixed: 403 `{ "error": "Profile not found" }`, no write.
- D3 (F-089) every arm with onboardingExempt false, unonboarded caller (for anonymousTolerant arms too, because a user is present) — today: not (403 and error "Onboarding required"); fixed: 403 `{ "error": "Onboarding required" }`, no write.
Run `npx jest --ci src/__tests__/api/auth-ring`, append the output. Commit `test(05-03): write-handler ring PRESERVE and DEFECT characterization`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/auth-ring</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci src/__tests__/api/auth-ring` exits 0 on the unmodified tree
    - `node -e 'const s=require("fs").readFileSync("src/__tests__/api/auth-ring/writeHandlerTable.ts","utf8");process.exit((s.match(/id: "/g)||[]).length>=39?0:1)'` exits 0
    - `evidence/slice-3-characterization-handlers.txt` contains the node enumeration output and the measured anonymous status and body per arm
    - `node scripts/check-characterization-tags.mjs src/__tests__/api/auth-ring/write-handlers-characterization.test.ts src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` exits 0
    - `write-handlers-ring-defect.test.ts` declares `GUARDED` as an empty set
    - `npx tsc --noEmit` exits 0 (the invoke closures type-check against each handler's own signature)
  </acceptance_criteria>
  <done>Forty write arms are one table whose PRESERVE half fixes the bytes that must survive and whose DEFECT half turns each arm's guarding into a recorded, one-token change.</done>
</task>

<task type="auto">
  <name>Task 2: Pin F-028's four anonymous 200s</name>
  <files>src/__tests__/api/events/anonymous-personalized-defect.test.ts</files>
  <read_first>
    - src/app/api/events/following/route.ts, src/app/api/events/friends-activity/route.ts, src/app/api/events/friends-organizing/route.ts, src/app/api/events/[id]/friends/route.ts — their anonymous branches (research § B: following :20-21, friends-activity :14-15, friends-organizing :15-16, [id]/friends :17-18)
    - src/__tests__/api/events/friends-defect.test.ts — whether it pins an anonymous case for events/[id]/friends (if it does, 05-06 moves that assertion too and records it)
    - 05-RESEARCH.md § C7 — why rsvp GET, clubs/[id]/events and notifications/count are NOT in this file
  </read_first>
  <action>
Create `src/__tests__/api/events/anonymous-personalized-defect.test.ts` with the docblock `DEFECT characterization — F-028 (the four routes DEC-39 answers with 401)` and `Status: OPEN — assertions move in 05-06`. Mock `@/lib/supabase/server` with the shared fake. For each of GET /api/events/following, GET /api/events/friends-activity, GET /api/events/friends-organizing and GET /api/events/[id]/friends, with no user: assert today's status 200 and exact body — `{ "events": [] }`, `{ "events": [] }`, `{ "events": [] }`, `{ "friends": [], "count": 0 }` respectively (measure first; pin what the handlers return). For each route also assert that a signed-in caller with a users row does not get 401 — a row that stays unchanged when the anonymous rows move. Record in the evidence file which component calls each route and whether that caller renders only for a signed-in user and ignores a non-ok response (C7: FollowedClubsEventsSection, FriendsActivitySection via page.tsx's user guard, FriendsOrganizingSection on the protected /friends page, FriendsGoing via EventDetailClient's user guard). Run the suite and `src/__tests__/api/events/friends-defect.test.ts` unedited. Commit `test(05-03): pin F-028 anonymous 200s on four personalized routes`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/events/anonymous-personalized-defect.test.ts src/__tests__/api/events/friends-defect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The suite passes on unmodified source with four anonymous-200 cases and four signed-in cases
    - `node scripts/check-characterization-tags.mjs src/__tests__/api/events/anonymous-personalized-defect.test.ts` exits 0 and the docblock cites `F-028`
    - `evidence/slice-3-characterization-handlers.txt` names each route's client caller and its signed-in guard
    - `git diff --stat HEAD~1 -- src/app/api/events` prints nothing
  </acceptance_criteria>
  <done>The four routes that will start answering 401 have today's degraded 200 pinned, and the evidence says why no public surface depends on them.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the handler net bites and that no production file moved</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-mutation-check-handlers.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-characterization-handlers.txt</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-mutation-check.txt — cycle format
    - the suites written in Tasks 1-2
  </read_first>
  <action>
Run these cycles, each mutating SOURCE only, running the named suite, recording the red test names, restoring with `git checkout -- <file>` and showing `git diff --exit-code -- <file>` exit 0:
1. In `src/app/api/events/[id]/save/route.ts` delete the legacy helper call in POST → P2 row for save POST red.
2. In `src/app/api/events/[id]/save/route.ts` DELETE, insert an early 403 `{ error: "Account suspended" }` for every caller → D1 row for save DELETE red (the today-shape assertion fails).
3. In `src/app/api/notifications/[id]/route.ts` change the anonymous 401 body text → P1 row red.
4. In `src/app/api/onboarding/complete/route.ts` return 403 `{ error: "Onboarding required" }` first → P3 row red.
5. In `src/app/api/events/following/route.ts` return 401 for anonymous → F-028 row red.
Append the cycles to `evidence/slice-3-mutation-check-handlers.txt`. Append to `evidence/slice-3-characterization-handlers.txt`: `git diff --stat <05-01 head> -- src/app src/lib src/server` (empty), `node scripts/check-characterization-tags.mjs --all` (exit 0, count), `npx tsc --noEmit` (exit 0), and `npx jest --ci` (the full suite, passed count at or above the before-floor). Commit `docs(05-03): handler characterization mutation evidence`.
  </action>
  <verify>
    <automated>node scripts/check-characterization-tags.mjs --all && npx tsc --noEmit && git diff --exit-code -- src/app src/lib src/server</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/slice-3-mutation-check-handlers.txt` records 5 cycles, each with a named red test and a restore line with exit 0
    - `npx jest --ci` passes with the passed count at or above `evidence/floor.before.txt`'s Jest row and 0 failed
    - `git status --porcelain -- src/app src/lib src/server` shows no modified tracked file
  </acceptance_criteria>
  <done>The handler net is proven to notice both a lost guard and an added one, on source proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → handler | the table calls every write arm directly, bypassing the proxy, which is exactly the path REFAC-11 says must hold on its own |
| mutation → source | temporary edits to route files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | a write arm missed by a hand-typed list, left unguarded after slice 3 | mitigate | the arm list is derived from the tree by a node enumeration recorded in evidence and compared to the table (Task 1 acceptance: at least 39 descriptors) |
| T-05-03-02 | Tampering | a mutation left in a route file | mitigate | every cycle ends with `git diff --exit-code`; Task 3 verify re-checks src/app, src/lib, src/server |
| T-05-03-03 | Tampering | a route signature widened to fit a shared table type | mitigate | invoke closures call each handler as its own type; `npx tsc --noEmit` in acceptance |
| T-05-03-04 | Repudiation | today's anonymous bytes guessed rather than measured | mitigate | the evidence file records the measured status and body per arm before they are pinned |
| T-05-03-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci src/__tests__/api/auth-ring src/__tests__/api/events/anonymous-personalized-defect.test.ts` exits 0
- `node scripts/check-characterization-tags.mjs --all` exits 0
- No production file under src/ modified
</verification>

<success_criteria>
Every write arm slice 3 will guard has its surviving bytes pinned and its defect pinned, and F-028's four routes have today's 200 on record, before any handler changes.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/__tests__/api/auth-ring/writeHandlerTable.ts` (`WRITE_ARMS`, persona builders), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts` (PRESERVE), `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT F-088/F-089, `GUARDED`), `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (DEFECT F-028), `evidence/slice-3-characterization-handlers.txt`, `evidence/slice-3-mutation-check-handlers.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-03-SUMMARY.md` when done. Deferred items found go under "Deferred items found" in the SUMMARY; 05-08 registers them.
</output>
