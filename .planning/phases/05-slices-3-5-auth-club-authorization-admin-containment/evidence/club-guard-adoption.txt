# Club guard adoption — plan 05-10 (REFAC-12, DEC-40, DEC-41, F-087)

Recorded 2026-09-24 on the local tree. Base: c407515 (05-09 head). Every command below is quoted
with its exit code; outputs are verbatim unless marked as an excerpt.

## Task 1 — CLUB_ROLES, and requireClubRole at the nine club-route files

### 1a. Census BEFORE (c407515): every from("club_members") in the nine files

$ for f in <nine files>; do git show c407515:"$f" | grep -n 'from("club_members")'; done
src/app/api/clubs/[id]/analytics/route.ts:
  30:      .from("club_members")
src/app/api/clubs/[id]/events/route.ts:
  31:        .from("club_members")
src/app/api/clubs/[id]/invites/route.ts:
  33:      .from("club_members")
  87:      .from("club_members")
  129:        .from("club_members")
src/app/api/clubs/[id]/members/role/route.ts:
  22:    .from("club_members")
  40:    .from("club_members")
  59:    .from("club_members")
src/app/api/clubs/[id]/members/route.ts:
  29:      .from("club_members")
  45:      .from("club_members")
  102:      .from("club_members")
  128:      .from("club_members")
  156:      .from("club_members")
src/app/api/clubs/[id]/route.ts:
  62:      .from("club_members")
  174:    .from("club_members")
src/app/api/clubs/[id]/transfer/route.ts:
  23:    .from("club_members")
  41:    .from("club_members")
  56:    .from("club_members")
  66:    .from("club_members")
  73:      .from("club_members")
src/app/api/clubs/banner/route.ts:
  37:      .from("club_members")
src/app/api/clubs/logo/route.ts:
  37:      .from("club_members")

Total: 22 reads. Gate-site reads (a membership decision about the CALLER): 12 —
  analytics:29 (#1), events:30 (#2, flag), invites:32 (#3) and :86 (#4), members/role:21 (#5),
  members:28 (#6) and :101 (#7), clubs/[id]:61 (#8) and :173 (#9), transfer:22 (#10),
  banner:36 (#11), logo:36 (#12).
Non-gate reads and writes (research § C "Not authorization decisions"): 10 —
  invites:128 (existing-member target read), members/role:39 (target read) and :58 (role update),
  members:44 (listing), :127 (target read) and :155 (delete), transfer:40 (target read),
  :55 (promote), :65 (demote) and :72 (rollback).

### 1b. Census AFTER Task 1

src/app/api/clubs/[id]/analytics/route.ts: club_members=0 requireClubRole(=1
src/app/api/clubs/[id]/events/route.ts: club_members=0 requireClubRole(=1
src/app/api/clubs/[id]/invites/route.ts: club_members=1 requireClubRole(=2
  114:        .from("club_members")
src/app/api/clubs/[id]/members/role/route.ts: club_members=2 requireClubRole(=1
  39:    .from("club_members")
  58:    .from("club_members")
src/app/api/clubs/[id]/members/route.ts: club_members=3 requireClubRole(=2
  37:      .from("club_members")
  114:      .from("club_members")
  142:      .from("club_members")
src/app/api/clubs/[id]/route.ts: club_members=0 requireClubRole(=2
src/app/api/clubs/[id]/transfer/route.ts: club_members=4 requireClubRole(=1
  40:    .from("club_members")
  55:    .from("club_members")
  66:    .from("club_members")
  74:      .from("club_members")
src/app/api/clubs/banner/route.ts: club_members=0 requireClubRole(=1
src/app/api/clubs/logo/route.ts: club_members=0 requireClubRole(=1

Total: 10 reads, all non-gate (the 10 listed above, at their new line numbers). Gate-site reads: 12 -> 0.
requireClubRole( call sites in the nine files: 12 (one per gate/flag, § C rows 1-12).

### 1c. Role sets and messages adopted (§ C rows 1-12)

| # | Site | Accepted set | 403 message |
|---|------|--------------|-------------|
| 1 | clubs/[id]/analytics GET | CLUB_ROLES | You must be a club member to view analytics |
| 2 | clubs/[id]/events GET (flag, gate.ok) | CLUB_ROLES | — (anonymous skips the guard) |
| 3 | clubs/[id]/invites GET | ["owner"] | Only the club owner can view invitations |
| 4 | clubs/[id]/invites POST | ["owner"] | Only the club owner can send invitations |
| 5 | clubs/[id]/members/role PATCH | ["owner"] | Only the club owner can change roles |
| 6 | clubs/[id]/members GET | CLUB_ROLES | You must be a member of this club to view members |
| 7 | clubs/[id]/members DELETE | ["owner"] | Only the club owner can remove members |
| 8 | clubs/[id] PATCH | ["owner"] | Only the club owner can update club details |
| 9 | clubs/[id] DELETE | ["owner"] | Only the club owner can delete the club |
| 10 | clubs/[id]/transfer POST | ["owner"] | Only the club owner can transfer ownership |
| 11 | clubs/banner POST | ["owner"] | Only the club owner can upload a banner |
| 12 | clubs/logo POST | ["owner"] | Only the club owner can upload a logo |

The four GET arms (#1, #2, #3, #6) moved from a bare createClient() + auth.getUser() onto
createRequestContext(); #1, #3 and #6 use requireUser(ctx), whose 401 body {"error":"Unauthorized"}
is byte-equal to the inline one. #2 keeps anonymous access (no guard without a user).
Transfer (#10): the demotion now filters .eq("club_id", clubId).eq("user_id", user.id) (DEC-40);
its writes stay on the service factory until Task 3.

### 1d. No-bypass property (executable code of requireClubRole.ts references no admin / .roles)

$ node -e 'const s=require("fs").readFileSync("src/server/authz/requireClubRole.ts","utf8").replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,"");process.exit(/admin|\.roles/.test(s)?1:0)'
exit 0

### 1e. DEFECT D5 (transfer demotion filter) — ledger protocol, steps 2-4

Step 2, the unedited DEFECT suite against the fixed source (red on exactly the D5 row), excerpt:
$ npx jest --ci src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  ● D5 POST /api/clubs/[id]/transfer as the owner › today: the demotion is filtered by the caller's membership id (moves to club_id, user_id per DEC-40)

    expect(received).toEqual(expected) // deep equality

    - Expected  - 2
    + Received  + 7

      Array [
        Object {
    -     "column": "id",
    +     "column": "club_id",
    +     "op": "eq",
    +     "value": "5eed0000-0000-4000-8000-0c91000000c1",
    +   },
    +   Object {
    +     "column": "user_id",
          "op": "eq",
    -     "value": "5eed0000-0000-4000-8000-0c91000000b1",
    +     "value": "5eed0000-0000-4000-8000-0c9100000001",
        },
      ]

      348 |     const demote = updates(mockElevated.calls, "club_members")[1];
      349 |     expect(demote.payload).toEqual({ role: "organizer" });
    > 350 |     expect(filters(demote)).toEqual([
          |                             ^
      351 |       { op: "eq", column: "id", value: OWNER_MEMBERSHIP_ID },
      352 |     ]);
      353 |   });

      at Object.<anonymous> (src/__tests__/api/clubs/club-owner-writes-defect.test.ts:350:29)
Tests:       1 failed, 12 passed, 13 total
exit 1

Step 3, the moved suite against the fixed source:
$ npx jest --ci src/__tests__/api/clubs/club-owner-writes-defect.test.ts
Tests:       13 passed, 13 total
exit 0

Step 4, the moved suite against the PRE-FIX transfer source (git show c407515:<transfer route>), then restore:
$ git show HEAD:"src/app/api/clubs/[id]/transfer/route.ts" > "src/app/api/clubs/[id]/transfer/route.ts"; npx jest --ci src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  ● D5 POST /api/clubs/[id]/transfer as the owner › fixed (DEC-40): the demotion is filtered by (club_id, user_id), not the caller's membership id
Tests:       1 failed, 12 passed, 13 total
exit 1
$ cp <saved fixed copy> "src/app/api/clubs/[id]/transfer/route.ts"; cmp <saved fixed copy> "src/app/api/clubs/[id]/transfer/route.ts"
cmp identical (exit 0)

### 1f. Floor after Task 1

$ npx jest --ci src/__tests__/api/clubs src/server
Test Suites: 13 passed, 13 total / Tests: 190 passed, 190 total (after the D5 move); exit 0
$ npx jest --ci
Test Suites: 65 passed, 65 total
Tests:       1135 passed, 1135 total
exit 0
$ npx tsc --noEmit
exit 0 (no output)
$ npm run lint
✖ 19 problems (0 errors, 19 warnings)
exit 0 (all 19 warnings are in files this plan does not touch)
$ node scripts/check-characterization-tags.mjs --all | tail -1
ok 29 files
exit 0
$ git diff --quiet c407515 -- src/__tests__/api/clubs/club-gates-characterization.test.ts; echo $?
0
$ node scripts/check-elevated-ratchet.mjs
committed=25 live=24 delta=-1
retired=1 (the list shrank — this is the intended direction)
  - src/app/auth/callback/route.ts
PASS: the live census is a subset of the committed allow-list
exit 0

Jest count: 1133 -> 1135 (+2 seam rows in src/server/__tests__/requireClubRole.test.ts: the unexpected
stored role, and CLUB_ROLES/isClubRole). Two existing seam rows used roles the schema does not admit
("member", "officer") and no longer type-check against the narrowed ClubRole; they now use
"organizer" and CLUB_ROLES with the same assertions. That file is a seam unit test, not a
characterization suite (no PRESERVE/DEFECT tag).

## Task 2 — event-side club flags and gates, inline admin checks to hasRole, DI-25 for events/[id] PATCH

### 2a. Census BEFORE (c407515): club_members reads and inline admin decisions in the four files

src/app/api/events/[id]/analytics/route.ts:
  53:      .from("club_members")
src/app/api/events/[id]/reviews/route.ts:
  152:        .from("club_members")
src/app/api/events/[id]/route.ts:
  126:        isAdmin = (profile?.roles ?? []).includes("admin");
  209:    const isAdmin = roles.includes("admin");
  228:        .from("club_members")
  240:        .from("club_members")
  392:      canDelete = (profile?.roles ?? []).includes("admin");
  397:        .from("club_members")
src/app/api/events/create/route.ts:
  143:    if (roles.includes("admin")) {
  155:          .from("club_members")

Gate/flag membership reads: 6 (events/[id]/analytics:52 #13, reviews:151 #14, events/[id] PATCH :227
and :239 #15 (both arms of the composite), DELETE :396 #16, create:154 #17). Non-gate reads: 0.
Inline admin decisions: 4 (events/[id] GET :126, PATCH :209, DELETE :392, create :143).

### 2b. Census AFTER Task 2

src/app/api/events/[id]/analytics/route.ts: club_members=0 requireClubRole(=1 hasRole(ctx.profile=0 includes("admin")=0
src/app/api/events/[id]/reviews/route.ts: club_members=0 requireClubRole(=1 hasRole(ctx.profile=0 includes("admin")=0
src/app/api/events/[id]/route.ts: club_members=0 requireClubRole(=3 hasRole(ctx.profile=3 includes("admin")=0
src/app/api/events/create/route.ts: club_members=0 requireClubRole(=1 hasRole(ctx.profile=1 includes("admin")=0

Membership reads 6 -> 0; requireClubRole( call sites 6 (§ C rows 13-17, #15 counted twice for its
two arms); inline admin decisions 4 -> 0, each now hasRole(ctx.profile, "admin") on the request
context's profile read (`ctx.profile !== null && hasRole(ctx.profile, "admin")`).

| # | Site | Kind | Accepted set | Message |
|---|------|------|--------------|---------|
| 13 | events/[id]/analytics GET | gate (after the 400 for a club-less event) | CLUB_ROLES | You must be a club member to view analytics |
| 14 | events/[id]/reviews GET | flag isOrganizer = (await requireClubRole(...)).ok | CLUB_ROLES | — |
| 15 | events/[id] PATCH | composite arm; isClubMember from .ok on both branches | CLUB_ROLES | composite 403 "You do not have permission to edit this event" unchanged |
| 16 | events/[id] DELETE | composite arm | CLUB_ROLES | composite 403 "You do not have permission to delete this event" unchanged |
| 17 | events/create POST | flag (auto-approve), inside the unchanged approved-club condition | CLUB_ROLES | — |

events/[id]/analytics GET moved onto createRequestContext + requireUser (401 bytes identical).
events/[id]/reviews GET keeps its own createClient and its `authError || !user` 401; only the flag changed.
events/create keeps its own "name, email, roles" profile read (the organizer label); only the admin
decision moved to the context's profile.

### 2c. events/[id] GET pending_edits gate

Moved: the GET now builds createRequestContext() and strips pending_edits unless the caller is the
creator or `ctx.profile !== null && hasRole(ctx.profile, "admin")`. The condition the plan set was
met: events-detail-characterization.test.ts passes unedited (2d). The read order changes (auth and
profile before the event read, and the profile is now read for the creator too); no PRESERVE row
pins read order, and the response bytes are unchanged.

### 2d. DI-25: directUpdates

$ grep -n "TablesUpdate<\"events\">" "src/app/api/events/[id]/route.ts"
243:    const directUpdates: TablesUpdate<"events"> = {};
Every value it carries is unchanged: the EDITABLE_FIELDS body values, pending_edits null (admin
clearing) and the merged pending_edits object.

### 2e. hasRole's parameter type

src/lib/roles.ts: hasRole(user: User, ...) -> hasRole(user: Pick<User, "roles">, ...). The predicate
reads only `roles`; the full User type made hasRole(ctx.profile, "admin") (the plan's literal form)
fail to type-check. Type-only; every existing caller passes a User, which still satisfies it.
(Rule 3 deviation: src/lib/roles.ts is not in the plan's files list.)

### 2f. Untagged suite, mock wiring only

src/__tests__/api/events/reviews.test.ts "includes anonymized comments for organizer" went red: its
club_members mock row had no `role`, which the guard (correctly) treats as a deny. A real row always
carries role (NOT NULL, club_members_role_check). One fixture line gained role: "organizer"; no
assertion changed:
diff --git a/src/__tests__/api/events/reviews.test.ts b/src/__tests__/api/events/reviews.test.ts
index 0a925d2..0af4646 100644
--- a/src/__tests__/api/events/reviews.test.ts
+++ b/src/__tests__/api/events/reviews.test.ts
@@ -288,7 +288,7 @@ describe("GET /api/events/:id/reviews", () => {
     });
     // User is a club member (organizer)
     mockQueryResults.set("club_members", {
-      data: { id: "cm-1", user_id: "user-123", club_id: "club-1" },
+      data: { id: "cm-1", user_id: "user-123", club_id: "club-1", role: "organizer" },
       error: null,
     });
     mockQueryResults.set("rsvps", { data: null, error: null });

### 2g. Floor after Task 2

$ npx jest --ci src/__tests__/api/events src/__tests__/api/clubs
Test Suites: 22 passed, 22 total
Tests:       371 passed, 371 total
exit 0
$ npx jest --ci
Test Suites: 65 passed, 65 total
Tests:       1135 passed, 1135 total
exit 0
$ npx tsc --noEmit
exit 0 (no output)
$ npm run lint
✖ 19 problems (0 errors, 19 warnings)
exit 0 (warnings all in untouched files)
$ git diff --stat c407515 -- 'src/__tests__/api/events/*-characterization.test.ts' src/__tests__/api/clubs/club-gates-characterization.test.ts
(no output)
$ grep -n 'includes("admin")' "src/app/api/events/[id]/route.ts" src/app/api/events/create/route.ts
(no output) exit 1

## Task 3 — owner club writes through the elevated door (F-087, DEC-41)

### 3a. The writes that moved

| Handler | Write | Before | After |
|---------|-------|--------|-------|
| clubs/[id] PATCH | clubs update (whitelisted columns only, typed TablesUpdate<"clubs">) | cookie client | getElevatedClient() |
| clubs/[id] DELETE | clubs update { status: "deleted" } | cookie client | getElevatedClient() |
| clubs/[id] DELETE | admin_audit_log insert club_deleted (same row shape) | dynamic import of the service module | getElevatedClient(); the insert's error is now console.error'd with ctx.requestId |
| clubs/[id]/members/role PATCH | club_members update { role } | cookie client | getElevatedClient() |
| clubs/[id]/transfer POST | promote, demote (club_id, user_id), rollback, audit insert | createServiceClient() | getElevatedClient() |

Every write runs AFTER requireClubRole(..., ["owner"], <site message>) permits (Task 1). The reads
(the caller's membership, the target member, the club name for confirmName) stay on the cookie
client. Validation and every response body are unchanged.

$ grep -c 'getElevatedClient()' "src/app/api/clubs/[id]/route.ts"; grep -c 'lib/supabase/service' "src/app/api/clubs/[id]/route.ts"
2
0
$ grep -c 'getElevatedClient()' "src/app/api/clubs/[id]/members/role/route.ts"; grep -c 'lib/supabase/service' "src/app/api/clubs/[id]/members/role/route.ts"
1
0
$ grep -c 'getElevatedClient()' "src/app/api/clubs/[id]/transfer/route.ts"; grep -c 'lib/supabase/service' "src/app/api/clubs/[id]/transfer/route.ts"
1
0
$ grep -n 'TablesUpdate<"clubs">' "src/app/api/clubs/[id]/route.ts"
96:    const updates: TablesUpdate<"clubs"> = {};

### 3b. DEFECT D1, D2, D4 — ledger protocol, steps 2-4

Step 2, the unedited DEFECT suite against the fixed source (red on exactly the four "today" rows):
$ npx jest --ci src/__tests__/api/clubs
PASS node src/__tests__/api/clubs/analytics.test.ts
PASS node src/__tests__/api/clubs/club-gates-characterization.test.ts
FAIL node src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  ● D1 PATCH /api/clubs/[id] as the owner › today: the clubs update is issued on the cookie client, not the elevated one
  ● D1 PATCH /api/clubs/[id] as the owner › today: the update is filtered to the club named in the path
  ● D2 DELETE /api/clubs/[id] as the owner › today: the soft-delete update is issued on the cookie client
  ● D4 PATCH /api/clubs/[id]/members/role as the owner › today: the club_members update is issued on the cookie client
Tests:       4 failed, 121 passed, 125 total
exit 1

Step 3, the moved suite against the fixed source:
$ npx jest --ci src/__tests__/api/clubs
PASS node src/__tests__/api/clubs/club-owner-writes-defect.test.ts
PASS node src/__tests__/api/clubs/analytics.test.ts
PASS node src/__tests__/api/clubs/club-gates-characterization.test.ts
Tests:       125 passed, 125 total
exit 0

Step 4, the moved suite against the three PRE-FIX route sources (git show HEAD:<file> for each, HEAD = d8cf84e), then restore:
$ npx jest --ci src/__tests__/api/clubs
FAIL node src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  ● D1 PATCH /api/clubs/[id] as the owner › fixed: the clubs update is issued on the elevated client, and the cookie client records none
  ● D1 PATCH /api/clubs/[id] as the owner › fixed: the elevated update is filtered to the club named in the path
  ● D2 DELETE /api/clubs/[id] as the owner › fixed: the soft-delete update is issued on the elevated client, and the cookie client records none
  ● D4 PATCH /api/clubs/[id]/members/role as the owner › fixed: the club_members update is issued on the elevated client, and the cookie client records none
PASS node src/__tests__/api/clubs/club-gates-characterization.test.ts
PASS node src/__tests__/api/clubs/analytics.test.ts
Tests:       4 failed, 121 passed, 125 total
exit 1
$ cp <saved fixed copy> <file>; cmp <saved fixed copy> <file>   # for each of the three
cmp identical: src/app/api/clubs/[id]/route.ts
cmp identical: src/app/api/clubs/[id]/members/role/route.ts
cmp identical: src/app/api/clubs/[id]/transfer/route.ts

### 3c. The ratchet

$ node scripts/check-elevated-ratchet.mjs
committed=25 live=22 delta=-3
retired=3 (the list shrank — this is the intended direction)
  - src/app/api/clubs/\[id\]/route.ts
  - src/app/api/clubs/\[id\]/transfer/route.ts
  - src/app/auth/callback/route.ts
PASS: the live census is a subset of the committed allow-list
exit 0

Live 24 -> 22: clubs/[id]/route.ts (its dynamic import of the service module) and clubs/[id]/transfer/route.ts
(its static import) left the census. members/role never imported the service module, so it was never in
the census. The committed list is NOT regenerated here (it may only shrink, and DEC-49 regenerates it
once, in 05-15); the live census being a strict subset is the ratchet's pass condition.

### 3d. Playwright — e2e/specs/club-authorization.spec.ts on a clean reset and seed

Reset/seed output filtered for key/secret/token/password/jwt lines; none was present in the kept lines.
$ lsof -i :3000
(no output) exit 1
$ supabase db reset --local
Finished supabase db reset on branch main.
reset exit 0
$ npx tsx scripts/seed/load.ts
seed: target http://127.0.0.1:54321 (local stack)
seed: 10 personas, 5 clubs, 6 memberships, 5 events, 2 rsvps, 0 saved events
seed: pinned now 2026-06-01T16:00:00.000Z (America/Toronto)
seed exit 0
$ npx playwright test e2e/specs/club-authorization.spec.ts
  ✓   1 [setup] › e2e/auth.setup.ts:59:8 › authenticate as onboarded_student (409ms)
  ✓   2 [setup] › e2e/auth.setup.ts:59:8 › authenticate as mid_onboarding_student (281ms)
  ✓   3 [setup] › e2e/auth.setup.ts:59:8 › authenticate as club_member (281ms)
  ✓   4 [setup] › e2e/auth.setup.ts:59:8 › authenticate as club_owner (560ms)
  ✓   5 [setup] › e2e/auth.setup.ts:59:8 › authenticate as multi_club_organizer (253ms)
  ✓   6 [setup] › e2e/auth.setup.ts:59:8 › authenticate as cross_club_attacker (308ms)
  ✓   7 [setup] › e2e/auth.setup.ts:59:8 › authenticate as admin (234ms)
  ✓   8 [setup] › e2e/auth.setup.ts:59:8 › authenticate as banned_permanent (210ms)
  ✓   9 [setup] › e2e/auth.setup.ts:59:8 › authenticate as suspended_active (202ms)
  ✓  10 [setup] › e2e/auth.setup.ts:59:8 › authenticate as suspension_expired (234ms)
  ✓  11 [chromium] › e2e/specs/club-authorization.spec.ts:73:7 › the cross-club attacker on the approved club (REFAC-12) › cannot update the club (191ms)
  ✓  12 [chromium] › e2e/specs/club-authorization.spec.ts:80:7 › the cross-club attacker on the approved club (REFAC-12) › cannot delete the club (148ms)
  ✓  13 [chromium] › e2e/specs/club-authorization.spec.ts:87:7 › the cross-club attacker on the approved club (REFAC-12) › cannot view invitations (105ms)
  ✓  14 [chromium] › e2e/specs/club-authorization.spec.ts:94:7 › the cross-club attacker on the approved club (REFAC-12) › cannot send invitations (105ms)
  ✓  15 [chromium] › e2e/specs/club-authorization.spec.ts:101:7 › the cross-club attacker on the approved club (REFAC-12) › cannot view members (106ms)
  ✓  16 [chromium] › e2e/specs/club-authorization.spec.ts:108:7 › the cross-club attacker on the approved club (REFAC-12) › cannot remove members (105ms)
  ✓  17 [chromium] › e2e/specs/club-authorization.spec.ts:117:7 › the cross-club attacker on the approved club (REFAC-12) › cannot change member roles (103ms)
  ✓  18 [chromium] › e2e/specs/club-authorization.spec.ts:126:7 › the cross-club attacker on the approved club (REFAC-12) › cannot transfer ownership (103ms)
  ✓  19 [chromium] › e2e/specs/club-authorization.spec.ts:133:7 › the cross-club attacker on the approved club (REFAC-12) › cannot view club analytics (108ms)
  ✓  20 [chromium] › e2e/specs/club-authorization.spec.ts:140:7 › the cross-club attacker on the approved club (REFAC-12) › cannot view the analytics of the club's event (106ms)
  ✓  21 [chromium] › e2e/specs/club-authorization.spec.ts:147:7 › the cross-club attacker on the approved club (REFAC-12) › cannot upload the club's banner (104ms)
  ✓  22 [chromium] › e2e/specs/club-authorization.spec.ts:154:7 › the cross-club attacker on the approved club (REFAC-12) › cannot upload the club's logo (107ms)
  ✓  23 [chromium] › e2e/specs/club-authorization.spec.ts:165:7 › the club's organizer (not the owner) › cannot update the club (owner only) (104ms)
  ✓  24 [chromium] › e2e/specs/club-authorization.spec.ts:172:7 › the club's organizer (not the owner) › can view members (106ms)
  ✓  25 [chromium] › e2e/specs/club-authorization.spec.ts:179:7 › the club's organizer (not the owner) › can view club analytics (128ms)
  ✓  26 [chromium] › e2e/specs/club-authorization.spec.ts:187:7 › the club owner › can view invitations (109ms)
  ✓  27 [chromium] › e2e/specs/club-authorization.spec.ts:193:7 › the club owner › can view members (107ms)
  ✓  28 [chromium] › e2e/specs/club-authorization.spec.ts:202:7 › the club owner › can view club analytics (104ms)
  ✓  29 [chromium] › e2e/specs/club-authorization.spec.ts:206:7 › the club owner › FIXED F-087: the owner's own club PATCH answers 200, and status stays unwritable (177ms)
  29 passed (15.9s)
playwright exit 0

The FIXED F-087 test asserts: the owner's PATCH of the current description -> 200 with club.description equal
to the sent value; a PATCH carrying { description: <current>, status: "rejected" } -> 200; a follow-up GET
shows status still "approved" and the description unchanged (T-05-10-03).

Afterwards the stack was reset and seeded again, so it is left as 05-09 left it:
reset exit 0
seed: target http://127.0.0.1:54321 (local stack)
seed: 10 personas, 5 clubs, 6 memberships, 5 events, 2 rsvps, 0 saved events
seed: pinned now 2026-06-01T16:00:00.000Z (America/Toronto)
seed exit 0
$ lsof -i :3000
(no output) exit 1

### 3e. Floor after Task 3

$ npx jest --ci
Test Suites: 65 passed, 65 total
Tests:       1135 passed, 1135 total
exit 0
$ npx tsc --noEmit
exit 0 (no output)
$ npm run lint
✖ 19 problems (0 errors, 19 warnings)
exit 0 (warnings all in untouched files)
$ node scripts/check-characterization-tags.mjs --all | tail -1
ok 29 files
$ git diff --stat c407515 -- src/__tests__/api/clubs/club-gates-characterization.test.ts 'src/__tests__/api/events/*-characterization.test.ts'
(no output)
