# Defect ledger — Phase 5

**Plan:** 05-01 · **Phase:** 05-slices-3-5-auth-club-authorization-admin-containment · **Recorded:** 2026-09-24

Opened by plan 05-01, before any Phase 5 fix exists. There is one row per DEFECT assertion that
moved because the defect it pinned was deliberately fixed. Each row's assertions move in the same
commit as the fix. The finding's status and resolution in `.planning/audit/findings.json` are
edited by the fixing plan or its slice-close plan (DEC-57), not here.

**Characterization tags are file-level** (05-RESEARCH.md C15: `scripts/check-characterization-tags.mjs`
reads only the leading docblock). So a pin that must change and lives in a PRESERVE file is first
**moved** into a new `<name>-defect.test.ts` whose docblock says `DEFECT` and cites its F-nnn. The
move happens in the same commit as the fix, and it gets a row below with "moved out of PRESERVE" in
the Old assertion cell. A PRESERVE file is never edited to make it pass. The one sanctioned
exception is DEC-38's mock-seam change to the callback PRESERVE suite (zero assertion changes). It
gets its own row.

Protocol followed for every row:

1. Apply the fix.
2. Run the DEFECT suite unedited. It must go **red**: the before-assertions fail against the
   fixed code.
3. Move the assertions to the fixed shape. The suite must go **green**.
4. Put the pre-fix source back temporarily and run the moved suite. It must go **red**: the
   after-assertions fail against the defect. Then restore the fixed source and prove it is
   byte-identical with `cmp`.
5. The PRESERVE suites covering the same surface pass **unedited** throughout.

Commit hashes are filled in by the commit that follows each fix, because a commit cannot name its
own hash. Every row carries the `INTENTIONAL BEHAVIOUR CHANGE` commit it belongs to when the fix
changes wire bytes.

## Ledger

| F-nnn | Suite | Old assertion | New assertion | Commit | Plan |
|-------|-------|---------------|---------------|--------|------|
| F-003 (config half; census step one of two) | `src/lib/__tests__/env-assertions-defect.test.ts` (DEFECT; the file is unchanged apart from its expected list, the comment over that list, and the count in the first test's title) | `EXPECTED_TODAY` = the 15 pairs measured on a7b02a5: auth-debug `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL` ×2; callback `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL` ×2, `SUPABASE_SERVICE_ROLE_KEY`; signout `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL`; `lib/supabase/client.ts` ANON_KEY, URL; `lib/supabase/server.ts` ANON_KEY, URL; `lib/supabase/service.ts` URL, `SUPABASE_SERVICE_ROLE_KEY` | `EXPECTED_TODAY` = the callback's four: `src/app/auth/callback/route.ts` `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL` ×2, `SUPABASE_SERVICE_ROLE_KEY`. Protocol steps 2–4 are in `evidence/env-and-guards.txt` §1e: unedited suite red (−11), moved suite green, pre-fix source red (+11), and the fixed source restored `cmp`-identical. No wire bytes change, so this is not an INTENTIONAL BEHAVIOUR CHANGE row. | refactor(05-04) `7bef995` | 05-04 |
| F-003 (proxy half) | `src/proxy-defect.test.ts` (DEFECT) | row a: NEXT_PUBLIC_SUPABASE_URL unset, anonymous `/profile` → no Location, status 200, `createServerClient` not called (the env pass-through) | row a: → 500, body `Internal Server Error`, not JSON, no Location, `createServerClient` not called, `[Middleware] Error:` logged with a `MissingEnvError`. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-003 (proxy half) | `src/proxy-defect.test.ts` (DEFECT) | none: row a had no `/api/*` variant (added in the move, as the plan requires) | row a2: NEXT_PUBLIC_SUPABASE_URL unset, anonymous `/api/events` → 500, JSON content type, body `{"error":"Failed to process request"}`, no client built. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-088 | `src/proxy-defect.test.ts` (DEFECT) | row b: `auth.getUser` rejecting → no Location, status 200, logged (the catch passed the request through) | row b: → 500 `Internal Server Error`, no Location, `console.error("[Middleware] Error:", failure)`. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-088 | `src/proxy-defect.test.ts` (DEFECT) | row c: users read error XX000 on `/my-events` → one users read, no Location, status 200 (a failed read counted as not banned) | row c: → one users read, 500, no Location, no sign-out. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-088 (with DEC-35) | `src/proxy-defect.test.ts` (DEFECT) | row d (POST `/api/events/x/save`): PGRST116 → one users read, no Location, status 200, no sign-out | row d-api: → one users read, 403, JSON content type, body `{"error":"Profile not found"}`, no Location, no sign-out. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-088 (with DEC-35) | `src/proxy-defect.test.ts` (DEFECT) | row d (GET `/my-events`): PGRST116 → one users read, no Location, status 200, no sign-out | row d-page: → one users read, `auth.signOut` called once, 307 to `https://proxy.test/?error=profile_sync_failed`, and the sign-out's clearing cookie (`Max-Age=0`) is on the redirect. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. An extra mutation (§4) drops the cookie copy and turns only this row red. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-062 | `src/proxy-defect.test.ts` (DEFECT) | row e: banned POST `/api/events/x/save` → 307 to `/banned`, content type not JSON | row e: → 403, no Location, content type contains `application/json`, body `{"error":"Account suspended"}`. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-089 | `src/proxy-defect.test.ts` (DEFECT) | row f: un-onboarded (DB `false`), no `needs_onboarding` cookie, `/my-events` → no Location, status 200 | row f: → 307 to `/onboarding` after one users read (database truth). Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-089 | `src/proxy-defect.test.ts` (DEFECT) | row g: onboarded (DB `true`) with a stale `needs_onboarding=1` cookie on `/` → 307 to `/onboarding` | row g: → no Location, status 200. Protocol steps 2–4: `evidence/proxy-refactor.txt` §1–§3. | fix(05-05) proxy commit `e2d6d3a` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-004 | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | moved out of PRESERVE: `route.test.ts` test 7 "appends admin to the existing roles for an address in ADMIN_EMAILS" — `mockUpdate` called with `{ roles: ["user","admin"] }`, `mockUpdateEq` with `("id","u-admin")` | with the allowlist variable set to the signing-in address: `mockUpdate` and `mockUpdateEq` never called, one upsert, redirect to `https://callback.test/` with no `error`. The 05-04-head test 7 is red against the fixed route (§2). Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-004 (with F-040; the FO-05 missing-key case) | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | moved out of PRESERVE: `route.test.ts` test 8 "skips profile sync entirely and still admits the user when the service key is absent" — `createServiceClient` not called, no upsert, 307 to `/` with no `error`, no onboarding cookie | the door throwing `new MissingEnvError("SUPABASE_SERVICE_ROLE_KEY")`: no upsert, no update, `signOut` called once, 307 to `https://callback.test/?error=profile_sync_failed`, no `needs_onboarding` cookie. Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-077 | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | absolute `next=https://evil.test/x` → Location origin `https://evil.test`, pathname `/x` | Location is exactly `https://callback.test/` (origin `https://callback.test`, pathname `/`), no sign-out. Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-077 | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | protocol-relative `next=//evil.test/x` → host `evil.test`, origin `https://evil.test`, pathname `/x` | Location is exactly `https://callback.test/`. Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-077 | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | slash-backslash `next=/\evil.test/x` → host `evil.test`, origin `https://evil.test`, pathname `/x` (the case F-077's register wording alone would miss, 05-02 cycle 6c) | Location is exactly `https://callback.test/`. The same-origin control (`/my-events`) is unchanged and green. Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| FO-05 (DEC-38 (b)) | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | none: new fixed-shape row (the pre-fix route logged an upsert error and admitted the user) | upsert returning `{ error }` → `signOut` once, 307 to `/?error=profile_sync_failed` on the request origin, no onboarding cookie. Red against the pre-fix route (§4). Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| FO-05 (DEC-38 (b)) | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | none: new fixed-shape row (the pre-fix route read `!profile?.onboarding_completed` from a null profile and sent the user to `/onboarding`) | profile read returning `{ data: null }` → same fail-closed shape; the select column list stays `onboarding_completed, roles`. Red against the pre-fix route (§4). Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| FO-05 (DEC-38 (b); extra row beyond the plan) | `src/app/auth/callback/route-defect.test.ts` (DEFECT) | none: new fixed-shape row | with the exchange setting a session cookie through the captured `setAll` and the sign-out clearing it, the failed-sync redirect carries the clearing cookie (value empty, `Max-Age=0`), not the session. Red against the pre-fix route (§4). Protocol steps 2–4: `evidence/callback-refactor.txt` §1–§4. | fix(05-05) callback commit `077a081` (INTENTIONAL BEHAVIOUR CHANGE) | 05-05 |
| F-003 (config half; census step two of two) | `src/lib/__tests__/env-assertions-defect.test.ts` (DEFECT; changed: the expected list, the comment over it, the first test's title, the describe title and the Status line) | `EXPECTED_TODAY` = the callback's four: `src/app/auth/callback/route.ts` `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_SUPABASE_URL` ×2, `SUPABASE_SERVICE_ROLE_KEY` | `EXPECTED_TODAY` = `[]`. Unedited census red against the fixed callback (−6 +1), moved census green, pre-fix callback restored red (+6 −1), fixed callback restored `cmp`-identical: `evidence/callback-refactor.txt` §5. No wire bytes change from the census itself. | fix(05-05) callback commit `077a081` | 05-05 |
| DEC-38 mock-seam (no finding; the sanctioned PRESERVE exception) | `src/app/auth/callback/route.test.ts` (PRESERVE) | `beforeEach`: `mockCreateServiceClient.mockReturnValue({ from: mockFrom })` | `mockCreateServiceClient.mockReturnValue({ from: mockFrom, auth: { admin: { deleteUser: mockDeleteUser } } })`, because the orphan deletion now reaches the service factory through the elevated door. Zero assertions changed: the six remaining `it` blocks are byte-identical to c14fdc8 (`evidence/callback-refactor.txt` §3). The two moved tests and their two "Tests cover" bullets were removed (one bullet points to `route-defect.test.ts`). The 05-04-head suite run against the fixed route shows test 4 red without this seam change (§2). | fix(05-05) callback commit `077a081` | 05-05 |
| F-088 (F-062 related; DEC-24 ban asymmetry) | `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (DEFECT, new) | moved out of PRESERVE: `save-characterization.test.ts` describe "ban asymmetry — pinned for Phase 5 (DEC-24)", "honours a permanently banned caller's DELETE: 200 { saved: false } and the row removed": 200, `{ saved: false }`, `savedRows` empty | a permanently banned caller's save DELETE → 403 `{"error":"Account suspended"}`, the caller's saved row still present, no insert, update, delete or rpc. The block was deleted from the Phase 4 suite with nothing else changed (deletions only, `evidence/handler-adoption-events.txt` Task 1 §5). Protocol steps 2–4: §1 (unedited red), §2 (moved green), §3 (pre-fix source red, fixed source restored `cmp`-identical). | fix(05-06) events-family commit `aa50ff6` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-088 (F-062 related; DEC-24 ban asymmetry) | `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (DEFECT, new) | moved out of PRESERVE: `rsvp-characterization.test.ts` describe "ban asymmetry — pinned for Phase 5 (DEC-24)", "honours a banned caller's DELETE: 200 'RSVP cancelled' and the row cancelled": 200, `{ success: true, message: "RSVP cancelled" }`, caller's row `cancelled` | a permanently banned caller's rsvp DELETE → 403 `{"error":"Account suspended"}`, the caller's RSVP still `going`, the other users' rows unchanged, no insert, update, delete or rpc. Same protocol evidence as the row above. | fix(05-06) events-family commit `aa50ff6` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-088 D1 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT; the only edit is 14 ids added to `GUARDED`) | banned caller, 7 rows (the events-family arms without the legacy helper: `events/[id]/save DELETE`, `events/[id]/rsvp DELETE`, `events/[id]/report POST`, `events/[id]/reviews POST`, `events/[id] PATCH`, `events/[id] DELETE`, `events/upload-image POST`): `refusedWith(outcome, 403, "Account suspended")` is false | exactly 403 `{"error":"Account suspended"}`, and the call log is exactly `["users.select"]` (the context's one read). Protocol steps 2–4: `evidence/handler-adoption-events.txt` Task 1 §1–§3. | fix(05-06) events-family commit `aa50ff6` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-088 D2 (DEC-35) | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | no profile row, 14 rows (the 12 events-family arms plus `recommendations/feedback POST` and `user/engagement POST`): `refusedWith(outcome, 403, "Profile not found")` is false | exactly 403 `{"error":"Profile not found"}`, no insert, update, delete or rpc. Protocol steps 2–4: Task 1 §1–§3. | fix(05-06) events-family commit `aa50ff6` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-089 D3 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | un-onboarded caller, the same 14 rows: `refusedWith(outcome, 403, "Onboarding required")` is false | exactly 403 `{"error":"Onboarding required"}`, no insert, update, delete or rpc. Protocol steps 2–4: Task 1 §1–§3. | fix(05-06) events-family commit `aa50ff6` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-028 (DEC-39) | `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (DEFECT) | A1: anonymous `GET /api/events/following` → 200, body exactly `{"events":[]}` | → 401, body exactly `{"error":"Unauthorized"}` and not `{"events":[]}`. The signed-in row S1 (200, `club_followers.select` reached) is unchanged and green. Protocol steps 2–4: `evidence/handler-adoption-events.txt` Task 2 §1–§3. | fix(05-06) F-028 commit `7ff08c1` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-028 (DEC-39) | `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (DEFECT) | A2: anonymous `GET /api/events/friends-activity` → 200, body exactly `{"events":[]}` | → 401 `{"error":"Unauthorized"}`, not `{"events":[]}`. S2 unchanged and green. Protocol steps 2–4: Task 2 §1–§3. | fix(05-06) F-028 commit `7ff08c1` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-028 (DEC-39) | `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (DEFECT) | A3: anonymous `GET /api/events/friends-organizing` → 200, body exactly `{"events":[]}` | → 401 `{"error":"Unauthorized"}`, not `{"events":[]}`. S3 unchanged and green. Protocol steps 2–4: Task 2 §1–§3. | fix(05-06) F-028 commit `7ff08c1` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-028 (DEC-39) | `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (DEFECT) | A4: anonymous `GET /api/events/[id]/friends` → 200, body exactly `{"friends":[],"count":0}` | → 401 `{"error":"Unauthorized"}`, not `{"friends":[],"count":0}`. S4 unchanged and green. Protocol steps 2–4: Task 2 §1–§3. | fix(05-06) F-028 commit `7ff08c1` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-028 (DEC-39; the file is the F-071 DEFECT suite) | `src/__tests__/api/events/friends-defect.test.ts` (DEFECT) | "an unauthenticated caller short-circuits before any of this": status 200, body `{ friends: [], count: 0 }`, rpc not called | status 401, body `{ error: "Unauthorized" }`, rpc still not called. The docblock gained one paragraph naming the move; no other test changed. Protocol steps 2–4: Task 2 §1–§3. | fix(05-06) F-028 commit `7ff08c1` (INTENTIONAL BEHAVIOUR CHANGE) | 05-06 |
| F-088 D1 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT; the only edit is 12 ids added to `GUARDED`, 26 total) | banned caller, 10 rows (the clubs-family arms without the legacy helper: `clubs/[id]/follow POST`, `clubs/[id]/follow DELETE`, `clubs/[id]/invites POST`, `clubs/[id]/members/role PATCH`, `clubs/[id]/members DELETE`, `clubs/[id] PATCH`, `clubs/[id] DELETE`, `clubs/[id]/transfer POST`, `clubs/banner POST`, `clubs/logo POST`): `refusedWith(outcome, 403, "Account suspended")` is false | exactly 403 `{"error":"Account suspended"}`, and the call log is exactly `["users.select"]` (the context's one read). `clubs POST` and `clubs/[id]/appeal POST` (the two legacy-helper arms) keep their PRESERVE P2 bytes, now from `requireActiveUser`. Protocol steps 2–4: `evidence/handler-adoption-rest.txt` Task 1 §1–§3. | fix(05-07) clubs-family commit `aa2191e` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-088 D2 (DEC-35) | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | no profile row, 12 rows (the 10 above plus `clubs POST` and `clubs/[id]/appeal POST`): `refusedWith(outcome, 403, "Profile not found")` is false | exactly 403 `{"error":"Profile not found"}`, no insert, update, delete or rpc. Protocol steps 2–4: Task 1 §1–§3. | fix(05-07) clubs-family commit `aa2191e` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-089 D3 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | un-onboarded caller, the same 12 rows: `refusedWith(outcome, 403, "Onboarding required")` is false | exactly 403 `{"error":"Onboarding required"}`, no insert, update, delete or rpc. Protocol steps 2–4: Task 1 §1–§3. | fix(05-07) clubs-family commit `aa2191e` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-088 D1 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT; 13 ids added to `GUARDED`, 39 total, plus the completeness test and the Status line) | banned caller, 12 rows (the Task 2 arms without the legacy helper: `users/[id]/follow DELETE`, `users/[id] PATCH`, `profile/avatar POST`, `profile/banner POST`, `profile/inferred-tags DELETE`, `profile/interests PUT`, `notifications/[id] PATCH`, `notifications POST`, `organizer-requests POST`, `onboarding/complete POST`, `interactions POST`, `feedback POST`): `refusedWith(outcome, 403, "Account suspended")` is false | exactly 403 `{"error":"Account suspended"}`, and the call log is exactly `["users.select"]`. `users/[id]/follow POST` (the last legacy-helper arm) keeps its PRESERVE P2 bytes, now from `requireActiveUser`. Protocol steps 2–4: `evidence/handler-adoption-rest.txt` Task 2 §1–§3. | fix(05-07) remaining-arms commit `c620d16` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-088 D2 (DEC-35) | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | no profile row, 13 rows (the 12 above plus `users/[id]/follow POST`): `refusedWith(outcome, 403, "Profile not found")` is false | exactly 403 `{"error":"Profile not found"}`, no insert, update, delete or rpc. Includes both onboarding exemptions (they still call `requireActiveUser`) and both anonymous-tolerant writers (guarded when a user is present). Protocol steps 2–4: Task 2 §1–§3. | fix(05-07) remaining-arms commit `c620d16` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-089 D3 | `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` (DEFECT) | un-onboarded caller, 11 rows (the 13 minus DEC-34's two exemptions, `users/[id] PATCH` and `onboarding/complete POST`, which have no D3 row): `refusedWith(outcome, 403, "Onboarding required")` is false | exactly 403 `{"error":"Onboarding required"}`, no insert, update, delete or rpc. PRESERVE P3 (both exemptions admit an un-onboarded caller) and P1/P5 (the anonymous-tolerant arms) pass unedited. Protocol steps 2–4: Task 2 §1–§3; the completeness test's red run is §3b. | fix(05-07) remaining-arms commit `c620d16` (INTENTIONAL BEHAVIOUR CHANGE) | 05-07 |
| F-087 (DEC-40 half) | `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (DEFECT; D5 row and the Status line only) | D5 "today": the transfer demotion `update({ role: "organizer" })` is filtered by the caller's membership id, `[{ eq id OWNER_MEMBERSHIP_ID }]` | D5 "fixed (DEC-40)": filtered by `[{ eq club_id CLUB_ID }, { eq user_id OWNER.id }]`, because `requireClubRole` reports the role only. The promotion, the audit insert and the elevated-only write list are unchanged. Protocol steps 2–4: `evidence/club-guard-adoption.txt` §1e (unedited red on D5 only, moved green, pre-fix transfer source red, restored `cmp`-identical). `club-gates-characterization.test.ts` passes unedited. No wire bytes change, so this is not an INTENTIONAL BEHAVIOUR CHANGE row. | refactor(05-10) club-route gates commit `a6fd637` | 05-10 |
| F-087 | `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (DEFECT; Status line now `FIXED in 05-10`) | D1 "today" ×2: `PATCH /api/clubs/[id]` as the owner issues one `clubs` update on the cookie client (fake A) and none on the elevated client (fake B); that update is filtered `[{ eq id CLUB_ID }]` | D1 "fixed" ×2: one `clubs` update on fake B, none on fake A; the fake-B update is filtered `[{ eq id CLUB_ID }]`. The whitelist row ("stays": payload exactly `{ description: "x" }`, no `status`) is unchanged. Protocol steps 2–4: `evidence/club-guard-adoption.txt` §3b (unedited red on exactly the four "today" rows D1×2, D2, D4; moved green; the three pre-fix route sources red on the same four; restored `cmp`-identical). `club-gates-characterization.test.ts` (107 PRESERVE) passes unedited throughout. | fix(05-10) owner-writes commit `4531ee2` (INTENTIONAL BEHAVIOUR CHANGE) | 05-10 |
| F-087 | `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (DEFECT) | D2 "today": `DELETE /api/clubs/[id]` as the owner answers `{ success: true }` and issues the `{ status: "deleted" }` update on fake A, filtered `[{ eq id CLUB_ID }]`, none on fake B | D2 "fixed": the same 200 `{ success: true }`, the same payload and filter, on fake B; none on fake A. The `club_deleted` audit row stays on fake B (unchanged row). Protocol steps 2–4: `evidence/club-guard-adoption.txt` §3b (unedited red on exactly the four "today" rows D1×2, D2, D4; moved green; the three pre-fix route sources red on the same four; restored `cmp`-identical). `club-gates-characterization.test.ts` (107 PRESERVE) passes unedited throughout. | fix(05-10) owner-writes commit `4531ee2` (INTENTIONAL BEHAVIOUR CHANGE) | 05-10 |
| F-087 | `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (DEFECT) | D4 "today": `PATCH /api/clubs/[id]/members/role` as the owner issues the `{ role: "organizer" }` update on fake A, filtered `[{ eq id ORGANIZER_MEMBERSHIP_ID }]`, none on fake B | D4 "fixed": the same payload and filter on fake B; none on fake A. D3 (the caller's membership read on fake A) and the D5 "stays" rows are unchanged; two D5 titles changed "today:" to "stays:" with no assertion change. Protocol steps 2–4: `evidence/club-guard-adoption.txt` §3b (unedited red on exactly the four "today" rows D1×2, D2, D4; moved green; the three pre-fix route sources red on the same four; restored `cmp`-identical). `club-gates-characterization.test.ts` (107 PRESERVE) passes unedited throughout. | fix(05-10) owner-writes commit `4531ee2` (INTENTIONAL BEHAVIOUR CHANGE) | 05-10 |
| F-087 | `e2e/specs/club-authorization.spec.ts` (Playwright; the test titled `DEFECT F-087` becomes `FIXED F-087`) | the owner PATCHes the approved club's current description → 500 `{"error":"Failed to update club"}`; the description is unchanged afterwards | the same PATCH → 200 with `club.description` equal to the sent value; a second PATCH carrying `{ description: <current>, status: "rejected" }` → 200; a follow-up GET shows `status` still `approved` and the description unchanged. Run after a clean `supabase db reset --local` and seed: `evidence/club-guard-adoption.txt` §3d. | fix(05-10) owner-writes commit `4531ee2` (INTENTIONAL BEHAVIOUR CHANGE) | 05-10 |
