---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 10
type: execute
wave: 9
depends_on: ["05-09"]
files_modified:
  - src/server/authz/requireClubRole.ts
  - src/server/__tests__/requireClubRole.test.ts
  - src/app/api/clubs/[id]/analytics/route.ts
  - src/app/api/clubs/[id]/events/route.ts
  - src/app/api/clubs/[id]/invites/route.ts
  - src/app/api/clubs/[id]/members/role/route.ts
  - src/app/api/clubs/[id]/members/route.ts
  - src/app/api/clubs/[id]/route.ts
  - src/app/api/clubs/[id]/transfer/route.ts
  - src/app/api/clubs/banner/route.ts
  - src/app/api/clubs/logo/route.ts
  - src/app/api/events/[id]/analytics/route.ts
  - src/app/api/events/[id]/reviews/route.ts
  - src/app/api/events/[id]/route.ts
  - src/app/api/events/create/route.ts
  - src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  - src/__tests__/api/clubs/analytics.test.ts
  - src/__tests__/api/events/analytics.test.ts
  - src/__tests__/api/events/reviews.test.ts
  - src/__tests__/api/events/date-validation.test.ts
  - e2e/specs/club-authorization.spec.ts
  - src/server/db/elevated/REGISTRY.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/club-guard-adoption.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-12]

must_haves:
  truths:
    - "Every club-membership decision in an API handler goes through requireClubRole with exactly the role set that site accepted before (owner, or owner and organizer), and every deny keeps its exact 403 message"
    - "No club gate grants anything to a site-wide admin who is not a member"
    - "The inline admin checks in events/[id] and events/create decide through hasRole on the request profile"
    - "A club owner can edit their club, soft-delete it and change a member's role (F-087), through the elevated door with a column whitelist, so status, created_by and id stay unwritable"
    - "The two DI-25 blocking sites slice 4 owns (clubs/[id] PATCH update payload, events/[id] PATCH directUpdates) are typed with the generated TablesUpdate types"
  artifacts:
    - path: "src/server/authz/requireClubRole.ts"
      provides: "CLUB_ROLES and a ClubRole narrowed to its members"
      contains: "export const CLUB_ROLES = [\"owner\", \"organizer\"] as const"
    - path: "src/server/db/elevated/REGISTRY.md"
      provides: "rows for owner club edit/soft-delete, owner role change and transfer, club-deletion audit record"
      contains: "src/app/api/clubs/[id]/route.ts"
  key_links:
    - from: "src/app/api/clubs/[id]/route.ts"
      to: "src/server/authz/requireClubRole.ts"
      via: "requireClubRole(ctx.supabase, clubId, user.id, [\"owner\"], \"Only the club owner can update club details\") before the elevated write"
      pattern: "requireClubRole\\("
    - from: "src/app/api/clubs/[id]/route.ts"
      to: "src/server/db/elevated/index.ts"
      via: "getElevatedClient() for the owner's clubs update and soft-delete"
      pattern: "getElevatedClient\\(\\)"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "requireClubRole's result shape is not widened; transfer demotes by (club_id, user_id) instead (DEC-40)"
    - "src/__tests__/api/clubs/club-gates-characterization.test.ts and every other PRESERVE suite pass unedited"
    - "No RLS policy is widened to make a cookie-client write work (research Pitfall 10)"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: from("club_members") -->

<objective>
Slice 4, refactor: collapse the hand-rolled club-membership checks into `requireClubRole` (REFAC-12, DEC-40), move the events-side inline admin checks to `hasRole`, and make owner club writes work through the door (F-087, DEC-41).

Purpose: ROADMAP criterion 2's authz half — "the 19 hand-rolled club-membership checks collapse into requireClubRole" — with every deny byte preserved and no admin bypass; and the Validated workflow "club organizers create and edit clubs … manage member roles" actually working, which research C11 measured as broken in both environments.

Output: the guard adopted at all 17 sites, `CLUB_ROLES`, owner writes through the door with REGISTRY rows, the F-087 pins flipped, two DI-25 sites typed.
</objective>

<slice_outcome>
Green when `club-gates-characterization.test.ts` passes unedited, `club-owner-writes-defect.test.ts` passes in its fixed shape, the club-authorization spec's F-087 test is FIXED (owner PATCH 200, status unchanged), every hand-rolled gate-site `club_members` read is gone from the 13 files, and `npx jest --ci` is green.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: CLUB_ROLES, and requireClubRole at the nine club-route files</name>
  <files>src/server/authz/requireClubRole.ts, src/server/__tests__/requireClubRole.test.ts, src/app/api/clubs/[id]/analytics/route.ts, src/app/api/clubs/[id]/events/route.ts, src/app/api/clubs/[id]/invites/route.ts, src/app/api/clubs/[id]/members/role/route.ts, src/app/api/clubs/[id]/members/route.ts, src/app/api/clubs/[id]/route.ts, src/app/api/clubs/[id]/transfer/route.ts, src/app/api/clubs/banner/route.ts, src/app/api/clubs/logo/route.ts, src/__tests__/api/clubs/analytics.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/club-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md</files>
  <read_first>
    - src/server/authz/requireClubRole.ts (74 lines) — the no-bypass docblock, the result shape, `ClubRole = Tables<"club_members">["role"]` (string)
    - src/server/__tests__/requireClubRole.test.ts
    - 05-RESEARCH.md § C rows 1-12 — per-site accepted roles and exact messages
    - src/__tests__/api/clubs/club-gates-characterization.test.ts — the bytes that must not move
    - each of the nine route files at the § C lines (the write arms already carry createRequestContext and the ring guards from 05-07)
  </read_first>
  <action>
In `requireClubRole.ts` add `export const CLUB_ROLES = ["owner", "organizer"] as const;` and redefine `export type ClubRole = (typeof CLUB_ROLES)[number];`, keeping `acceptedRoles: readonly ClubRole[]`, the message parameter, the no-bypass docblock (add C1 and DEC-40) and the result shape; narrow the stored role with a type guard (`isClubRole(value: string): value is ClubRole`) so an unexpected DB value is a deny, never a permit. Extend `requireClubRole.test.ts` with an unexpected-role row (deny, actualRole reported) — a seam unit test, not a characterization suite.

At each site replace the hand-rolled membership select and its deny branch with `const gate = await requireClubRole(ctx.supabase, clubId, user.id, <roles>, "<exact message>"); if (!gate.ok) return gate.response;` — roles and messages exactly per § C: clubs/[id]/analytics GET `CLUB_ROLES` "You must be a club member to view analytics"; clubs/[id]/events GET flag `CLUB_ROLES` using `gate.ok` as the boolean (anonymous allowed; skip the guard when there is no user, as today); invites GET `["owner"]` "Only the club owner can view invitations"; invites POST `["owner"]` "Only the club owner can send invitations"; members/role PATCH `["owner"]` "Only the club owner can change roles"; members GET `CLUB_ROLES` "You must be a member of this club to view members"; members DELETE `["owner"]` "Only the club owner can remove members"; clubs/[id] PATCH `["owner"]` "Only the club owner can update club details"; clubs/[id] DELETE `["owner"]` "Only the club owner can delete the club"; transfer POST `["owner"]` "Only the club owner can transfer ownership"; clubs/banner and clubs/logo POST `["owner"]` "Only the club owner can upload a banner" / "…a logo" (after their existing 400 on a missing clubId). GET arms that do not yet have a context adopt `createRequestContext()` and `requireUser(ctx)` while keeping any anonymous bytes the PRESERVE table records. Transfer: the demotion that used the caller's membership id now filters `.eq("club_id", clubId).eq("user_id", user.id)` (DEC-40); the writes stay where they are until Task 3. Target-member reads and data listings (research § C "Not authorization decisions") stay plain selects.

Run `src/__tests__/api/clubs` (the PRESERVE table must pass unedited), the full `npx jest --ci`, `npx tsc --noEmit`, `npm run lint`. If an untagged suite (for example `src/__tests__/api/clubs/analytics.test.ts`) fails only because its mock chain no longer matches the guard's query shape, adjust the mock wiring without changing an assertion and record it in `evidence/slice-3-fixture-completions.md` under a slice-4 heading. Record in `evidence/club-guard-adoption.txt` a before/after census of gate-site membership reads in these nine files. Commit `refactor(05-10): club-route gates go through requireClubRole with their exact role sets (REFAC-12)`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/clubs src/server && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/authz/requireClubRole.ts` contains `export const CLUB_ROLES = ["owner", "organizer"] as const` and `(typeof CLUB_ROLES)[number]`
    - `git diff <05-09 head> -- src/__tests__/api/clubs/club-gates-characterization.test.ts` prints nothing and the suite passes
    - Each of the nine route files contains `requireClubRole(`; `evidence/club-guard-adoption.txt` shows the gate-site membership-read count in these files falling to the non-gate reads only
    - `requireClubRole.ts` contains no reference to `roles` or `admin` in executable code (the no-bypass property), checked by `node -e 'const s=require("fs").readFileSync("src/server/authz/requireClubRole.ts","utf8").replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,"");process.exit(/admin|\.roles/.test(s)?1:0)'`
  </acceptance_criteria>
  <done>The nine club routes decide membership in one place, with the same role sets and the same messages as before, and the guard still cannot be bypassed.</done>
</task>

<task type="auto">
  <name>Task 2: Event-side club flags and gates, inline admin checks to hasRole, DI-25 typing for events/[id] PATCH</name>
  <files>src/app/api/events/[id]/analytics/route.ts, src/app/api/events/[id]/reviews/route.ts, src/app/api/events/[id]/route.ts, src/app/api/events/create/route.ts, src/__tests__/api/events/analytics.test.ts, src/__tests__/api/events/reviews.test.ts, src/__tests__/api/events/date-validation.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/club-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md</files>
  <read_first>
    - 05-RESEARCH.md § C rows 13-17 and § E "Inline admin checks to remove" (events/[id] GET pending gate, PATCH, DELETE; events/create)
    - src/app/api/events/[id]/route.ts — GET pending gate, PATCH composite (admin, creator, club member → isClubMember moderation effect), DELETE composite, directUpdates near the PATCH update
    - src/app/api/events/create/route.ts — the admin check and the auto-approve membership read
    - src/lib/roles.ts hasRole; src/lib/supabase/types.ts TablesUpdate
    - src/__tests__/api/events/events-detail-characterization.test.ts — must pass unedited (it pins the GET)
  </read_first>
  <action>
events/[id]/analytics GET: `requireClubRole(ctx.supabase, event.club_id, user.id, CLUB_ROLES, "You must be a club member to view analytics")` after the existing 400 for an event with no club. events/[id]/reviews GET: the isOrganizer flag becomes `(await requireClubRole(…, CLUB_ROLES)).ok` when a user and a club exist. events/[id] PATCH and DELETE: the club-member arm of each composite becomes `requireClubRole(…, CLUB_ROLES).ok`, the admin arm becomes `hasRole(ctx.profile, "admin")`, and the composite deny messages stay "You do not have permission to edit this event" / "…delete this event"; `isClubMember`'s effect on moderation is unchanged. events/create: the admin check becomes `hasRole(ctx.profile, "admin")` and the auto-approve membership read becomes `requireClubRole(…, CLUB_ROLES).ok` — keep the approved-club condition exactly. events/[id] GET: move its pending-event admin check to `hasRole` on a `createRequestContext()` profile only if `events-detail-characterization.test.ts` passes unedited afterwards; if it does not, leave the GET's current read in place and record why in `evidence/club-guard-adoption.txt`. Type the PATCH's `directUpdates` as `TablesUpdate<"events">` (DI-25 site `events/[id]/route.ts`, research § H) without changing any value it carries.

Run every events suite; PRESERVE suites unedited; untagged suites may need mock wiring only (record in `evidence/slice-3-fixture-completions.md`). Run the full `npx jest --ci`, `npx tsc --noEmit`, `npm run lint`. Commit `refactor(05-10): event-side club decisions through requireClubRole; admin checks through hasRole (REFAC-12, REFAC-13)`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/events src/__tests__/api/clubs && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four files contains `requireClubRole(`; `src/app/api/events/[id]/route.ts` and `src/app/api/events/create/route.ts` contain `hasRole(ctx.profile, "admin")` and no `.includes("admin")` in the PATCH, DELETE and create paths
    - `git diff <05-09 head> -- 'src/__tests__/api/events/*-characterization.test.ts' src/__tests__/api/clubs/club-gates-characterization.test.ts` prints nothing and those suites pass
    - `src/app/api/events/[id]/route.ts` contains `TablesUpdate<"events">`
    - `npx jest --ci` exits 0, 0 failed
  </acceptance_criteria>
  <done>Every club decision on the events side goes through the same guard, admin is decided by the one role predicate, and the event read and write bytes are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Owner club writes through the elevated door (F-087), with REGISTRY rows and the DEFECT pins moved</name>
  <files>src/app/api/clubs/[id]/route.ts, src/app/api/clubs/[id]/members/role/route.ts, src/app/api/clubs/[id]/transfer/route.ts, src/__tests__/api/clubs/club-owner-writes-defect.test.ts, e2e/specs/club-authorization.spec.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/club-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/clubs/[id]/route.ts (PATCH whitelist, DELETE confirmName and the dynamic service import for the audit insert), src/app/api/clubs/[id]/members/role/route.ts, src/app/api/clubs/[id]/transfer/route.ts
    - src/server/db/elevated/index.ts and REGISTRY.md (rows from 05-05)
    - evidence/phase-05-decisions.md § DEC-41; 05-RESEARCH.md § C11 and § H (clubs/[id]/route.ts:129 DI-25 site)
    - src/__tests__/api/clubs/club-owner-writes-defect.test.ts and e2e/specs/club-authorization.spec.ts (the F-087 test)
  </read_first>
  <action>
After each owner gate (Task 1), perform the owner write on `getElevatedClient()`:
- clubs/[id] PATCH: build `updates` only from the existing `allowedFields` list, typed `TablesUpdate<"clubs">` (DI-25 site), and run `getElevatedClient().from("clubs").update(updates).eq("id", clubId).select("*").single()`; success and error bytes unchanged (`{ club }`, 500 "Failed to update club"). `status`, `created_by` and `id` can never enter `updates` because they are not in the whitelist.
- clubs/[id] DELETE: the soft-delete `update({ status: "deleted" }).eq("id", clubId)` and the audit insert (same row shape: admin_user_id, action "club_deleted", target_type "club", target_id, metadata { club_name }) both on `getElevatedClient()`; inspect the audit insert's error and `console.error` it with the context's requestId; remove the dynamic import of the service module.
- members/role PATCH: the club_members role update on `getElevatedClient()`, keeping the existing validation and bytes.
- transfer POST: replace the service factory with `getElevatedClient()` for the two role updates, the rollback and the audit insert; demotion by (club_id, user_id) from Task 1.
Remove every service-module import from these three files.

Add REGISTRY rows: `Owner edits club details or soft-deletes the club` | `src/app/api/clubs/[id]/route.ts` | `clubs has no owner UPDATE policy; an owner policy needs status immutability and is deferred to Phase 7's per-table RLS work (DEC-41); the handler's column whitelist keeps status, created_by and id unwritable` | 05; `Owner changes a member's role or transfers ownership` | `src/app/api/clubs/[id]/members/role/route.ts`, `src/app/api/clubs/[id]/transfer/route.ts` | `club_members UPDATE is admin-only; an owner UPDATE policy is deferred with the clubs policy (DEC-41)` | 05; `Record a club deletion or ownership transfer in admin_audit_log` | `src/app/api/clubs/[id]/route.ts`, `src/app/api/clubs/[id]/transfer/route.ts` | `after F-007 no client role may insert audit rows; the door is the only writer` | 05.

Ledger protocol on `club-owner-writes-defect.test.ts`: unedited-red; move PATCH, DELETE and members/role rows to "the write is on fake B and fake A records no clubs or club_members update", keep the whitelist row, move transfer's demotion filter to `(club_id, user_id)`; restore-red; restore and `cmp`; Status `FIXED in 05-10`. In `e2e/specs/club-authorization.spec.ts` flip the `DEFECT F-087` test to `FIXED F-087`: the same-description PATCH returns 200 with `club.description` equal to the sent value, and a second PATCH carrying `{ description: <current>, status: "rejected" }` returns 200 while a follow-up GET shows `status` still `approved`. Run the spec after a clean reset and seed. Record everything in `evidence/club-guard-adoption.txt`, plus `node scripts/check-elevated-ratchet.mjs` (live shrinks by the files that left the census; exit 0). Commit `fix(05-10): club owners can edit, delete and manage roles through the elevated door (F-087)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE`: owner edit, soft-delete and role change start working in both environments; the delete no longer reports success for a no-op.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/clubs && node scripts/check-elevated-ratchet.mjs && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/clubs/[id]/route.ts`, `members/role/route.ts` and `transfer/route.ts` each contain `getElevatedClient()` and no import of the service module
    - `src/app/api/clubs/[id]/route.ts` contains `TablesUpdate<"clubs">`
    - `club-owner-writes-defect.test.ts` passes with `FIXED in 05-10`; `club-gates-characterization.test.ts` passes unedited
    - The club-authorization spec's `FIXED F-087` test passes on a clean reset (recorded) and asserts the status stayed `approved`
    - `REGISTRY.md` has rows naming `src/app/api/clubs/[id]/route.ts` and `src/app/api/clubs/[id]/transfer/route.ts`
    - `node scripts/check-elevated-ratchet.mjs` exits 0 with a smaller live count than 24
  </acceptance_criteria>
  <done>A club owner can manage their club again, only through a registered door with a whitelist, and the RLS ring still refuses everyone who tries directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member → club route | the authz ring now in requireClubRole |
| owner → elevated door | owner writes bypass RLS by design; the handler's whitelist is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Elevation of Privilege | a role set widened during adoption (organizer admitted to an owner-only route) | mitigate | PRESERVE wrong-role rows pass unedited (Task 1 acceptance) |
| T-05-10-02 | Elevation of Privilege | an admin bypass added to the club guard | mitigate | executable-code check that requireClubRole references no roles/admin; admin-without-membership PRESERVE rows |
| T-05-10-03 | Tampering | an owner setting their club's status (self-approval) through the elevated write | mitigate | column whitelist excludes status; the e2e FIXED F-087 test asserts status stays approved |
| T-05-10-04 | Repudiation | elevated owner writes with no recorded justification | mitigate | three REGISTRY rows in the same commit; audit insert errors logged |
| T-05-10-05 | Denial of Service | event edit or create broken by the composite rewrite | mitigate | events PRESERVE suites unedited and green; auto-approve flag row green |
| T-05-10-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0
- `npx playwright test e2e/specs/club-authorization.spec.ts` green on a clean reset
- `node scripts/check-elevated-ratchet.mjs`, `npm run lint`, `npx tsc --noEmit` clean
</verification>

<success_criteria>
Club authorization lives in one guard with unchanged bytes, admin is decided by the one predicate, and club owners can manage their clubs through a registered, whitelisted door.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `CLUB_ROLES` and the narrowed `ClubRole`; `requireClubRole` at 17 sites; `hasRole` at the events inline admin checks; owner writes through `getElevatedClient()`; three REGISTRY rows; `TablesUpdate<"clubs">` and `TablesUpdate<"events">` typing; the F-087 pins flipped; `evidence/club-guard-adoption.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-10-SUMMARY.md` when done.
</output>
