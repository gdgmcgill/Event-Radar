---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 13
type: execute
wave: 12
depends_on: ["05-12"]
files_modified:
  - src/app/api/admin/analytics/events/route.ts
  - src/app/api/admin/analytics/users/route.ts
  - src/app/api/admin/analytics/users/route.test.ts
  - src/app/api/admin/audit-log/route.ts
  - src/app/api/admin/clubs/[id]/route.ts
  - src/app/api/admin/clubs/route.ts
  - src/app/api/admin/events/[id]/edits/route.ts
  - src/app/api/admin/events/[id]/route.ts
  - src/app/api/admin/events/[id]/status/route.ts
  - src/app/api/admin/events/route.ts
  - src/app/api/admin/experiments/[id]/results/route.ts
  - src/app/api/admin/experiments/[id]/route.ts
  - src/app/api/admin/experiments/route.ts
  - src/app/api/admin/featured/[id]/route.ts
  - src/app/api/admin/featured/route.ts
  - src/app/api/admin/organizer-requests/[id]/route.ts
  - src/app/api/admin/organizer-requests/route.ts
  - src/app/api/admin/organizers/route.ts
  - src/app/api/admin/reports/[id]/route.ts
  - src/app/api/admin/reports/route.ts
  - src/app/api/admin/stats/route.ts
  - src/app/api/admin/users/[id]/ban/route.ts
  - src/app/api/admin/users/[id]/route.ts
  - src/app/api/admin/users/route.ts
  - src/app/api/recommendations/analytics/route.ts
  - src/app/api/recommendations/batch/route.ts
  - src/app/api/moderation/reviews/[targetType]/[targetId]/route.ts
  - src/app/admin/layout.tsx
  - src/app/moderation/layout.tsx
  - src/lib/admin.ts
  - src/__tests__/api/admin/admin-guard-defect.test.ts
  - e2e/specs/admin-guard.spec.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-guard-adoption.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-13]

must_haves:
  truths:
    - "Every admin API arm decides admin through requireRole(ctx, \"admin\"): anonymous callers get 401 {\"error\":\"Unauthorized\"}, signed-in non-admins get 403 {\"error\":\"Forbidden\"}, admins proceed exactly as before"
    - "The three routes that answered 401 to non-admins now answer 403 (F-061)"
    - "The admin and moderation page layouts and the moderation reviews route decide admin through the request context and hasRole, with their redirects and bodies unchanged"
    - "src/lib/admin.ts no longer exists and nothing imports it"
  artifacts:
    - path: "src/app/api/admin/stats/route.ts"
      provides: "the adopted shape"
      contains: "requireRole(ctx, \"admin\")"
    - path: "src/__tests__/api/admin/admin-guard-defect.test.ts"
      provides: "FIXED_ARMS holding all 33 former helper arms"
      contains: "FIXED in 05-13"
  key_links:
    - from: "src/app/api/admin/**/route.ts"
      to: "src/server/authz/requireRole.ts"
      via: "createRequestContext() then requireRole(ctx, \"admin\") as the first statements"
      pattern: "requireRole\\(ctx, \"admin\"\\)"
    - from: "src/app/moderation/layout.tsx"
      to: "src/server/context.ts"
      via: "getRequestContext() and hasRole(profile, \"admin\")"
      pattern: "getRequestContext\\(\\)"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No admin handler's service-client use, write, or success body changes here (05-14 and 05-15 own those); calculate-popularity is not touched (05-14)"
    - "admin-guard-characterization.test.ts passes unedited"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: verifyAdmin -->

<objective>
Slice 5, refactor A: every admin decision through the seam (DEC-44). Replace `verifyAdmin()` at its 33 callsites in 25 route files with `requireRole(ctx, "admin")`, move the moderation reviews route and the two admin page layouts to the request context and `hasRole`, and delete `src/lib/admin.ts`.

Purpose: ROADMAP criterion 3's "verifyAdmin() guards every admin route" is delivered by the seam that replaces it (CONTEXT Area 3), and F-061's 401/403 distinction only exists once anonymous and non-admin are separate branches.

Output: 25 route files, one moderation route, two layouts, one deleted helper, the F-061 rows flipped, the e2e pin flipped.
</objective>

<slice_outcome>
Green when `admin-guard-characterization.test.ts` passes unedited, `admin-guard-defect.test.ts` passes with the 33 arms in FIXED_ARMS, `src/lib/admin.ts` is gone, the admin-guard spec shows anonymous 401, and `npx jest --ci` is green.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: requireRole at the analytics, audit-log, clubs, events and experiments admin routes</name>
  <files>src/app/api/admin/analytics/events/route.ts, src/app/api/admin/analytics/users/route.ts, src/app/api/admin/analytics/users/route.test.ts, src/app/api/admin/audit-log/route.ts, src/app/api/admin/clubs/[id]/route.ts, src/app/api/admin/clubs/route.ts, src/app/api/admin/events/[id]/edits/route.ts, src/app/api/admin/events/[id]/route.ts, src/app/api/admin/events/[id]/status/route.ts, src/app/api/admin/events/route.ts, src/app/api/admin/experiments/[id]/results/route.ts, src/app/api/admin/experiments/[id]/route.ts, src/app/api/admin/experiments/route.ts, src/__tests__/api/admin/admin-guard-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/server/authz/requireRole.ts and src/server/context.ts — requireRole(ctx, role, message = "Forbidden"), 401 via requireUser, fail-closed on a null profile
    - src/app/api/admin/reports/route.ts lines 1-15 — the helper shape being replaced (research PATTERNS § "Admin sites")
    - each of the twelve route files — where the helper is called, what it destructures (supabase, user), and the deny body
    - src/app/api/admin/analytics/users/route.test.ts — mocks `@/lib/admin`; it must be rewired to the seam without changing an assertion
    - src/__tests__/api/admin/admin-guard-characterization.test.ts and admin-guard-defect.test.ts
  </read_first>
  <action>
In each arm of the twelve files, replace the helper call and its deny branch with `const ctx = await createRequestContext(); const auth = requireRole(ctx, "admin"); if (!auth.ok) return auth.response;` as the first statements, then use `ctx.supabase` where the handler used the helper's `supabase` and `auth.user` where it used `user`. `admin/clubs` GET is a legacy-401 site: after the change its non-admin answer is 403 `{"error":"Forbidden"}` (F-061). Keep every other line — service-client uses, logAdminAction calls (still passing adminEmail until 05-14), bodies — unchanged. Rewire `src/app/api/admin/analytics/users/route.test.ts` from mocking `@/lib/admin` to mocking `@/lib/supabase/server` with a client whose getUser and users profile read produce the admin or non-admin caller; keep every `expect(` line byte-identical (an untagged suite; the rewiring is recorded in the evidence file). Add the arms of these files to FIXED_ARMS in the DEFECT file following the ledger protocol (unedited-red, move, restore-red, cmp); one ledger row per file. Record in `evidence/admin-guard-adoption.txt`. Commit `refactor(05-13): admin analytics, audit-log, clubs, events and experiments decide through requireRole (F-061)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (anonymous callers now 401; admin/clubs GET non-admins now 403).
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/admin src/app/api/admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the twelve route files contains `requireRole(ctx, "admin")` once per exported verb and no import of `@/lib/admin`
    - `git diff <05-12 head> -- src/__tests__/api/admin/admin-guard-characterization.test.ts` prints nothing and the suite passes
    - `git diff <05-12 head> -- src/app/api/admin/analytics/users/route.test.ts | command grep -E '^[-+].*expect\('` prints nothing, and the suite passes
    - The DEFECT suite passes with this task's arms in FIXED_ARMS
  </acceptance_criteria>
  <done>Half the admin surface decides admin through the seam with 401 and 403 finally distinct.</done>
</task>

<task type="auto">
  <name>Task 2: requireRole at the featured, organizer, report, stats, users and recommendations admin routes</name>
  <files>src/app/api/admin/featured/[id]/route.ts, src/app/api/admin/featured/route.ts, src/app/api/admin/organizer-requests/[id]/route.ts, src/app/api/admin/organizer-requests/route.ts, src/app/api/admin/organizers/route.ts, src/app/api/admin/reports/[id]/route.ts, src/app/api/admin/reports/route.ts, src/app/api/admin/stats/route.ts, src/app/api/admin/users/[id]/ban/route.ts, src/app/api/admin/users/[id]/route.ts, src/app/api/admin/users/route.ts, src/app/api/recommendations/analytics/route.ts, src/app/api/recommendations/batch/route.ts, src/__tests__/api/admin/admin-guard-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - each of the thirteen route files (helper call sites; organizer-requests GET and recommendations/batch POST are legacy-401 sites; recommendations/analytics already calls the helper, research C4)
    - src/__tests__/api/admin/admin-guard-defect.test.ts
  </read_first>
  <action>
Same replacement as Task 1 in all thirteen files. `admin/organizer-requests` GET and `recommendations/batch` POST become 403 for non-admins (F-061). `admin/users/[id]` PATCH keeps its current write logic unchanged here (05-14 fixes F-091). Add these arms to FIXED_ARMS; FIXED_ARMS now holds all 33 former helper arms (calculate-popularity's two rows stay unfixed until 05-14). Ledger rows; evidence. Commit `refactor(05-13): admin featured, organizer, report, stats, users and recommendations decide through requireRole (F-061)` with an `INTENTIONAL BEHAVIOUR CHANGE` body naming the two legacy-401 routes.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/admin src/app/api/admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the thirteen files contains `requireRole(ctx, "admin")` and no import of `@/lib/admin`
    - `admin-guard-defect.test.ts` passes with 33 ids in FIXED_ARMS; the PRESERVE suite passes unedited
  </acceptance_criteria>
  <done>Every former helper callsite decides admin through the seam.</done>
</task>

<task type="auto">
  <name>Task 3: Moderation reviews route and the two layouts through the context; delete the helper; flip the e2e pin</name>
  <files>src/app/api/moderation/reviews/[targetType]/[targetId]/route.ts, src/app/admin/layout.tsx, src/app/moderation/layout.tsx, src/lib/admin.ts, e2e/specs/admin-guard.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-guard-adoption.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/moderation/reviews/[targetType]/[targetId]/route.ts lines 1-60 — the inline admin check at 27-33 (and the creator path)
    - src/app/admin/layout.tsx (24 lines) and src/app/moderation/layout.tsx lines 1-40 — getUser, the profile read, the redirects ("/" and "/admin-login"), and the moderation layout's extra columns (email, name, avatar_url) used for display
    - src/server/context.ts getRequestContext; src/lib/roles.ts hasRole
    - e2e/specs/admin-guard.spec.ts; e2e/specs/admin-moderation-queue.spec.ts and admin-login-cookie-equivalence.spec.ts (must stay green)
  </read_first>
  <action>
Moderation reviews route: create the context once, replace the inline admin computation with `hasRole(ctx.profile, "admin")`, and keep the non-admin creator path and every body unchanged. `src/app/admin/layout.tsx`: `const ctx = await getRequestContext();` no user → `redirect("/")`; `!hasRole(ctx.profile, "admin")` → `redirect("/")`. `src/app/moderation/layout.tsx`: `getRequestContext()`; no user → `redirect("/admin-login")`; `!hasRole(ctx.profile, "admin")` → `redirect("/")`; keep its separate display read of email, name and avatar_url (a display read, not an authorization decision). `src/app/admin-login/page.tsx` is a client page and stays as it is. Delete `src/lib/admin.ts` after `command grep -rn "@/lib/admin" src` prints nothing. Flip the e2e pin in `admin-guard.spec.ts` to `FIXED F-061: anonymous admin call answers 401` with body `{ error: "Unauthorized" }`. Run `npx jest --ci`, `npx tsc --noEmit`, `npm run lint`, and after a clean reset and seed `npx playwright test e2e/specs/admin-guard.spec.ts e2e/specs/admin-moderation-queue.spec.ts e2e/specs/admin-login-cookie-equivalence.spec.ts`; record. Commit `refactor(05-13): layouts and moderation reviews decide admin through the context; delete src/lib/admin.ts`.
  </action>
  <verify>
    <automated>test ! -e src/lib/admin.ts && npx jest --ci && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/lib/admin.ts` succeeds and `command grep -rn "@/lib/admin" src` prints nothing
    - `src/app/admin/layout.tsx` and `src/app/moderation/layout.tsx` contain `getRequestContext()` and `hasRole(`
    - The three admin specs pass after a clean reset (recorded exit=0); admin-guard's F-061 test is titled `FIXED F-061`
    - `npx jest --ci` exits 0
  </acceptance_criteria>
  <done>There is one admin predicate and one admin guard in the codebase; the old helper is gone and every admin surface still behaves for admins exactly as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous or student → admin API and pages | the admin guard, now the seam's requireRole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Elevation of Privilege | an admin arm left ungated during the swap | mitigate | FIXED_ARMS holds all 33; PRESERVE student-403 rows unedited and green; grep shows no remaining helper import |
| T-05-13-02 | Elevation of Privilege | a null profile treated as admin | mitigate | requireRole fails closed on a null profile (its own unit suite); unchanged |
| T-05-13-03 | Denial of Service | admins locked out of moderation | mitigate | admin-moderation-queue and admin-login-cookie-equivalence specs green after a clean reset |
| T-05-13-04 | Repudiation | contract (endpoints.json) disagreeing with 401 for anonymous | accept | endpoints.json already encodes anonymous 401 for 26 of 27 admin rows (research § E); 05-14 regenerates the mechanism text and calculate-popularity |
| T-05-13-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0; `npx tsc --noEmit`, `npm run lint` clean
- The three admin Playwright specs green after a clean reset
- `src/lib/admin.ts` absent
</verification>

<success_criteria>
Every admin route and page decides through the seam, anonymous and non-admin callers are distinguishable, and the retired helper is deleted.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `requireRole(ctx, "admin")` in 25 admin route files; the moderation reviews route and both layouts on `getRequestContext()`/`hasRole`; `src/lib/admin.ts` deleted; FIXED_ARMS complete for the helper arms; `e2e/specs/admin-guard.spec.ts` flipped; `evidence/admin-guard-adoption.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-13-SUMMARY.md` when done.
</output>
