---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 16
type: execute
wave: 15
depends_on: ["05-15"]
files_modified:
  - supabase/migrations/20260923130000_users_grants_audit_log_insert.sql
  - supabase/tests/database/050-users-privilege-escalation.test.sql
  - supabase/tests/database/055-admin-audit-log-insert.test.sql
  - scripts/pgtap-mutation-check.sh
  - src/lib/supabase/types.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-privilege-before.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/schema-push-slice-5.txt
autonomous: true
requirements: [REFAC-13]

must_haves:
  truths:
    - "At the RLS ring, a non-admin cannot set their own roles, clear their own ban, or insert their own users row; they can still update every profile column the app writes, including inferred_tags (F-006, C3)"
    - "Saving and unsaving an event still work and still move saved_events_count, because the counter trigger runs as its definer (C3)"
    - "No anonymous or authenticated caller — admin included — can insert, update or delete admin_audit_log rows; only the service role writes them (F-007)"
    - "Every cookie-client users update in the application writes only granted columns"
    - "The migrations rebuild from zero locally, the committed types match the local database, and pgTAP passes unseeded and seeded"
  artifacts:
    - path: "supabase/migrations/20260923130000_users_grants_audit_log_insert.sql"
      provides: "column-scoped users UPDATE grant, INSERT revoked, anon UPDATE revoked, own-row policy rewritten with TO and WITH CHECK, SECURITY DEFINER counter trigger, audit-log INSERT policies dropped and write privileges revoked"
      contains: "GRANT UPDATE (name, avatar_url, banner_url, pronouns, year, faculty, visibility, interest_tags, inferred_tags, onboarding_completed, updated_at)"
    - path: "supabase/tests/database/050-users-privilege-escalation.test.sql"
      provides: "self-escalation and self-unban denied; profile update, inferred_tags update and save still allowed"
      min_lines: 90
    - path: "supabase/tests/database/055-admin-audit-log-insert.test.sql"
      provides: "audit-log writes denied to anon, authenticated and admin; allowed to service_role"
      min_lines: 60
  key_links:
    - from: "supabase/migrations/20260923130000_users_grants_audit_log_insert.sql"
      to: "src/app/api/profile/inferred-tags/route.ts"
      via: "inferred_tags is in the grant, so the cookie-client update keeps working"
      pattern: "inferred_tags"
    - from: "scripts/pgtap-mutation-check.sh"
      to: "supabase/migrations/20260923130000_users_grants_audit_log_insert.sql"
      via: "the rewritten own-row UPDATE policy is mutated by name across files"
      pattern: "users_grants_audit_log_insert"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No pgTAP test writes into the auth schema (D-21); an allowed write reaching an auth.users foreign key is asserted as 23503"
    - "types.ts is generated from the local database only, never hand-edited"
    - "No application code changes in this plan; if a users update needs an ungranted column, stop and record it (the fix would be the door, 05-15's rule), never widen the grant silently"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 5, refactor D: close the two Critical privilege paths at the RLS ring by fix-forward migration — self-escalation and self-unban through a table-wide UPDATE grant on users (F-006) and forged audit rows (F-007) — with the C3 corrections that keep event saving and inferred-tag edits working (DEC-47), prove both directions with pgTAP, and apply the schema to the LOCAL stack only.

Purpose: ROADMAP criterion 3 and the phase goal's "privilege-escalation paths are denied … at the RLS ring". Every application path that needs the withheld columns already goes through the door (05-05, 05-14, 05-15), so the grant can land without breaking a workflow.

Output: one migration, two pgTAP files, the before-state proof, the mutation-check glob, the local schema push.
</objective>

<slice_outcome>
Green when the pgTAP files were measured red against the pre-migration schema, the reset succeeds with the migration, the types diff is empty, `supabase test db --local` passes unseeded and seeded, the mutation check and the manual grant mutations both turn the suite red, and the save, profile and wizard Playwright specs still pass.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first — the pgTAP files are run red against today's schema before the migration exists. 3. Production never touched; the schema push is the LOCAL rebuild only. 4. Migrations fix-forward only; types regenerated with `--local` on CLI 2.115.0. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write 050 and 055, run them red against today's schema, then write the migration</name>
  <files>supabase/tests/database/050-users-privilege-escalation.test.sql, supabase/tests/database/055-admin-audit-log-insert.test.sql, supabase/migrations/20260923130000_users_grants_audit_log_insert.sql, scripts/pgtap-mutation-check.sh, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-privilege-before.txt</files>
  <read_first>
    - supabase/migrations/20260915214553_baseline.sql lines 593-615 (update_saved_events_count, SECURITY INVOKER), 633-641 (admin_audit_log), 1122-1150 (every users column), 1680 and 1700 (the two users-adjacent triggers), 1973 and 2169 (the two audit INSERT policies), 2237-2297 (users policies), 2526-2528 and 2700-2702 (the table-wide grants)
    - supabase/migrations/20260923120000_events_insert_club_scope.sql (05-11) — the header and helper style to repeat
    - supabase/tests/database/020-rls-policy-gaps.test.sql and 060-club-tenant-isolation.test.sql — assertion shapes, fixtures, the 23503 convention
    - 05-RESEARCH.md § C3, § "F-006 / F-007 migration skeleton" and its open point on INSERT
    - evidence/phase-05-decisions.md § DEC-47
    - `command grep -rn -A3 'from("users")' src/app src/lib | command grep "\.update("` — every application users update, to check against the grant
  </read_first>
  <action>
**Grant audit first.** List every users update still issued on a cookie client (after 05-15: profile/avatar, profile/banner, profile/inferred-tags, profile/interests, users/[id] PATCH, and any other the grep finds) with the exact columns each payload can carry, and record in `evidence/rls-privilege-before.txt` that each column is in the planned grant list. A column outside it stops the task with the finding recorded.

**pgTAP files**, own fixtures in the `00000000-0000-4000-8000-0000000050xx` and `…0055xx` blocks (grep the tests directory for collisions). `050-users-privilege-escalation.test.sql`: fixture users S (student, roles user) and B (banned: banned_at set), an event with `created_by` NULL; impersonation proof; S `UPDATE public.users SET roles = '{user,admin}' WHERE id = S` → throws 42501; B `UPDATE public.users SET banned_at = NULL WHERE id = B` → throws 42501; S updates `name, pronouns, year, faculty, visibility, interest_tags, banner_url, avatar_url, onboarding_completed` → `results_eq` on RETURNING name; S updates `inferred_tags` → allowed (RETURNING); S `INSERT INTO public.users (id, email, roles)` for a new id → throws 42501; anon `UPDATE public.users SET name = 'x'` → throws 42501; S inserts into saved_events for the fixture event → allowed, then as owner `saved_events_count` for S is 1; S deletes that saved_events row → as owner the count is 0. `055-admin-audit-log-insert.test.sql`: fixture admin A (roles admin) and student S; anon INSERT (no RETURNING) → 42501; S INSERT → 42501; A INSERT → 42501; S `UPDATE public.admin_audit_log SET action = 'x'` and `DELETE FROM public.admin_audit_log` → 42501 (no row is needed: the table-privilege check precedes any row, so this needs no auth.users fixture; today both succeed on zero rows, which is why they are red before the migration); `SET LOCAL ROLE service_role` INSERT with a fixture admin_user_id → 23503 (privilege passed, row reached the auth.users key).

**Red against today.** With the migration file not yet present, run `supabase db reset --local` and `supabase test db --local`; record in `evidence/rls-privilege-before.txt` which assertions fail (expected: the roles, banned_at, users INSERT, anon UPDATE and every audit-log deny assertion; the allow rows pass). This is the proof the files bite.

**Migration** `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql`, with the header form naming F-006, F-007, C3, DEC-47, local-only until DI-23: `DROP POLICY IF EXISTS "Admins can insert audit log"` and `"Service role can insert audit log"` on public.admin_audit_log; `REVOKE INSERT, UPDATE, DELETE ON public.admin_audit_log FROM anon, authenticated`; `REVOKE UPDATE ON public.users FROM anon, authenticated`; `REVOKE INSERT ON public.users FROM anon, authenticated`; `GRANT UPDATE (name, avatar_url, banner_url, pronouns, year, faculty, visibility, interest_tags, inferred_tags, onboarding_completed, updated_at) ON public.users TO authenticated`; `DROP POLICY IF EXISTS "Users can update own profile" ON public.users` and recreate it `FOR UPDATE TO authenticated USING ((SELECT auth.uid()) = id) WITH CHECK ((SELECT auth.uid()) = id)`; `CREATE OR REPLACE FUNCTION public.update_saved_events_count() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''` with the same INSERT/DELETE logic against `public.users`; `REVOKE ALL ON FUNCTION public.update_saved_events_count() FROM PUBLIC, anon, authenticated`; a COMMENT on the function. Append `supabase/migrations/*_users_grants_audit_log_insert.sql` to POLICY_MIGRATION_GLOBS with a comment naming 05-16.
  </action>
  <verify>
    <automated>node scripts/check-migration-filenames.mjs && test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-privilege-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/rls-privilege-before.txt` records the grant audit table and a red pgTAP run naming the failing deny assertions of 050 and 055
    - The migration contains the exact GRANT column list above, `SECURITY DEFINER`, `SET search_path = ''`, `REVOKE INSERT, UPDATE, DELETE ON public.admin_audit_log FROM anon, authenticated`, `REVOKE INSERT ON public.users FROM anon, authenticated`, and `TO authenticated` on the rewritten policy
    - 050 and 055 each start with an impersonation proof and use `'42501'` for denials; 050 asserts saved_events_count moves up and back down
    - `node scripts/check-migration-filenames.mjs` exits 0 and counts 6 migrations; the four earlier migrations and 05-11's are unchanged
  </acceptance_criteria>
  <done>The escalation and forgery paths have tests proven to fail today, and the fix that should make them pass is written.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the schema to the LOCAL stack, regenerate types, run pgTAP unseeded and seeded, mutate, and re-run the affected browser flows</name>
  <files>src/lib/supabase/types.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/schema-push-slice-5.txt</files>
  <read_first>
    - .github/workflows/ci.yml `types` and `e2e` jobs
    - evidence/schema-push-slice-4.txt — the sequence to repeat
    - scripts/pgtap-mutation-check.sh METHOD CONTRACT
  </read_first>
  <action>
LOCAL ONLY; record every command and exit code in `evidence/schema-push-slice-5.txt`: `supabase --version` (2.115.0 or stop); `supabase db reset --local`; `supabase gen types typescript --local --schema public > src/lib/supabase/types.ts`; `git diff --stat src/lib/supabase/types.ts` (expected empty — grants, policies and a trigger function's body do not change generated types, research A2; any change is inspected and recorded, and committed only if it is exactly what the migration implies); `supabase test db --local` UNSEEDED (all files PASS); `npx tsx scripts/seed/load.ts`; `supabase test db --local` SEEDED (all PASS); `bash scripts/pgtap-mutation-check.sh` (exit 0; the rewritten own-row policy's removal turns 050 red). Grants are not policies, so also run two manual mutation cycles: comment out the `REVOKE UPDATE ON public.users` line (050's roles and banned_at rows turn red) and the audit `REVOKE` line (055 turns red), each followed by reset, test db, restore from git, reset, test db green. Then from a clean reset and seed, with port 3000 free: `npx playwright test e2e/specs/save-and-rsvp.spec.ts e2e/specs/public-profile-privacy.spec.ts e2e/specs/ban-and-onboarding-ring.spec.ts e2e/specs/admin-audit-row.spec.ts e2e/specs/admin-write-paths.spec.ts` (save, profile PATCH, wizard PATCH, audit writes and admin writes all still work under the grant). `npx jest --ci` and `npx tsc --noEmit`. Leave the stack reset and seeded. Commit the migration, 050, 055, the glob and (if changed) types together: `fix(05-16): column-scoped users grant and audit-log write revocation close self-escalation and forged audit rows (F-006, F-007)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE` (local only until DI-23: a user can no longer set their own roles or clear their own ban through PostgREST; nobody but the service role can write the audit log). After the commit, `supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -` prints nothing. Never run `supabase db push`, never pass `--linked`.
  </action>
  <verify>
    <automated>supabase test db --local && supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/schema-push-slice-5.txt` shows CLI 2.115.0, reset exit=0, UNSEEDED and SEEDED pgTAP with every file PASS (9 files), the mutation-check exit=0, and both manual grant mutation cycles red then green
    - The five Playwright specs passed from a clean reset (recorded exit=0)
    - `supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -` prints nothing
    - No command in the evidence file contains `--linked` or `db push`
  </acceptance_criteria>
  <done>Self-escalation, self-unban and audit forgery are refused by the database locally, saving and profile edits still work, and the proof is shown to bite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated role → users table | direct PostgREST PATCH, the F-006 path |
| anon/authenticated → admin_audit_log | the F-007 forgery path |
| saved_events trigger → users | a caller-invoked write the grant would otherwise break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16-01 | Elevation of Privilege | self-grant of admin and self-unban (F-006, Critical) | mitigate | column-scoped grant; 050 asserts 42501 for roles and banned_at; manual mutation proves the rows bite |
| T-05-16-02 | Repudiation | forged audit rows from anon or any user (F-007, Critical) | mitigate | INSERT policies dropped, write privileges revoked; 055 asserts 42501 for anon, student and admin |
| T-05-16-03 | Denial of Service | event saving broken by the grant (C3, measured) | mitigate | SECURITY DEFINER counter; 050 asserts the count moves; save-and-rsvp spec green |
| T-05-16-04 | Denial of Service | profile, inferred-tags or onboarding writes broken by the grant | mitigate | grant audit of every cookie-client users update; inferred_tags in the grant; profile and wizard specs green |
| T-05-16-05 | Elevation of Privilege | a SECURITY DEFINER function hijacked through search_path | mitigate | `SET search_path = ''`, schema-qualified body, EXECUTE revoked from PUBLIC, anon, authenticated |
| T-05-16-06 | Tampering | a production write while applying the schema | mitigate | local reset only; acceptance asserts no `--linked` or `db push` |
| T-05-16-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `supabase test db --local` PASS unseeded and seeded; types diff empty
- The five affected Playwright specs green from a clean reset
- `npx jest --ci`, `npx tsc --noEmit` clean
</verification>

<success_criteria>
The two Critical RLS-ring holes are closed locally with both directions proven and the proof shown to bite, and every workflow that writes users still works.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql`, `supabase/tests/database/050-users-privilege-escalation.test.sql`, `supabase/tests/database/055-admin-audit-log-insert.test.sql`, the mutation-check glob, `evidence/rls-privilege-before.txt`, `evidence/schema-push-slice-5.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-16-SUMMARY.md` when done.
</output>
