---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 12
type: execute
wave: 11
depends_on: ["05-11"]
files_modified:
  - src/__tests__/api/admin/adminArmTable.ts
  - src/__tests__/api/admin/admin-guard-characterization.test.ts
  - src/__tests__/api/admin/admin-guard-defect.test.ts
  - src/__tests__/api/admin/admin-users-patch-defect.test.ts
  - src/__tests__/pages/public-profile-defect.test.tsx
  - src/__tests__/api/events/pending-edits-defect.test.ts
  - e2e/specs/admin-guard.spec.ts
  - e2e/specs/csrf-origin.spec.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-characterization.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-mutation-check.txt
autonomous: true
requirements: [REFAC-13, REFAC-17]

must_haves:
  truths:
    - "Every admin arm (the 33 former admin-verify callsites plus both calculate-popularity verbs) has its non-admin and admin outcome pinned PRESERVE and its anonymous outcome pinned — PRESERVE where it is already 401, DEFECT F-061 where it is 403"
    - "calculate-popularity's anonymous and non-admin 200 (FO-01) is pinned DEFECT F-001"
    - "The admin role-change route's defects (cookie-client write, admin stripped, no audit row, self-change allowed, unvalidated roles) are pinned DEFECT F-091"
    - "The public profile page's anonymous rendering of a private profile and its email select are pinned DEFECT F-005"
    - "GET /api/events/[id]'s failure to return pending_edits to the creator and admins is pinned DEFECT F-086"
    - "A cross-site POST with a foreign Origin or Sec-Fetch-Site cross-site succeeds today — pinned DEFECT F-090 through the real harness"
  artifacts:
    - path: "src/__tests__/api/admin/adminArmTable.ts"
      provides: "one descriptor per admin arm with invoke closures typed through each handler's own export"
      min_lines: 120
    - path: "src/__tests__/api/admin/admin-guard-defect.test.ts"
      provides: "DEFECT F-061/F-001 with a FIXED_ARMS set 05-13 and 05-14 edit"
      min_lines: 80
    - path: "src/__tests__/pages/public-profile-defect.test.tsx"
      provides: "DEFECT F-005 at the page and metadata level (jsdom project)"
      min_lines: 60
    - path: "e2e/specs/csrf-origin.spec.ts"
      provides: "DEFECT F-090 cross-site arms, restored afterwards"
      min_lines: 50
  key_links:
    - from: "src/__tests__/api/admin/admin-guard-defect.test.ts"
      to: "src/__tests__/api/admin/adminArmTable.ts"
      via: "FIXED_ARMS lives in the DEFECT file, so the PRESERVE file's inputs never change"
      pattern: "FIXED_ARMS"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No production source file changes in this plan"
    - "The e2e specs never call calculate-popularity (it writes popularity scores that other specs read) and restore every save they make"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 5, characterization: pin the admin guard at every admin arm, the fail-open popularity route, the three behaviour defects slice 5 fixes (F-091, F-005, F-086), and the CSRF exposure (F-090) — against unmodified source.

Purpose: ROADMAP criterion 3 ("every fail-open endpoint fails closed, verifyAdmin() guards every admin route, every remaining service-role use goes through the door") and criterion 4's CSRF half change about 45 handlers and the proxy; the characterize-first discipline requires every byte that must survive and every defect that must move to be on record first.

Output: an admin arm table, PRESERVE and DEFECT admin suites, three behaviour DEFECT suites, two Playwright specs, mutation evidence.
</objective>

<slice_outcome>
Green when every new suite passes on unmodified source, `audit-shape.test.ts` is green as it stands, the two specs pass on a clean reset and leave no saved row behind, and every mutation cycle shows its named test red.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Admin arm table with PRESERVE and DEFECT guard suites</name>
  <files>src/__tests__/api/admin/adminArmTable.ts, src/__tests__/api/admin/admin-guard-characterization.test.ts, src/__tests__/api/admin/admin-guard-defect.test.ts</files>
  <read_first>
    - src/lib/admin.ts (17 lines) — the helper every arm calls today
    - 05-RESEARCH.md § B rows for the admin-verify deny (30 callsites 403 Forbidden, 3 callsites 401 for anyone: admin/clubs GET, admin/organizer-requests GET, recommendations/batch POST) and calculate-popularity, § E
    - src/app/api/admin/calculate-popularity/route.ts — the inline createClient from @supabase/supabase-js, the ADMIN_API_KEY gate on both verbs, the update_event_popularity rpc
    - src/__tests__/api/auth-ring/writeHandlerTable.ts — the descriptor and invoke-closure pattern to reuse
    - src/app/api/admin/analytics/users/route.test.ts — an existing untagged admin test that mocks the helper module (it is rewired in 05-13, not here)
  </read_first>
  <action>
Enumerate the admin arms from the tree with a node one-liner over `src/app/api/admin/**/route.ts` plus `src/app/api/recommendations/analytics/route.ts` and `src/app/api/recommendations/batch/route.ts`, recording the list (expected: 33 admin-verify arms in 25 files plus calculate-popularity POST and GET). `adminArmTable.ts` exports `ADMIN_ARMS` with `id`, `invoke(req)` closures that dynamically import the route and call the verb with its own params type, `makeRequest()`, and flags `legacy401` (the three sites) and `machineKey` (calculate-popularity). Persona builders: anonymous, student (onboarded, roles ["user"]), admin (roles ["user","admin"]).

Mocks in both suites: `@/lib/supabase/server` → fake A; `@/lib/supabase/service` → fake B; `@supabase/supabase-js` `createClient` → fake B (calculate-popularity's inline client) with `rpc.update_event_popularity` scripted to succeed; ADMIN_API_KEY unset (the production condition, F-040).

**PRESERVE** `admin-guard-characterization.test.ts` (docblock `PRESERVE characterization — admin guard at every admin arm (REFAC-13)`): for the 30 non-legacy admin-verify arms, student → 403 `{ "error": "Forbidden" }` (measure each body first; pin the measurement); for the three legacy401 arms, anonymous → 401 `{ "error": "Unauthorized" }`; for every arm including calculate-popularity, admin → not 401 and not 403.

**DEFECT** `admin-guard-defect.test.ts` (docblock `DEFECT characterization — F-061, F-001`, `Status: OPEN — rows move in 05-13 and 05-14`): an inline `FIXED_ARMS: ReadonlySet<string>` (empty). For each arm not in FIXED_ARMS assert today, in FIXED_ARMS assert fixed: non-legacy arms, anonymous → today 403 `{ "error": "Forbidden" }`, fixed 401 `{ "error": "Unauthorized" }`; legacy401 arms, student → today 401, fixed 403 `{ "error": "Forbidden" }`; calculate-popularity GET and POST, anonymous → today 200 and the rpc called on fake B, fixed 401 with no rpc; student → today 200, fixed 403 with no rpc. Run `npx jest --ci src/__tests__/api/admin` and record. Commit `test(05-12): admin guard PRESERVE and DEFECT characterization (F-061, F-001)`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/admin && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both suites pass on unmodified source
    - `ADMIN_ARMS` has at least 35 descriptors and `evidence/slice-5-characterization.txt` holds the node enumeration and the measured deny body per arm
    - The DEFECT docblock cites `F-061` and `F-001` and declares an empty `FIXED_ARMS`
    - `node scripts/check-characterization-tags.mjs src/__tests__/api/admin/admin-guard-characterization.test.ts src/__tests__/api/admin/admin-guard-defect.test.ts` exits 0
  </acceptance_criteria>
  <done>Every admin arm's surviving bytes and its F-061/F-001 defects are on record, arm by arm.</done>
</task>

<task type="auto">
  <name>Task 2: Behaviour DEFECT pins — admin role change (F-091), public profile (F-005), pending edits (F-086)</name>
  <files>src/__tests__/api/admin/admin-users-patch-defect.test.ts, src/__tests__/pages/public-profile-defect.test.tsx, src/__tests__/api/events/pending-edits-defect.test.ts</files>
  <read_first>
    - src/app/api/admin/users/[id]/route.ts — the cookie-client write, the admin strip, no audit call
    - src/app/users/[id]/page.tsx — generateMetadata (service client, no session read), the page's reads of users, saved_events, events, club_members, rsvps and get_friends, and `isPublic = target.visibility !== "private"`
    - src/app/api/users/[id]/route.ts near the visibility validation — the two values public and private
    - src/app/api/events/[id]/route.ts GET — the transform and the dead stripping branch; src/__tests__/api/events/events-detail-characterization.test.ts layer (b)
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md § DI-36
    - jest.config.js — the jsdom project matches *.test.tsx
  </read_first>
  <action>
**`admin-users-patch-defect.test.ts`** (DEFECT F-091; Status OPEN, moves in 05-14): admin caller, fake A (cookie) and fake B (service) mocked separately; pin today, measuring first: PATCH another user's id with `{ roles: ["user", "admin"] }` → the users update is issued on fake A and its roles payload lacks "admin"; no admin_audit_log insert on either fake; PATCH the caller's own id → today's status (admitted); `{ roles: ["user", "superuser"] }` → today's behaviour (the update is attempted with the invalid value, or whatever is measured).

**`src/__tests__/pages/public-profile-defect.test.tsx`** (jsdom project; DEFECT F-005; Status OPEN, moves in 05-15): mock `@/lib/supabase/service` (target row with `visibility: "private"`, an email, a name), `@/lib/supabase/server` (anonymous: getUser returns no user), and `next/navigation` (`notFound` and `redirect` throw sentinel errors). Import `generateMetadata` and the default export from `@/app/users/[id]/page`. Pin today: `await generateMetadata({ params: Promise.resolve({ id }) })` resolves with the target's name in its title; `await Page({ params })` does not throw the notFound sentinel; the recorded users select column string contains `email`. Add rows that do not move: a public target viewed anonymously renders (no notFound).

**`src/__tests__/api/events/pending-edits-defect.test.ts`** (DEFECT F-086; Status OPEN, moves in 05-14): with the real `transformEventFromDB`, an event row carrying `pending_edits: { title: "x" }` and `created_by` = the caller: GET as the creator → today `event.pending_edits` is undefined; GET as an admin → undefined; GET as another signed-in user → undefined (this row does not move). Run each with `events-detail-characterization.test.ts` unedited.

Run `npx jest --ci src/__tests__/api/admin src/__tests__/pages src/__tests__/api/events/pending-edits-defect.test.ts src/__tests__/moderation/audit-shape.test.ts` and record. Commit `test(05-12): pin F-091, F-005 and F-086 before slice 5 fixes them`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/admin src/__tests__/pages src/__tests__/api/events/pending-edits-defect.test.ts src/__tests__/moderation/audit-shape.test.ts src/__tests__/api/events/events-detail-characterization.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three suites pass on unmodified source; `audit-shape.test.ts` and `events-detail-characterization.test.ts` pass unedited
    - Docblocks cite F-091, F-005 and F-086 respectively, each with `Status: OPEN`; `node scripts/check-characterization-tags.mjs --all` exits 0
    - `public-profile-defect.test.tsx` asserts the users select string contains `email` today and that the private anonymous render does not call notFound
  </acceptance_criteria>
  <done>The three behaviours slice 5 changes on purpose are pinned as they are today, each against its registered finding.</done>
</task>

<task type="auto">
  <name>Task 3: Admin-guard and CSRF Playwright pins; mutation evidence</name>
  <files>e2e/specs/admin-guard.spec.ts, e2e/specs/csrf-origin.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-characterization.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-mutation-check.txt</files>
  <read_first>
    - e2e/specs/admin-moderation-queue.spec.ts — how the admin persona is used
    - e2e/fixtures.ts, scripts/seed/personas.ts IDS (secondApprovedEvent)
    - src/app/api/events/[id]/save/route.ts — POST and DELETE contract (the CSRF target)
  </read_first>
  <action>
**`e2e/specs/admin-guard.spec.ts`** (F-061): a fresh request context with no storage state → GET `/api/admin/stats` → today 403, titled `DEFECT F-061: anonymous admin call answers 403`; onboarded_student → 403 `{ error: "Forbidden" }` (PRESERVE); admin → 200 (PRESERVE). Never call calculate-popularity.

**`e2e/specs/csrf-origin.spec.ts`** (F-090) as onboarded_student against `IDS.secondApprovedEvent`, each test restoring with a plain DELETE (no Origin header) in a finally block: `DEFECT F-090: a POST carrying Origin https://evil.example is accepted` → today 200 and the event is saved; `DEFECT F-090: a POST carrying Sec-Fetch-Site cross-site is accepted` → today 200; PRESERVE rows: a POST with Origin `http://127.0.0.1:3000` → 200; a POST with no Origin and no Sec-Fetch-Site → 200. Run both specs after a clean reset and seed; record in `evidence/slice-5-characterization.txt` together with a check that `saved_events` holds no row for onboarded_student and secondApprovedEvent afterwards.

Mutation cycles on SOURCE with restore proofs, recorded in `evidence/slice-5-mutation-check.txt`: (1) make the admin helper return isAdmin true for everyone → PRESERVE student-403 rows red; (2) give admin/clubs GET a 403 for non-admins → the legacy401 DEFECT row red; (3) set a default ADMIN_API_KEY check that 401s when unset in calculate-popularity → DEFECT F-001 rows red; (4) stop stripping admin in the admin users route → F-091 row red; (5) add `notFound()` for private targets in the page → F-005 row red; (6) copy pending_edits in the GET → F-086 creator row red. Then `node scripts/check-characterization-tags.mjs --all`, full `npx jest --ci`, `npx tsc --noEmit`, and `git diff --stat <05-11 head> -- src/app src/lib src/server` (empty). Commit `test(05-12): admin-guard and CSRF pins; slice 5 mutation evidence`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/check-characterization-tags.mjs --all && git diff --exit-code -- src/app src/lib src/server</automated>
  </verify>
  <acceptance_criteria>
    - Both specs passed after a clean reset (recorded with exit=0); csrf-origin leaves no saved row (recorded)
    - `csrf-origin.spec.ts` contains two tests titled with `DEFECT F-090`; `admin-guard.spec.ts` contains one titled `DEFECT F-061`
    - `evidence/slice-5-mutation-check.txt` records 6 cycles with named red tests and restore exits 0
    - `npx jest --ci` exits 0; no tracked production file modified
  </acceptance_criteria>
  <done>Slice 5's net is complete and proven to bite, and the CSRF exposure is demonstrated rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous or student → admin API | the admin guard |
| foreign origin → state-changing API | the CSRF boundary (F-090) |
| anonymous → public profile page | the F-005 disclosure path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | Elevation of Privilege | an admin arm missed by the table, left on the old helper | mitigate | arm list derived from the tree and recorded; at least 35 descriptors (Task 1 acceptance) |
| T-05-12-02 | Tampering | the CSRF spec leaving a saved row that changes other specs' state | mitigate | finally-block DELETE and a post-run saved_events check (Task 3 acceptance) |
| T-05-12-03 | Tampering | the popularity RPC run by an e2e spec, changing feed ordering | mitigate | prohibition: specs never call calculate-popularity; the unit suite mocks the rpc |
| T-05-12-04 | Repudiation | a defect pinned without a registered id | mitigate | F-061, F-001, F-005, F-086, F-090, F-091 all registered in 05-01; tag gate green |
| T-05-12-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0
- Both new specs green after a clean reset
- `node scripts/check-characterization-tags.mjs --all` exits 0; no production file modified
</verification>

<success_criteria>
Every admin arm, the fail-open route, three behaviour defects and the CSRF exposure are pinned before slice 5 changes any of them.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/__tests__/api/admin/adminArmTable.ts` (`ADMIN_ARMS`), `src/__tests__/api/admin/admin-guard-characterization.test.ts` (PRESERVE), `src/__tests__/api/admin/admin-guard-defect.test.ts` (DEFECT F-061/F-001, `FIXED_ARMS`), `src/__tests__/api/admin/admin-users-patch-defect.test.ts` (DEFECT F-091), `src/__tests__/pages/public-profile-defect.test.tsx` (DEFECT F-005), `src/__tests__/api/events/pending-edits-defect.test.ts` (DEFECT F-086), `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts`, `evidence/slice-5-characterization.txt`, `evidence/slice-5-mutation-check.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-12-SUMMARY.md` when done. Deferred items found go under "Deferred items found"; 05-19 registers them.
</output>
