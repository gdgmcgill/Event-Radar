---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-04"]
files_modified:
  - src/proxy.ts
  - src/proxy-defect.test.ts
  - src/app/auth/callback/route.ts
  - src/app/auth/callback/route.test.ts
  - src/app/auth/callback/route-defect.test.ts
  - src/lib/__tests__/env-assertions-defect.test.ts
  - src/server/db/elevated/REGISTRY.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/proxy-refactor.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/callback-refactor.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "The proxy fails closed on its own errors: a missing env var, a thrown getUser, or a failed profile read produces 500 (JSON under /api/, plain text otherwise), never a pass-through"
    - "A banned caller's /api/* request gets 403 {\"error\":\"Account suspended\"} with a JSON content type; a banned caller's page request still redirects to /banned with the query preserved"
    - "Onboarding is read from the database in the same single users query as the ban columns; deleting the cookie no longer lets an un-onboarded user browse, and a stale cookie no longer traps an onboarded user"
    - "A signed-in user with no profile row gets 403 {\"error\":\"Profile not found\"} on /api/* and is signed out and sent to /?error=profile_sync_failed on a page"
    - "Sign-in never writes users.roles, a failed profile sync signs the user out instead of admitting them, and next can only redirect to the app's own origin"
    - "Every non-McGill rejection still signs out, deletes the orphaned auth user and redirects with error=not_mcgill"
  artifacts:
    - path: "src/proxy.ts"
      provides: "advisory, fail-closed ring: one users read, JSON 403 for /api/*, DB-truth onboarding"
      contains: "Failed to process request"
    - path: "src/app/auth/callback/route.ts"
      provides: "no role grant, fail-closed profile sync, same-origin next, both service-role uses through the door"
      contains: "profile_sync_failed"
    - path: "src/server/db/elevated/REGISTRY.md"
      provides: "the first two register rows"
      contains: "src/app/auth/callback/route.ts"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/env.ts"
      via: "supabaseUrl()/supabaseAnonKey() inside the try, so a missing variable reaches the fail-closed catch"
      pattern: "supabaseUrl\\(\\)"
    - from: "src/app/auth/callback/route.ts"
      to: "src/server/db/elevated/index.ts"
      via: "getElevatedClient() for auth.admin.deleteUser and the profile upsert/read"
      pattern: "getElevatedClient\\(\\)"
    - from: "src/proxy.ts"
      to: "e2e/fixtures.ts"
      via: "PROTECTED_ROUTES stays a one-line array literal the fixture parser can read"
      pattern: "const PROTECTED_ROUTES = \\["
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "src/proxy-characterization.test.ts and src/proxy.test.ts are not edited; in src/app/auth/callback/route.test.ts no assertion changes — only the two moved tests, their two docblock bullets and the service mock's auth member"
    - "config.matcher in src/proxy.ts is byte-identical; nothing is added between createServerClient and auth.getUser"
    - "The McGill enforcement path (isMcGillEmail, sign-out, error=not_mcgill) is not weakened"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: ADMIN_EMAILS -->

<objective>
Slice 3, refactor part 2: make the two entry points of the auth ring fail closed — the proxy (F-003, F-062, F-088, F-089; DEC-36) and the OAuth callback (F-004, F-077, FO-05; DEC-38).

Purpose: ROADMAP criterion 1 requires "middleware is advisory-only, the ban check fails closed … the onboarding guard cannot be bypassed", and the Next.js 16 proxy docs say authorization must be re-made in each handler. The proxy keeps redirects and hygiene but stops being the thing that silently lets traffic through; the callback stops granting roles and stops admitting users whose profile it could not write.

Output: the rewritten proxy with its DEFECT suite flipped, the rewritten callback with its F-004 pins moved and F-077 flipped, the env census emptied, and the first two REGISTRY rows.
</objective>

<slice_outcome>
Green when `src/proxy-characterization.test.ts` and `src/proxy.test.ts` pass unedited, `src/proxy-defect.test.ts` passes with every row in its fixed shape, the six remaining callback PRESERVE tests pass with their bodies byte-identical, `route-defect.test.ts` passes with F-077 and F-004 fixed, the env census is empty, and `npm run build` succeeds.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the proxy to fail closed, answer /api/* in JSON, and read onboarding from the database</name>
  <files>src/proxy.ts, src/proxy-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/proxy-refactor.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/proxy.ts (whole file) — preserve lines 20-87 (client creation, the no-logic-before-getUser comment, cookie cleanup), line 114 (PROTECTED_ROUTES literal) and 147-158 (config) byte-identical
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md lines 240-260 — Node runtime default, "always verify inside each handler"
    - src/proxy-characterization.test.ts and src/proxy-defect.test.ts — every assertion that must stay green or move
    - evidence/phase-05-decisions.md § DEC-35 and § DEC-36 — the exact bodies
    - src/lib/env.ts, src/lib/ban.ts (isBanned)
    - evidence/defect-ledger.md — the protocol this move follows
  </read_first>
  <action>
Rewrite `src/proxy.ts` per DEC-36. Keep the rate-limit call first. Delete the env pass-through; inside the try read `supabaseUrl()` and `supabaseAnonKey()` from `@/lib/env` into the same local names the body already uses. Define `const isApi = path.startsWith("/api/")` once. For `user` on any path outside BAN_EXEMPT_PATHS (unchanged: `/banned`, `/auth/signout`, `/auth/callback`), issue ONE read `supabase.from("users").select("banned_at, ban_expires_at, onboarding_completed").eq("id", user.id).single()`:
- error with code `PGRST116` → no-row branch: `isApi` → `NextResponse.json({ error: "Profile not found" }, { status: 403 })`; otherwise `await supabase.auth.signOut()`, then redirect to a clone of `nextUrl` with pathname `/` and search replaced by `error=profile_sync_failed`, copying every cookie set on `supabaseResponse` (the sign-out's clearing cookies) onto the redirect response.
- any other error, or no data → `throw new Error("[Middleware] profile read failed")` (reaches the catch).
- `isBanned(profile)` (from `@/lib/ban`) → `isApi` → `NextResponse.json({ error: "Account suspended" }, { status: 403 })`; otherwise today's `/banned` redirect from a `nextUrl.clone()` (query preserved).
Keep the anonymous PROTECTED_ROUTES redirect exactly, with the array literal on one line. Replace the cookie onboarding guard with: `user`, a profile was read, `profile.onboarding_completed !== true`, and `path` is not `/onboarding` and does not start with `/api/` or `/auth/` → redirect to `/onboarding`. The `needs_onboarding` cookie is no longer read (it remains a hint the callback sets). Replace the catch: keep `console.error("[Middleware] Error:", e)`, then return `isApi` → `NextResponse.json({ error: "Failed to process request" }, { status: 500 })`, else `new NextResponse("Internal Server Error", { status: 500 })` (compute isApi from `request.nextUrl.pathname` inside the catch). Add a file docblock naming DEC-36, F-003, F-062, F-088, F-089 and stating the proxy is advisory: every handler re-decides through the seam.

Follow the ledger protocol: run `src/proxy-defect.test.ts` unedited and record the red rows; move each row to its fixed shape — (a) env unset: `/profile` anonymous → 500 plain text and an added `/api/events` variant → 500 JSON `{"error":"Failed to process request"}`; (b) getUser rejects → 500; (c) read error XX000 → 500; (d) PGRST116 → `/api/events/x/save` POST 403 `{"error":"Profile not found"}`, `/my-events` → 307 to `/` with `error=profile_sync_failed` and `auth.signOut` called once; (e) banned POST `/api/events/x/save` → 403 `{"error":"Account suspended"}` with `content-type` containing `application/json`; (f) cookie absent, DB false, `/my-events` → 307 `/onboarding`; (g) onboarded with stale cookie on `/` → no Location — and update the Status paragraph to `FIXED in 05-05 by <commit>`; restore the old proxy temporarily and see the moved rows red, restore and `cmp`. `src/proxy-characterization.test.ts` and `src/proxy.test.ts` must pass unedited throughout. One ledger row per moved row. Record all runs in `evidence/proxy-refactor.txt`, plus `npm run build` (the proxy must bundle with its new imports). Commit `fix(05-05): proxy fails closed, answers /api/* with JSON 403, reads onboarding from the database (F-003, F-062, F-088, F-089)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE` listing: banned /api/* now 403 JSON; ring errors now 500; no-row users signed out on pages, 403 on /api/*; onboarding enforced from the database (cookie deletion no longer bypasses it; a stale cookie no longer traps an onboarded user).
  </action>
  <verify>
    <automated>npx jest --ci src/proxy.test.ts src/proxy-characterization.test.ts src/proxy-defect.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git diff <05-04 head> -- src/proxy-characterization.test.ts src/proxy.test.ts` prints nothing, and both suites pass
    - `src/proxy-defect.test.ts` passes and its docblock contains `FIXED in 05-05`
    - `src/proxy.ts` contains `Failed to process request`, `Profile not found`, `Account suspended`, `profile_sync_failed`, and `select("banned_at, ban_expires_at, onboarding_completed")` exactly once
    - `node -e 'const s=require("fs").readFileSync("src/proxy.ts","utf8");process.exit(/const PROTECTED_ROUTES = \[[^\]\n]*\];/.test(s)&&!s.includes("return NextResponse.next({ request });\n  }\n\n  try")?0:1)'` exits 0
    - `evidence/proxy-refactor.txt` records the unedited-red run, the moved-green run, the restore-red run and `npm run build` exit=0
    - `evidence/defect-ledger.md` gains one row per moved proxy-defect row (at least 7), each citing its F-nnn
  </acceptance_criteria>
  <done>The proxy is advisory and fails closed; a banned API client gets JSON it can parse; onboarding is database truth.</done>
</task>

<task type="auto">
  <name>Task 2: Callback grants no roles, fails closed on profile sync, validates next, and reaches the service role only through the door</name>
  <files>src/app/auth/callback/route.ts, src/app/auth/callback/route.test.ts, src/app/auth/callback/route-defect.test.ts, src/lib/__tests__/env-assertions-defect.test.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/callback-refactor.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/auth/callback/route.ts (whole file)
    - src/app/auth/callback/route.test.ts lines 1-230 and 270-370 — the docblock bullets, the three mocks, beforeEach, tests 4-8
    - src/app/auth/callback/route-defect.test.ts (05-02)
    - src/server/db/elevated/index.ts and src/server/db/elevated/REGISTRY.md (the table header at :12-13, the "_(none)_" row and the "empty in this phase" paragraph)
    - evidence/phase-05-decisions.md § DEC-38 and § DEC-47 (why the upsert must go through the door once INSERT on users is revoked)
    - src/lib/utils.ts isMcGillEmail — unchanged
  </read_first>
  <action>
Rewrite `src/app/auth/callback/route.ts` per DEC-38:
- Delete the module-level allowlist parse (the ADMIN_EMAILS read), `isAdminEmail`, and the role-update block (F-004). Remove the `@supabase/supabase-js` import, `@/lib/supabase/service` import and every service-key read.
- `createServerClient` takes `supabaseUrl()` and `supabaseAnonKey()`.
- Non-McGill path: unchanged except the orphan deletion uses `getElevatedClient().auth.admin.deleteUser(user.id)` inside the existing try/catch.
- Profile sync in its own try/catch: `const elevated = getElevatedClient()`; upsert the unchanged payload with `{ onConflict: "id", ignoreDuplicates: false }`; then `.select("onboarding_completed, roles").eq("id", user.id).single()` — keep this column list byte-identical (PRESERVE test 5 pins it; roles is read and unused). An upsert error, a read error, a null profile, or any throw (including MissingEnvError) → fail closed: `await supabase.auth.signOut()`, then redirect to `/?error=profile_sync_failed` on the request origin with every accumulated cookie attached exactly as the success path attaches them, and no onboarding cookie.
- `next` (F-077): compute a safe destination — accept `next` only when it starts with `/`, its second character is neither `/` nor a backslash, and `new URL(next, requestUrl.origin).origin === requestUrl.origin`; otherwise `/`. The success redirect uses it.
Keep every log line prefixed `[Callback]`.

Tests, per the ledger protocol, in one commit with the fix:
- In `route.test.ts`: delete the two `it` blocks "appends admin to the existing roles…" and "skips profile sync entirely…" and their two "Tests cover" bullets (replace them with one bullet pointing to route-defect.test.ts); in `beforeEach` change only `mockCreateServiceClient.mockReturnValue({ from: mockFrom })` to also carry `auth: { admin: { deleteUser: mockDeleteUser } }` (the seam moved; DEC-38). Nothing else changes. With a scratchpad node script, extract the six remaining `it(` blocks from `git show <05-04 head>:src/app/auth/callback/route.test.ts` and from the working tree and assert them byte-identical; paste its output into `evidence/callback-refactor.txt`.
- In `route-defect.test.ts`: flip the F-077 rows (the three hostile values → Location `https://callback.test/`, pathname `/`; the control stays `/my-events`); add the moved F-004 tests as fixed-shape assertions — with the allowlist env variable set to the user's address, `mockUpdate` is never called; with `mockCreateServiceClient` throwing `new MissingEnvError("SUPABASE_SERVICE_ROLE_KEY")`, `mockSignOut` is called, Location has `error=profile_sync_failed`, and no `needs_onboarding` cookie is set — plus two new rows: upsert returning an error, and the profile read returning `{ data: null }`, each failing closed the same way. Update the docblock to cite F-004 and F-077 with `FIXED in 05-05`.
- In `env-assertions-defect.test.ts`: the expected list becomes empty.
Ledger rows for each moved test and the census step.

Register in `src/server/db/elevated/REGISTRY.md`: replace the `_(none)_` row with two rows — `Delete the orphaned auth.users row of a rejected non-McGill sign-in (auth.admin.deleteUser)` | `src/app/auth/callback/route.ts` | `auth.users is owned by GoTrue; its admin API needs the service role and no RLS policy can grant it` | 05; and `Upsert and read the signing-in user's own public.users row at sign-in` | `src/app/auth/callback/route.ts` | `The row may not exist yet, the upsert writes email (withheld from authenticated by the F-006 column grant) and INSERT on users is revoked from authenticated by DEC-47; the callback is the only writer` | 05 — and rewrite the "empty in this phase" paragraph to say rows are now added per migration, with the header `**Phase:** 03 · **Plan:** 03-03` kept and a line `Rows added from Phase 5 (05-05 onward).`

Run `npx jest --ci src/app/auth src/lib/__tests__/env-assertions-defect.test.ts`, `node scripts/check-elevated-ratchet.mjs` (live 24, committed 25, exit 0 — the callback left the census), `npm run lint`, `npx tsc --noEmit`; record in `evidence/callback-refactor.txt`. Commit `fix(05-05): callback grants no roles, fails closed on profile sync, validates next (F-004, F-077, FO-05)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE`: sign-in no longer grants admin from an allowlist; a failed profile sync signs the user out with error=profile_sync_failed; off-origin next values land on /.
  </action>
  <verify>
    <automated>npx jest --ci src/app/auth src/lib/__tests__/env-assertions-defect.test.ts && node scripts/check-elevated-ratchet.mjs && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/auth/callback/route.ts` contains `getElevatedClient()`, `profile_sync_failed`, `select("onboarding_completed, roles")`, `isMcGillEmail`, `not_mcgill`, and does not import `@supabase/supabase-js`
    - `evidence/callback-refactor.txt` shows the six remaining PRESERVE `it` blocks byte-identical to the 05-04 head and all eight route.test.ts-plus-defect runs green
    - `route-defect.test.ts` cites F-004 and F-077 with `FIXED in 05-05` and contains the missing-key, upsert-error, null-profile and three hostile-next cases
    - The env census suite asserts an empty set and passes
    - `node scripts/check-elevated-ratchet.mjs` exits 0 and prints `live=24`
    - `src/server/db/elevated/REGISTRY.md` has two rows naming `src/app/auth/callback/route.ts` and no `_(none)_` row
    - `evidence/defect-ledger.md` gains rows for F-004 (two), F-077 (three), FO-05 (two) and the F-003 census step
  </acceptance_criteria>
  <done>Signing in can no longer escalate, can no longer admit a user whose profile could not be written, and can no longer send a freshly authenticated user off-origin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → proxy | every request; the ring now fails closed on its own errors |
| OAuth provider → callback | the code exchange and the redirect target cross here |
| callback → service role | orphan deletion and profile upsert, now through the door |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Elevation of Privilege | proxy catch passing traffic through on error (F-088) | mitigate | catch returns 500; DEFECT rows b and c assert it (Task 1 acceptance) |
| T-05-05-02 | Denial of Service | lockout of every user if the single users read breaks (Pitfall 7) | mitigate | PGRST116 is distinguished from other errors; PRESERVE rows 3-5 prove active, banned and expired users route exactly as before; the full Playwright floor in 05-08 signs in all ten personas |
| T-05-05-03 | Spoofing | open redirect through next (F-077), including slash-backslash | mitigate | same-origin resolution plus leading-slash rules; three hostile cases in route-defect acceptance |
| T-05-05-04 | Elevation of Privilege | admin granted at sign-in from an environment allowlist (F-004) | mitigate | the grant code is deleted; a test asserts no users update even with the variable set |
| T-05-05-05 | Elevation of Privilege | a user admitted with no profile row (FO-05) and later treated as unbanned | mitigate | callback fails closed with sign-out; proxy and seam answer 403 Profile not found |
| T-05-05-06 | Spoofing | McGill enforcement weakened while rewriting the callback | mitigate | PRESERVE test 4 unedited and green; acceptance requires isMcGillEmail and not_mcgill present |
| T-05-05-07 | Tampering | e2e harness breaks because PROTECTED_ROUTES stops parsing | mitigate | acceptance regex asserts the one-line literal |
| T-05-05-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0, 0 failed
- `npm run build` exit 0 recorded in `evidence/proxy-refactor.txt`
- `node scripts/check-elevated-ratchet.mjs` live=24
- `node scripts/check-characterization-tags.mjs --all` exits 0
</verification>

<success_criteria>
The ring's two entry points fail closed, the callback cannot grant roles or send users off-origin, and every PRESERVE contract held through the rewrite.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** rewritten `src/proxy.ts` and `src/app/auth/callback/route.ts`; `src/proxy-defect.test.ts` and `src/app/auth/callback/route-defect.test.ts` flipped; the env census emptied; REGISTRY rows "auth-user deletion" and "first-sign-in profile sync"; `evidence/proxy-refactor.txt`, `evidence/callback-refactor.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-05-SUMMARY.md` when done.
</output>
