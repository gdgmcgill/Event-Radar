---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 09
type: execute
wave: 8
depends_on: ["05-08"]
files_modified:
  - src/__tests__/api/clubs/club-gates-characterization.test.ts
  - src/__tests__/api/clubs/club-owner-writes-defect.test.ts
  - e2e/specs/club-authorization.spec.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-characterization.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-ring-before.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-mutation-check.txt
autonomous: true
requirements: [REFAC-12]

must_haves:
  truths:
    - "Every one of the 17 club gate and flag sites has its anonymous, non-member, wrong-role and right-role outcome pinned PRESERVE with the site's exact 403 message, before any site changes"
    - "The owner-write defect (F-087) is pinned at the unit level (which client performs the write) and at the real stack (club_owner PATCH on their own club answers 500 today)"
    - "cross_club_attacker's 403 on every club mutation route is pinned through the real harness, so slice 4 can prove it still holds"
    - "Today's RLS-ring state is measured, not assumed: the attacker's forged approved event insert succeeds (F-008), owner updates to clubs and club_members affect 0 rows (F-087), and the denials that already hold are listed"
  artifacts:
    - path: "src/__tests__/api/clubs/club-gates-characterization.test.ts"
      provides: "PRESERVE table over the 17 sites of research § C"
      min_lines: 200
    - path: "src/__tests__/api/clubs/club-owner-writes-defect.test.ts"
      provides: "DEFECT F-087: owner writes land on the cookie client today"
      min_lines: 80
    - path: "e2e/specs/club-authorization.spec.ts"
      provides: "cross-club 403s, member-vs-owner 403s, owner reads, DEFECT F-087 owner PATCH 500"
      min_lines: 100
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-ring-before.txt"
      provides: "rolled-back psql probes of the club-scoped tables as attacker and owner"
      min_lines: 40
  key_links:
    - from: "src/__tests__/api/clubs/club-owner-writes-defect.test.ts"
      to: "@/lib/supabase/service"
      via: "two distinct fakes — cookie and elevated — so the suite can say which client performed each write"
      pattern: "jest\\.mock\\(\"@/lib/supabase/service\""
    - from: "e2e/specs/club-authorization.spec.ts"
      to: "e2e/fixtures.ts"
      via: "persona storage states and seeded ids"
      pattern: "storageStateFor\\(\"cross_club_attacker\"\\)"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No production source file changes; the RLS probes run inside BEGIN … ROLLBACK and leave no row"
    - "The spec never changes seeded values: the owner PATCH sends the club's current description, and no mutation route is called with a body that would succeed for the attacker"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 4, characterization: pin every club-membership decision before it collapses into `requireClubRole` (REFAC-12), pin the owner-write defect (F-087) at both the unit and the real-stack level, and measure the RLS ring as it stands.

Purpose: ROADMAP criterion 2 — "a cross-club access attempt returns 403 at the authz ring and is independently denied at the RLS ring". The authz half must be preserved byte for byte across 17 sites; the RLS half is where research measured three holes (C10, C11). Nothing changes until both are on record.

Output: a PRESERVE table suite, a DEFECT suite, a Playwright spec, the RLS before-probe, mutation evidence.
</objective>

<slice_outcome>
Green when both Jest suites and the new spec pass on unmodified source, `evidence/rls-ring-before.txt` shows each probe's outcome with ROLLBACK, and each mutation cycle shows its named test red.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: PRESERVE table over the 17 club sites and the F-087 DEFECT suite</name>
  <files>src/__tests__/api/clubs/club-gates-characterization.test.ts, src/__tests__/api/clubs/club-owner-writes-defect.test.ts</files>
  <read_first>
    - 05-RESEARCH.md § C "Club authorization call sites" — the 17 rows: file:line, gate or flag, accepted roles, exact deny bytes; and the "Not authorization decisions" list
    - each of the 17 sites' route files at the cited lines: src/app/api/clubs/[id]/analytics, events, invites (GET, POST), members/role, members (GET, DELETE), [id] (PATCH, DELETE), transfer, src/app/api/clubs/banner, logo, src/app/api/events/[id]/analytics, events/[id]/reviews GET, events/[id] PATCH and DELETE, events/create
    - src/__tests__/api/auth-ring/writeHandlerTable.ts — the persona builders and the invoke-closure pattern to reuse
    - src/__tests__/helpers/fakeSupabase.ts — `calls`, tables
    - scripts/seed/personas.ts MEMBERSHIPS — the roles in use are owner and organizer only (C1)
  </read_first>
  <action>
**PRESERVE `club-gates-characterization.test.ts`** — docblock `PRESERVE characterization — club membership decisions at the 17 sites of research § C (REFAC-12)`, naming C1 (only owner and organizer exist) and R9 (no admin bypass). Mock `@/lib/supabase/server` with fake A and `@/lib/supabase/service` with a separate fake B. Fixture: club C1 (approved) with an owner row, an organizer row; club C2 owned by the attacker; an admin user with roles ["user","admin"] and no membership; each caller has an onboarded, unbanned users row. For every gate site assert, measuring first and recording the measurement in `evidence/slice-4-characterization.txt`:
- anonymous → the site's current status and body;
- non-member (the attacker, owner of C2) → 403 with the site's exact message from § C;
- wrong role (the organizer on an owner-only site) → the same 403 message;
- admin with no membership → the same 403 message (R9: no bypass);
- right role → not 401/403 (the handler proceeds; only the status class is asserted).
For the four flag sites (clubs/[id]/events isOrganizer, events/[id]/reviews isOrganizer, events/create auto-approve, events/[id] PATCH and DELETE composite member arms) pin the observable difference between a member and a non-member: the measured response field or status, and for events/create the inserted row's status (`approved` for a member of an approved club, `pending` otherwise) read from fake A's calls.

**DEFECT `club-owner-writes-defect.test.ts`** — docblock `DEFECT characterization — F-087: owner club writes run on the cookie client, where RLS denies them`, `Status: OPEN — assertions move in 05-10`. With the owner as caller:
- PATCH /api/clubs/[id] `{ description: "x", status: "rejected" }` → the clubs update is recorded on fake A (cookie) and not on fake B; the update payload has no `status` key (this whitelist assertion does not move).
- DELETE /api/clubs/[id] `{ confirmName: <club name> }` → the `status: "deleted"` update is on fake A; the admin_audit_log insert (`action: "club_deleted"`) is on fake B.
- PATCH /api/clubs/[id]/members/role → the club_members update is on fake A.
- POST /api/clubs/[id]/transfer → today's writes on fake B, with the demotion filtered by the caller's membership `id`.
Run `npx jest --ci src/__tests__/api/clubs`; append the output. Commit `test(05-09): club gate PRESERVE table and F-087 DEFECT pins`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/clubs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both suites pass on unmodified source
    - The PRESERVE suite has a row for each of the 17 sites (count the site ids in its table: at least 17) and asserts each gate site's 403 message string exactly as research § C lists it
    - The PRESERVE suite contains an admin-without-membership case asserting 403 on at least the owner-only sites
    - `node scripts/check-characterization-tags.mjs src/__tests__/api/clubs/club-gates-characterization.test.ts src/__tests__/api/clubs/club-owner-writes-defect.test.ts` exits 0 and the DEFECT docblock cites F-087
    - `git status --porcelain -- src/app src/lib src/server` shows no modified tracked file
  </acceptance_criteria>
  <done>Every club decision slice 4 will rewrite has its bytes on record, and the owner-write defect is pinned where it can be seen without a database.</done>
</task>

<task type="auto">
  <name>Task 2: Club-authorization Playwright spec and the RLS-ring before-probe</name>
  <files>e2e/specs/club-authorization.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-ring-before.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-characterization.txt</files>
  <read_first>
    - e2e/specs/club-owner-surfaces.spec.ts — how club surfaces are asserted today
    - e2e/fixtures.ts, scripts/seed/personas.ts IDS (approvedClub, otherClub, memberOfApproved, ownerOfApproved) and CLUBS (names)
    - 05-RESEARCH.md § C deny bytes and § D "RLS ring matrix" (the measured probes this task re-runs)
    - supabase/tests/database/000-setup.sql — how impersonation is expressed (role authenticated + request.jwt.claims)
  </read_first>
  <action>
**`e2e/specs/club-authorization.spec.ts`** — docblock naming REFAC-12, F-087, DEC-40, DEC-41. Using `page.request` under each persona's storage state and `IDS.approvedClub`:
- cross_club_attacker: PATCH `/api/clubs/<id>` → 403 `{ error: "Only the club owner can update club details" }`; DELETE `/api/clubs/<id>` with `{ confirmName: "x" }` → 403 `Only the club owner can delete the club`; GET and POST `/api/clubs/<id>/invites` → 403 `Only the club owner can view invitations` / `…send invitations`; GET `/api/clubs/<id>/members` → 403 `You must be a member of this club to view members`; DELETE members `{ memberId: IDS.memberOfApproved }` → 403 `Only the club owner can remove members`; PATCH `/members/role` → 403 `Only the club owner can change roles`; POST `/transfer` → 403 `Only the club owner can transfer ownership`; GET `/api/clubs/<id>/analytics` → 403 `You must be a club member to view analytics`. Every body string is the one the spec measures on first run and must equal § C; a mismatch is recorded, not papered over.
- club_member (organizer): PATCH `/api/clubs/<id>` → 403 owner-only message; GET members and analytics → 200.
- club_owner: GET invites, members and analytics → 200; and a test titled `DEFECT F-087: the owner's own club PATCH answers 500` that reads the club's current description via GET `/api/clubs/<id>` and PATCHes that same description → today 500 `{ error: "Failed to update club" }` (no value can change on success later, because the value is the current one).
Run the spec after a clean reset and seed; append to `evidence/slice-4-characterization.txt`.

**RLS before-probe.** With `docker exec supabase_db_Event-Radar psql -U postgres`, run each probe inside `BEGIN; SET LOCAL ROLE authenticated; SELECT set_config('request.jwt.claims', '{"sub":"<persona id>","role":"authenticated"}', true); … ROLLBACK;` against the seeded ids and record statement, outcome (row count or SQLSTATE) and ROLLBACK into `evidence/rls-ring-before.txt`: attacker INSERT into events of an approved event for approvedClub with `created_by` = club_owner (expected: succeeds, F-008), the same with `created_by` = attacker (expected: succeeds), club_owner UPDATE clubs on approvedClub (expected: UPDATE 0, F-087), club_owner UPDATE club_members role on memberOfApproved (expected: UPDATE 0), attacker DELETE FROM club_members where club_id = approvedClub (expected: DELETE 0), club_owner DELETE of memberOfApproved (expected: DELETE 1), attacker INSERT into club_invitations for approvedClub (expected: 42501), and an anonymous (SET LOCAL ROLE anon) INSERT into events (record whatever it does). No INSERT uses RETURNING (research § D note). Commit `test(05-09): club authorization spec and RLS-ring before-probe`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-ring-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/specs/club-authorization.spec.ts` passed with exit=0 after a clean reset, recorded in `evidence/slice-4-characterization.txt`
    - The spec contains a test titled with `DEFECT F-087` and at least 9 cross_club_attacker assertions
    - `evidence/rls-ring-before.txt` records 8 probes, each followed by `ROLLBACK`, and the F-008 forged insert's outcome
    - A post-probe query shows the seeded events count unchanged (recorded)
  </acceptance_criteria>
  <done>The authz ring's cross-club denials are pinned through the real app, and the RLS ring's actual state is measured, holes included.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the slice-4 net bites; tag gate; unmodified proof</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-mutation-check.txt</files>
  <read_first>
    - the suites and spec from Tasks 1-2
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-mutation-check.txt — cycle format
  </read_first>
  <action>
Mutation cycles on SOURCE, each with the named red test and a `git diff --exit-code` restore: (1) in `src/app/api/clubs/[id]/invites/route.ts` POST drop the `.eq("role", "owner")` filter → the PRESERVE wrong-role row for invites POST red; (2) change the members GET deny message text → PRESERVE row red; (3) in `src/app/api/clubs/[id]/route.ts` PATCH construct the update on the service factory instead of the cookie client → DEFECT PATCH row red; (4) in `src/app/api/events/create/route.ts` force `status: "approved"` → the auto-approve flag row red; (5) add a site-wide `roles.includes("admin")` bypass in front of the members DELETE check → the admin-without-membership row red. Record all in `evidence/slice-4-mutation-check.txt` with `node scripts/check-characterization-tags.mjs --all`, `npx tsc --noEmit`, full `npx jest --ci`, and `git diff --stat <05-08 head> -- src/app src/lib src/server` (empty). Commit `docs(05-09): slice 4 characterization mutation evidence`.
  </action>
  <verify>
    <automated>node scripts/check-characterization-tags.mjs --all && npx jest --ci && git diff --exit-code -- src/app src/lib src/server</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/slice-4-mutation-check.txt` records 5 cycles, each with a named red test and a restore exit 0
    - `npx jest --ci` exits 0, 0 failed
    - No tracked file under src/app, src/lib or src/server is modified
  </acceptance_criteria>
  <done>The club net is shown to notice a widened role set, a changed message, a moved write, a forced approval and an admin bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| club member → club routes | the authz ring (club_members lookup) |
| authenticated role → club tables | the RLS ring, probed directly as each persona |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Elevation of Privilege | an admin bypass slipping into club gates during slice 4 | mitigate | PRESERVE admin-without-membership rows; mutation cycle 5 proves they bite |
| T-05-09-02 | Tampering | RLS probes leaving rows in the seeded database | mitigate | every probe in BEGIN … ROLLBACK; the events count re-checked afterwards (Task 2 acceptance) |
| T-05-09-03 | Tampering | the spec changing seeded club values | mitigate | the owner PATCH sends the current description; attacker calls are all denied |
| T-05-09-04 | Repudiation | 403 messages transcribed from research rather than measured | mitigate | each message measured on first run and compared to § C; mismatches recorded |
| T-05-09-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0; `npx playwright test e2e/specs/club-authorization.spec.ts` green on a clean reset
- `node scripts/check-characterization-tags.mjs --all` exits 0
- No production file modified
</verification>

<success_criteria>
Slice 4 has a byte-level net for all 17 club decisions, the owner-write defect is pinned twice, and the RLS ring's real state is on record before any policy or handler changes.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/__tests__/api/clubs/club-gates-characterization.test.ts` (PRESERVE), `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (DEFECT F-087), `e2e/specs/club-authorization.spec.ts`, `evidence/slice-4-characterization.txt`, `evidence/rls-ring-before.txt`, `evidence/slice-4-mutation-check.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-09-SUMMARY.md` when done. Deferred items found go under "Deferred items found"; 05-11 registers them.
</output>
