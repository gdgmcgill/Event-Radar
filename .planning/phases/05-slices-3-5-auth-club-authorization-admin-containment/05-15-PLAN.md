---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 15
type: execute
wave: 14
depends_on: ["05-12", "05-14"]
files_modified:
  - src/app/api/admin/clubs/[id]/route.ts
  - src/app/api/admin/events/[id]/edits/route.ts
  - src/app/api/admin/events/[id]/status/route.ts
  - src/app/api/admin/organizer-requests/[id]/route.ts
  - src/app/api/admin/organizers/route.ts
  - src/app/api/admin/reports/[id]/route.ts
  - src/app/api/admin/reports/route.ts
  - src/app/api/admin/users/[id]/ban/route.ts
  - src/app/api/clubs/[id]/appeal/route.ts
  - src/app/api/events/[id]/appeal/route.ts
  - src/app/api/clubs/route.ts
  - src/app/api/moderation/reviews/[targetType]/[targetId]/route.ts
  - src/app/api/profile/avatar/route.ts
  - src/app/api/profile/banner/route.ts
  - src/app/api/recommendations/batch/route.ts
  - src/app/api/users/[id]/route.ts
  - src/app/api/users/me/suggestions/route.ts
  - src/app/users/[id]/page.tsx
  - src/__tests__/api/service-role-routing.test.ts
  - src/__tests__/pages/public-profile-defect.test.tsx
  - e2e/specs/admin-write-paths.spec.ts
  - e2e/specs/public-profile-privacy.spec.ts
  - src/server/db/elevated/REGISTRY.md
  - eslint.elevated-allowlist.mjs
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/service-role-migration.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/allowlist-shrink.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-13]

must_haves:
  truths:
    - "Every remaining non-cron service-role use goes through getElevatedClient() with a REGISTRY row that says why RLS cannot express it; every operation an existing policy already permits runs on the cookie client instead"
    - "The elevated allow-list holds exactly the two cron routes, and the lint boundary is green with no other exception"
    - "An anonymous visitor gets 404 for a private profile, and no profile page or its metadata selects the owner's email (F-005)"
    - "Admin approval of a club, report resolution, organizer-request approval and ban/unban still work end to end on the real stack after their operations were split between the cookie client and the door"
  artifacts:
    - path: "src/__tests__/api/service-role-routing.test.ts"
      provides: "for each migrated handler, which client performed each operation (cookie fake vs elevated fake)"
      min_lines: 120
    - path: "eslint.elevated-allowlist.mjs"
      provides: "exactly the two cron routes"
      contains: "src/app/api/cron/send-reminders/route.ts"
    - path: "e2e/specs/admin-write-paths.spec.ts"
      provides: "admin writes through their split clients, with fixture rows created and removed through the local service client"
      min_lines: 80
    - path: "e2e/specs/public-profile-privacy.spec.ts"
      provides: "private profile 404 for anonymous, 200 for a signed-in viewer, no email in the page, visibility restored"
      min_lines: 50
  key_links:
    - from: "src/app/users/[id]/page.tsx"
      to: "src/server/db/elevated/index.ts"
      via: "one shared loader used by generateMetadata and the page, reading a narrowed column list through the door"
      pattern: "getElevatedClient\\(\\)"
    - from: "eslint.elevated-allowlist.mjs"
      to: "scripts/check-elevated-ratchet.mjs"
      via: "regenerated with --write once, the shrink recorded as a diff"
      pattern: "LEGACY_ELEVATED_CALLSITES"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No RLS policy is widened to make a cookie-client move work (research Pitfall 10); an operation no existing policy permits goes through the door with a row"
    - "eslint.elevated-allowlist.mjs is never hand-edited; the two cron routes are not touched (Phase 6)"
    - "Every moved operation is correct against the unmigrated production policy set, which equals the local baseline set (research § F)"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: createServiceClient -->

<objective>
Slice 5, refactor C: the service-role client gets exactly one registered door (DEC-49). Migrate the remaining 17 route files and the public profile page off direct `createServiceClient()` use — to the cookie client where an existing policy permits the operation, through `getElevatedClient()` with a REGISTRY row otherwise — fix F-005 (DEC-48), and regenerate the allow-list to the two cron routes.

Purpose: ROADMAP criterion 3's "every remaining service-role use goes through src/server/db/elevated/ with a registered justification", and the phase goal's "exactly one registered door".

Output: 18 migrated files, a routing test, nine REGISTRY rows, the F-005 fix, two Playwright specs, the allow-list shrunk to two.
</objective>

<slice_outcome>
Green when `node scripts/check-elevated-ratchet.mjs` reports committed=2 live=2, `npm run lint` is 0 errors, the routing test proves every operation's client, `public-profile-defect.test.tsx` passes in its fixed shape, and both specs pass on a clean reset leaving no fixture rows.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Admin-family service-role sites split between the cookie client and the door, proven by a routing test and an end-to-end admin spec</name>
  <files>src/app/api/admin/clubs/[id]/route.ts, src/app/api/admin/events/[id]/edits/route.ts, src/app/api/admin/events/[id]/status/route.ts, src/app/api/admin/organizer-requests/[id]/route.ts, src/app/api/admin/organizers/route.ts, src/app/api/admin/reports/[id]/route.ts, src/app/api/admin/reports/route.ts, src/app/api/admin/users/[id]/ban/route.ts, src/__tests__/api/service-role-routing.test.ts, e2e/specs/admin-write-paths.spec.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/service-role-migration.txt</files>
  <read_first>
    - 05-RESEARCH.md § F (the per-file operation table and "RLS permits on cookie client?" column) — the source of truth for which operation moves where
    - .planning/audit/rls/pg_policies.json or the baseline's CREATE POLICY lines for clubs, moderation_reviews, club_members, events, organizer_requests, event_reports, users, notifications — re-verify each "permits" cell before moving an operation
    - each of the eight route files (every createServiceClient use and what it writes)
    - src/server/db/elevated/index.ts and REGISTRY.md
    - e2e/env.ts, e2e/specs/no-profile-row.spec.ts (the pattern for a temporary auth user)
  </read_first>
  <action>
For each file, replace the service factory with the context's cookie client for operations an existing admin policy permits, and `getElevatedClient()` for the rest, per research § F: admin/clubs/[id] PATCH — clubs update, moderation_reviews insert and club_members upsert on `ctx.supabase`; the creator's users.roles update and the notifications insert on the door. admin/events/[id]/edits — events on the cookie client; notifications on the door. admin/events/[id]/status — events and moderation_reviews on the cookie client; notifications on the door. admin/organizer-requests/[id] — organizer_requests, clubs reads and club_members upsert on the cookie client; users.roles update and notifications on the door. admin/organizers GET, admin/reports GET, admin/reports/[id] PATCH — cookie client only. admin/users/[id]/ban POST and DELETE — users ban columns and notifications on the door; events, clubs and moderation_reviews writes on the cookie client. Re-verify each permitted operation against the policy text first; if a cell is wrong, keep that operation on the door with an honest row and record the correction in the evidence file. Remove each file's service-module import. Response bytes unchanged.

REGISTRY rows (one per operation family, listing every calling module): `Notify another user (notifications insert)` | the admin callers | `notifications INSERT is granted to service_role only; a notification for another user cannot be a caller-scoped policy` | 05; `Set another user's roles on approval (club_organizer)` | admin/clubs/[id], admin/organizer-requests/[id] | `users has no admin UPDATE policy and the F-006 grant withholds roles` | 05; `Ban or unban a user (banned_at, ban_expires_at, ban_reason)` | admin/users/[id]/ban | `users has no admin UPDATE policy; the ban columns are withheld from authenticated by the F-006 grant` | 05.

**`src/__tests__/api/service-role-routing.test.ts`** (untagged unit suite): mock `@/lib/supabase/server` with fake A and `@/lib/supabase/service` with fake B; for each migrated admin handler, run its success path as an admin and assert which fake recorded each table operation (for example, admin/clubs/[id] approve: clubs update, moderation_reviews insert and club_members upsert on A; users update and notifications insert on B).

**`e2e/specs/admin-write-paths.spec.ts`**: `test.describe.serial`; beforeAll with the local service client creates a temporary auth user `p5-fixture-organizer@mail.mcgill.ca` plus its public.users row, a pending club created by it, an event_reports row it filed against `IDS.approvedEvent`, and an organizer_requests row for it. As admin: approve the club → 200, then (service-client reads) the club is approved, the creator has an owner club_members row, its roles include club_organizer, and a notification exists for it; resolve the report → 200 and its status changed; approve the organizer request → 200; ban the fixture user then unban → both 200 and banned_at cleared at the end. afterAll deletes every fixture row and the auth user; record a query proving they are gone. Run with the admin specs after a clean reset and seed; record everything in `evidence/service-role-migration.txt`. Commit `refactor(05-15): admin service-role sites split between the cookie client and the registered door (REFAC-13)`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/service-role-routing.test.ts src/__tests__/api/admin && node scripts/check-elevated-ratchet.mjs && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the eight files imports `@/lib/supabase/service`; those that write notifications or users columns contain `getElevatedClient()`
    - `service-role-routing.test.ts` passes and covers all eight files
    - `admin-write-paths.spec.ts` passed after a clean reset (recorded exit=0) and the fixture-cleanup query shows zero rows
    - REGISTRY.md has the three rows above; `admin-guard-characterization.test.ts` passes unedited
    - `node scripts/check-elevated-ratchet.mjs` exits 0 with a smaller live count
  </acceptance_criteria>
  <done>The admin surface uses the service role only where no policy can serve it, every such use is registered, and the approval, report, request and ban flows are proven on the real stack.</done>
</task>

<task type="auto">
  <name>Task 2: The non-admin service-role sites</name>
  <files>src/app/api/clubs/[id]/appeal/route.ts, src/app/api/events/[id]/appeal/route.ts, src/app/api/clubs/route.ts, src/app/api/moderation/reviews/[targetType]/[targetId]/route.ts, src/app/api/profile/avatar/route.ts, src/app/api/profile/banner/route.ts, src/app/api/recommendations/batch/route.ts, src/app/api/users/[id]/route.ts, src/app/api/users/me/suggestions/route.ts, src/__tests__/api/service-role-routing.test.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/service-role-migration.txt</files>
  <read_first>
    - 05-RESEARCH.md § F rows for these nine files
    - each file's service-client uses
    - src/__tests__/api/auth-ring/write-handlers-characterization.test.ts and the Phase 4 PRESERVE suites that touch these routes — must pass unedited
  </read_first>
  <action>
Per research § F: clubs/[id]/appeal — moderation_reviews insert on the cookie client (creator appeal policy); clubs update, the cross-user users read and notifications on the door. events/[id]/appeal — events update and moderation_reviews on the cookie client; users read and notifications on the door. clubs POST — clubs insert, the owner club_members insert and the creator's roles update on the door (one row). moderation/reviews GET — reviews, targets and the gate on the cookie client; only the author-name enrichment on the door. profile/avatar and profile/banner — the users self-update on the cookie client (own-row policy; avatar_url and banner_url are in the F-006 grant). recommendations/batch — the compute_user_scores rpc on the door. users/[id] PATCH — the self-update on the cookie client (every payload column is in the grant). users/me/suggestions — cross-user reads on the door. Remove every service-module import.

REGISTRY rows: `Club appeal: reset a rejected club to pending` | clubs/[id]/appeal | `clubs has no owner UPDATE policy (DEC-41)` | 05; `Read another user's name for appeals and review listings` | clubs/[id]/appeal, events/[id]/appeal, moderation/reviews | `users has own-row and admin SELECT policies only; a cross-user name read would need a public-read policy that also exposes email and ban columns` | 05; `Create a club with its owner membership and the creator's organizer role` | clubs POST | `clubs INSERT is admin-only, club_members has no self-owner insert, and roles is withheld by the F-006 grant` | 05; `Batch score computation (rpc compute_user_scores)` | recommendations/batch | `a privileged batch write over every user (F-075 revokes its public EXECUTE in Phase 6)` | 05; `Friend suggestions read other users' profiles, follows, memberships and RSVPs` | users/me/suggestions | `those tables expose other users' rows only to themselves` | 05. Extend `service-role-routing.test.ts` to these nine files. Run the full `npx jest --ci`, lint, tsc, the ratchet; record. Commit `refactor(05-15): non-admin service-role sites through the cookie client or the registered door (REFAC-13)`.
  </action>
  <verify>
    <automated>npx jest --ci && node scripts/check-elevated-ratchet.mjs && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the nine files imports `@/lib/supabase/service`; profile/avatar, profile/banner and users/[id] contain no `getElevatedClient()` (cookie client only)
    - `service-role-routing.test.ts` covers all seventeen migrated route files and passes
    - Every PRESERVE suite passes unedited; `npx jest --ci` exits 0
    - REGISTRY.md has the five rows above
  </acceptance_criteria>
  <done>No non-cron route reaches the service role except through the door, and every door use says why.</done>
</task>

<task type="auto">
  <name>Task 3: F-005 public profile through a narrowed door read with one shared gate; regenerate the allow-list to the two cron routes</name>
  <files>src/app/users/[id]/page.tsx, src/__tests__/pages/public-profile-defect.test.tsx, e2e/specs/public-profile-privacy.spec.ts, src/server/db/elevated/REGISTRY.md, eslint.elevated-allowlist.mjs, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/allowlist-shrink.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/users/[id]/page.tsx (whole file: generateMetadata 33-48, the page's getUser self-redirect, the target read at 63-65, the saved_events/events/club_members/rsvps/get_friends reads 100-140, isPublic at 143, the display filter at 159-161)
    - src/__tests__/pages/public-profile-defect.test.tsx
    - scripts/seed/load.ts near line 275 (every persona's visibility is "public")
    - scripts/check-elevated-ratchet.mjs lines 1-60 and 300-347 (--write; the allow-list header and the one-sanctioned-growth note)
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/boundary-widening.txt — the precedent for recording a regeneration diff
    - evidence/phase-05-decisions.md § DEC-48, § DEC-49
  </read_first>
  <action>
**F-005.** In `src/app/users/[id]/page.tsx` add one module-level loader used by both `generateMetadata` and the page: it takes the target id and the viewer (from `getRequestContext()`), reads the target through `getElevatedClient()` selecting exactly `id, name, avatar_url, banner_url, pronouns, year, faculty, visibility, interest_tags, created_at`, and returns null when the target is missing or when the viewer is anonymous and `visibility === "private"`. Both callers `notFound()` on null. The page's own reads of the target's saved_events, events, club_members, rsvps and get_friends go through the same door; the self-view redirect to `/profile` and `isPublic` handling stay. Remove the service-module import. REGISTRY row: `Public profile of another user` | `src/app/users/[id]/page.tsx` | `users has no public-read policy; adding one would expose email, roles and ban columns at row level; the select is narrowed and anonymous viewers of private profiles get 404 (DEC-48)` | 05. Ledger protocol on `public-profile-defect.test.tsx` (anonymous private → notFound in both metadata and page; the select string has no `email`; the public anonymous row unchanged); Status `FIXED in 05-15`.

**`e2e/specs/public-profile-privacy.spec.ts`**: as onboarded_student PATCH `/api/users/<IDS.onboarded_student>` with `{ visibility: "private" }` → 200; a fresh anonymous context requests `/users/<id>` → 404; club_member's context requests it → 200; with visibility restored to `public` in a finally block (the seed's value), an anonymous request returns 200 and its HTML does not contain onboarded_student's email. Run after a clean reset and seed; record.

**Allow-list.** Run `node scripts/check-elevated-ratchet.mjs --write`, then `node scripts/check-elevated-ratchet.mjs` → `committed=2 live=2 delta=0`; `git diff eslint.elevated-allowlist.mjs` shows only removals and the list is exactly `src/app/api/cron/send-feedback-requests/route.ts` and `src/app/api/cron/send-reminders/route.ts`; update the generated header's note only through the script if it writes one. Record the before/after lists, the diff, `npm run lint` (0 errors) and `command grep -rln "@/lib/supabase/service" src --include='*.ts' --include='*.tsx'` filtered of tests (only src/lib/supabase, src/server/db/elevated and the two cron routes) in `evidence/allowlist-shrink.txt`. Commit `fix(05-15): public profiles read through a narrowed door with one visibility gate; allow-list shrinks to the two cron routes (F-005)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (anonymous visitors get 404 for private profiles; email is never read for a profile page).
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/pages && node scripts/check-elevated-ratchet.mjs && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-elevated-ratchet.mjs` prints `committed=2 live=2 delta=0`
    - `eslint.elevated-allowlist.mjs` lists exactly the two cron routes
    - `src/app/users/[id]/page.tsx` contains `getElevatedClient()`, `notFound()` and the ten-column select, and `node -e 'const s=require("fs").readFileSync("src/app/users/[id]/page.tsx","utf8");process.exit(/select\([^)]*email/.test(s)?1:0)'` exits 0
    - `public-profile-defect.test.tsx` passes with `FIXED in 05-15`
    - `public-profile-privacy.spec.ts` passed after a clean reset (recorded) and onboarded_student's visibility is `public` afterwards (recorded)
    - `npm run lint` exits 0 with 0 errors
  </acceptance_criteria>
  <done>Private profiles stop leaking to anonymous readers, email is never read for a profile page, and the service role has exactly one door plus the two cron routes Phase 6 owns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → service role | now only through getElevatedClient(), each use registered |
| anonymous → public profile | F-005's disclosure path |
| spec → local database | fixture rows for admin writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15-01 | Elevation of Privilege | a new file reaching the service role outside the door | mitigate | allow-list regenerated to 2; ratchet and lint green (Task 3 acceptance) |
| T-05-15-02 | Denial of Service | an admin flow broken by moving an operation to a cookie client RLS actually denies | mitigate | each cell re-verified against policy text; routing unit test; admin-write-paths e2e on the real stack |
| T-05-15-03 | Information Disclosure | private profile and email readable anonymously (F-005, High) | mitigate | narrowed select; shared gate in metadata and page; unit and e2e assertions including no email in HTML |
| T-05-15-04 | Tampering | a policy widened to make a move work | mitigate | prohibition; a non-permitted operation stays on the door with a row |
| T-05-15-05 | Tampering | fixture auth user and rows left behind | mitigate | afterAll cleanup and a zero-rows query recorded |
| T-05-15-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- Ratchet `committed=2 live=2`; `npm run lint` 0 errors; `npx tsc --noEmit` clean
- `npx jest --ci` exits 0
- Both new specs green after a clean reset
</verification>

<success_criteria>
The service-role client has one registered door (plus Phase 6's two cron routes), every use is justified in writing, and the one unjustified use — the public profile page — is fixed.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** 17 route files and the public profile page migrated; `src/__tests__/api/service-role-routing.test.ts`; `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts`; nine REGISTRY rows; `eslint.elevated-allowlist.mjs` regenerated to two entries; `evidence/service-role-migration.txt`, `evidence/allowlist-shrink.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-15-SUMMARY.md` when done.
</output>
