---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 19
type: execute
wave: 18
depends_on: ["05-18"]
files_modified:
  - package.json
  - package-lock.json
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/di-25-bump.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.phase-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.phase-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-close.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/PHASE-5-COMPLETION.md
autonomous: true
requirements: [REFAC-11, REFAC-12, REFAC-13, REFAC-17, REFAC-18]

must_haves:
  truths:
    - "@supabase/supabase-js is on 2.116.0, proven to type-check in a throwaway worktree before the bump, as the last code change of the phase (DI-25, DEC-56)"
    - "After slice 5, every persona still signs in, non-McGill sign-in is still rejected, banned users are still blocked, organizers still reach their club surfaces, and every Validated workflow is re-confirmed from a clean reset"
    - "Authorization decisions happen in one fails-closed place: no legacy ban helper, no admin helper, one getSession call (non-gating health), the elevated allow-list holds only the two cron routes"
    - "Each of the five ROADMAP success criteria and each of the five requirements is answered clause by clause against committed evidence; a requirement is Complete only when every clause of its own sentence is met"
    - "Every intentional behaviour change, every rule-resolved checkpoint, every local-only closure pending DI-23 and every owner action — first among them provisioning Upstash before any push to main — is written in the completion note"
  artifacts:
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/PHASE-5-COMPLETION.md"
      provides: "criteria and requirements clause by clause, re-confirmation, behaviour changes, rule-resolved checkpoints, local-only closures, owner actions, floor, CI observation, citation count"
      min_lines: 200
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.phase-after.txt"
      provides: "the phase after-floor from a clean reset"
      min_lines: 80
  key_links:
    - from: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/PHASE-5-COMPLETION.md"
      to: ".planning/audit/findings.json"
      via: "every finding the note calls closed is Fixed with a resolution citing the note; every local-only one is Open with closes_in_phase 08"
      pattern: "PHASE-5-COMPLETION"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "The supabase-js change is one `npm install @supabase/supabase-js@2.116.0`, reviewed as a diff; @supabase/ssr is not moved (DI-43)"
    - "No finding is marked Fixed without its validation criterion met by committed evidence; no requirement is ticked on a met success criterion alone"
    - "CI is recorded UNOBSERVED unless the owner pushed; no push is made"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: verifyAdmin -->
<!-- planner-discipline-allow: checkBanStatus -->

<objective>
Close slice 5 and the phase: take the deferred supabase-js minor (DI-25) as the last code change, run the full floor from a clean reset, re-confirm every Validated workflow, reconcile the register and the requirements, and write the Phase 5 completion note.

Purpose: ROADMAP criterion 5 and the program's rule that a requirement is measured against its own sentence. The note is where the phase's rule-resolved decisions, its local-only closures and its owner actions become visible to the person who must act on them before any deploy.

Output: the bump, the after-floor, `evidence/slice-5-close.md`, `evidence/PHASE-5-COMPLETION.md`, the register, REQUIREMENTS.md and the ROADMAP requirement-states block.
</objective>

<slice_outcome>
Green when the worktree proof shows 0 type errors on 2.116.0, the bump's lockfile diff is limited to the @supabase packages it pins, the after-floor is green on every row from a clean reset, `validate.mjs --check findings` passes, and the completion note reports `missing=0` citations.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first. 3. Production never touched; `.env.local` never read; no push. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes; the note cites a committed path for every claim. 10. Registers edited surgically. 11. A requirement is measured against its own sentence.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: DI-25 — prove @supabase/supabase-js 2.116.0 in a throwaway worktree, then bump it as its own commit</name>
  <files>package.json, package-lock.json, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/di-25-bump.txt</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/di-25-enumeration.txt — the eight blocking sites measured on 2.116.0
    - 05-RESEARCH.md § H — which slice cleared each site
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Package Legitimacy Audit — @supabase/supabase-js verified OK
    - evidence/phase-05-decisions.md § DEC-56
  </read_first>
  <action>
Throwaway proof first, recorded in `evidence/di-25-bump.txt`: `git worktree add --detach <scratchpad>/di25 HEAD`; in it `npm ci`, `npm install --no-save @supabase/supabase-js@2.116.0`, `npx tsc --noEmit` → 0 errors (if any error appears, record it, stop, and name the site and the slice that should have cleared it — do not bump); `npx jest --ci` there as a second signal; then `git worktree remove --force <scratchpad>/di25` and `git worktree list` showing one line. Then in the main tree: `npm install @supabase/supabase-js@2.116.0`; review the lockfile diff — only `@supabase/supabase-js` and the `@supabase/*` packages it pins (auth-js, functions-js, postgrest-js, realtime-js, storage-js) and their own dependency changes; `lockfileVersion` unchanged; `npm ls @supabase/supabase-js` shows a single 2.116.0 (the @supabase/ssr 0.7.0 peer range ^2.43.4 is satisfied); `npm audit --audit-level=high --omit=dev` exit 0; `npx tsc --noEmit`, `npm run lint`, `npx jest --ci`, the CI-env `npm run build` from 05-04. Commit `chore(05-19): @supabase/supabase-js 2.81.1 → 2.116.0 (DI-25)` whose body lists the eight sites — `admin/events/[id]/edits`, `admin/events/[id]`, `admin/experiments/[id]`, `admin/featured/[id]`, `admin/users/[id]`, `clubs/[id]`, `events/[id]` PATCH, `lib/audit.ts` — each with the commit that cleared it, and states that @supabase/ssr stays on 0.7.0 (DI-43).
  </action>
  <verify>
    <automated>node -p "require('./node_modules/@supabase/supabase-js/package.json').version" | grep -x 2.116.0 && npx tsc --noEmit && npm audit --audit-level=high --omit=dev && test "$(git worktree list | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/di-25-bump.txt` shows the worktree tsc with 0 errors, the worktree removed, and `git worktree list` with one line
    - `node -p "require('./node_modules/@supabase/supabase-js/package.json').version"` prints `2.116.0`; `node -p "require('./node_modules/@supabase/ssr/package.json').version"` prints `0.7.0`
    - The lockfile diff summary in the evidence names only @supabase packages and their dependencies
    - `npx tsc --noEmit`, `npm run lint`, `npx jest --ci`, the audit gate and the CI-env build exit 0
  </acceptance_criteria>
  <done>The SDK minor deferred since Phase 2 is taken, provably safely, as the last code change of the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Phase after-floor from a clean reset, the slice-5 close note, and the register</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.phase-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.phase-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-5-close.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md, .planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md</files>
  <read_first>
    - evidence/floor.before.txt, evidence/floor.slice-3-after.txt, evidence/floor.slice-4-after.txt — every command and number
    - evidence/slice-3-close.md and evidence/slice-4-close.md — the close-note shape
    - the 05-12..05-18 SUMMARYs — "Deferred items found"
    - .planning/audit/findings.json records F-001, F-005, F-006, F-007, F-040, F-058, F-061, F-067, F-072, F-073, F-086, F-090, F-091 — validation criteria
  </read_first>
  <action>
**After-floor.** Port 3000 free; reset; UNSEEDED pgTAP; seed; SEEDED pgTAP; reset and seed; `npx playwright test`; `npx jest --ci --json --outputFile=<scratchpad>/jest.json` plus the one-liner proving the only pending tests are the Upstash contract suite's; lint; tsc; audit; ratchet; migration filenames; tag gate; `validate.mjs --quick`; `supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -` (empty); the CI-env build; and the censuses: env non-null assertions 0; legacy ban helper `command grep -rn "checkBanStatus" src` 0; admin helper `command grep -rn "verifyAdmin" src` 0; `getSession(` only in `src/app/api/health/route.ts`; service-module value imports only in src/lib/supabase, src/server/db/elevated and the two cron routes; `(supabase as any)` 0; gate-site club_members reads outside requireClubRole 0. Write `evidence/floor.phase-after.txt` (line 1 `head=<sha>`, a before / slice-3 / slice-4 / phase-after summary table, raw blocks with exit codes) and `evidence/playwright.phase-after.txt`. Required: Playwright 0 failed; Jest 0 failed with skipped tests only in the contract suite; pgTAP 9 files PASS both runs; ratchet `committed=2 live=2`. One flake re-run allowed, both logs kept.

**Slice-5 close note** `evidence/slice-5-close.md`: floor and Playwright tables; the 16-row Validated re-confirmation (admins approve, ban, review reports with actions written to admin_audit_log — admin-audit-row.spec and admin-write-paths.spec; every persona signs in; non-McGill rejected; banned blocked; organizers reach club surfaces); closures; slice 5's INTENTIONAL BEHAVIOUR CHANGEs with commits.

**Register.** Fixed, each with a resolution citing commits and `evidence/PHASE-5-COMPLETION.md`, only where the validation criterion is met by committed evidence: F-001, F-005, F-061, F-067, F-072, F-073, F-086, F-090, F-091. F-006 and F-007: Open, closes_in_phase "08", resolution "fixed and proven locally (migration 20260923130000, 050, 055); production closes with the DI-23 repair". F-058: resolution noting the /api/admin/* 429 clause delivered (05-17), status Open at "06" for the wrapper and correlation halves. F-040: resolution noting the ADMIN_API_KEY reader deleted (05-14) after ADMIN_EMAILS (05-05), status Open at "06" for CRON_SECRET. Regenerate FOUNDATION_AUDIT.md; `validate.mjs --check findings` exits 0. Register SUMMARY-listed deferred items from the next DI id. Commit `docs(05-19): phase 5 after-floor, slice 5 close, register`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && head -1 .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.phase-after.txt | grep -E '^head=[0-9a-f]{40}$'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.phase-after.txt` ends with 0 failed and exit=0 from a clean reset
    - `evidence/floor.phase-after.txt` shows pgTAP 9 files PASS unseeded and seeded, ratchet `committed=2 live=2 delta=0`, Jest 0 failed with pending tests only in `upstash.contract.test.ts`, the types diff empty, and every census row at its target
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i);process.exit(["F-001","F-005","F-061","F-067","F-072","F-073","F-086","F-090","F-091"].every(i=>g(i).resolution)&&g("F-006").closes_in_phase==="08"&&g("F-007").closes_in_phase==="08"?0:1)'` exits 0
    - `evidence/slice-5-close.md` has a Validated table with as many rows as `grep -c '^- ✓' .planning/PROJECT.md`
  </acceptance_criteria>
  <done>Slice 5 is closed on a green floor from a clean reset, and the register says exactly what Phase 5 closed, what waits for Phase 8, and what Phase 6 still owns.</done>
</task>

<task type="auto">
  <name>Task 3: The Phase 5 completion note, requirement states, and the ROADMAP record</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/PHASE-5-COMPLETION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 5 — the five success criteria verbatim; § Phase 4 "Requirement states" block — the shape to add for Phase 5
    - .planning/REQUIREMENTS.md lines 76-83 and the traceability rows for REFAC-11, 12, 13, 17, 18
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md (whole) — structure, verdict tables, the citation-count command in § 8
    - every Phase 5 evidence file named in the phase-wide roll-up below; evidence/defect-ledger.md; evidence/phase-05-decisions.md; evidence/deferred-items.md
  </read_first>
  <action>
Write `evidence/PHASE-5-COMPLETION.md` (metadata line; Tree and Phase range lines; a summary box) with:
1. How to read this note (the citation rule).
2. The five success criteria, each quoted verbatim and answered clause by clause in a `| Clause | Verdict | Evidence |` table with MET or PARTIAL — criterion 1 (getUser everywhere: the getsession gate; advisory proxy; ban fails closed; onboarding not bypassable; validated config), criterion 2 (requireClubRole at 17 sites; 403 at the authz ring; denied at the RLS ring — MET locally, reaching production with DI-23, said in those words), criterion 3 (fail-open endpoints; admin guard everywhere; one registered door and the two cron routes Phase 6 owns), criterion 4 (distributed store — code in place, store unprovisioned, contract test skipped: say exactly that; admin routes limited; CSRF assessed and protected), criterion 5 (the three slice floors and re-confirmations).
3. REFAC-11, 12, 13, 17, 18 quoted verbatim and measured clause by clause; Complete only when every clause is met, otherwise PARTIAL naming the unmet clause (REFAC-18's "works across serverless instances" is unproven until a store exists — record it honestly).
4. The Validated re-confirmation after all three slices (16 rows).
5. Every INTENTIONAL BEHAVIOUR CHANGE with its finding, commit and authority (DEC): banned callers refused on every write arm incl. DELETE; banned /api/* answered 403 JSON; ring errors 500; no-profile users 403 or signed out; DB-truth onboarding; un-onboarded direct writes refused; four F-028 routes 401; callback grants no roles, fails closed, same-origin next; auth-debug deleted; owner club edit/delete/role change working; forged approved event refused (local); anonymous admin calls 401 and three routes' non-admins 403; calculate-popularity closed; admin role change working and audited; audit rows written; pending edits visible to their owner; private profiles 404 to anonymous; users self-escalation and audit forgery refused (local); admin routes rate limited; cross-site mutations refused.
6. Rule-resolved checkpoints: every DEC-33..DEC-57 taken without an owner, with the ones an owner should review first (DEC-34, DEC-41, DEC-45, DEC-47, DEC-50, DEC-52), and the one human checkpoint (05-18) with who answered.
7. Local-only closures pending DI-23: F-006, F-007, F-008, F-016, with their proofs.
8. Owner actions, in this order: (a) provision Upstash on Vercel and set UPSTASH_REDIS_REST_URL/TOKEN or KV_REST_API_URL/TOKEN BEFORE any push to main, because Vercel deploys main and a production server without a store refuses to start (DEC-50, DI-42); (b) an owner-authorized read-only count of production users with onboarding_completed false or null and of auth users with no users row, before deploy (A6); (c) optionally, an earlier targeted production apply of 20260923120000 and 20260923130000 (both Critical/High fixes wait for DI-23 otherwise); (d) the DI-41 invite-acceptance UX decision; (e) DI-22, DI-39, DI-40 unchanged.
9. Deferred items and assumptions: every DI-41 onward and A1-A6 with final state.
10. The phase floor at close (the after-floor summary).
11. CI observation: the run id and the three job conclusions if the owner pushed, otherwise the line `CI run UNOBSERVED — no push was authorized in this phase`.
12. Citation count: the Phase 4 § 8 command adapted to this phase directory, its output `distinct=<n> missing=0`.

Then: in `.planning/REQUIREMENTS.md` set REFAC-13, REFAC-17 and REFAC-18 checkboxes and traceability rows to `Complete (05-19)` or `PARTIAL` with the unmet clause, and re-check REFAC-11 (05-08) and REFAC-12 (05-11) against their final evidence; in `.planning/ROADMAP.md`, under Phase 5, add a "Requirement states (recorded by plan 05-19)" block in the Phase 4 form, stating each requirement's state and the criteria verdicts, pointing to the completion note — use a scoped Edit, never a whole-file rewrite. Update `evidence/deferred-items.md` with every item's final state. Commit `docs(05-19): phase 5 completion note, requirement states`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && grep -c 'missing=0' .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/PHASE-5-COMPLETION.md</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/PHASE-5-COMPLETION.md` contains the five criteria verbatim, at least five verdict tables, and each of `REFAC-11`, `REFAC-12`, `REFAC-13`, `REFAC-17`, `REFAC-18` followed by `Complete` or `PARTIAL`
    - It contains `BEFORE any push to main`, `UPSTASH_REDIS_REST_URL`, `KV_REST_API_URL`, `DI-23`, `F-006`, `F-007`, `F-008`, `F-016`, and either a CI run id or `CI run UNOBSERVED`
    - Its citation-count line reports `missing=0`
    - The Validated table has as many rows as `grep -c '^- ✓' .planning/PROJECT.md`
    - `.planning/REQUIREMENTS.md` rows for REFAC-13, REFAC-17 and REFAC-18 each read `Complete (05-19)` or `PARTIAL` with a clause named; `.planning/ROADMAP.md` Phase 5 contains `Requirement states`
    - `git status --porcelain -- src/ supabase/ e2e/` prints nothing after the commit
  </acceptance_criteria>
  <done>Phase 5 closes with every criterion and requirement answered against its own words, every deliberate change and rule-resolved decision visible, and the owner told exactly what must happen before the next deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | the supabase-js minor |
| repository → Vercel production | main deploys automatically; the note is the owner's pre-deploy checklist |
| register → Phase 8 certification | closures claimed here are audited against their criteria later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19-SC | Tampering | npm install of @supabase/supabase-js 2.116.0 | mitigate | verified OK in 04-RESEARCH's audit; exact version; throwaway-worktree proof first; lockfile diff limited to @supabase packages; audit gate 0 high |
| T-05-19-01 | Denial of Service | a push to main deploying a build that refuses to boot without Upstash | mitigate | owner action (a) first in the note, in the words "BEFORE any push to main" (acceptance) |
| T-05-19-02 | Repudiation | a closure or requirement claimed without evidence | mitigate | resolution per finding; clause-by-clause tables; citation count missing=0 |
| T-05-19-03 | Elevation of Privilege | a local-only RLS fix read as live in production | mitigate | F-006, F-007, F-008, F-016 kept Open at "08" with the words "production closes with the DI-23 repair" |
| T-05-19-04 | Tampering | ROADMAP.md clobbered by a whole-file write | mitigate | scoped Edit only (prohibition in the action) |
</threat_model>

<verification>
- Worktree proof and bump recorded; tsc, lint, jest, audit, build green
- `npx playwright test` green from a clean reset; pgTAP 9 files PASS both runs; ratchet 2/2
- `validate.mjs --check findings` exits 0; the completion note reports missing=0
</verification>

<success_criteria>
Phase 5 ends with the SDK current, a green floor from a clean database, a register and requirement record that say exactly what closed and where, and a completion note an owner can act on before the next deploy.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `@supabase/supabase-js` 2.116.0; `evidence/di-25-bump.txt`, `evidence/floor.phase-after.txt`, `evidence/playwright.phase-after.txt`, `evidence/slice-5-close.md`, `evidence/PHASE-5-COMPLETION.md`; register, REQUIREMENTS.md and ROADMAP requirement-state updates.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-19-SUMMARY.md` when done.
</output>
