---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 17
type: execute
wave: 16
depends_on: ["05-12", "05-16"]
files_modified:
  - src/server/ratelimit/types.ts
  - src/server/ratelimit/policy.ts
  - src/server/ratelimit/memoryStore.ts
  - src/server/ratelimit/index.ts
  - src/server/ratelimit/policy.test.ts
  - src/server/ratelimit/memoryStore.test.ts
  - src/middlewareRateLimit.ts
  - src/server/csrf.ts
  - src/server/__tests__/csrf.test.ts
  - src/proxy.ts
  - e2e/specs/csrf-origin.spec.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/ratelimit.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf-assessment.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-13, REFAC-17, REFAC-18]

must_haves:
  truths:
    - "Rate limiting sits behind a RateLimitStore interface, so the store can be swapped for a distributed one without touching the policy or the proxy"
    - "/api/admin/* is rate limited: the 601st admin GET and the 121st admin mutation from one IP to one path within a minute get 429 with the existing body and headers"
    - "Public budgets, the 429 body and every rate-limit header are unchanged; the synchronous applyApiRateLimit and its PRESERVE suite are unchanged in behaviour"
    - "A non-GET /api/* request carrying Sec-Fetch-Site cross-site, or an Origin that does not match the host, gets 403 {\"error\":\"Cross-site request blocked\"}; same-origin browser requests and header-less machine callers pass"
    - "The CSRF exposure is assessed in writing against the Supabase cookie attributes as installed, with every residual named and owned"
  artifacts:
    - path: "src/server/ratelimit/index.ts"
      provides: "applyRateLimit(req, store) and getRateLimitStore()"
      exports: ["applyRateLimit", "getRateLimitStore"]
    - path: "src/server/ratelimit/policy.ts"
      provides: "one place budgets live: public and admin, key, client IP, the 429 response"
      exports: ["rateLimitPolicy", "clientIp", "tooManyRequests", "ADMIN_BUDGETS"]
    - path: "src/server/csrf.ts"
      provides: "isCrossSiteMutation(req) and crossSiteBlocked()"
      exports: ["isCrossSiteMutation", "crossSiteBlocked"]
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf-assessment.md"
      provides: "REFAC-17's written assessment"
      min_lines: 60
  key_links:
    - from: "src/proxy.ts"
      to: "src/server/ratelimit/index.ts"
      via: "await applyRateLimit(request, getRateLimitStore()) as the first statement"
      pattern: "applyRateLimit\\(request, getRateLimitStore\\(\\)\\)"
    - from: "src/proxy.ts"
      to: "src/server/csrf.ts"
      via: "the origin check after the rate limit and before any env or session work"
      pattern: "isCrossSiteMutation\\(request\\)"
    - from: "src/middlewareRateLimit.ts"
      to: "src/server/ratelimit/policy.ts"
      via: "the synchronous wrapper shares the policy, the memory bucket map and the 429 builder, so there is one copy of the budgets"
      pattern: "rateLimitPolicy"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "src/middlewareRateLimit.test.ts, src/proxy-characterization.test.ts and src/proxy.test.ts pass unedited"
    - "No package is installed in this plan (05-18 installs Upstash behind a checkpoint)"
    - "The CSRF check never blocks a request that carries neither Origin nor Sec-Fetch-Site (machine callers, Phase 6's cron routes)"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 5, refactor E: put the limiter behind a store interface with the admin budgets (REFAC-13's rate-limit clause, the first half of REFAC-18, DEC-50), and add the CSRF origin check with its written assessment (REFAC-17, F-090, DEC-52).

Purpose: ROADMAP criterion 4 — "rate limiting runs from a distributed store … and now covers /api/admin/*; CSRF exposure is assessed … and protection added on state-changing routes wherever exposure remains". This plan does everything that needs no new package; 05-18 plugs the Upstash store into the interface behind a package-legitimacy checkpoint.

Output: `src/server/ratelimit/` (types, policy, memory store, entry point) with tests, the proxy rewired, `src/server/csrf.ts` with tests, the CSRF e2e flipped, and `evidence/csrf-assessment.md`.
</objective>

<slice_outcome>
Green when the ratelimit and csrf suites pass, `src/middlewareRateLimit.test.ts` and the proxy PRESERVE suites pass unedited, an equivalence test shows the async path and the synchronous wrapper decide identically on public paths, the csrf-origin spec's F-090 tests are FIXED, and the assessment names every residual.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RateLimitStore, one policy with admin budgets, the memory store, and the proxy on the async entry point</name>
  <files>src/server/ratelimit/types.ts, src/server/ratelimit/policy.ts, src/server/ratelimit/memoryStore.ts, src/server/ratelimit/index.ts, src/server/ratelimit/policy.test.ts, src/server/ratelimit/memoryStore.test.ts, src/middlewareRateLimit.ts, src/proxy.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/ratelimit.txt</files>
  <read_first>
    - src/middlewareRateLimit.ts (132 lines) — the globalThis store (:9-18), LIMITS, ADMIN_PREFIX, HIGH_FREQUENCY_POST_PREFIXES, getIp, the fixed-window-from-first-hit logic, the 429 body and headers (:110-127)
    - src/middlewareRateLimit.test.ts (whole file) — the PRESERVE contract, called synchronously
    - src/proxy.ts lines 1-10 after 05-05; src/proxy-characterization.test.ts case 8
    - 05-RESEARCH.md § Pattern 4, Pitfall 5, Pitfall 9; evidence/phase-05-decisions.md § DEC-50
  </read_first>
  <behavior>
    - rateLimitPolicy returns null for non-/api paths; for public paths GET limit 300, non-GET 30, high-frequency POST prefixes 300; for /api/admin/* GET 600 and non-GET 120; the key is `${method}:${pathname}:${ip}`; the window is 60000 ms
    - clientIp prefers x-real-ip, then the first x-forwarded-for hop, then "unknown"
    - MemoryRateLimitStore allows `limit` requests in a window and denies the next with resetAtMs equal to the first hit plus the window; a new window starts after resetAtMs
    - applyRateLimit with the memory store returns 429 with body { error: "Too Many Requests", message: "Rate limit exceeded. Try again in N seconds." } and headers Retry-After, X-RateLimit-Limit, X-RateLimit-Remaining "0", X-RateLimit-Reset — identical to today's
    - the 601st admin GET and the 121st admin POST from one IP to one path return 429 with X-RateLimit-Limit "600" and "120"
    - for a table of public requests, applyRateLimit(req, memory) and applyApiRateLimit(req) make the same decision and emit the same headers
  </behavior>
  <action>
Tests first. `types.ts`: `RateDecision = { allowed: boolean; limit: number; resetAtMs: number }` and `interface RateLimitStore { consume(key: string, limit: number, windowMs: number): Promise<RateDecision> }`. `policy.ts`: `WINDOW_MS = 60_000`, the public budgets moved here unchanged, `ADMIN_BUDGETS = { GET: 600, mutation: 120 } as const` with DEC-50's rationale in a comment, `clientIp(req)`, `rateLimitPolicy(req): { key: string; limit: number; windowMs: number } | null`, and `tooManyRequests(limit: number, resetAtMs: number, nowMs: number): NextResponse` producing today's exact body and headers. `memoryStore.ts`: `consumeSync(key, limit, windowMs, nowMs)` over the same `globalThis.__uni_verse_mw_rate_limit__` map with the same prune-then-fixed-window semantics, and `class MemoryRateLimitStore implements RateLimitStore` whose `consume` returns `Promise.resolve(consumeSync(…))`. `index.ts`: `applyRateLimit(req, store): Promise<NextResponse | null>` (policy → consume → 429 or null) and `getRateLimitStore(): RateLimitStore` returning one module-level MemoryRateLimitStore (05-18 adds the Upstash selection).

Rewrite `src/middlewareRateLimit.ts` so `applyApiRateLimit(req)` stays synchronous, keeps returning null for non-/api and for `/api/admin/*` (the admin budget is a separate policy used by the async path — the PRESERVE "exempts /api/admin/* from the public budget" case stays true), and otherwise uses `rateLimitPolicy`, `consumeSync` and `tooManyRequests`, so the budgets exist once. Keep its export name and signature; keep its comment header. In `src/proxy.ts` the first statements become `const rateLimitResponse = await applyRateLimit(request, getRateLimitStore()); if (rateLimitResponse) return rateLimitResponse;`.

`policy.test.ts` holds the budget, key, IP and equivalence tests (distinct IPs per test, as the PRESERVE file's docblock requires); `memoryStore.test.ts` holds the window tests with injected `nowMs`. Run `npx jest --ci src/server/ratelimit src/middlewareRateLimit.test.ts src/proxy-characterization.test.ts src/proxy-defect.test.ts src/proxy.test.ts`, the full suite, lint and tsc; record in `evidence/ratelimit.txt`, including a count of the /api GET calls one `/moderation` page load issues (from the moderation pages' fetch calls) as the input to the admin budget's rationale. Commit `feat(05-17): rate limiting behind a store interface; /api/admin/* budgets 600 GET and 120 mutation per minute (REFAC-13, REFAC-18)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (admin routes can now return 429).
  </action>
  <verify>
    <automated>npx jest --ci src/server/ratelimit src/middlewareRateLimit.test.ts src/proxy-characterization.test.ts src/proxy.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `git diff <05-16 head> -- src/middlewareRateLimit.test.ts src/proxy-characterization.test.ts src/proxy.test.ts` prints nothing and all three pass
    - `src/server/ratelimit/policy.ts` contains `ADMIN_BUDGETS` with `600` and `120`; `src/server/ratelimit/index.ts` exports `applyRateLimit` and `getRateLimitStore`
    - `policy.test.ts` contains a 601st-admin-GET case asserting 429 and `X-RateLimit-Limit` "600", a 121st-admin-POST case asserting "120", and an equivalence test over at least five public requests
    - `src/proxy.ts` contains `applyRateLimit(request, getRateLimitStore())`
    - The public budget literals 300 and 30 appear in `src/server/ratelimit/policy.ts` and not in `src/middlewareRateLimit.ts` (one copy)
  </acceptance_criteria>
  <done>Admin routes are rate limited, the budgets live in one place, and the store is an interface a distributed implementation can fill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The CSRF origin check in the proxy, its e2e flip, and the written assessment</name>
  <files>src/server/csrf.ts, src/server/__tests__/csrf.test.ts, src/proxy.ts, e2e/specs/csrf-origin.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf-assessment.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - node_modules/@supabase/ssr/dist/main/utils/constants.js lines 1-15 — the cookie defaults as installed (sameSite, httpOnly, secure, path, maxAge)
    - node_modules/next/dist/docs/01-app/02-guides/data-security.md near line 550 — Next's own Server Action origin check (Origin vs Host / X-Forwarded-Host)
    - 05-RESEARCH.md § Pattern 5, § C14, § Security Domain, § Assumptions A3; evidence/phase-05-decisions.md § DEC-52
    - e2e/specs/csrf-origin.spec.ts (the F-090 pins); src/app/invites/[token]/page.tsx 138-147; src/app/api/recommendations/route.ts near 230
    - next.config.js (HSTS header)
  </read_first>
  <behavior>
    - GET, HEAD and OPTIONS are never cross-site mutations
    - non-/api paths are never checked
    - Sec-Fetch-Site "cross-site" on a POST to /api/* → true
    - Origin present and its host differs from x-forwarded-host (else host, else nextUrl.host) → true; the literal "null" Origin and an unparseable Origin → true
    - same-origin Origin (including with x-forwarded-host set to the same host) → false; Sec-Fetch-Site same-origin or none with a matching or absent Origin → false
    - neither header present → false
    - crossSiteBlocked() → 403 { error: "Cross-site request blocked" }
  </behavior>
  <action>
Tests first (`src/server/__tests__/csrf.test.ts`, building `new NextRequest(url, { method, headers })`), then `src/server/csrf.ts` exporting `isCrossSiteMutation(req: NextRequest): boolean` per the behaviour list (use `new URL(origin).host` inside try/catch; never a regex) and `crossSiteBlocked(): NextResponse`, with a docblock citing DEC-52, F-090 and Next's Server Action precedent. In `src/proxy.ts`, immediately after the rate-limit statements and before the env reads and client creation: `if (isCrossSiteMutation(request)) return crossSiteBlocked();`. Proxy suites pass unedited (they send neither header).

Flip `e2e/specs/csrf-origin.spec.ts`: the two `DEFECT F-090` tests become `FIXED F-090` asserting 403 `{ error: "Cross-site request blocked" }` and that no saved row was created; the same-origin and header-less PRESERVE tests stay green and still restore their saves. Run it with `save-and-rsvp.spec.ts` (a real browser POST, which sends a same-origin Origin) after a clean reset and seed; ledger rows; record in `evidence/csrf.txt`.

Write `evidence/csrf-assessment.md` (metadata line): 1) the installed cookie attributes quoted from the constants file (SameSite=Lax, HttpOnly false, no Secure, path /, max-age 400 days) and what Lax does and does not send cross-site; 2) route handlers have no built-in origin check (Server Actions do; `command grep -rn '"use server"' src` shows none), so before 05-17 Lax was the only control (F-090); 3) the control added, its placement, what it blocks and what it deliberately passes (header-less machine callers, Phase 6's cron routes); 4) residuals, each with severity and owner — the /invites/[token] GET auto-accept (Low, token is the secret and RLS pins the invitee email; DI-41), the GET /api/recommendations experiment-assignment write (Low, benign), the needs_onboarding cookie (none: now a hint only, DEC-36), legacy browsers without Sec-Fetch-Site (covered by Origin on POST), the missing Secure attribute (mitigated by HSTS in next.config.js; changing cookie options is the deferred ssr major, DI-43), research A3 on Chrome's Lax-POST window (the origin check covers it regardless); 5) the tests that prove each arm. Commit `feat(05-17): reject cross-site state-changing API requests in the proxy; CSRF assessment (REFAC-17, F-090)` with an `INTENTIONAL BEHAVIOUR CHANGE` body.
  </action>
  <verify>
    <automated>npx jest --ci src/server/__tests__/csrf.test.ts src/proxy-characterization.test.ts src/proxy-defect.test.ts && test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/csrf-assessment.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/csrf.ts` exports `isCrossSiteMutation` and `crossSiteBlocked` and contains `Cross-site request blocked`
    - `csrf.test.ts` has at least 10 cases covering every behaviour line
    - `src/proxy.ts` calls `isCrossSiteMutation(request)` after `applyRateLimit(` and before `supabaseUrl()` (checked by index order in a node one-liner recorded in the evidence)
    - The csrf-origin spec's two F-090 tests are titled `FIXED F-090` and passed with `save-and-rsvp.spec.ts` after a clean reset (recorded exit=0)
    - `evidence/csrf-assessment.md` contains `SameSite`, `Lax`, `DI-41`, `DI-43`, `/invites/[token]` and `Sec-Fetch-Site`
  </acceptance_criteria>
  <done>Cross-site state changes are refused at the edge, legitimate browsers and machines pass, and the exposure is assessed in writing with every residual owned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any origin → /api/* mutations | the CSRF boundary |
| client IP → limiter | IP-keyed budgets |
| admin credential → /api/admin/* | now budgeted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17-01 | Spoofing | cross-site request forgery on state-changing routes (F-090) | mitigate | Sec-Fetch-Site and Origin check in the proxy; unit cases and the FIXED e2e tests |
| T-05-17-02 | Denial of Service | the origin check blocking legitimate same-origin browsers or machine callers | mitigate | header-less and same-origin cases pass in unit and e2e; save-and-rsvp browser POST green |
| T-05-17-03 | Denial of Service | a limiter that starves admin work | mitigate | admin budgets 2x/4x public with a measured /moderation fan-out in evidence; per-path keys |
| T-05-17-04 | Denial of Service | brute force or scraping of admin endpoints | mitigate | 601st GET and 121st mutation 429 tests |
| T-05-17-05 | Spoofing | rate-limit key spoofed through X-Forwarded-For | mitigate | x-real-ip preferred (Vercel sets it); XFF first hop fallback; research A5 |
| T-05-17-06 | Tampering | budget drift between the sync wrapper and the async path | mitigate | budgets exist once in policy.ts; equivalence test |
| T-05-17-SC | Tampering | package installs | accept | none in this plan |
</threat_model>

<verification>
- `npx jest --ci` exits 0; `npx tsc --noEmit`, `npm run lint` clean
- csrf-origin and save-and-rsvp specs green after a clean reset
- `evidence/csrf-assessment.md` present
</verification>

<success_criteria>
Admin routes are rate limited through a swappable store, cross-site mutations are refused, and the CSRF exposure is assessed in writing.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`, `consumeSync`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`), `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`), their tests, the proxy wiring, the CSRF e2e flip, `evidence/ratelimit.txt`, `evidence/csrf.txt`, `evidence/csrf-assessment.md`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-17-SUMMARY.md` when done.
</output>
