---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/specs/save-and-rsvp.spec.ts
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/audit/quality/phase-05-slice-defects.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/di-38-fix.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/findings-registration.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-11, REFAC-12, REFAC-13, REFAC-17, REFAC-18]

must_haves:
  truths:
    - "Every defect Phase 5 pins or fixes has a registered F-nnn before any DEFECT suite cites it: F-086 (DI-36), F-087 (owner club writes, C11), F-088 (auth ring fails open), F-089 (onboarding cookie bypass), F-090 (no origin check), F-091 (admin role change broken)"
    - "Every finding that carried closes_in_phase \"05\" is either kept in Phase 5 by name or re-pointed to 06, 07 or 08 per the CONTEXT C16 amendment, so the completion note is measured against a register that says what Phase 5 actually owns"
    - "The before-floor is measured on a named commit that already carries the DI-38 fix, so no later slice-close run can be red for the known harness race"
    - "Every rule-resolved choice the later plans execute against is a numbered decision (DEC-33 onward) with its evidence and the alternative rejected, written before any plan executes it"
  artifacts:
    - path: ".planning/audit/quality/phase-05-slice-defects.md"
      provides: "One anchored section per new finding F-086..F-091, reproductions re-run or re-read on the base commit"
      min_lines: 150
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt"
      provides: "The before-floor: jest, lint, tsc, audit, ratchet, filenames, tag gate, validate, pgTAP unseeded and seeded, Playwright, and the six Phase 5 censuses"
      min_lines: 80
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md"
      provides: "DEC-33 through DEC-57"
      min_lines: 250
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md"
      provides: "Phase 5 deferred register: inherited DI dispositions, research assumptions A1-A6, new DI-41..DI-43"
      min_lines: 60
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md"
      provides: "Ledger header, the five-step move protocol, and an empty table later plans append to"
      min_lines: 15
  key_links:
    - from: ".planning/audit/findings.json"
      to: ".planning/audit/quality/phase-05-slice-defects.md"
      via: "each new record's evidence is a path relative to .planning/audit/ with an anchor that validate.mjs --check findings resolves"
      pattern: "quality/phase-05-slice-defects.md#f-0(8[6-9]|9[01])"
    - from: "e2e/specs/save-and-rsvp.spec.ts"
      to: "/api/users/saved-events"
      via: "the saved state is re-read after page.reload() through page.request.get instead of a response interception raced against the reload"
      pattern: "page\\.request\\.get\\(\"/api/users/saved-events\"\\)"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "findings.json is edited by per-record insertion and single-field edits only, never parsed and re-serialized; FOUNDATION_AUDIT.md only through gen-foundation-audit.mjs"
    - "No file under src/, supabase/ or the repository configuration changes in this plan; the only e2e change is the DI-38 fix"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door."

(The ROADMAP goal is outcome-shaped, not a user story. The discrepancy is surfaced to the orchestrator; no story is invented. In this program a vertical slice is one research slice — auth, clubs, admin — landing characterize, refactor, verify end to end before the next starts.)

<objective>
Wave 1, alone: register, measure, decide. Give every later Phase 5 plan a finding id to cite, a floor to compare against, and a written decision to execute.

Purpose: CONTEXT Area 4 requires DEFECT suites to cite a registered F-nnn and the tag gate (`scripts/check-characterization-tags.mjs`) rejects any id the register does not hold, so registration strictly precedes every characterization plan. Research C16 measured about fifty findings carrying `closes_in_phase: "05"` that CONTEXT defers, so the register is re-pointed now rather than discovered at close. DI-38 (the `save-and-rsvp.spec.ts:53` race) is fixed first so the floor is measured on a harness that does not flake in the auth slice.

Output: the DI-38 fix, the before-floor, six new findings plus the re-pointing, the Phase 5 decision record DEC-33..DEC-57, the Phase 5 deferred register, and the defect-ledger skeleton.
</objective>

<slice_outcome>
Green when `node .planning/audit/tools/validate.mjs --check findings` passes at 91 findings, `node scripts/check-characterization-tags.mjs --all` still exits 0, `evidence/floor.before.txt` line 1 is `base=<sha>` of the DI-38 commit with every block exit-coded, and `evidence/phase-05-decisions.md` holds DEC-33 through DEC-57.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-CONTEXT.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/audit/findings.schema.json
@.planning/audit/SEVERITY_SLA.md
</context>

<phase_locked_constraints>
1. Behaviour preservation — wire bytes of every Validated workflow stay identical except where a registered F-nnn fix is a commit's stated purpose; a deliberate change carries `INTENTIONAL BEHAVIOUR CHANGE` in its commit body.
2. Characterize first — PRESERVE/DEFECT tags are file-level (C15); a PRESERVE file is never edited to make it pass; a pin that must change moves into a `-defect.test.ts` file in the same commit as the fix, with a row in `evidence/defect-ledger.md`.
3. Production is never touched — no `supabase db push`, no `--linked`, no dashboard or Management API write, no Vercel change, no `git push`; `.env.local` is never read or modified.
4. Migrations are new fix-forward files only; the four applied migrations are never edited; `src/lib/supabase/types.ts` is regenerated only with `--local` on Supabase CLI 2.115.0.
5. Every code change is correct against the migrated local schema AND the unmigrated production schema (migrations reach production only through DI-23 in Phase 8).
6. Jest is the only unit runner (node project: `src/**/*.test.ts` minus hooks; jsdom: `*.test.tsx` and `src/hooks/**`); test files are type-checked; handlers in tests are typed as the handler's own type and never widened.
7. Stage by explicit path only; `.agents/`, `docs/product-master-plan.md` and `skills-lock.json` stay untracked.
8. One executor owns Docker and port 3000 at a time; Playwright's webServer never reuses a server; macOS has no `timeout` binary.
9. Evidence is verbatim: invoking command above, raw output, `exit=<code>` below; notes carry `**Plan:** · **Phase:** · **Recorded:**`.
10. findings.json is edited surgically; FOUNDATION_AUDIT.md is regenerated; endpoints.json changes only through `.planning/audit/tools/classify-inventory.mjs`.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Fix the DI-38 harness race, then measure the Phase 5 before-floor on that commit</name>
  <files>e2e/specs/save-and-rsvp.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/di-38-fix.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt</files>
  <read_first>
    - e2e/specs/save-and-rsvp.spec.ts lines 1-100 — the test "a student saves an event and it appears on their profile" and its Promise.all at lines 76-82 that races a saved-events response against page.reload()
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md § DI-38 — the diagnosis, the measured 5-of-10 failure rate, and the fix shape
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.phase-after.txt lines 1-60 — the block layout and summary-table shape to reproduce
    - .github/workflows/ci.yml — the e2e job's step order (reset, seed, pgTAP, playwright) this task reproduces locally
    - playwright.config.ts lines 90-170 — one worker, production build, never reuses a server
    - 05-RESEARCH.md § Validation Architecture "Measured floors (2026-09-23)" — the numbers the summary table compares against
  </read_first>
  <action>
**DI-38 first.** Record `git rev-parse HEAD` as `pre=<sha>` at the top of `evidence/di-38-fix.txt`. In `e2e/specs/save-and-rsvp.spec.ts`, in the test "a student saves an event and it appears on their profile" only, replace the `Promise.all([page.waitForResponse(… saved-events …), page.reload()])` pair with a plain `await page.reload()` followed by `const savedList = await page.request.get("/api/users/saved-events")`. Keep the three assertions that follow byte-identical in meaning: status 200, `savedEventIds` contains `IDS.approvedEvent`, and the Unsave control visible after the reload. Change no other test and no other line. Prove the fix: from `supabase db reset --local` and `npx tsx scripts/seed/load.ts`, run `npx playwright test e2e/specs/save-and-rsvp.spec.ts` once (the setup project writes the storage states), then `npx playwright test --project=chromium --no-deps e2e/specs/save-and-rsvp.spec.ts -g "a student saves an event" --repeat-each=10` (the `--no-deps` form avoids 100 persona sign-ins against the local 30-per-5-minutes limit). Append both outputs with exit codes to `evidence/di-38-fix.txt`; the repeat run must show 10 passed, 0 failed (DI-38 measured 5 of 10 failing). Commit `test(05-01): re-read saved state after reload through page.request (DI-38)` staging only the spec and the evidence file.

**Before-floor, on the DI-38 commit.** Line 1 of `evidence/floor.before.txt` is `base=<git rev-parse HEAD>` after that commit. Append one block per command, each as the command line, the verbatim output tail carrying the numbers, and `exit=<code>`, in this order: `npx jest --ci`; `npm run lint`; `npx tsc --noEmit`; `npm audit --audit-level=high --omit=dev`; `node scripts/check-elevated-ratchet.mjs`; `node scripts/check-migration-filenames.mjs`; `node scripts/check-characterization-tags.mjs --all`; `node .planning/audit/tools/validate.mjs --quick` (name its by-design FAILs); `supabase db reset --local`; `supabase test db --local` labelled UNSEEDED; `npx tsx scripts/seed/load.ts`; `supabase test db --local` labelled SEEDED; a second reset plus seed, then `npx playwright test` (port 3000 free first: `lsof -i :3000` exit 1). Then six censuses, each with its `command grep` line and count: env non-null assertions (`command grep -rnoE 'process\.env\.[A-Z_]+!' src`), session-accessor calls (`command grep -rn 'getSession(' src`), the admin-verify helper's callsites (`command grep -rn 'verifyAdmin()' src | command grep -v '\.test\.'`), the legacy ban helper's callsites (`command grep -rn 'checkBanStatus()' src`), club membership reads (`command grep -rn 'from("club_members")' src/app src/lib`), and the client-cast census (`command grep -rn '(supabase as any)' src`). Above the blocks write a summary table: command, measured now, research value (Jest 744 passed / 0 skipped / 50 suites; pgTAP Files=6 Tests=86; Playwright 40/0; ratchet committed=25 live=25; tag gate ok 18), delta. The measured value is the floor for every later plan; a delta is stated, never rounded away. Leave the stack reset and seeded and port 3000 free. Commit `docs(05-01): phase 5 before-floor`.
  </action>
  <verify>
    <automated>head -1 .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt | grep -E '^base=[0-9a-f]{40}$' && grep -c '^exit=' .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/specs/save-and-rsvp.spec.ts` contains `page.request.get("/api/users/saved-events")` and `git diff <pre> -- e2e/specs/save-and-rsvp.spec.ts` touches only the saves-an-event test
    - `evidence/di-38-fix.txt` records the `--repeat-each=10` run with `10 passed` and exit=0
    - Line 1 of `evidence/floor.before.txt` matches `^base=[0-9a-f]{40}$` and equals the DI-38 commit
    - `grep -c '^exit=' evidence/floor.before.txt` is at least 14, and the file contains the labels `UNSEEDED` and `SEEDED`
    - The summary table's Playwright row reports a number read from this run with 0 failed
    - The six census blocks are present, each with a count
    - `git status --porcelain -- src/ supabase/ package.json package-lock.json` prints nothing
  </acceptance_criteria>
  <done>The race that flaked the harness twice in Phase 4 is gone by measurement, and every later plan compares against a floor measured on a named commit, including the counts Phase 5 exists to drive to zero.</done>
</task>

<task type="auto">
  <name>Task 2: Register F-086..F-091, re-point every deferred "05" finding, regenerate the audit report</name>
  <files>.planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md, .planning/audit/quality/phase-05-slice-defects.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/findings-registration.txt</files>
  <read_first>
    Context budget: [FULL] = read the whole file; [SECTION] = locate with grep, then read only that range.
    - [FULL] .planning/audit/findings.schema.json — required fields, the category enum (schema-drift, authn, authz, cache-exposure, injection, validation, performance, observability, dead-code, dependency, config), the status enum (Open, Fixed, Risk-accepted), the optional resolution field
    - [FULL] .planning/audit/SEVERITY_SLA.md — exposure-adjusted severity; the rationale must argue from it
    - [SECTION] .planning/audit/findings.json — the F-079 and F-085 records as house style; the last record (F-085) after which new records are inserted; the closes_in_phase line of every record listed in the re-pointing map below (`grep -n '"closes_in_phase"'`)
    - [SECTION] .planning/audit/quality/phase-04-slice-defects.md lines 1-60 — the metadata line, the method notes, and how a heading becomes the anchor the evidence field cites (`## F-079 — rsvp counts load every row` → `#f-079--rsvp-counts-load-every-row`)
    - [SECTION] .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md § DI-36 — F-086's reproduction
    - [SECTION] 05-RESEARCH.md § Corrections C2, C7, C10, C11, C12, C14, C16 and § Inventories A, B — the reproductions for F-087..F-091 and the re-pointing map
    - [SECTION] 04-01-PLAN.md Task 2 — the surgical insertion method and the numstat check
    - [SECTION] .planning/audit/tools/validate.mjs lines 760-805 — what `--check findings` asserts
  </read_first>
  <action>
**Evidence document.** Create `.planning/audit/quality/phase-05-slice-defects.md` with the metadata line `**Plan:** 05-01 · **Phase:** 05-slices-3-5-auth-club-authorization-admin-containment · **Recorded:** <date>`, a method note (local stack only, `docker exec supabase_db_Event-Radar psql -U postgres` inside BEGIN … ROLLBACK, no key printed, production not read), and six sections whose headings produce exactly these anchors: `## F-086 — pending edits never reach their creator` (#f-086--pending-edits-never-reach-their-creator), `## F-087 — owner club writes are denied by rls` (#f-087--owner-club-writes-are-denied-by-rls), `## F-088 — the auth ring fails open on its own errors` (#f-088--the-auth-ring-fails-open-on-its-own-errors), `## F-089 — the onboarding guard is a deletable cookie` (#f-089--the-onboarding-guard-is-a-deletable-cookie), `## F-090 — no origin check on state-changing api routes` (#f-090--no-origin-check-on-state-changing-api-routes), `## F-091 — admin role changes cannot land and strip admin` (#f-091--admin-role-changes-cannot-land-and-strip-admin). Each section carries the reproduction re-run on the Task 1 base commit (code facts re-read with file:line, RLS facts re-probed in a rolled-back transaction) and the exact pointers.

**Six records**, inserted after F-085 by editing the array tail, every schema field present, `status` "Open", `risk_acceptance` null, `closes_in_phase` "05":
- F-086, Low, validation — GET /api/events/[id] never returns pending_edits, so the creator's pending-edit notice and edit prefill cannot render and the route's stripping gate is dead. Paths: `src/app/api/events/[id]/route.ts` (the GET stripping branch), `src/lib/tagMapping.ts` (transformEventFromDB), `src/components/events/EventDetailView.tsx` 207-213. Fix: attach pending_edits after the shared transform for the creator and admins only. Validation: `src/__tests__/api/events/pending-edits-defect.test.ts` moves and `events-detail-characterization.test.ts` passes unedited.
- F-087, Medium, authz — club owners cannot edit or delete their club or change member roles: clubs has no owner UPDATE policy and club_members UPDATE is admin-only, so PATCH returns 500, DELETE returns success without deleting and writes an audit row, and the role change returns 500. Paths: `src/app/api/clubs/[id]/route.ts` (PATCH update, DELETE soft-delete), `src/app/api/clubs/[id]/members/role/route.ts` (the update). Fix: owner write through the elevated door after `requireClubRole(…, ["owner"])` with a column whitelist. Validation: `club-owner-writes-defect.test.ts` moves, `e2e/specs/club-authorization.spec.ts` "DEFECT F-087" flips to 200, pgTAP 060 asserts a direct owner UPDATE on clubs still affects 0 rows. related F-016, F-022.
- F-088, Medium, authz — the authorization ring fails open on its own errors: the proxy's outer catch passes the request through, a failed ban read counts as not banned, the legacy ban helper admits a caller with no profile row, and most state-changing handler arms (including the DELETE arms of save and rsvp) carry no ban check. Paths: `src/proxy.ts` 92-111 and 140-144, `src/lib/ban.ts` 20-37. Rationale: the proxy blocks on the happy path, so exploitation needs a proxy fault or a missing row, but the handler ring Next.js documents as authoritative has no ban check on those arms. Validation: `src/proxy-defect.test.ts` and `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts` move; `e2e/specs/ban-and-onboarding-ring.spec.ts` green. related F-003, F-062, F-069.
- F-089, Low, authz — the onboarding guard is a deletable one-hour cookie enforced on page requests only, so an un-onboarded account can browse by deleting it and can call every write API directly. Paths: `src/proxy.ts` 123-136, `src/app/auth/callback/route.ts` (the cookie set), `src/app/api/onboarding/complete/route.ts`. Validation: the onboarding rows of the two DEFECT suites above move; a mid-onboarding direct POST returns 403 in e2e. related F-088.
- F-090, Low, authz — no state-changing API route checks Origin or Sec-Fetch-Site; forgery is prevented only by the SameSite=Lax default of the Supabase auth cookies, and two GET handlers change state (`src/app/invites/[token]/page.tsx` 138-147, `src/app/api/recommendations/route.ts` near 230). Validation: `src/server/__tests__/csrf.test.ts`, `e2e/specs/csrf-origin.spec.ts` flips, `evidence/csrf-assessment.md` exists.
- F-091, Medium, authz — PATCH /api/admin/users/[id] cannot change another user's roles (cookie client, no admin UPDATE policy on users → 500), strips "admin" from every submitted role array, and writes no audit row. Paths: `src/app/api/admin/users/[id]/route.ts`, `src/app/moderation/users/page.tsx` 53-57. Validation: `src/__tests__/api/admin/admin-users-patch-defect.test.ts` moves. related F-004, F-006.
Each record's evidence is `quality/phase-05-slice-defects.md#<anchor>`; each severity_rationale argues from SEVERITY_SLA.md in the finding's own terms.

**Re-pointing (C16), single-field edits of closes_in_phase only.** First list every record whose closes_in_phase is "05" or null with a node one-liner and put the list in the evidence file. Then: to "06" — F-002, F-017, F-026, F-029, F-037, F-038, F-039, F-040, F-041, F-042, F-054, F-055, F-058, F-059, F-074, F-075, F-076, F-078; to "07" — F-009, F-010, F-011, F-012, F-013, F-014, F-015, F-018, F-019, F-020, F-021, F-022, F-023, F-069; to "08" — F-024, F-030, F-031, F-032, F-033, F-034, F-035, F-036, F-070. Keep "05": F-001, F-003, F-004, F-005, F-006, F-007, F-008, F-016, F-027, F-028, F-061, F-062, F-067, F-072, F-073, F-077 and the six new records. F-080/F-081 stay "04" (DI-39/DI-40). If any "05" record is in none of these sets, stop and record it in the evidence file rather than guessing. Where a finding keeps a Phase 5 clause after re-pointing (F-058's admin-429 clause, F-040's ADMIN_API_KEY/ADMIN_EMAILS readers), say so in the evidence file's table; resolution text is added when the clause closes.

**Regenerate and prove.** Run `node .planning/audit/tools/gen-foundation-audit.mjs` then `node .planning/audit/tools/validate.mjs --check findings`, and append both, the re-pointing table (id, old, new) and `git diff --numstat -- .planning/audit/findings.json` to `evidence/findings-registration.txt`. The deletion count equals the number of re-pointed lines plus at most one array-tail line. Run the tag gate again (unchanged count). Commit `docs(05-01): register F-086..F-091 and re-point deferred phase-5 findings`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/audit/tools/validate.mjs --check findings` exits 0 and reports 91 findings
    - `node -e 'const f=require("./.planning/audit/findings.json");process.exit(["F-086","F-087","F-088","F-089","F-090","F-091"].every(i=>f.some(r=>r.id===i&&r.status==="Open"&&r.closes_in_phase==="05"))?0:1)'` exits 0
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i).closes_in_phase;process.exit(g("F-002")==="06"&&g("F-009")==="07"&&g("F-030")==="08"&&g("F-070")==="08"&&g("F-008")==="05"&&g("F-027")==="05"?0:1)'` exits 0
    - `grep -c '^## F-0' .planning/audit/quality/phase-05-slice-defects.md` is 6
    - `evidence/findings-registration.txt` holds the before list of "05"/null records, the re-pointing table, the validate output and the numstat line
    - `git status --porcelain -- src/ e2e/ supabase/` prints nothing
  </acceptance_criteria>
  <done>Every defect Phase 5 will cite has a schema-valid record whose evidence anchor resolves, and the register says which phase owns every finding CONTEXT deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Write DEC-33..DEC-57, open the Phase 5 deferred register and the defect ledger</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md lines 1-60 — the header, numbering note and section shape (Decision, Evidence, Alternative rejected, Executed by)
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md lines 1-20 and 230-253 — the register shape and the "Next new item id: DI-41" line
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md lines 1-20 — the ledger header and five-step protocol
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md § "The two D- sequences" and the DI-21..DI-35 index
    - 05-CONTEXT.md in full including the amendments, and 05-RESEARCH.md § Corrections C1-C17, § Open Questions, § Assumptions Log — the source of every decision below
  </read_first>
  <action>
**The decision record.** Create `evidence/phase-05-decisions.md` with the metadata line, a header stating that no phase owner was present so every decision is a rule-resolved default the owner may override by appending a signed paragraph under its section before the executing plan runs (the plan then stops before changing a file), and one section per decision headed `## DEC-nn — <title>` with Decision, Evidence, Alternative rejected, Executed by. Confirm DEC-32 is the highest in use first (`command grep -rhoE 'DEC-[0-9]+' .planning | sort -V | tail -1`).
- DEC-33 Slice order and plan map: 3 → 4 → 5, 05-02..05-08 slice 3, 05-09..05-11 slice 4, 05-12..05-19 slice 5; the CSRF origin check lands in slice 5 beside the limiter (REFAC-17 with REFAC-18), not slice 3.
- DEC-34 Write-guard contract (C2): every state-changing non-admin authenticated arm under src/app/api runs requireActiveUser then requireOnboarded as its first statements after createRequestContext; exemptions from requireOnboarded are exactly POST /api/onboarding/complete and PATCH /api/users/[id] (self); POST /api/interactions and POST /api/feedback apply both guards only when ctx.user is present; GET arms gain neither; where an arm's anonymous response today is not 401 {"error":"Unauthorized"} it keeps its own anonymous branch first. INTENTIONAL BEHAVIOUR CHANGE: banned callers are refused on DELETE arms too (the Phase 4 ban asymmetry).
- DEC-35 No profile row (research Q3): seam and proxy /api/* answer 403 {"error":"Profile not found"}; a page request signs out and redirects to /?error=profile_sync_failed.
- DEC-36 Proxy fail-closed shape: one users read selecting banned_at, ban_expires_at, onboarding_completed; PGRST116 is the no-row branch, any other read error throws; the outer catch returns 500 JSON {"error":"Failed to process request"} under /api/ and a plain-text 500 "Internal Server Error" otherwise, logging with the kept [Middleware] prefix; onboarding reads the database, the needs_onboarding cookie remains a hint only.
- DEC-37 Validated config (C8, C9): lazy readers throwing MissingEnvError at first read; production means VERCEL_ENV === "production"; the service-key read stays in src/lib/supabase/service.ts as serviceRoleKey(); the door exports assertElevatedConfigured(); src/instrumentation.ts register() checks only when NEXT_RUNTIME is "nodejs" and NEXT_PHASE is not "phase-production-build".
- DEC-38 Callback (F-004, F-077, FO-05): the admin grant and its allowlist are deleted; profile sync failure signs out and redirects with error=profile_sync_failed; next is accepted only when it starts with "/", its second character is neither "/" nor a backslash, and it resolves to the request origin, else "/"; both service-role uses go through the door; the profile read keeps the column list "onboarding_completed, roles" so PRESERVE test 5 passes unedited; the PRESERVE suite's service mock gains an auth.admin.deleteUser member because the seam moved (zero assertion changes, a rule-resolved deviation from "PRESERVE files otherwise stay unedited", ledger row).
- DEC-39 F-028 split (C7) and F-027: 401 for events/following, events/friends-activity, events/friends-organizing, events/[id]/friends in slice 3; rsvp GET, clubs/[id]/events and notifications/count go to Phase 6 (REFAC-19); /api/auth-debug is deleted; F-028 is re-pointed to "06" at slice-3 close with a resolution naming the four fixed.
- DEC-40 Club guard (C1): CLUB_ROLES = ["owner","organizer"]; accepted sets are exactly research § C's per site; no admin bypass; transfer demotes by (club_id, user_id) instead of widening the guard's result.
- DEC-41 Owner club writes (C11, F-087, research Q1): through getElevatedClient after requireClubRole(["owner"]), with the handler's existing column whitelist so status, created_by and id are never writable; INTENTIONAL BEHAVIOUR CHANGE (owner edit, delete and role change start working); the RLS ring keeps denying direct owner writes.
- DEC-42 F-008 policy (C10, research Q2): TO authenticated, WITH CHECK is_admin() OR (created_by = auth.uid() AND (pending OR (approved AND club_id not null AND caller is a member AND the club is approved))); a pending event may name another club.
- DEC-43 F-016: proven by an e2e spec that mints its invitation through POST /api/clubs/[id]/invites, accepts through /invites/[token], and restores membership through DELETE /api/clubs/[id]/members; the finding stays Open with a local-proof resolution and closes_in_phase "08" (DI-23).
- DEC-44 Admin guard (F-061): requireRole(ctx, "admin") at every former admin-verify callsite; anonymous 401, non-admin 403 {"error":"Forbidden"} (the three routes answering 401 to non-admins move to 403); the admin and moderation layouts use getRequestContext and hasRole with their redirects unchanged.
- DEC-45 Admin role changes (C12, F-091): door write; roles validated against user_role (user, admin, club_organizer) else 400 {"error":"Invalid role","field":"roles"}; "user" is still always included; a roles change whose target id equals the caller → 403 {"error":"You cannot change your own roles"} (a name-only edit is unchanged); the admin role is no longer stripped; logAdminAction action "updated", targetType "user", metadata { roles }; INTENTIONAL BEHAVIOUR CHANGE ×2.
- DEC-46 Audit writer (F-072, F-073, C5): logAdminAction writes through the door without the absent email column, gains an optional requestId, inspects the insert result and logs a rejection with console.error; both moderation pages read audit rows then users id, name, email by id on the cookie client.
- DEC-47 F-006/F-007 migration (C3): column-scoped UPDATE grant on users to authenticated (name, avatar_url, banner_url, pronouns, year, faculty, visibility, interest_tags, inferred_tags, onboarding_completed, updated_at); UPDATE revoked from anon; INSERT on users revoked from anon and authenticated (research open point); update_saved_events_count() becomes SECURITY DEFINER SET search_path = ''; the users own-row UPDATE policy is rewritten with TO authenticated; both audit-log INSERT policies dropped and INSERT, UPDATE, DELETE revoked from anon and authenticated.
- DEC-48 F-005 (C6): the public profile page reads through the door with the select narrowed to id, name, avatar_url, banner_url, pronouns, year, faculty, visibility, interest_tags, created_at; one shared gate in generateMetadata and the page: anonymous viewer and visibility "private" → notFound(); INTENTIONAL BEHAVIOUR CHANGE.
- DEC-49 Service-role containment (research § F): operations an existing policy permits move to the cookie client; the rest go through the door, one REGISTRY row per operation family; the allowlist is regenerated once, in 05-15, to exactly the two cron routes.
- DEC-50 Rate limiter: RateLimitStore interface; applyApiRateLimit stays synchronous and byte-identical; public budgets unchanged; admin 600 GET and 120 mutation per IP per path per 60 s (rationale: a /moderation load fans out GETs and moderators batch-approve, so 2x/4x the public budgets clears interactive use while bounding scripted abuse to 2 writes/s per path per IP); Upstash fixedWindow 60 s, timeout 1000 ms, a timeout allows the request and is logged (availability over limiting — rate limiting is not an authz control); client IP prefers x-real-ip; UPSTASH_REDIS_REST_URL/TOKEN with KV_REST_API_URL/TOKEN fallback; required at boot only when VERCEL_ENV is production; the Playwright webServer blanks all four names; the contract test is skipped, never passed, without a store.
- DEC-51 Upstash pins 2.1.0 and 1.38.4 go through a BLOCKING package-legitimacy checkpoint in 05-18: the planner's supply-chain rule makes legitimacy checkpoints never auto-approvable, and CONTEXT's rule-resolved "not blocking" was taken with no owner present, which cannot stand in for human verification. Fallback if the human prefers a release older than 30 days: ratelimit 2.0.8 with a compatible redis pin.
- DEC-52 CSRF (F-090, C14): the proxy rejects a non-GET/HEAD/OPTIONS /api/* request when Sec-Fetch-Site is cross-site or an Origin header is present and its host differs from x-forwarded-host ?? host, with 403 {"error":"Cross-site request blocked"}; requests with neither header pass; the two state-changing GETs are Low residuals with no code change (DI-41).
- DEC-53 DI-36 → F-086: pending_edits attached in GET /api/events/[id] for the creator and admins after the unchanged shared transform; INTENTIONAL BEHAVIOUR CHANGE, no seeded row affected.
- DEC-54 No seed change in Phase 5: specs create and restore their own state through the product API or, for fixtures the API cannot create, through the local service-role client from e2e/env.ts under the config's assertSeedTargetAllowed; the seed's sha256 determinism proof and PERSONA_KEYS are untouched.
- DEC-55 Contract regeneration: endpoints.json expected_status changes only through classify-inventory.mjs with classification-rules.md updated in the same commit — slice 3 (R4 mid-onboarding on write-only rows, the four F-028 verdicts), slice 5 (the admin mechanism text, the calculate-popularity verdict); the api.auth-debug row stays because versions.txt route_ts_count=94 is the audit-time baseline.
- DEC-56 DI-25: @supabase/supabase-js to exactly 2.116.0 as the last code change of the phase, after a throwaway-worktree tsc proof; @supabase/ssr stays on 0.7 (DI-43).
- DEC-57 Findings: F-086..F-091 registered and the C16 re-pointing map applied (05-01 Task 2); migration-dependent closures (F-006, F-007, F-008, F-016) stay Open with a local-proof resolution and closes_in_phase "08" because production closes only with the DI-23 repair; code-only closures become Fixed.

**The deferred register.** Create `evidence/deferred-items.md` for Phase 5 in the Phase 4 format with: a table disposing inherited items (DI-22 owner, unchanged; DI-23 Phase 8; DI-25 per DEC-56; DI-26 Phase 6; DI-33 Phase 8; DI-36 → F-086; DI-38 CLOSED by Task 1 with its evidence; DI-39 and DI-40 owner, unchanged; every other open DI with its existing owner); a table disposing research assumptions A1-A6 (A1 closed by 05-04's build proof; A2 closed by 05-11/05-16 type regeneration; A3 carried into the CSRF assessment; A4 mitigated by reading both env names; A5 mitigated by x-real-ip; A6 owner action); and three new items: DI-41 (/invites/[token] auto-accepts on GET, Low CSRF residual, owner the phase owner), DI-42 (pre-deploy owner actions: provision Upstash with both env-name pairs BEFORE any push to main because Vercel deploys main and a production boot without the store fails by DEC-50; a read-only count of production users with onboarding_completed false/null or no users row; optional early production apply of the two Phase 5 migrations), DI-43 (@supabase/ssr 0.7 → 0.12, Phase 6 close-out at the earliest, Phase 8 by default). End with "Next new item id: DI-44."

**The defect ledger.** Create `evidence/defect-ledger.md` with the Phase 4 header and protocol adapted to Phase 5 (apply the fix; run the DEFECT suite unedited and see red; move the assertions and see green; restore the pre-fix source temporarily and see the moved suite red, then restore and `cmp`; PRESERVE suites pass unedited throughout) and the empty table `| F-nnn | Suite | Old assertion | New assertion | Commit | Plan |`.

Commit `docs(05-01): phase 5 decisions DEC-33..DEC-57, deferred register, defect ledger`.
  </action>
  <verify>
    <automated>grep -c '^## DEC-' .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEC-' evidence/phase-05-decisions.md` is 25; the first heading is DEC-33 and the last DEC-57
    - `evidence/phase-05-decisions.md` contains `**Plan:** 05-01` and each of the strings `Profile not found`, `Cross-site request blocked`, `You cannot change your own roles`, `600`, `120`, `VERCEL_ENV`, `2.116.0`
    - `grep -cE '^## DI-4[1-3]' evidence/deferred-items.md` is 3 and the file contains `Next new item id: DI-44`
    - `evidence/defect-ledger.md` contains the table header `| F-nnn | Suite | Old assertion | New assertion | Commit | Plan |`
    - `git status --porcelain -- src/ e2e/ supabase/ package.json package-lock.json` prints nothing
  </acceptance_criteria>
  <done>Every design call the later plans need is a numbered, overridable decision with evidence, and every deferral has an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop → local Supabase (Docker) | every DB command targets 127.0.0.1; the seed guard refuses other targets |
| laptop `.env.local` → production | holds production credentials; nothing here opens it |
| register → later plans | findings ids become DEFECT citations; a wrong id breaks traceability silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | findings.json reformatted while re-pointing, hiding record changes | mitigate | single-field edits; numstat deletions equal re-pointed lines plus one (Task 2 acceptance) |
| T-05-01-02 | Repudiation | a DEFECT suite citing an id nobody registered | mitigate | F-086..F-091 registered before any suite exists; tag gate re-run |
| T-05-01-03 | Information Disclosure | a key reaching an evidence file from a psql probe or `supabase status` | mitigate | keys read into shell variables only; the grep for key-shaped strings in the plan's evidence files prints nothing before commit |
| T-05-01-04 | Tampering | `supabase db reset` aimed at production | mitigate | `--local` on every call; no `--linked` anywhere |
| T-05-01-05 | Denial of Service | a flaky harness masking a real auth regression in slice 3 | mitigate | DI-38 fixed and proven 10/10 before the floor (Task 1 acceptance) |
| T-05-01-SC | Tampering | package installs | accept | no package installed in this plan |
</threat_model>

<verification>
- `node .planning/audit/tools/validate.mjs --check findings` exits 0 at 91 findings
- `node scripts/check-characterization-tags.mjs --all` exits 0
- `evidence/floor.before.txt` line 1 is `base=<sha>`, all blocks exit-coded, Playwright 0 failed
- `git diff --name-only <pre> HEAD -- src/ supabase/` prints nothing
</verification>

<success_criteria>
The phase starts from a measured floor on a named commit with a non-flaky harness, a register that holds an id for every defect it will touch and says which phase owns every finding it defers, and a numbered decision for every rule-resolved choice.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** the DI-38 fix in `e2e/specs/save-and-rsvp.spec.ts`; `.planning/audit/quality/phase-05-slice-defects.md`; findings F-086, F-087, F-088, F-089, F-090, F-091 and the C16 re-pointing; `evidence/floor.before.txt`, `evidence/di-38-fix.txt`, `evidence/findings-registration.txt`, `evidence/phase-05-decisions.md` (DEC-33..DEC-57), `evidence/deferred-items.md` (DI-41..DI-43), `evidence/defect-ledger.md`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-01-SUMMARY.md` when done, with a section "Decisions the phase owner may override" listing DEC-34, DEC-41, DEC-45, DEC-47, DEC-50, DEC-51 and DEC-52 with one sentence each on what an override changes.
</output>
