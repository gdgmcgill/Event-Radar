---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 14
type: execute
wave: 13
depends_on: ["05-12", "05-13"]
files_modified:
  - src/app/api/admin/calculate-popularity/route.ts
  - src/app/api/admin/users/[id]/route.ts
  - src/lib/audit.ts
  - src/app/api/admin/clubs/[id]/route.ts
  - src/app/api/admin/events/[id]/edits/route.ts
  - src/app/api/admin/events/[id]/route.ts
  - src/app/api/admin/events/[id]/status/route.ts
  - src/app/api/admin/events/route.ts
  - src/app/api/admin/featured/[id]/route.ts
  - src/app/api/admin/featured/route.ts
  - src/app/api/admin/organizer-requests/[id]/route.ts
  - src/app/api/admin/reports/[id]/route.ts
  - src/app/api/admin/users/[id]/ban/route.ts
  - src/app/moderation/page.tsx
  - src/app/moderation/audit-log/page.tsx
  - src/app/api/events/[id]/route.ts
  - src/__tests__/api/admin/admin-guard-defect.test.ts
  - src/__tests__/api/admin/admin-users-patch-defect.test.ts
  - src/__tests__/moderation/audit-shape.test.ts
  - src/__tests__/api/events/pending-edits-defect.test.ts
  - e2e/specs/admin-audit-row.spec.ts
  - src/server/db/elevated/REGISTRY.md
  - .planning/audit/tools/classify-inventory.mjs
  - .planning/audit/inventory/classification-rules.md
  - .planning/audit/inventory/endpoints.json
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-behaviours.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/audit-writer.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/contract-regen-slice-5.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-13]

must_haves:
  truths:
    - "calculate-popularity fails closed: anonymous 401, non-admin 403, admin 200, on both verbs, with the recompute running only through the registered door (FO-01, F-001)"
    - "An admin can change another user's roles and the change lands, is validated against the role enum, keeps admin when admin is sent, refuses the admin's own roles, and writes one audit row (F-091)"
    - "Every moderation action writes exactly one admin_audit_log row through the door, a rejected audit write is logged loudly, and both moderation pages show who acted without selecting a column that does not exist (F-073, F-072)"
    - "The creator and admins see an event's pending edits on its detail route; nobody else does (F-086)"
    - "The endpoint contract names the seam as the admin mechanism and says calculate-popularity is admin-only"
  artifacts:
    - path: "src/lib/audit.ts"
      provides: "logAdminAction through getElevatedClient(), no admin_email, optional requestId, error inspected"
      contains: "getElevatedClient()"
    - path: "e2e/specs/admin-audit-row.spec.ts"
      provides: "one moderation action → exactly one new audit row, on the real stack"
      min_lines: 50
  key_links:
    - from: "src/app/api/admin/calculate-popularity/route.ts"
      to: "src/server/db/elevated/index.ts"
      via: "getElevatedClient().rpc(\"update_event_popularity\", …) after requireRole"
      pattern: "getElevatedClient\\(\\)"
    - from: "src/app/moderation/page.tsx"
      to: "users"
      via: "audit rows first, then users id, name, email by id on the cookie client"
      pattern: "\\.in\\(\"id\""
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No migration: the audit fix works against the unmigrated production schema (DEC-46)"
    - "The shared transformEventFromDB is not changed; list routes are unaffected by F-086"
    - "The e2e spec never touches seeded rows; its fixture event is created and removed with the local service client"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: ADMIN_API_KEY -->
<!-- planner-discipline-allow: admin_email -->
<!-- planner-discipline-allow: (supabase as any) -->

<objective>
Slice 5, refactor B: fix the admin behaviours slice 5 owns — the fail-open popularity route (F-001/FO-01), the broken admin role change (F-091, DEC-45), the silently discarded audit log (F-073) and its empty activity panel (F-072, DEC-46), and pending edits never reaching their creator (F-086, DEC-53) — and regenerate the admin half of the endpoint contract (DEC-55).

Purpose: ROADMAP criterion 3's "every fail-open endpoint fails closed", and the Validated workflow "admins … with actions written to admin_audit_log", which production has never actually done (F-073).

Output: four fixed behaviours, three REGISTRY rows, four DEFECT suites moved, one e2e spec, the regenerated contract.
</objective>

<slice_outcome>
Green when the four DEFECT suites pass in their fixed shapes, `admin-guard-characterization.test.ts` and `events-detail-characterization.test.ts` pass unedited, `admin-audit-row.spec.ts` shows exactly one new audit row per action, and `validate.mjs --check endpoints` passes.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically; endpoints.json only through classify-inventory.mjs.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: calculate-popularity fails closed (F-001) and admin role changes land correctly (F-091)</name>
  <files>src/app/api/admin/calculate-popularity/route.ts, src/app/api/admin/users/[id]/route.ts, src/__tests__/api/admin/admin-guard-defect.test.ts, src/__tests__/api/admin/admin-users-patch-defect.test.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/admin-behaviours.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/admin/calculate-popularity/route.ts (whole file: createAdminClient, both verbs' ADMIN_API_KEY gate, the rpc and its cast)
    - src/app/api/admin/users/[id]/route.ts (after 05-13: requireRole; the admin strip; the name path; the 500 body)
    - src/lib/audit.ts (AuditAction includes "updated", AuditTargetType includes "user")
    - src/lib/supabase/types.ts — the user_role enum values and the update_event_popularity signature
    - evidence/phase-05-decisions.md § DEC-45; .planning/audit/findings.json F-001 and F-091 validation criteria
    - the two DEFECT suites
  </read_first>
  <action>
**calculate-popularity (F-001, FO-01).** Delete `createAdminClient`, the `@supabase/supabase-js` import and both ADMIN_API_KEY gates. Each verb starts with `const ctx = await createRequestContext(); const auth = requireRole(ctx, "admin"); if (!auth.ok) return auth.response;` and runs the recompute on `getElevatedClient()`; remove the `as never` cast on the rpc name when the generated types allow it (record if they do not). Success and error bodies otherwise unchanged.

**Admin role change (F-091, DEC-45).** Keep the requireRole gate and the `updated_at` and `name` handling. When `roles` is in the body: each value must be one of `user`, `admin`, `club_organizer`, else 400 `{ "error": "Invalid role", "field": "roles" }`; if the target id equals `auth.user.id`, 403 `{ "error": "You cannot change your own roles" }`; do not strip `admin`; still add `user` when absent. Perform the update on `getElevatedClient()` with `updateData` typed `TablesUpdate<"users">` (DI-25 site `admin/users/[id]`); keep `{ user: data }` and the 500 `{ "error": "Internal server error" }` bytes. After a successful roles change call `logAdminAction({ adminUserId: auth.user.id, action: "updated", targetType: "user", targetId: id, metadata: { roles } })` (the requestId field arrives in Task 2).

REGISTRY rows: `Recompute event popularity scores (rpc update_event_popularity)` | `src/app/api/admin/calculate-popularity/route.ts` | `writes event_popularity_scores, which are service-role-only by design (F-010); invoked on behalf of an admin, not a row owner` | 05; `Change another user's roles or name as an admin` | `src/app/api/admin/users/[id]/route.ts` | `users has no admin UPDATE policy and the F-006 column grant withholds roles from authenticated; the change is an audited admin action (F-004, F-091)` | 05.

Ledger protocol: add both calculate-popularity arms to FIXED_ARMS in `admin-guard-defect.test.ts` (anonymous 401 and student 403 with no rpc; the admin row stays green in the PRESERVE file); move every `admin-users-patch-defect.test.ts` row to its fixed shape (update on fake B; roles keep admin; one audit insert on fake B; self 403 bytes; invalid 400 bytes); Status paragraphs `FIXED in 05-14`. Run the admin suites, the PRESERVE file unedited, `node scripts/check-elevated-ratchet.mjs` (calculate-popularity leaves the census), lint and tsc; record in `evidence/admin-behaviours.txt`. Commit `fix(05-14): calculate-popularity fails closed; admin role changes land, validate, keep admin, and are audited (F-001, F-091)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (anonymous and non-admin callers refused on calculate-popularity; the moderation role toggle starts working; admin is no longer stripped; self role changes refused).
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/admin && node scripts/check-elevated-ratchet.mjs && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/admin/calculate-popularity/route.ts` contains `requireRole(ctx, "admin")` twice and `getElevatedClient()`, and does not import `@supabase/supabase-js`
    - `node -e 'const s=require("fs").readFileSync("src/app/api/admin/calculate-popularity/route.ts","utf8");process.exit(s.includes("ADMIN_API_KEY")?1:0)'` exits 0
    - `src/app/api/admin/users/[id]/route.ts` contains `Invalid role`, `You cannot change your own roles`, `getElevatedClient()`, `logAdminAction(` and `TablesUpdate<"users">`
    - `admin-guard-defect.test.ts` and `admin-users-patch-defect.test.ts` pass with `FIXED in 05-14`; `admin-guard-characterization.test.ts` passes unedited
    - REGISTRY.md has rows naming both route files
  </acceptance_criteria>
  <done>The last fail-open admin route fails closed, and admins can actually manage roles — audited, validated, and never on themselves.</done>
</task>

<task type="auto">
  <name>Task 2: The audit writer through the door, loud on failure; both moderation pages read who acted by id (F-073, F-072)</name>
  <files>src/lib/audit.ts, src/app/api/admin/clubs/[id]/route.ts, src/app/api/admin/events/[id]/edits/route.ts, src/app/api/admin/events/[id]/route.ts, src/app/api/admin/events/[id]/status/route.ts, src/app/api/admin/events/route.ts, src/app/api/admin/featured/[id]/route.ts, src/app/api/admin/featured/route.ts, src/app/api/admin/organizer-requests/[id]/route.ts, src/app/api/admin/reports/[id]/route.ts, src/app/api/admin/users/[id]/ban/route.ts, src/app/api/admin/users/[id]/route.ts, src/app/moderation/page.tsx, src/app/moderation/audit-log/page.tsx, src/__tests__/moderation/audit-shape.test.ts, e2e/specs/admin-audit-row.spec.ts, src/server/db/elevated/REGISTRY.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/audit-writer.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/lib/audit.ts (44 lines); every logAdminAction call (`command grep -rn "logAdminAction(" src/app`) and each call's adminEmail argument
    - src/app/moderation/page.tsx lines 40-130 and 400-440 (the admin_email select at :80 with the F-072 cast pair, the render at :426); src/app/moderation/audit-log/page.tsx lines 1-40 and 210-235
    - src/__tests__/moderation/audit-shape.test.ts (whole file: the DEFECT F-072/F-073 assertions and their mocks)
    - supabase/migrations/20260915214553_baseline.sql admin_audit_log definition (admin_user_id → auth.users; no email column) and the "Admins can view all profiles" users policy
    - evidence/phase-05-decisions.md § DEC-46; 05-RESEARCH.md § C5
    - e2e/env.ts, playwright.config.ts (the local-target guard)
  </read_first>
  <action>
`src/lib/audit.ts`: import `getElevatedClient` instead of the service factory; drop the email parameter and the email column from the insert (this is what clears DI-25's audit.ts site); add an optional `requestId?: string`; inspect the insert's `{ error }` and on error call `console.error("[Audit] admin_audit_log insert rejected", { action, targetType, targetId, requestId, code: error.code, message: error.message })`; keep the function's signature otherwise and its resolve-without-throwing contract. Update every caller: remove the email argument and pass `requestId: ctx.requestId`. REGISTRY row: `Insert admin_audit_log rows for every moderation action` | `src/lib/audit.ts` (logAdminAction) | `after F-007 no client role may insert; the door is the only writer, so the record cannot be forged by the actor it records` | 05 — and delete the "note, not a row" section about audit.ts that 04-03 wrote, replacing it with one sentence pointing at the row.

Moderation pages (C5): in both pages select `id, admin_user_id, action, target_type, target_id, metadata, created_at` (no email column) on the cookie client, then read `users` `id, name, email` with `.in("id", <distinct admin ids>)` on the cookie client and render the actor from that map (name, else the email's local part, else the first 8 characters of admin_user_id as today's fallback). Remove the `(supabase as any)` and `as Promise<…>` pair in `src/app/moderation/page.tsx` (F-072). No visual change for seeded data: seeded audit rows do not exist.

Ledger protocol on `audit-shape.test.ts`: unedited-red; move the F-072 and F-073 assertions to the fixed shape (the dashboard select has no email column; the audit insert payload has no email key and is sent to the elevated client; a rejected insert produces the console.error line); restore-red; cmp; Status `FIXED in 05-14`.

**`e2e/specs/admin-audit-row.spec.ts`** (F-073, F-007 validation): beforeAll, with the local service client from `localStackEnv()`, insert a fixture event (`status: "pending"`, `created_by` null, a title prefixed `p5-audit-fixture`, start and end dates in the future). As admin: count `admin_audit_log` rows with `target_id` = the fixture id through the service client (0); PATCH the moderation status route for the fixture to approved (read the route for the body shape) → 200; count again → exactly 1, with `action` approved and `admin_user_id` = `IDS.admin`. afterAll: delete the fixture's audit rows, notifications and the event through the service client. Run it and the admin-moderation-queue spec after a clean reset and seed; record in `evidence/audit-writer.txt` with the Jest runs. Commit `fix(05-14): audit writes through the door without the absent email column, loud on failure; moderation pages read actors by id (F-073, F-072)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (audit rows are written again; Recent Activity populates).
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/moderation src/__tests__/api/admin src/app/api/admin && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/audit.ts` contains `getElevatedClient()`, `requestId`, `[Audit] admin_audit_log insert rejected`, and `node -e 'process.exit(require("fs").readFileSync("src/lib/audit.ts","utf8").includes("admin_email")?1:0)'` exits 0
    - `command grep -rn "adminEmail" src/app src/lib` prints nothing
    - `src/app/moderation/page.tsx` contains `.in("id"` and no `(supabase as any)`
    - `audit-shape.test.ts` passes with `FIXED in 05-14`
    - `admin-audit-row.spec.ts` passed after a clean reset with exactly one new row (recorded exit=0), and the fixture rows are gone afterwards (recorded)
    - `node scripts/check-elevated-ratchet.mjs` exits 0 (src/lib/audit.ts left the census)
  </acceptance_criteria>
  <done>Every moderation action is recorded once, through the only writer, and the activity panel shows who did it.</done>
</task>

<task type="auto">
  <name>Task 3: Pending edits reach their creator and admins (F-086), and the admin half of the contract is regenerated</name>
  <files>src/app/api/events/[id]/route.ts, src/__tests__/api/events/pending-edits-defect.test.ts, .planning/audit/tools/classify-inventory.mjs, .planning/audit/inventory/classification-rules.md, .planning/audit/inventory/endpoints.json, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/contract-regen-slice-5.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/events/[id]/route.ts GET (the transform call and the dead stripping branch); src/lib/tagMapping.ts transformEventFromDB (unchanged)
    - src/__tests__/api/events/events-detail-characterization.test.ts layer (b) — must pass unedited
    - 05-RESEARCH.md § G; evidence/phase-05-decisions.md § DEC-53 and § DEC-55
    - .planning/audit/tools/classify-inventory.mjs lines 100-160 (VIA_ADMIN and the calculate-popularity verdict), .planning/audit/inventory/classification-rules.md (the admin mechanism text and FO-01's row)
  </read_first>
  <action>
**F-086.** In GET `/api/events/[id]`, after `transformEventFromDB`, when the caller is the row's `created_by` or `hasRole(profile, "admin")`, return the event with `pending_edits: data.pending_edits`; otherwise leave the transformed event as it is (the dead stripping branch is replaced by this one conditional). Use the request context already present in the GET (from 05-10) or create one. Ledger protocol on `pending-edits-defect.test.ts` (creator and admin rows move to "present and equal to the row's value", the other-user row stays absent; Status `FIXED in 05-14`); `events-detail-characterization.test.ts` passes unedited. Commit `fix(05-14): creator and admins receive pending_edits on the event detail route (F-086)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (no seeded row carries pending_edits).

**Contract (DEC-55).** In `classify-inventory.mjs` set the admin mechanism text to name `requireRole(ctx, "admin")` in `src/server/authz/requireRole.ts`, and change `api.admin.calculate-popularity`'s verdict from the machine-secret verdict to the admin verdict with a note that FO-01 was fixed in 05-14; mirror both in `classification-rules.md`. Run the script (dry run first), list every endpoints.json row whose fields changed, and require the set to be the admin rows whose mechanism text changed plus calculate-popularity's expected_status (anonymous 401, students 403, admin 200, machine_no_credential 401); record in `evidence/contract-regen-slice-5.txt` with `validate.mjs --check endpoints` and `--quick`. Commit `docs(05-14): endpoint contract names the seam's admin guard; calculate-popularity is admin-only`.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/events && node .planning/audit/tools/validate.mjs --check endpoints</automated>
  </verify>
  <acceptance_criteria>
    - `pending-edits-defect.test.ts` passes with `FIXED in 05-14`; `events-detail-characterization.test.ts` passes with `git diff <05-13 head>` on it empty
    - `node -e 'const e=require("./.planning/audit/inventory/endpoints.json");const s=e.find(r=>r.id==="api.admin.calculate-popularity").expected_status;process.exit(s.anonymous===401&&s.onboarded_student===403&&s.admin===200?0:1)'` exits 0
    - `evidence/contract-regen-slice-5.txt` lists every changed row id and nothing outside the allowed set
    - `node .planning/audit/tools/validate.mjs --check endpoints` exits 0
  </acceptance_criteria>
  <done>A creator can see their pending edits again, and the contract says what the admin surface now enforces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anyone → calculate-popularity | formerly open to the internet on a service-role client |
| admin → another user's roles | a privilege-changing write, now audited |
| server → admin_audit_log | the accountability record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14-01 | Elevation of Privilege | anonymous service-role write through calculate-popularity (F-001, Critical) | mitigate | requireRole on both verbs; DEFECT rows assert anonymous 401 and student 403 with no rpc; ADMIN_API_KEY gate removed |
| T-05-14-02 | Elevation of Privilege | an admin granting themselves roles, or an invalid role string stored | mitigate | self roles change 403; enum validation 400; fixed DEFECT rows in acceptance |
| T-05-14-03 | Repudiation | moderation actions silently unrecorded (F-073, High) | mitigate | door writer without the absent column, error inspected and logged; e2e asserts exactly one row per action |
| T-05-14-04 | Information Disclosure | pending edits exposed to non-owners | mitigate | attached only for creator or admin; the other-user row stays absent; PRESERVE detail suite unedited |
| T-05-14-05 | Tampering | e2e fixture rows left in the local database | mitigate | afterAll deletes fixture event, notifications and audit rows; recorded |
| T-05-14-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0; `npx tsc --noEmit`, `npm run lint` clean
- `admin-audit-row.spec.ts` and the admin specs green after a clean reset
- `validate.mjs --check endpoints` exits 0; ratchet exit 0
</verification>

<success_criteria>
The fail-open admin route is closed, admin role changes work and are audited, audit writes land and are visible, pending edits reach their owner, and the contract matches.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** fixed `calculate-popularity` and `admin/users/[id]` routes; `src/lib/audit.ts` through the door with `requestId`; both moderation pages' actor enrichment; F-086 in the detail GET; REGISTRY rows (popularity recompute, admin role change, audit-log writer); `e2e/specs/admin-audit-row.spec.ts`; the regenerated admin contract; `evidence/admin-behaviours.txt`, `evidence/audit-writer.txt`, `evidence/contract-regen-slice-5.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-14-SUMMARY.md` when done.
</output>
