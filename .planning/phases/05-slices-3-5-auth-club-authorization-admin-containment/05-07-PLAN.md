---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 07
type: execute
wave: 6
depends_on: ["05-03", "05-06"]
files_modified:
  - src/app/api/clubs/route.ts
  - src/app/api/clubs/[id]/appeal/route.ts
  - src/app/api/clubs/[id]/follow/route.ts
  - src/app/api/clubs/[id]/invites/route.ts
  - src/app/api/clubs/[id]/members/role/route.ts
  - src/app/api/clubs/[id]/members/route.ts
  - src/app/api/clubs/[id]/route.ts
  - src/app/api/clubs/[id]/transfer/route.ts
  - src/app/api/clubs/banner/route.ts
  - src/app/api/clubs/logo/route.ts
  - src/app/api/users/[id]/follow/route.ts
  - src/app/api/users/[id]/route.ts
  - src/app/api/profile/avatar/route.ts
  - src/app/api/profile/banner/route.ts
  - src/app/api/profile/inferred-tags/route.ts
  - src/app/api/profile/interests/route.ts
  - src/app/api/notifications/[id]/route.ts
  - src/app/api/notifications/route.ts
  - src/app/api/organizer-requests/route.ts
  - src/app/api/onboarding/complete/route.ts
  - src/app/api/interactions/route.ts
  - src/app/api/feedback/route.ts
  - src/lib/ban.ts
  - src/server/context.ts
  - src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts
  - src/__tests__/api/clubs/analytics.test.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-rest.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "Every remaining non-admin write arm (clubs family, user follow, profile, notifications, organizer requests, onboarding completion, interactions, feedback) refuses a banned or profile-less caller, and every non-exempt one refuses an un-onboarded caller, before reading anything"
    - "The onboarding wizard still completes: PATCH /api/users/[id] (self) and POST /api/onboarding/complete admit an un-onboarded caller"
    - "Anonymous interaction tracking and anonymous feedback still work; the guards apply only when a user is present"
    - "No state-changing handler relies on the legacy ban helper, and the helper no longer exists; isBanned remains the one ban predicate"
    - "Every write arm in the handler table is guarded (GUARDED holds all 39)"
  artifacts:
    - path: "src/lib/ban.ts"
      provides: "isBanned only"
      exports: ["isBanned"]
    - path: "src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts"
      provides: "every arm in GUARDED; docblock FIXED in 05-07"
      contains: "FIXED in 05-07"
  key_links:
    - from: "src/app/api/users/[id]/route.ts"
      to: "src/server/authz/requireActiveUser.ts"
      via: "requireActiveUser only — the onboarding exemption DEC-34 names"
      pattern: "requireActiveUser\\(ctx\\)"
    - from: "src/app/api/interactions/route.ts"
      to: "src/server/authz/requireOnboarded.ts"
      via: "guards applied only when ctx.user is present"
      pattern: "if \\(ctx\\.user\\)"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "club_members checks in the clubs family are not touched (slice 4, 05-10); service-client uses are not moved (slice 5, 05-15)"
    - "No PRESERVE suite is edited; untagged suites may gain fixture rows only"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<!-- planner-discipline-allow: checkBanStatus -->

<objective>
Slice 3, refactor part 4: guard the remaining 25 write arms (DEC-34), honouring the two onboarding exemptions (C2) and the two anonymous-tolerant writers, then delete the legacy ban helper `checkBanStatus` now that it has no caller.

Purpose: completes REFAC-11's handler ring. After this plan every state-changing non-admin arm decides ban, onboarding and missing profile itself, so the proxy is genuinely advisory.

Output: 22 route files guarded, the legacy helper deleted, the handler-ring DEFECT suite fully flipped.
</objective>

<slice_outcome>
Green when GUARDED holds every arm id in the table, the handler-ring PRESERVE suite passes unedited, `checkBanStatus` has zero occurrences under `src/`, and `npx jest --ci` is green with 0 failed.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Guard the clubs-family write arms</name>
  <files>src/app/api/clubs/route.ts, src/app/api/clubs/[id]/appeal/route.ts, src/app/api/clubs/[id]/follow/route.ts, src/app/api/clubs/[id]/invites/route.ts, src/app/api/clubs/[id]/members/role/route.ts, src/app/api/clubs/[id]/members/route.ts, src/app/api/clubs/[id]/route.ts, src/app/api/clubs/[id]/transfer/route.ts, src/app/api/clubs/banner/route.ts, src/app/api/clubs/logo/route.ts, src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts, src/__tests__/api/clubs/analytics.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-rest.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - the ten route files — each write arm's auth branch, anonymous response, legacy ban call (clubs POST, clubs/[id]/appeal POST) and first read; note that clubs/[id] DELETE has no try/catch and reads the request body after the owner check
    - src/app/api/events/[id]/save/route.ts after 05-06 — the adopted shape to copy
    - src/__tests__/api/auth-ring/write-handlers-characterization.test.ts ANONYMOUS_BYTES for the clubs arms
    - 05-RESEARCH.md § C (club gate sites) — only to confirm this task leaves every club_members check exactly as it is
  </read_first>
  <action>
For clubs POST, clubs/[id]/appeal POST, clubs/[id]/follow POST and DELETE, clubs/[id]/invites POST, clubs/[id]/members/role PATCH, clubs/[id]/members DELETE, clubs/[id] PATCH and DELETE, clubs/[id]/transfer POST, clubs/banner POST and clubs/logo POST: insert as the first statements `const ctx = await createRequestContext();`, `const active = requireActiveUser(ctx); if (!active.ok) return active.response;`, `const onboarded = requireOnboarded(ctx); if (!onboarded.ok) return onboarded.response;`; delete the legacy helper calls and imports in the two legacy files; replace the handler's own getUser and 401 branch with `ctx`/`active.user`; keep an explicit anonymous branch with today's bytes wherever ANONYMOUS_BYTES records something other than 401 `{"error":"Unauthorized"}`; use `ctx.supabase` for the cookie client. Leave every club_members query, every service-client use (including clubs/[id] DELETE's dynamic import) and every response body unchanged. GET arms in these files are not touched. Add the 12 clubs arm ids to GUARDED. If `src/__tests__/api/clubs/analytics.test.ts` or another untagged suite fails only for a missing users fixture row, add the row (no assertion change) and record it in `evidence/slice-3-fixture-completions.md`. Follow the ledger protocol for the GUARDED flip; record runs in `evidence/handler-adoption-rest.txt`. Commit `fix(05-07): clubs-family write arms fail closed on ban, onboarding and missing profile (F-088, F-089)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE` naming the refused personas.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api src/app/api && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Each of the ten files contains `requireActiveUser(ctx)` once per write arm and `requireOnboarded(ctx)` once per write arm
    - `write-handlers-characterization.test.ts` passes with `git diff <05-06 head>` on it empty
    - `write-handlers-ring-defect.test.ts` passes with the 12 clubs ids added to GUARDED (26 total)
    - `git diff <05-06 head> -- 'src/app/api/clubs' | command grep -E '^[-+].*from\("club_members"\)'` prints nothing
  </acceptance_criteria>
  <done>Club writes refuse banned, profile-less and un-onboarded callers themselves, and the club-membership logic slice 4 owns is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the user, profile, notification, organizer-request, onboarding and telemetry arms; delete the legacy ban helper</name>
  <files>src/app/api/users/[id]/follow/route.ts, src/app/api/users/[id]/route.ts, src/app/api/profile/avatar/route.ts, src/app/api/profile/banner/route.ts, src/app/api/profile/inferred-tags/route.ts, src/app/api/profile/interests/route.ts, src/app/api/notifications/[id]/route.ts, src/app/api/notifications/route.ts, src/app/api/organizer-requests/route.ts, src/app/api/onboarding/complete/route.ts, src/app/api/interactions/route.ts, src/app/api/feedback/route.ts, src/lib/ban.ts, src/server/context.ts, src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-rest.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - the twelve route files — auth branches; users/[id] PATCH's `user.id !== id` 403 and its service-client update; onboarding/complete's cookie deletion; interactions and feedback's `user?.id || null` anonymous handling
    - src/components/onboarding/OnboardingWizard.tsx lines 55-90 — the wizard's two API calls (C2)
    - src/lib/ban.ts, src/server/context.ts docblock
    - evidence/phase-05-decisions.md § DEC-34
  </read_first>
  <action>
Guard, with the same first-statement shape as Task 1:
- users/[id]/follow POST and DELETE, profile/avatar POST, profile/banner POST, profile/inferred-tags DELETE, profile/interests PUT, notifications/[id] PATCH, notifications POST, organizer-requests POST → requireActiveUser then requireOnboarded.
- users/[id] PATCH and onboarding/complete POST → requireActiveUser only (the onboarding exemptions, DEC-34/C2); users/[id] keeps its `user.id !== id` 403 exactly after the guard.
- interactions POST and feedback POST → `const ctx = await createRequestContext();` then `if (ctx.user) {` requireActiveUser, then requireOnboarded, returning their responses `}`; anonymous requests proceed exactly as today with `ctx.user?.id ?? null` where the handler used `user?.id || null` (keep the handler's own falsy semantics).
Keep every service-client use (profile/avatar, profile/banner, users/[id]) where it is — slice 5 moves them. Add the 13 arm ids to GUARDED; GUARDED now holds every id in `WRITE_ARMS` (assert it in the DEFECT file with a completeness test comparing the two sets) and set its Status to `FIXED in 05-06 and 05-07`.

Then delete `checkBanStatus` from `src/lib/ban.ts` along with the imports only it used; `isBanned` and its interface stay. Remove every remaining mention of the deleted helper under `src/`, including the `src/server/context.ts` docblock sentence that describes it (rewrite that sentence to name requireActiveUser). Confirm `command grep -rn "checkBanStatus" src` prints nothing.

Run the full `npx jest --ci`, `npx tsc --noEmit`, `npm run lint`, `node scripts/check-characterization-tags.mjs --all`; record in `evidence/handler-adoption-rest.txt` with the ledger rows. Commit `fix(05-07): remaining write arms guarded; delete the legacy ban helper (F-088, F-089)` with an `INTENTIONAL BEHAVIOUR CHANGE` body (banned users refused on follows, profile, notifications, organizer requests, onboarding completion and signed-in telemetry; un-onboarded users refused on direct writes except the two wizard calls).
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit && npm run lint && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -rn "checkBanStatus" src` prints nothing
    - `src/lib/ban.ts` exports `isBanned` and nothing that reads Supabase
    - `src/app/api/users/[id]/route.ts` and `src/app/api/onboarding/complete/route.ts` contain `requireActiveUser(ctx)` and do not contain `requireOnboarded(`
    - `src/app/api/interactions/route.ts` and `src/app/api/feedback/route.ts` contain `if (ctx.user)`
    - `write-handlers-ring-defect.test.ts` contains a completeness test asserting GUARDED equals the set of WRITE_ARMS ids, and passes
    - `write-handlers-characterization.test.ts` P3 (exemptions admitted) and P1 (anonymous bytes, including the anonymous-tolerant arms) pass unedited
    - `npx jest --ci` exits 0 with 0 failed
  </acceptance_criteria>
  <done>Every state-changing non-admin arm decides ban, onboarding and missing profile itself; the wizard and anonymous telemetry still work; the fail-open helper is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → write handler | direct API calls, proxy bypassed |
| onboarding wizard → API | the two calls an un-onboarded user must still be able to make |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | a banned or un-onboarded caller writing through a remaining arm | mitigate | GUARDED equals WRITE_ARMS, asserted by a completeness test (Task 2 acceptance) |
| T-05-07-02 | Denial of Service | onboarding made impossible to finish (research Pitfall 8) | mitigate | exemptions on users/[id] PATCH and onboarding/complete; P3 PRESERVE rows unedited and green; the Playwright wizard-call check in 05-08 |
| T-05-07-03 | Denial of Service | anonymous tracking and feedback broken | mitigate | guards only when ctx.user; P1 anonymous rows for both arms unedited and green |
| T-05-07-04 | Elevation of Privilege | a fail-open ban helper re-imported later | mitigate | helper deleted; grep for its name under src prints nothing (acceptance) |
| T-05-07-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0, 0 failed
- `npx tsc --noEmit`, `npm run lint` clean
- `command grep -rn "checkBanStatus" src` prints nothing
</verification>

<success_criteria>
The handler ring is complete: every write arm fails closed on ban, onboarding and missing profile, with the wizard and anonymous telemetry preserved and the legacy helper gone.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** guard adoption in 22 route files; the legacy ban helper deleted from `src/lib/ban.ts`; GUARDED complete with its completeness test; `evidence/handler-adoption-rest.txt`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-07-SUMMARY.md` when done.
</output>
