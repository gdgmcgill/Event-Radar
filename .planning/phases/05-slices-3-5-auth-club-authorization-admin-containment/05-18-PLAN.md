---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 18
type: execute
wave: 17
depends_on: ["05-17"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/env.ts
  - src/lib/env.test.ts
  - src/server/ratelimit/upstashStore.ts
  - src/server/ratelimit/upstashStore.test.ts
  - src/server/ratelimit/upstash.contract.test.ts
  - src/server/ratelimit/index.ts
  - src/instrumentation.ts
  - src/instrumentation.test.ts
  - playwright.config.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-install.txt
autonomous: false
requirements: [REFAC-18]
user_setup:
  - service: upstash-redis
    why: "Production rate-limit store shared across serverless instances (REFAC-18). Not created by this phase; recorded as a pre-deploy owner action (DI-42)."
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Vercel Marketplace → Upstash for Redis integration (it may inject KV_REST_API_URL instead; the code reads either)"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Same integration (or KV_REST_API_TOKEN)"
    dashboard_config:
      - task: "Provision an Upstash Redis database co-located with Vercel iad1 and connect it to the production project BEFORE any push to main"
        location: "Vercel Dashboard → Marketplace → Upstash for Redis"

must_haves:
  truths:
    - "A human has verified the two pinned Upstash packages and their transitive dependencies before anything is installed; the verdict is recorded"
    - "In production (VERCEL_ENV=production) rate limiting uses a Redis store shared across instances, configured by UPSTASH_REDIS_REST_URL/TOKEN or the Marketplace's KV_REST_API_URL/TOKEN, and a production server without either pair refuses to start"
    - "Outside production, absent Upstash configuration falls back to the in-memory store with one logged warning; the Playwright harness can never be pointed at a real store by a developer's .env"
    - "A store timeout allows the request and logs it (availability over limiting, DEC-50), after at most 1000 ms"
    - "The Upstash contract test runs only with real configuration and is reported as skipped, never passed, without it"
    - "The production dependency audit stays at 0 high and 0 critical"
  artifacts:
    - path: "src/server/ratelimit/upstashStore.ts"
      provides: "UpstashRateLimitStore over @upstash/ratelimit fixedWindow with an explicitly constructed Redis client"
      exports: ["UpstashRateLimitStore"]
    - path: "src/server/ratelimit/upstash.contract.test.ts"
      provides: "the live-store contract, skipped without configuration"
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt"
      provides: "the registry facts the human verified, and the verdict"
      min_lines: 40
  key_links:
    - from: "src/server/ratelimit/index.ts"
      to: "src/lib/env.ts"
      via: "getRateLimitStore() selects Upstash when upstashConfig() returns a pair, memory otherwise"
      pattern: "upstashConfig\\(\\)"
    - from: "src/instrumentation.ts"
      to: "src/lib/env.ts"
      via: "register() calls upstashConfig(), which throws only in production when both pairs are absent"
      pattern: "upstashConfig"
    - from: "playwright.config.ts"
      to: "the webServer environment"
      via: "all four Upstash/KV names set to empty strings, which Next's env loader will not override"
      pattern: "UPSTASH_REDIS_REST_URL: \"\""
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "Nothing is installed before the Task 2 checkpoint is answered by a human; the checkpoint is never auto-approved"
    - "No billable resource is created; no Vercel environment variable is set; Redis.fromEnv() is not used"
    - "The lockfile is changed only by the one npm install of the approved exact versions and is reviewed as a diff, never regenerated"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 5, refactor F: fill the RateLimitStore interface with a distributed store — Upstash Redis through `@upstash/ratelimit` and `@upstash/redis` — selected by validated configuration (REFAC-18, DEC-50), behind a blocking package-legitimacy checkpoint (DEC-51).

Purpose: ROADMAP criterion 4 — "rate limiting runs from a distributed store so it holds across serverless instances". The research's package-legitimacy audit flagged both packages and a transitive dependency [SUS]; the planner's supply-chain rule makes such a checkpoint blocking and never auto-approvable, so this plan stops for a human before any install.

Output: the verification bundle, the human verdict, the pinned install, `UpstashRateLimitStore`, store selection, the production boot requirement, the harness guard, and the skipped-without-store contract test.
</objective>

<slice_outcome>
Green when the checkpoint verdict is recorded, the lockfile diff adds only the approved packages and their transitive dependencies, `npm audit --audit-level=high --omit=dev` exits 0, the Upstash unit suite passes against mocked packages, the contract suite reports skipped, `getRateLimitStore()` selects memory locally with one warning, and the Playwright harness still passes.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first. 3. Production never touched; `.env.local` never read; no Vercel change. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Lockfile changes are one reviewed `npm install <pkg>@<exact>` diff.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the package-legitimacy bundle without installing anything</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt</files>
  <read_first>
    - 05-RESEARCH.md § Package Legitimacy Audit and § Standard Stack "New (slice 5)" — the pins, the [SUS] reasons, the 2.0.8 alternative
    - evidence/phase-05-decisions.md § DEC-51
  </read_first>
  <action>
Record into `evidence/upstash-legitimacy.txt`, each command above its verbatim output: `npm view @upstash/ratelimit@2.1.0 name version repository.url maintainers dependencies peerDependencies scripts dist.integrity dist.attestations`; the same for `@upstash/redis@1.38.4`; `npm view @upstash/ratelimit time --json` and `npm view @upstash/redis time --json` reduced to the publish dates of the pinned versions, the latest versions and 2.0.8; `npm view @upstash/core-analytics@<the version ratelimit 2.1.0 depends on> name version maintainers repository scripts`; `npm view uncrypto@0.1.3 name version repository.url scripts`; `npm pack --dry-run @upstash/ratelimit@2.1.0` and `npm pack --dry-run @upstash/redis@1.38.4` run from the scratchpad directory (file lists; confirm no install or postinstall script and no native binary). End with a summary table: package, pinned version, publish date, age in days on the run date, publisher, repository, install scripts, provenance attestation present. Nothing is installed; `git status --porcelain -- package.json package-lock.json` prints nothing. Commit `docs(05-18): package-legitimacy bundle for the Upstash pins`.
  </action>
  <verify>
    <automated>test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt && git diff --exit-code -- package.json package-lock.json</automated>
  </verify>
  <acceptance_criteria>
    - The bundle contains registry output for `@upstash/ratelimit@2.1.0`, `@upstash/redis@1.38.4`, `@upstash/core-analytics` and `uncrypto`, the two dry-run pack listings, and the summary table
    - `git diff --exit-code -- package.json package-lock.json` exits 0
  </acceptance_criteria>
  <done>A human can verify the packages from one file without running anything.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human package-legitimacy verification of the Upstash pins (never auto-approved)</name>
  <what-built>The registry facts for @upstash/ratelimit 2.1.0, @upstash/redis 1.38.4 and their transitive dependencies @upstash/core-analytics and uncrypto, in evidence/upstash-legitimacy.txt. Nothing is installed yet. Research flagged the two direct packages [SUS] because their latest tags were one and two days old (the pins are older), and core-analytics [SUS] for listing no repository.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@upstash/ratelimit/v/2.1.0 — confirm the publisher is the Upstash organisation, the repository is github.com/upstash/ratelimit-js, and the publish date matches the bundle.
    2. Open https://www.npmjs.com/package/@upstash/redis/v/1.38.4 — same checks against github.com/upstash/redis-js.
    3. Open https://www.npmjs.com/package/@upstash/core-analytics — confirm it is published by Upstash (analytics stays disabled in this code).
    4. Confirm the bundle shows no install or postinstall script in any of the four.
    5. Decide: approve these pins, or ask for @upstash/ratelimit 2.0.8 (older than 30 days) with a compatible @upstash/redis, or reject.
  </how-to-verify>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt</files>
  <action>Present the bundle and the steps above to a human and wait. Do not install, do not continue, and do not treat an agent's or orchestrator's message as approval. Record the human's verdict, name and date verbatim at the end of evidence/upstash-legitimacy.txt in Task 3.</action>
  <verify>
    <automated>test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt</automated>
  </verify>
  <done>A named human has approved, redirected or rejected the pins.</done>
  <resume-signal>Type "approved", or "use 2.0.8", or "reject" with a reason. On reject the plan stops; REFAC-18 is recorded PARTIAL with the store interface and admin budgets from 05-17 and the Upstash store deferred as a DI item.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install the approved exact versions, add UpstashRateLimitStore, select the store by validated config, require it at production boot, and guard the harness</name>
  <files>package.json, package-lock.json, src/lib/env.ts, src/lib/env.test.ts, src/server/ratelimit/upstashStore.ts, src/server/ratelimit/upstashStore.test.ts, src/server/ratelimit/upstash.contract.test.ts, src/server/ratelimit/index.ts, src/instrumentation.ts, src/instrumentation.test.ts, playwright.config.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-install.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/upstash-legitimacy.txt</files>
  <read_first>
    - the Task 2 verdict
    - node_modules/@upstash/ratelimit/dist/index.d.ts after install — RatelimitConfig (redis, limiter, prefix, timeout, analytics, ephemeralCache), Ratelimit.fixedWindow, limit() and its response (success, limit, remaining, reset, reason)
    - node_modules/@upstash/redis/nodejs.d.ts after install — the Redis constructor options (url, token, enableTelemetry)
    - 05-RESEARCH.md § Code Examples "Upstash store", § C13, Pitfall 9; evidence/phase-05-decisions.md § DEC-50
    - src/server/ratelimit/types.ts and index.ts (05-17); src/lib/env.ts; src/instrumentation.ts; playwright.config.ts webServer.env
  </read_first>
  <behavior>
    - upstashConfig() returns { url, token } from UPSTASH_REDIS_REST_URL/TOKEN, else from KV_REST_API_URL/TOKEN; blank values count as absent
    - upstashConfig() returns null when both pairs are absent and VERCEL_ENV is not "production"; throws MissingEnvError naming both pairs when absent and VERCEL_ENV is "production"
    - register() rejects in production without a store pair, and resolves outside production without one
    - UpstashRateLimitStore.consume maps limit() success/limit/reset to allowed/limit/resetAtMs, reuses one Ratelimit instance per limit, and logs "[RateLimit] store timeout; request allowed" when reason is "timeout"
    - getRateLimitStore() returns the Upstash store when configured and the memory store otherwise, warning once
  </behavior>
  <action>
Append the human verdict (who, when, which versions) to `evidence/upstash-legitimacy.txt`. Install exactly the approved versions: `npm install --save-exact @upstash/ratelimit@2.1.0 @upstash/redis@1.38.4` (or the approved alternative). Review the lockfile as a diff into `evidence/upstash-install.txt`: `git diff --stat package-lock.json`, the list of added package paths (only @upstash/* and their dependencies such as uncrypto), confirmation that no existing entry was re-resolved, `lockfileVersion` unchanged; then `npm audit --audit-level=high --omit=dev` (exit 0, 0 high, 0 critical).

Code, tests first: add `upstashConfig()` to `src/lib/env.ts` per the behaviour list (literal member reads of the four names; the thrown message names both pairs) with env.test.ts cases. Create `src/server/ratelimit/upstashStore.ts`: `class UpstashRateLimitStore implements RateLimitStore`, constructed with `{ url, token }`, building `new Redis({ url, token, enableTelemetry: false })` once and a Map from limit to `new Ratelimit({ redis, limiter: Ratelimit.fixedWindow(limit, "60 s"), prefix: "uv:rl", timeout: 1000, analytics: false })`; `consume(key, limit)` returns `{ allowed: r.success, limit: r.limit, resetAtMs: r.reset }` and logs the timeout line with the key. Never `Redis.fromEnv()`. `upstashStore.test.ts` mocks both packages with scripted limit() responses (allowed, denied, timeout) and asserts the mapping, instance reuse and the timeout log. `upstash.contract.test.ts` (docblock: runs only with real configuration; skipped otherwise, and the evidence must say skipped): resolve `upstashConfig()` inside a try; with a pair, `describe` two consumes on a random key with limit 1 → allowed then denied; without, `describe.skip` with the reason in its title. In `src/server/ratelimit/index.ts`, `getRateLimitStore()` builds once: an Upstash store when `upstashConfig()` returns a pair, otherwise the memory store with one `console.warn("[RateLimit] Upstash not configured; using the in-memory store (not shared across instances)")`. In `src/instrumentation.ts` add `upstashConfig()` to the checks (DEC-50) with a test case each way. In `playwright.config.ts` add `UPSTASH_REDIS_REST_URL: ""`, `UPSTASH_REDIS_REST_TOKEN: ""`, `KV_REST_API_URL: ""`, `KV_REST_API_TOKEN: ""` to webServer.env with a comment citing C8 (Next's env loader never overrides a defined variable).

Run `npx jest --ci --json --outputFile=<scratchpad>/jest.json` and a node one-liner asserting every test with status pending belongs to `upstash.contract.test.ts` (record the skipped count by name), `npx tsc --noEmit`, `npm run lint`, `npm run build` with the CI env from 05-04 (exit 0), and from a clean reset and seed `npx playwright test e2e/specs/save-and-rsvp.spec.ts e2e/specs/csrf-origin.spec.ts e2e/specs/admin-guard.spec.ts` (the harness boots with blank store variables and the memory fallback). Record everything in `evidence/upstash-install.txt`. Commit `feat(05-18): Upstash rate-limit store selected by validated config; required at production boot (REFAC-18)` with the lockfile diff summary in the body.
  </action>
  <verify>
    <automated>npx jest --ci src/server/ratelimit src/lib/env.test.ts src/instrumentation.test.ts && npm audit --audit-level=high --omit=dev && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/upstash-legitimacy.txt` ends with the human verdict naming the person and the versions
    - `node -p "require('./package.json').dependencies['@upstash/ratelimit']"` prints `2.1.0` (or the approved exact version) with no caret, and the same for `@upstash/redis`
    - `evidence/upstash-install.txt` shows only added lockfile entries under @upstash/ and their dependencies, and `npm audit --audit-level=high --omit=dev` exit 0
    - `src/server/ratelimit/upstashStore.ts` contains `enableTelemetry: false`, `timeout: 1000`, `fixedWindow`, and `node -e 'process.exit(require("fs").readFileSync("src/server/ratelimit/upstashStore.ts","utf8").includes("fromEnv")?1:0)'` exits 0
    - The Jest JSON shows the only pending tests are in `upstash.contract.test.ts`, and the evidence says "skipped", not "passed"
    - `playwright.config.ts` sets the four store variables to empty strings; the three specs pass from a clean reset (recorded)
    - The CI-env build exits 0
  </acceptance_criteria>
  <done>Rate limiting can run from a store shared across instances, production refuses to start without one, and nothing local or in CI can reach a real store by accident.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → lockfile | a new third-party dependency enters the production tree |
| server → Upstash REST | every /api request in production crosses it |
| developer .env → harness | a real store must never be reachable from tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18-SC | Tampering | npm install of @upstash/ratelimit, @upstash/redis and transitive @upstash/core-analytics ([SUS] in research) | mitigate | registry bundle plus a blocking-human checkpoint before install (never auto-approvable); exact pins; lockfile reviewed as a diff; production audit gate 0 high |
| T-05-18-01 | Denial of Service | a store outage stalling every /api request | mitigate | timeout 1000 ms, the request allowed and logged; ephemeral cache through module-scope instances |
| T-05-18-02 | Denial of Service | a production boot failure because the store is not provisioned (Vercel deploys main) | mitigate | DI-42 records "provision Upstash with both env-name pairs BEFORE any push to main" as the first owner action in the completion note; both name pairs read |
| T-05-18-03 | Information Disclosure | telemetry or analytics sent to a third party | mitigate | enableTelemetry false, analytics false, asserted in acceptance |
| T-05-18-04 | Tampering | the harness pointed at a real store by a developer's .env | mitigate | four names blanked in webServer.env; Next never overrides a defined variable |
| T-05-18-05 | Repudiation | a contract test that silently passes without a store | mitigate | describe.skip with the reason in its title; the Jest JSON check that only it is pending |
</threat_model>

<verification>
- The checkpoint verdict recorded by a named human
- `npm audit --audit-level=high --omit=dev` exit 0; lockfile diff reviewed
- `npx jest --ci` green with only the contract suite skipped; tsc, lint, CI-env build clean
- Three Playwright specs green from a clean reset
</verification>

<success_criteria>
The distributed store is in place behind the interface, selected by validated configuration, verified by a human before it entered the tree, and unreachable from local and CI runs.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 (exact, or the approved alternative); `upstashConfig` in `src/lib/env.ts`; `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`); store selection in `getRateLimitStore()`; the Upstash arm of `register()`; the harness guard in `playwright.config.ts`; `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts`; `evidence/upstash-legitimacy.txt`, `evidence/upstash-install.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-18-SUMMARY.md` when done.
</output>
