---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 11
type: execute
wave: 10
depends_on: ["05-09", "05-10"]
files_modified:
  - supabase/migrations/20260923120000_events_insert_club_scope.sql
  - supabase/tests/database/060-club-tenant-isolation.test.sql
  - scripts/pgtap-mutation-check.sh
  - src/lib/supabase/types.ts
  - e2e/specs/club-invitation-acceptance.spec.ts
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/schema-push-slice-4.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-4-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.slice-4-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-close.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md
autonomous: true
requirements: [REFAC-12]

must_haves:
  truths:
    - "At the RLS ring, cross_club_attacker cannot insert an approved event into a club they are not a member of, cannot forge created_by, cannot delete another club's members and cannot invite to another club — each asserted with the SQLSTATE or row count, never lives_ok alone"
    - "In the allow direction a real member can insert an approved event into their approved club, anyone can submit a pending event (DEC-42), an owner can delete their own club's member and invite to their own club, and an admin can insert an event with no creator"
    - "Direct owner writes to clubs and club_members are still denied by RLS; the owner path is the registered door (C11)"
    - "The migrations rebuild from zero locally, the committed types match what the local database generates, and the pgTAP suite passes unseeded and seeded"
    - "A invites B, B opens the invitation and accepts it, B is a member — on the local stack (F-016)"
    - "Slice 4 closes on a green floor from a clean reset with every Validated workflow re-confirmed"
  artifacts:
    - path: "supabase/migrations/20260923120000_events_insert_club_scope.sql"
      provides: "is_club_member helper and the club-scoped events INSERT policy (F-008)"
      contains: "Authenticated users can insert events"
    - path: "supabase/tests/database/060-club-tenant-isolation.test.sql"
      provides: "both-direction RLS assertions for events, clubs, club_members, club_invitations"
      min_lines: 120
    - path: "e2e/specs/club-invitation-acceptance.spec.ts"
      provides: "F-016 integration through the product API, restored afterwards"
      min_lines: 50
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-close.md"
      provides: "floor, Playwright, Validated re-confirmation, findings, local-only closures"
      min_lines: 80
  key_links:
    - from: "supabase/migrations/20260923120000_events_insert_club_scope.sql"
      to: "src/lib/supabase/types.ts"
      via: "the new function appears in the generated types; regenerated with --local on CLI 2.115.0 in the same commit"
      pattern: "is_club_member"
    - from: "scripts/pgtap-mutation-check.sh"
      to: "supabase/migrations/20260923120000_events_insert_club_scope.sql"
      via: "POLICY_MIGRATION_GLOBS gains the new file so removing the policy must turn 060 red"
      pattern: "events_insert_club_scope"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file (20260915214553, 20260915230000, 20260915230100 and 20260916000000 are untouched)"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No pgTAP test writes into the auth schema (D-21); an allowed write that reaches an auth.users foreign key is asserted as SQLSTATE 23503"
    - "types.ts is never hand-edited; it is generated from the local database only"
    - "No policy is added that lets an owner UPDATE clubs or club_members directly (DEC-41)"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 4, the RLS ring and close: fix F-008 by a fix-forward migration, prove the club tables' tenant isolation in both directions with pgTAP, apply the schema to the LOCAL stack only (the [BLOCKING] push for this project), prove F-016 end to end, and close slice 4.

Purpose: ROADMAP criterion 2's second half — "independently denied at the RLS ring" — is false for events until F-008 is fixed (C10). CONTEXT Area 2 requires both-direction pgTAP and a green `supabase db reset` plus `supabase test db` after the slice.

Output: one migration, one pgTAP file, regenerated types, the mutation check extended, the F-016 spec, the slice-4 floor and close note.
</objective>

<slice_outcome>
Green when `supabase db reset --local` succeeds, the generated types equal the committed file, `supabase test db --local` passes unseeded and seeded including 060, the mutation check shows 060 red when the new policy is removed, the F-016 spec passes and restores membership, and the full floor is green from a clean reset.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/rls-ring-before.txt
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first. 3. Production never touched; `.env.local` never read; the schema push for this project is the LOCAL rebuild only. 4. Migrations fix-forward only; types regenerated with `--local` on CLI 2.115.0. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically. 11. A requirement is measured against its own sentence.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: The F-008 fix-forward migration and the 060 tenant-isolation pgTAP file</name>
  <files>supabase/migrations/20260923120000_events_insert_club_scope.sql, supabase/tests/database/060-club-tenant-isolation.test.sql, scripts/pgtap-mutation-check.sh</files>
  <read_first>
    - supabase/migrations/20260916000000_invitation_policy_fixes.sql lines 1-60, 130-150, 190-220 — the header (FIX FORWARD, BLAST RADIUS), helper-function comment and grant style, the DROP POLICY IF EXISTS + CREATE POLICY … TO authenticated shape
    - supabase/migrations/20260915214553_baseline.sql lines 309-340 (is_admin, is_club_owner), the events INSERT policy at line 2113, the FK lines 1735-1795 (events.created_by and clubs.created_by reference auth.users)
    - supabase/tests/database/000-setup.sql (tests.act_as, act_as_anon, act_as_owner) and 020-rls-policy-gaps.test.sql lines 1-120 — assertion shapes, fixture as the session role, impersonation proof first
    - src/__tests__/moderation/audit-shape.test.ts lines 1-30 — the house pattern that an insert reaching the FK check proves the shape was accepted
    - scripts/pgtap-mutation-check.sh lines 1-120 — POLICY_MIGRATION_GLOBS
    - evidence/phase-05-decisions.md § DEC-42; evidence/rls-ring-before.txt
    - src/app/api/events/create/route.ts and src/app/api/admin/events/route.ts — the two insert paths the policy must keep working
  </read_first>
  <action>
**Migration** `supabase/migrations/20260923120000_events_insert_club_scope.sql` (confirm with `node scripts/check-migration-filenames.mjs` that it parses): the 20260916000000 header form naming F-008, DEC-42, research C10, the local-only blast radius until DI-23. Contents: `CREATE OR REPLACE FUNCTION public.is_club_member(p_club_id uuid) RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path = ''` whose body is an EXISTS over `public.club_members` where `club_id = p_club_id` and `user_id = (SELECT auth.uid())`; `COMMENT ON FUNCTION`; `REVOKE ALL ON FUNCTION public.is_club_member(uuid) FROM PUBLIC, anon`; `GRANT EXECUTE … TO authenticated, service_role`. Then `DROP POLICY IF EXISTS "Authenticated users can insert events" ON public.events;` and `CREATE POLICY "Authenticated users can insert events" ON public.events FOR INSERT TO authenticated WITH CHECK (public.is_admin() OR (created_by = (SELECT auth.uid()) AND (status = 'pending' OR (status = 'approved' AND club_id IS NOT NULL AND public.is_club_member(club_id) AND EXISTS (SELECT 1 FROM public.clubs c WHERE c.id = events.club_id AND c.status = 'approved')))))`. Idempotent; nothing else.

**pgTAP** `supabase/tests/database/060-club-tenant-isolation.test.sql`: `BEGIN; SELECT plan(N);` … `SELECT * FROM finish(); ROLLBACK;`. Docblock stating the three shapes: WITH CHECK deny → `throws_ok(…, '42501')`; USING deny → `is_empty` then `tests.act_as_owner()` integrity check with `isnt_empty`; an allowed insert whose row references `auth.users` through `created_by` → `throws_ok(…, '23503')`, meaning RLS passed and the row reached the foreign key (D-21: no writes into the auth schema). Fixture as the session role with ids in the `00000000-0000-4000-8000-0000000060xx` block (grep supabase/tests/database first for collisions): users owner A, organizer M (member of A), attacker B (owner of club CB), admin X (roles admin); clubs CA (approved), CB (approved), CP (pending, owned by A); memberships. Assertions, each labelled with the persona and research § D row:
1. impersonation proof (`auth.uid()` equals the attacker);
2. attacker inserts approved into CA with created_by = A → 42501;
3. attacker inserts approved into CA with created_by = B → 42501;
4. attacker inserts pending into CA with created_by = B → 23503 (allowed, DEC-42);
5. M inserts approved into CA with created_by = M → 23503 (allowed);
6. A inserts approved into CP with created_by = A → 42501 (club not approved);
7. X inserts approved with created_by NULL → lives, then as owner the row exists (`isnt_empty`);
8. anon insert → 42501;
9. attacker UPDATE clubs on CA → `is_empty` of UPDATE … RETURNING, then integrity as owner;
10. A UPDATE clubs on CA → empty (C11: the owner path is the door);
11. A UPDATE club_members role on M's row → empty;
12. attacker DELETE M's membership in CA → empty, integrity as owner;
13. A DELETE M's membership in CA → `results_eq` on DELETE … RETURNING user_id equals M;
14. attacker INSERT club_invitations for CA → 42501;
15. A INSERT club_invitations for CA → allowed (RETURNING club_id equals CA, or 23503 if the inviter column references auth.users — measure and use the right shape, documented inline).
No deny INSERT uses RETURNING (research § D). Append `supabase/migrations/*_events_insert_club_scope.sql` to POLICY_MIGRATION_GLOBS in `scripts/pgtap-mutation-check.sh` with a comment naming 05-11. Do not run the reset here; Task 2 does.
  </action>
  <verify>
    <automated>node scripts/check-migration-filenames.mjs && test -s supabase/tests/database/060-club-tenant-isolation.test.sql</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-migration-filenames.mjs` exits 0 and counts 5 migrations
    - The migration contains `SECURITY DEFINER`, `SET search_path = ''`, `DROP POLICY IF EXISTS "Authenticated users can insert events"`, `TO authenticated` and `public.is_admin()`
    - 060 contains `tests.act_as(` before its first deny assertion, at least five `'42501'` assertions, at least two `'23503'` assertions and at least three `act_as_owner` integrity checks
    - `git diff --name-only <05-10 head> -- supabase/migrations/20260915214553_baseline.sql supabase/migrations/20260915230000_fk_indexes_and_policy_gaps.sql supabase/migrations/20260915230100_cron_compute_user_scores.sql supabase/migrations/20260916000000_invitation_policy_fixes.sql` prints nothing
    - `scripts/pgtap-mutation-check.sh` contains `events_insert_club_scope`
  </acceptance_criteria>
  <done>F-008's fix and its proof exist as files: an approved event can only be inserted into a club the caller belongs to, under their own id.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the schema to the LOCAL stack, regenerate types, run pgTAP unseeded and seeded, prove the policy bites</name>
  <files>src/lib/supabase/types.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/schema-push-slice-4.txt</files>
  <read_first>
    - .github/workflows/ci.yml `types` job — reset, generate, diff, test db: this task reproduces it locally
    - .planning/STATE.md decisions on the CLI pin (2.115.0) and the --local-only type gate (D-04)
    - scripts/pgtap-mutation-check.sh METHOD CONTRACT (destructive to local data; --local only)
  </read_first>
  <action>
This is the project's schema push, and it is LOCAL ONLY. Record into `evidence/schema-push-slice-4.txt`, each with command and exit code: `supabase --version` (must be 2.115.0; any other version stops the task, because generator output is not stable across versions); `supabase db reset --local` (must exit 0; output filtered of key-shaped strings); `supabase gen types typescript --local --schema public > src/lib/supabase/types.ts`; `git diff --stat src/lib/supabase/types.ts` (expected: only the new `is_club_member` function entry — record the diff; anything else stops the task); `supabase test db --local` labelled UNSEEDED (all files PASS, 060 included, 040's seed-only assertions SKIP); `npx tsx scripts/seed/load.ts`; `supabase test db --local` labelled SEEDED (all PASS, 0 SKIP); `bash scripts/pgtap-mutation-check.sh` (every listed policy's removal turns the suite red, including "Authenticated users can insert events" in both files that create it; exit 0); then a final `supabase db reset --local` and seed so the stack is left clean. Run `npx tsc --noEmit` and `npx jest --ci` against the regenerated types. Commit the migration, 060, the mutation-check glob and the regenerated types together: `fix(05-11): events INSERT policy binds created_by and club membership (F-008)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE` (local only until DI-23: a forged or cross-club approved event insert is now refused by the database). After the commit, `supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -` prints nothing (the CI drift gate, reproduced). Never run `supabase db push` and never pass `--linked`.
  </action>
  <verify>
    <automated>supabase test db --local && supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/schema-push-slice-4.txt` shows `supabase --version` 2.115.0, reset exit=0, UNSEEDED and SEEDED pgTAP runs with every file PASS, and the mutation check exit=0 naming the new policy
    - `supabase gen types typescript --local --schema public | diff -u src/lib/supabase/types.ts -` prints nothing
    - `src/lib/supabase/types.ts` contains `is_club_member`
    - `npx tsc --noEmit` and `npx jest --ci` exit 0
    - No command in the evidence file contains `--linked` or `db push`
  </acceptance_criteria>
  <done>The migration folder rebuilds locally with F-008 fixed, the types match the database, and the RLS ring is proven in both directions with a mutation check to show the proof bites.</done>
</task>

<task type="auto">
  <name>Task 3: F-016 integration spec, slice-4 floor from a clean reset, close note and register</name>
  <files>e2e/specs/club-invitation-acceptance.spec.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-4-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.slice-4-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-4-close.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md, .planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/app/api/clubs/[id]/invites/route.ts POST (returns `{ invite: { token } }`, 201) and GET; src/app/invites/[token]/page.tsx (reads the invitation, checks the email, auto-accepts); src/app/api/clubs/[id]/members/route.ts GET and DELETE `{ memberId }`
    - scripts/seed/personas.ts — onboarded_student's email, IDS.approvedClub
    - evidence/floor.slice-3-after.txt and evidence/slice-3-close.md — the shapes to repeat
    - .planning/audit/findings.json records F-008, F-016, F-087 — validation criteria
    - the 05-09 and 05-10 SUMMARYs — "Deferred items found"
  </read_first>
  <action>
**`e2e/specs/club-invitation-acceptance.spec.ts`** (DEC-43, F-016): `test.describe.serial`. As club_owner: POST `/api/clubs/<approvedClub>/invites` with onboarded_student's email → 201, capture `invite.token`. As onboarded_student: `page.goto("/invites/" + token)` → the page is not an error card (assert the absence of the "Wrong Account" and "Something Went Wrong" titles by role or test id, and the response status 200). As club_owner: GET `/api/clubs/<approvedClub>/invites` → the row for that email has `status: "accepted"`; GET `/api/clubs/<approvedClub>/members` → contains a member whose `user_id` (or nested user id) is `IDS.onboarded_student`. Cleanup in `afterAll` and on failure: as club_owner, DELETE `/api/clubs/<approvedClub>/members` with that member's id → 200, then GET members shows onboarded_student absent. A pre-check deletes any leftover membership for onboarded_student in approvedClub the same way, so re-runs are clean. The accepted invitation row remains (only pending duplicates are refused by the POST).

**Floor.** From port 3000 free, repeat the full slice-3 floor sequence (reset, UNSEEDED pgTAP, seed, SEEDED pgTAP, reset and seed, `npx playwright test`, Jest, lint, tsc, audit, ratchet, filenames, tag gate, validate --quick, censuses, plus a census of gate-site membership reads under src/app/api) into `evidence/floor.slice-4-after.txt` (line 1 `head=<sha>`) and `evidence/playwright.slice-4-after.txt`. pgTAP must now count 7 files. One flake re-run is allowed with both logs kept.

**Close note** `evidence/slice-4-close.md` (metadata line): floor and Playwright tables against slice 3's after; the 16-row Validated re-confirmation (club surfaces: club-owner-surfaces.spec, club-authorization.spec FIXED F-087, club-invitation-acceptance.spec; every persona still signs in; banned still blocked); the RLS matrix after, row by row against `evidence/rls-ring-before.txt`; closures; INTENTIONAL BEHAVIOUR CHANGEs (owner edit/delete/role change working; the forged approved insert refused locally); and the local-only closures pending DI-23 (F-008, F-016).

**Register and requirement.** F-087 → `Fixed` with resolution citing 05-10's commit and the close note. F-008 and F-016 → stay `Open`, `closes_in_phase` "08", resolution "fixed and proven on the local stack (<commits>, 060, club-invitation-acceptance.spec); production closes with the DI-23 migration repair" (DEC-57). Regenerate FOUNDATION_AUDIT.md; `validate.mjs --check findings` exits 0. Add any deferred items from the 05-09/05-10 SUMMARYs to `evidence/deferred-items.md` from the next free DI id. Measure REFAC-12 against its sentence; tick it with `Complete (05-11)` if every clause is evidenced (noting the RLS clause is proven locally and reaches production with DI-23), otherwise PARTIAL naming the clause. Commit `docs(05-11): slice 4 close — RLS ring proven, F-016 proven locally, floor green`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && head -1 .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-4-after.txt | grep -E '^head=[0-9a-f]{40}$'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.slice-4-after.txt` ends with 0 failed and exit=0 from a clean reset, and includes the club-invitation-acceptance spec
    - `evidence/floor.slice-4-after.txt` pgTAP rows show 7 files PASS unseeded and seeded; Jest 0 failed
    - After the run, a query shows onboarded_student has no membership in approvedClub (recorded)
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i);process.exit(g("F-087").status==="Fixed"&&g("F-008").closes_in_phase==="08"&&g("F-016").closes_in_phase==="08"&&g("F-008").resolution&&g("F-016").resolution?0:1)'` exits 0
    - `evidence/slice-4-close.md` has a Validated table with as many rows as `grep -c '^- ✓' .planning/PROJECT.md`
    - `.planning/REQUIREMENTS.md` REFAC-12 row reads `Complete (05-11)` or `PARTIAL` with the clause named
  </acceptance_criteria>
  <done>Slice 4 is closed: the authz ring and the RLS ring both deny cross-club writes, invitation acceptance works end to end locally, and the register says which closures wait for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated role → events table | direct PostgREST inserts, the path F-008 exploits |
| migration folder → local database | the only schema target in this phase |
| spec → seeded membership | the F-016 spec mutates and restores club membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Tampering | cross-club approved event with forged created_by (F-008) | mitigate | new WITH CHECK; 060 rows 2, 3, 6 assert 42501; mutation check turns 060 red when the policy is removed |
| T-05-11-02 | Denial of Service | the new policy refusing legitimate member or admin inserts (event creation broken) | mitigate | 060 allow rows 4, 5, 7; Playwright floor includes event creation paths; events/create PRESERVE rows green |
| T-05-11-03 | Tampering | a production write while "pushing" the schema | mitigate | local reset only; acceptance asserts no `--linked` or `db push` in the evidence |
| T-05-11-04 | Tampering | types.ts drifting from the database, or hand-edited | mitigate | generated with --local on 2.115.0; the CI diff reproduced and empty |
| T-05-11-05 | Tampering | the F-016 spec leaving onboarded_student a club member | mitigate | cleanup in afterAll plus a pre-check; post-run query recorded |
| T-05-11-06 | Elevation of Privilege | SECURITY DEFINER helper with a mutable search_path | mitigate | `SET search_path = ''`, schema-qualified body, EXECUTE revoked from PUBLIC and anon |
| T-05-11-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `supabase test db --local` PASS unseeded and seeded; types diff empty
- `npx playwright test` green from a clean reset
- `validate.mjs --check findings` exits 0
</verification>

<success_criteria>
Cross-club writes are denied at both rings with both directions proven, the schema rebuilds locally with matching types, F-016 is proven locally, and slice 4 closes green.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `supabase/migrations/20260923120000_events_insert_club_scope.sql` (`public.is_club_member`, the events INSERT policy), `supabase/tests/database/060-club-tenant-isolation.test.sql`, the mutation-check glob, regenerated `src/lib/supabase/types.ts`, `e2e/specs/club-invitation-acceptance.spec.ts`, `evidence/schema-push-slice-4.txt`, `evidence/floor.slice-4-after.txt`, `evidence/playwright.slice-4-after.txt`, `evidence/slice-4-close.md`; register and REFAC-12 updates.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-11-SUMMARY.md` when done.
</output>
