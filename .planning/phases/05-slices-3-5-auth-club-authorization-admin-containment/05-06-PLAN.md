---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 06
type: execute
wave: 5
depends_on: ["05-03", "05-05"]
files_modified:
  - src/app/api/events/[id]/save/route.ts
  - src/app/api/events/[id]/rsvp/route.ts
  - src/app/api/events/create/route.ts
  - src/app/api/events/[id]/invite/route.ts
  - src/app/api/events/[id]/appeal/route.ts
  - src/app/api/events/[id]/report/route.ts
  - src/app/api/events/[id]/reviews/route.ts
  - src/app/api/events/[id]/route.ts
  - src/app/api/events/upload-image/route.ts
  - src/app/api/recommendations/feedback/route.ts
  - src/app/api/user/engagement/route.ts
  - src/app/api/events/following/route.ts
  - src/app/api/events/friends-activity/route.ts
  - src/app/api/events/friends-organizing/route.ts
  - src/app/api/events/[id]/friends/route.ts
  - src/__tests__/api/events/save-characterization.test.ts
  - src/__tests__/api/events/rsvp-characterization.test.ts
  - src/__tests__/api/events/ban-asymmetry-defect.test.ts
  - src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts
  - src/__tests__/api/events/anonymous-personalized-defect.test.ts
  - src/__tests__/api/events/friends-defect.test.ts
  - src/__tests__/api/events/rsvp.test.ts
  - src/__tests__/api/events/reviews.test.ts
  - src/__tests__/api/events/date-validation.test.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-events.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "Every events-family write arm, plus recommendations/feedback and user/engagement, refuses a banned caller with 403 {\"error\":\"Account suspended\"}, a caller with no profile row with 403 {\"error\":\"Profile not found\"}, and an un-onboarded caller with 403 {\"error\":\"Onboarding required\"}, before reading anything — with the proxy bypassed"
    - "Each of those arms keeps its anonymous response bytes and its happy-path bytes exactly (PRESERVE suites unedited)"
    - "A banned user can no longer unsave or cancel an RSVP (the Phase 4 ban asymmetry is closed), recorded as an intentional behaviour change"
    - "The four F-028 routes answer an anonymous caller 401 {\"error\":\"Unauthorized\"}; signed-in callers are unaffected"
  artifacts:
    - path: "src/__tests__/api/events/ban-asymmetry-defect.test.ts"
      provides: "the save and rsvp DELETE ban pins moved out of the PRESERVE files, in their fixed shape"
      min_lines: 60
    - path: "src/app/api/events/[id]/save/route.ts"
      provides: "requireActiveUser + requireOnboarded on POST and DELETE"
      contains: "requireOnboarded"
  key_links:
    - from: "src/app/api/events/[id]/save/route.ts"
      to: "src/server/authz/requireActiveUser.ts"
      via: "first statements after createRequestContext()"
      pattern: "requireActiveUser\\(ctx\\)"
    - from: "src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts"
      to: "the events-family arms"
      via: "each guarded arm's id added to GUARDED in the commit that guards it"
      pattern: "GUARDED"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No PRESERVE suite is edited except removing the two 'ban asymmetry — pinned for Phase 5 (DEC-24)' blocks, which move to the DEFECT file in the same commit"
    - "Untagged legacy suites (rsvp.test.ts, reviews.test.ts, date-validation.test.ts) may gain fixture rows only; no assertion in them changes"
    - "The club_members checks inside events/[id] and events/create are not touched (slice 4, 05-10)"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 3, refactor part 3: put the seam guards in front of the events-family write arms (DEC-34) and close F-028 on its four routes (DEC-39).

Purpose: REFAC-11's "ban check fails closed" and "onboarding guard cannot be bypassed by direct API calls" are handler-ring properties; until each write arm runs the guards itself, a proxy fault admits a banned or un-onboarded caller (F-088, F-089). This plan covers 14 arms in 11 files; 05-07 covers the other 25.

Output: 11 handlers adopting the guards, the ban-asymmetry pins moved and flipped, the handler-ring DEFECT rows for these arms flipped, the four F-028 routes answering 401.
</objective>

<slice_outcome>
Green when every Phase 4 PRESERVE suite and the 05-02/05-03 PRESERVE suites pass unedited (the two moved blocks aside), the handler-ring DEFECT suite passes with these 14 arms in GUARDED, the F-028 suite passes in its fixed shape, and `npx jest --ci` is green with 0 failed.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Guard the events-family write arms and move the ban-asymmetry pins</name>
  <files>src/app/api/events/[id]/save/route.ts, src/app/api/events/[id]/rsvp/route.ts, src/app/api/events/create/route.ts, src/app/api/events/[id]/invite/route.ts, src/app/api/events/[id]/appeal/route.ts, src/app/api/events/[id]/report/route.ts, src/app/api/events/[id]/reviews/route.ts, src/app/api/events/[id]/route.ts, src/app/api/events/upload-image/route.ts, src/app/api/recommendations/feedback/route.ts, src/app/api/user/engagement/route.ts, src/__tests__/api/events/save-characterization.test.ts, src/__tests__/api/events/rsvp-characterization.test.ts, src/__tests__/api/events/ban-asymmetry-defect.test.ts, src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts, src/__tests__/api/events/rsvp.test.ts, src/__tests__/api/events/reviews.test.ts, src/__tests__/api/events/date-validation.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-events.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-fixture-completions.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/events/[id]/save/route.ts (125 lines) — the Phase 4 seam shape: createRequestContext, requireUser, the legacy ban call before `await params`
    - each of the other ten route files — find its auth branch (`getUser`), its anonymous response, the legacy ban call if any, and its first read
    - src/server/authz/requireActiveUser.ts and src/server/authz/requireOnboarded.ts
    - src/__tests__/api/events/save-characterization.test.ts lines 1-60 and 340-360, src/__tests__/api/events/rsvp-characterization.test.ts lines 1-60 and 445-470 — the two "ban asymmetry — pinned for Phase 5 (DEC-24)" describe blocks and the helpers they use
    - src/__tests__/api/auth-ring/write-handlers-characterization.test.ts — ANONYMOUS_BYTES for these arms (the bytes each must keep)
    - src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts — GUARDED and the fixed shapes
    - evidence/phase-05-decisions.md § DEC-34
  </read_first>
  <action>
For each arm — events/[id]/save POST and DELETE, events/[id]/rsvp POST and DELETE, events/create POST, events/[id]/invite POST, events/[id]/appeal POST, events/[id]/report POST, events/[id]/reviews POST, events/[id] PATCH and DELETE, events/upload-image POST, recommendations/feedback POST, user/engagement POST — make the first statements of the handler body: `const ctx = await createRequestContext();` then `const active = requireActiveUser(ctx); if (!active.ok) return active.response;` then `const onboarded = requireOnboarded(ctx); if (!onboarded.ok) return onboarded.response;`. Delete the legacy ban-helper call and its import, delete the handler's own `supabase.auth.getUser()` call and its 401 branch, use `ctx.supabase` for the cookie client and `active.user` for the user. Where ANONYMOUS_BYTES records an anonymous response other than 401 `{"error":"Unauthorized"}` for an arm, keep an explicit `if (!ctx.user) return <those exact bytes>` before requireActiveUser. Keep every read, write, service-client use, club_members check and error body otherwise unchanged; keep existing outer try/catch bodies byte-identical. Where a handler used `createClient()` only for auth, drop that import.

Move the ban asymmetry (DEC-34, C15) in the same commit as the save and rsvp change: create `src/__tests__/api/events/ban-asymmetry-defect.test.ts` with the docblock `DEFECT characterization — F-088 (F-062 related): the DELETE arms of save and rsvp admitted a banned caller` and `Status: FIXED in 05-06 by <commit>`, containing the two blocks' fixtures and cases with the assertions moved to the fixed shape: a permanently banned caller's DELETE on save returns 403 `{ "error": "Account suspended" }` and the saved row is still present; the same for rsvp DELETE with the RSVP row still not cancelled. Remove exactly those two describe blocks from the two PRESERVE files and nothing else (acceptance checks the diff is deletions only). In `write-handlers-ring-defect.test.ts` add the 14 arm ids to GUARDED.

Legacy untagged suites: run `src/__tests__/api/events/rsvp.test.ts`, `reviews.test.ts` and `date-validation.test.ts`; where one fails only because its authenticated fixture has no users row (the seam now reads it), add a users row with `onboarding_completed: true`, `banned_at: null`, `ban_expires_at: null` to that fixture — no assertion may change — and record each such edit in `evidence/slice-3-fixture-completions.md` (file, fixture, reason). Any other failure is a behaviour change: stop, fix the handler, never the test.

Ledger rows for every moved assertion (two ban-asymmetry moves plus one row per GUARDED addition group). Record in `evidence/handler-adoption-events.txt`: the unedited-red run of the DEFECT suites, the moved-green run, `git diff --stat` of the PRESERVE files (the two blocks only), and the full `npx jest --ci` result. Commit `fix(05-06): events-family write arms fail closed on ban, onboarding and missing profile (F-088, F-089)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE`: banned callers are refused on the save and rsvp DELETE arms and on every events-family write; un-onboarded callers are refused on direct writes; callers with no profile row are refused.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api src/app/api && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 11 route files contains `requireActiveUser(ctx)` and `requireOnboarded(ctx)`; `command grep -c "requireOnboarded(ctx)" src/app/api/events/[id]/save/route.ts` is 2 and the same for rsvp and events/[id]
    - `git diff <05-05 head> -- src/__tests__/api/events/save-characterization.test.ts src/__tests__/api/events/rsvp-characterization.test.ts` shows only deleted lines, all inside the two "ban asymmetry" describe blocks
    - `git diff <05-05 head> -- 'src/__tests__/api/events/*-characterization.test.ts' src/__tests__/api/auth-ring/write-handlers-characterization.test.ts` shows no other change, and all those suites pass
    - `src/__tests__/api/events/ban-asymmetry-defect.test.ts` passes and cites F-088 with `FIXED in 05-06`
    - `write-handlers-ring-defect.test.ts` passes with 14 ids in GUARDED
    - `evidence/slice-3-fixture-completions.md` lists every legacy-suite fixture edit, and `git diff <05-05 head> -- src/__tests__/api/events/rsvp.test.ts src/__tests__/api/events/reviews.test.ts src/__tests__/api/events/date-validation.test.ts | command grep -E '^[-+].*expect\('` prints nothing
  </acceptance_criteria>
  <done>The events-family writes decide ban, onboarding and missing profile themselves, with every byte the PRESERVE nets hold unchanged, and the ban asymmetry is closed on the record.</done>
</task>

<task type="auto">
  <name>Task 2: F-028 — the four personalized routes answer anonymous callers 401</name>
  <files>src/app/api/events/following/route.ts, src/app/api/events/friends-activity/route.ts, src/app/api/events/friends-organizing/route.ts, src/app/api/events/[id]/friends/route.ts, src/__tests__/api/events/anonymous-personalized-defect.test.ts, src/__tests__/api/events/friends-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/handler-adoption-events.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - the four route files — each anonymous branch returning a degraded 200
    - src/__tests__/api/events/anonymous-personalized-defect.test.ts
    - src/__tests__/api/events/friends-defect.test.ts — whether any assertion pins the anonymous 200 on events/[id]/friends
    - evidence/phase-05-decisions.md § DEC-39; 05-RESEARCH.md § C7 (callers and their signed-in guards)
  </read_first>
  <action>
In each of the four GET handlers, replace the anonymous degraded-200 branch with the seam: `const ctx = await createRequestContext(); const auth = requireUser(ctx); if (!auth.ok) return auth.response;` (401 `{"error":"Unauthorized"}`), keeping the signed-in path's reads and bytes unchanged (events/[id]/friends already uses the seam from Phase 4 — change only its anonymous branch). GETs gain neither ban nor onboarding guard (DEC-34). Follow the ledger protocol on `anonymous-personalized-defect.test.ts`: unedited-red, move the four anonymous rows to 401 `{"error":"Unauthorized"}`, restore-red, restore and `cmp`; the four signed-in rows stay green unedited; set the Status paragraph to `FIXED in 05-06 by <commit>`. If `friends-defect.test.ts` pins the anonymous 200 on events/[id]/friends, move that assertion to 401 in the same commit with its own ledger row; otherwise leave it untouched. Record runs in `evidence/handler-adoption-events.txt`. Commit `fix(05-06): four personalized routes answer anonymous callers 401 (F-028)` with a body starting `INTENTIONAL BEHAVIOUR CHANGE` naming the four routes and stating that their only callers render for signed-in users and ignore a non-ok response (C7), so no rendered output changes.
  </action>
  <verify>
    <automated>npx jest --ci src/__tests__/api/events/anonymous-personalized-defect.test.ts src/__tests__/api/events/friends-defect.test.ts && npx jest --ci</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four route files contains `requireUser(ctx)`; none contains the degraded anonymous body literal it returned before (checked by the DEFECT suite's fixed rows)
    - `anonymous-personalized-defect.test.ts` passes with four 401 rows and four unchanged signed-in rows, docblock `FIXED in 05-06`
    - `npx jest --ci` exits 0 with 0 failed
    - `evidence/defect-ledger.md` has four F-028 rows (plus one for friends-defect if moved)
  </acceptance_criteria>
  <done>"Not signed in" and "nothing here" are distinguishable on the four personalized routes, and no page shows anything different.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → write handler | direct API calls that bypass the proxy, the path F-088/F-089 exploit |
| handler → cookie client | unchanged reads and writes after the guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | a banned or un-onboarded caller writing through an arm the proxy did not stop | mitigate | 14 arms in GUARDED with the fixed-shape assertions (403 bytes, no write) in Task 1 acceptance |
| T-05-06-02 | Denial of Service | normal users locked out because a guard fires on a good profile (fixture or column mismatch) | mitigate | Phase 4 PRESERVE suites pass unedited; the full Playwright floor runs in 05-08 |
| T-05-06-03 | Tampering | a PRESERVE assertion edited to make the refactor pass | mitigate | diff of PRESERVE files must be deletions inside the two moved blocks only |
| T-05-06-04 | Information Disclosure | F-028 degraded 200s cacheable under the blanket s-maxage | mitigate | four routes now 401; the remaining cache work is Phase 6 (DEC-39) |
| T-05-06-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0, 0 failed
- `npx tsc --noEmit` and `npm run lint` clean
- `node scripts/check-characterization-tags.mjs --all` exits 0
</verification>

<success_criteria>
The events family decides ban, onboarding and missing-profile outcomes in the handler, the ban asymmetry is closed, F-028's four routes are honest about authentication, and nothing a PRESERVE suite holds moved.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** guard adoption in 11 route files and the four F-028 routes; `src/__tests__/api/events/ban-asymmetry-defect.test.ts`; GUARDED rows; `evidence/handler-adoption-events.txt`, `evidence/slice-3-fixture-completions.md`; ledger rows.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-06-SUMMARY.md` when done.
</output>
