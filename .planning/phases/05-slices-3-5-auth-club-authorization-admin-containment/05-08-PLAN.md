---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 08
type: execute
wave: 7
depends_on: ["05-07"]
files_modified:
  - e2e/specs/ban-and-onboarding-ring.spec.ts
  - e2e/specs/no-profile-row.spec.ts
  - .planning/audit/tools/classify-inventory.mjs
  - .planning/audit/inventory/classification-rules.md
  - .planning/audit/inventory/endpoints.json
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/contract-regen-slice-3.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-3-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.slice-3-after.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-close.md
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "Against the real local stack and a production build, a banned persona's /api/* call gets 403 JSON Account suspended and its page request lands on /banned; the mid-onboarding persona's direct POST gets 403 Onboarding required, its pages land on /onboarding even with the cookie deleted, and its two wizard calls still succeed"
    - "A signed-in account with no profile row gets 403 Profile not found on /api/* and is signed out to /?error=profile_sync_failed on a page"
    - "Every persona still signs in, non-McGill sign-in is still rejected, banned users are still blocked, organizers still reach their club surfaces — re-confirmed after slice 3 from a clean reset"
    - "The endpoint contract (endpoints.json expected_status) agrees with the code for F-028's four routes and for mid-onboarding callers on write-only routes"
    - "Slice 3's findings are closed in the register only where their validation criterion is met by committed evidence"
  artifacts:
    - path: "e2e/specs/ban-and-onboarding-ring.spec.ts"
      provides: "banned API 403 JSON, suspended and expired personas, mid-onboarding page and API rings, wizard exemptions"
      min_lines: 80
    - path: "e2e/specs/no-profile-row.spec.ts"
      provides: "a temporary auth user with no users row: API 403, page sign-out redirect, cleanup"
      min_lines: 60
    - path: ".planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-close.md"
      provides: "floor before/after, Playwright before/after, 16-row Validated re-confirmation, findings closed, intentional behaviour changes"
      min_lines: 100
  key_links:
    - from: "e2e/specs/no-profile-row.spec.ts"
      to: "e2e/env.ts"
      via: "localStackEnv() supplies the local service key for creating and deleting the temporary auth user; playwright.config.ts has already asserted the target is local"
      pattern: "localStackEnv\\(\\)"
    - from: ".planning/audit/tools/classify-inventory.mjs"
      to: ".planning/audit/inventory/classification-rules.md"
      via: "the R4 rule change and the four F-028 verdicts land in both in the same commit (DEC-55)"
      pattern: "mid_onboarding_student"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "endpoints.json is never hand-edited; the api.auth-debug row is not removed (DEC-55)"
    - "The seed is not changed (DEC-54); the temporary auth user is created and deleted by the spec"
    - "No source file under src/ changes in this plan"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Close slice 3: prove the auth ring end to end in the browser harness, bring the endpoint contract into agreement with the code, run the full floor from a clean reset, re-confirm every Validated workflow, and record the closures.

Purpose: ROADMAP criterion 5 and CONTEXT Area 4 require the harness and the Validated list after each slice, "in particular every persona can still sign in, non-McGill sign-in is still rejected, banned users are still blocked, and organizers still reach their club surfaces". Slice 4 may not start until this plan's floor is green.

Output: two Playwright specs, the regenerated contract, the slice-3 floor, `evidence/slice-3-close.md`, register updates and REFAC-11's state.
</objective>

<slice_outcome>
Green when `npx playwright test` passes from a clean reset with 0 failed and at least 40 plus the new tests, the Jest, pgTAP, lint, tsc, ratchet and tag-gate rows are at or above the before-floor, `validate.mjs --check findings` and `--check endpoints` pass, and `slice-3-close.md` answers every Validated bullet.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.before.txt
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000; webServer never reuses a server. 9. Evidence verbatim with exit codes. 10. findings.json surgical; FOUNDATION_AUDIT.md and endpoints.json only through their generators. 11. A requirement is measured against its own sentence.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Playwright specs for the ban, onboarding and no-profile-row rings</name>
  <files>e2e/specs/ban-and-onboarding-ring.spec.ts, e2e/specs/no-profile-row.spec.ts</files>
  <read_first>
    - e2e/specs/banned-redirect.spec.ts lines 1-60 — persona storage state, asserting on the URL and status, never on rendered content
    - e2e/fixtures.ts — IDS, storageStateFor, BANNED_PATH, SIGN_IN_AFFORDANCE
    - e2e/auth.setup.ts lines 55-120 — how a session is produced from the library's own cookie serializer (createServerClient + signInWithPassword, all setAll emissions accumulated, bypassCSP context, httpOnly false, sameSite Lax)
    - e2e/env.ts — localStackEnv(): url, anonKey, serviceRoleKey from `supabase status`
    - scripts/seed/personas.ts lines 168-330 — PERSONAS (names), IDS, the ban truth table
    - playwright.config.ts — one worker, the setup project, the guard on the local target
  </read_first>
  <action>
**`e2e/specs/ban-and-onboarding-ring.spec.ts`** — docblock naming F-062, F-088, F-089 and DEC-34/35/36. One describe per persona with `test.use({ storageState: storageStateFor(<persona>) })`:
- banned_permanent: `page.request.post("/api/events/" + IDS.approvedEvent + "/save")` → status 403, `content-type` header contains `application/json`, body deep-equals `{ error: "Account suspended" }`; `page.request.get("/api/users/saved-events")` → the same 403 JSON; `page.goto("/my-events")` → final pathname `BANNED_PATH`.
- suspended_active: the POST above → 403 `{ error: "Account suspended" }`.
- suspension_expired: `page.request.get("/api/users/saved-events")` → 200.
- mid_onboarding_student: `page.goto("/")` → pathname `/onboarding`; `await page.context().clearCookies({ name: "needs_onboarding" })` then `page.goto("/my-events")` → pathname `/onboarding` (database truth); the save POST → 403 `{ error: "Onboarding required" }`; `page.request.patch("/api/users/" + IDS.mid_onboarding_student, { data: { name: <this persona's seeded name from PERSONAS> } })` → 200 (exemption, no value changes); `page.request.post("/api/onboarding/complete")` → 200 `{ success: true }`.

**`e2e/specs/no-profile-row.spec.ts`** — docblock: DEC-35, F-088; the spec manages its own auth user because the persona setup cannot hold a session for a user the proxy signs out (DEC-54). `test.describe.serial`. beforeAll: `const stack = localStackEnv()`; an admin client `createClient(stack.url, stack.serviceRoleKey, { auth: { persistSession: false } })` creates `p5-no-profile@mail.mcgill.ca` with a random password and `email_confirm: true` (delete a leftover user with that email first); assert no `public.users` row exists for the new id; sign in exactly as `auth.setup.ts` does (createServerClient with accumulating setAll, signInWithPassword) and add the emitted cookies to a new browser context with `bypassCSP: true`. Test 1: the context's `request.post("/api/events/" + IDS.approvedEvent + "/save")` → 403 JSON `{ error: "Profile not found" }`. Test 2: a page in that context goes to `/` → final URL pathname `/` and `searchParams.get("error") === "profile_sync_failed"`; then `context.request.get("/api/users/saved-events")` → 401 (signed out). afterAll: `admin.auth.admin.deleteUser(id)` and close the context; assert the user is gone. Never print the service key or the password.

Run `npx playwright test e2e/specs/ban-and-onboarding-ring.spec.ts e2e/specs/no-profile-row.spec.ts` after `supabase db reset --local` and `npx tsx scripts/seed/load.ts` (port 3000 free first), record the output at the top of `evidence/playwright.slice-3-after.txt`. Commit `test(05-08): e2e ban, onboarding and no-profile-row rings`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -s .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.slice-3-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - Both specs pass against a clean reset and seed, recorded with exit=0 in `evidence/playwright.slice-3-after.txt`
    - `ban-and-onboarding-ring.spec.ts` contains the strings `Account suspended`, `Onboarding required`, `clearCookies`, `/api/onboarding/complete`
    - `no-profile-row.spec.ts` contains `profile_sync_failed`, `Profile not found`, `deleteUser`, and `localStackEnv()`
    - After the run, a service-client query for `p5-no-profile@mail.mcgill.ca` in auth.users returns no row (recorded)
    - `npx tsc --noEmit` exits 0 (specs are type-checked)
  </acceptance_criteria>
  <done>The ring's new behaviour is proven through a real browser, a real production build and the real local database, including the persona the seed cannot express.</done>
</task>

<task type="auto">
  <name>Task 2: Bring the endpoint contract into agreement with slice 3</name>
  <files>.planning/audit/tools/classify-inventory.mjs, .planning/audit/inventory/classification-rules.md, .planning/audit/inventory/endpoints.json, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/contract-regen-slice-3.txt</files>
  <read_first>
    - .planning/audit/tools/classify-inventory.mjs lines 1-40 (usage, merge-by-id contract), 100-160 (VERDICTS, the four F-028 rows' auth value), 270-400 (EXPECTED_OVERRIDES, derivePersonas R1-R14, main)
    - .planning/audit/inventory/classification-rules.md — § R4 (mid-onboarding) and § 6 row D-6 (F-028); the .md is the contract when the two disagree
    - evidence/phase-05-decisions.md § DEC-34, § DEC-39, § DEC-55
  </read_first>
  <action>
In `classify-inventory.mjs`: change the VERDICTS auth value of `api.events.following`, `api.events.friends-activity`, `api.events.friends-organizing` and `api.events.id.friends` (confirm each id in endpoints.json first) from anonymous to the authenticated verdict the file uses for signed-in-only routes, with a note citing F-028 and DEC-39; and change R4 in `derivePersonas` so that `mid_onboarding_student` is 403 when every method of the row is non-GET, the row id is not `api.users.id` or `api.onboarding.complete`, and the baseline student outcome is a success code — otherwise it inherits the student outcome as today — with a comment citing DEC-34. Update `classification-rules.md` § R4 and the D-6 row's status with the same rules in prose, naming DEC-34, DEC-39 and the commit. Run `node .planning/audit/tools/classify-inventory.mjs --dry-run`, then without it. With a node script, list every endpoints.json row whose `expected_status` changed between HEAD and the working tree, and require that the set is exactly the four F-028 rows plus the write-only rows whose mid_onboarding value moved to 403; paste the list into `evidence/contract-regen-slice-3.txt`. Run `node .planning/audit/tools/validate.mjs --check endpoints` and `--quick` (same pass count and the same by-design FAILs as the before-floor). Leave the `api.auth-debug` row in place (DEC-55). Commit `docs(05-08): endpoint contract agrees with slice 3 (F-028, DEC-34)`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check endpoints</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/audit/tools/validate.mjs --check endpoints` exits 0
    - `node -e 'const e=require("./.planning/audit/inventory/endpoints.json");const g=i=>e.find(r=>r.id===i).expected_status;process.exit(g("api.events.following").anonymous===401&&g("api.events.friends-activity").anonymous===401&&g("api.users.id").mid_onboarding_student!==403&&g("api.onboarding.complete").mid_onboarding_student!==403?0:1)'` exits 0
    - `evidence/contract-regen-slice-3.txt` lists every changed row id and nothing outside the allowed set
    - `classification-rules.md` names DEC-34 and DEC-39
  </acceptance_criteria>
  <done>The contract Phase 7 will generate its persona matrix from says what the code now does for slice 3's changes, derived by rule rather than typed.</done>
</task>

<task type="auto">
  <name>Task 3: Slice-3 floor from a clean reset, the close note, register and requirement updates</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-3-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/playwright.slice-3-after.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-close.md, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/deferred-items.md, .planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - evidence/floor.before.txt — every command and number to compare against
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md — the close-note sections
    - .planning/PROJECT.md § Requirements › Validated (16 bullets)
    - .planning/REQUIREMENTS.md line with REFAC-11 and its traceability row
    - the 05-02..05-07 SUMMARY files — "Deferred items found" sections
    - .planning/audit/findings.json records F-003, F-004, F-027, F-028, F-040, F-062, F-077, F-088, F-089 — each validation_criterion
  </read_first>
  <action>
**Floor.** Port 3000 free; `supabase db reset --local`; `supabase test db --local` (UNSEEDED); `npx tsx scripts/seed/load.ts`; `supabase test db --local` (SEEDED); a second reset plus seed; `npx playwright test`; then `npx jest --ci`, `npm run lint`, `npx tsc --noEmit`, `npm audit --audit-level=high --omit=dev`, `node scripts/check-elevated-ratchet.mjs`, `node scripts/check-migration-filenames.mjs`, `node scripts/check-characterization-tags.mjs --all`, `node .planning/audit/tools/validate.mjs --quick`, and the six censuses from the before-floor. Write `evidence/floor.slice-3-after.txt` (line 1 `head=<sha>`, summary table before vs after, raw blocks with exit codes) and append the full Playwright run to `evidence/playwright.slice-3-after.txt`. Required: Playwright 0 failed and passed at least the before-floor plus the new tests; Jest 0 failed with passed at or above the before-floor; pgTAP equal to the before-floor (no migration yet); ratchet exit 0 with live 24; env-assertion census 0; the legacy ban helper census 0; session-accessor census unchanged at the one health route. If Playwright fails once on a known flake, reset, seed and re-run once, keeping both logs; a second failure stops the plan.

**Close note.** Write `evidence/slice-3-close.md` (metadata line) with: the floor table; the Playwright before/after table; a Validated re-confirmation table with one row per PROJECT.md Validated bullet (16), each naming the test that re-confirms it after slice 3 or the diff showing it untouched — in particular every persona signs in (auth.setup, 10 setup tests), non-McGill sign-in rejected (callback PRESERVE test 4), banned users blocked (banned-redirect.spec and ban-and-onboarding-ring.spec), organizers reach club surfaces (club-owner-surfaces.spec), onboarding still completes (the wizard-call tests); the findings closed with their validation evidence; every INTENTIONAL BEHAVIOUR CHANGE of slice 3 with its commit; and a statement that no intentional visual change shipped on seeded data (the Playwright suite's existing assertions are unedited).

**Register.** Surgically set status `Fixed` with a `resolution` citing commits and `evidence/slice-3-close.md` for F-003, F-004, F-027, F-062, F-077, F-088, F-089 — each only if its validation_criterion is met by committed evidence (otherwise leave Open with a resolution naming the unmet clause). F-028: keep Open, set closes_in_phase to "06", resolution "four of eight routes answer 401 in Phase 5 (05-06, DEC-39); rsvp GET, clubs/[id]/events and notifications/count move with REFAC-19". F-040: add a resolution recording that the ADMIN_EMAILS reader is deleted (05-05) and the ADMIN_API_KEY reader goes in 05-14; status unchanged. Regenerate FOUNDATION_AUDIT.md; `validate.mjs --check findings` exits 0.

**Deferred items and requirement.** Add to `evidence/deferred-items.md` every item listed under "Deferred items found" in the 05-02..05-07 SUMMARYs, numbered from DI-44, each with found by, what, why not fixed, owner. Measure REFAC-11 against its sentence clause by clause in the close note; if every clause is evidenced, tick REFAC-11 in `.planning/REQUIREMENTS.md` and set its traceability row to `Complete (05-08) — see …/evidence/slice-3-close.md`; otherwise record PARTIAL naming the clause. Commit `docs(05-08): slice 3 close — floor, re-confirmation, register`.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && head -1 .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/floor.slice-3-after.txt | grep -E '^head=[0-9a-f]{40}$'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.slice-3-after.txt` ends with a run of 0 failed and exit=0 from a clean reset
    - `evidence/floor.slice-3-after.txt` Jest row shows 0 failed and passed at or above the before-floor; ratchet row `live=24`; env-assertion census 0
    - `evidence/slice-3-close.md` has a Validated table with as many rows as `grep -c '^- ✓' .planning/PROJECT.md` returns
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i);process.exit(["F-003","F-004","F-027","F-062","F-077","F-088","F-089"].every(i=>g(i).resolution)&&g("F-028").closes_in_phase==="06"?0:1)'` exits 0
    - `.planning/REQUIREMENTS.md` REFAC-11 row says `Complete (05-08)` or `PARTIAL` with the unmet clause named
  </acceptance_criteria>
  <done>Slice 3 is closed on a green harness from a clean database, every Validated workflow is re-confirmed, and the register says exactly what closed and why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → local auth admin API | the no-profile-row spec creates and deletes an auth user with the local service key |
| harness → local stack | every reset and seed targets 127.0.0.1 |
| contract generator → Phase 7 | the matrix Phase 7 asserts is generated from endpoints.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | the temporary auth user leaking into later specs or seeds | mitigate | afterAll deletes it and the plan records a query proving it is gone (Task 1 acceptance) |
| T-05-08-02 | Information Disclosure | the local service key or the temp password printed into evidence | mitigate | neither is logged; the evidence grep for key-shaped strings prints nothing before commit |
| T-05-08-03 | Tampering | a spec aimed at production | mitigate | playwright.config.ts's assertSeedTargetAllowed runs before the server starts; localStackEnv reads `supabase status` only |
| T-05-08-04 | Repudiation | a finding marked Fixed without its validation criterion met | mitigate | Fixed only with a resolution citing committed evidence; unmet clauses stay Open (Task 3 acceptance) |
| T-05-08-05 | Tampering | endpoints.json hand-edited | mitigate | only classify-inventory.mjs writes it; the changed-row list is proven to equal the allowed set |
| T-05-08-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx playwright test` green from a clean reset
- `npx jest --ci`, `npx tsc --noEmit`, `npm run lint` green
- `validate.mjs --check findings` and `--check endpoints` exit 0
</verification>

<success_criteria>
Slice 3 is proven end to end and closed, the contract agrees with the code, and slice 4 may start.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts`; the regenerated contract; `evidence/contract-regen-slice-3.txt`, `evidence/floor.slice-3-after.txt`, `evidence/playwright.slice-3-after.txt`, `evidence/slice-3-close.md`; register and REFAC-11 updates.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-08-SUMMARY.md` when done.
</output>
