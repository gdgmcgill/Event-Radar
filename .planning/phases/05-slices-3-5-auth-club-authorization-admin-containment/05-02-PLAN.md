---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/proxy-characterization.test.ts
  - src/proxy-defect.test.ts
  - src/app/auth/callback/route-defect.test.ts
  - src/lib/__tests__/env-assertions-defect.test.ts
  - src/server/__tests__/getsession-gate.test.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-characterization-ring.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-mutation-check-ring.txt
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "Every proxy behaviour slice 3 must keep (anonymous protected-route redirect with its encoded next, banned page redirect with the query preserved, expired suspension admitted, ban-exempt paths unread, onboarding exemptions, rate limit before auth) is pinned by a PRESERVE suite written against the unmodified proxy"
    - "Every proxy behaviour slice 3 must change (env pass-through, catch pass-through, failed ban read admitted, no-row admitted, banned /api/* answered with a 307, cookie-only onboarding) is pinned by a DEFECT suite citing F-003, F-062, F-088 or F-089"
    - "The callback's open redirect through next is pinned as DEFECT F-077 for absolute, protocol-relative and backslash values"
    - "The env non-null-assertion census and the one-getSession rule are machine checks, not prose"
    - "Every new assertion is proven to fail when the behaviour it pins is removed"
  artifacts:
    - path: "src/proxy-characterization.test.ts"
      provides: "PRESERVE suite for the proxy ring, mocked at @supabase/ssr"
      min_lines: 150
    - path: "src/proxy-defect.test.ts"
      provides: "DEFECT suite: F-003, F-062, F-088, F-089 today-shapes, flipped by 05-05"
      min_lines: 120
    - path: "src/app/auth/callback/route-defect.test.ts"
      provides: "DEFECT F-077 today-shapes; receives the moved F-004 tests in 05-05"
      min_lines: 80
    - path: "src/lib/__tests__/env-assertions-defect.test.ts"
      provides: "Census of process.env non-null assertions under src, today's exact set"
      min_lines: 40
    - path: "src/server/__tests__/getsession-gate.test.ts"
      provides: "PRESERVE gate: the only getSession( call under src is api/health"
      min_lines: 30
  key_links:
    - from: "src/proxy-characterization.test.ts"
      to: "src/proxy.ts"
      via: "PROTECTED_ROUTES re-derived by parsing the source literal, the e2e/fixtures.ts method"
      pattern: "PROTECTED_ROUTES"
    - from: "src/proxy-defect.test.ts"
      to: ".planning/audit/findings.json"
      via: "the leading docblock cites F-003, F-062, F-088, F-089, checked by the tag gate"
      pattern: "F-08[89]"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No production source file is modified: src/proxy.ts, src/app/auth/callback/route.ts, src/lib and src/app/api are byte-identical at the end of this plan"
    - "src/proxy.test.ts and src/app/auth/callback/route.test.ts are not edited"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 3, characterization part 1 of 2: pin the authentication ring — proxy, callback redirect target, env assertions, session accessor — before any of it changes.

Purpose: auth is the highest-blast-radius change in the program (ROADMAP: "Auth is deliberately slice 3"). The existing `src/proxy.test.ts` pins only the matcher (02-REVIEW WR-04 found it could not notice the redirect block being deleted), so the proxy's behaviour has no net today. CONTEXT Area 4 requires characterization first, file-level tags (C15), and research § B lists today's wire bytes.

Output: two proxy suites, the callback F-077 DEFECT file, two census gates, and mutation evidence — against unmodified source.
</objective>

<slice_outcome>
Green when all five new suites pass on the unmodified tree, the tag gate reports the new files, every mutation cycle in `evidence/slice-3-mutation-check-ring.txt` shows the named test going red, and `git diff --stat <05-01 head> -- src/proxy.ts src/app src/lib src/server/context.ts` prints nothing.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-CONTEXT.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: PRESERVE and DEFECT suites for the proxy ring, against the unmodified proxy</name>
  <files>src/proxy-characterization.test.ts, src/proxy-defect.test.ts</files>
  <read_first>
    - src/proxy.ts (whole file, 158 lines) — every branch pinned below; lines 51-56 (nothing between client creation and getUser), 92-111 (ban read and redirect), 114 (PROTECTED_ROUTES), 123-136 (cookie onboarding guard), 140-144 (catch)
    - src/proxy.test.ts lines 1-40 — the matcher PRESERVE suite's docblock voice; it is not edited
    - src/middlewareRateLimit.test.ts lines 1-80 — distinct-IP discipline for the shared globalThis bucket store
    - src/app/auth/callback/route.test.ts lines 60-120 — how @supabase/ssr is mocked with jest.mock and scripted jest.fn members
    - e2e/fixtures.ts lines 30-68 — the PROTECTED_ROUTES parser and signInRedirectFor's encodeURIComponent contract
    - 05-RESEARCH.md § B "Wire bytes today" — the exact today and after bytes per row
    - src/lib/ban.ts lines 9-13 — isBanned, the predicate the ring must agree with
  </read_first>
  <action>
Both files mock `@supabase/ssr` with `jest.mock`, returning a client whose `auth.getUser` and `auth.signOut` are scripted `jest.fn`s and whose `from("users").select(cols).eq("id", id).single()` chain returns a scripted `{ data, error }` and records the table, the column string and the eq arguments. Set placeholder `NEXT_PUBLIC_SUPABASE_URL` (`https://placeholder-project.supabase.co`) and `NEXT_PUBLIC_SUPABASE_ANON_KEY` in beforeEach and restore process.env afterAll. Build requests with `new NextRequest(url, { method, headers })` and cookies through the `cookie` header. Import `proxy` from `./proxy`. Give every test its own `x-forwarded-for` IP. Persona rows: active (onboarding_completed true, banned_at null), banned_permanent (banned_at set, expiry null), suspended_active (expiry 2099), suspension_expired (expiry 2020), unonboarded (onboarding_completed false). The mocked read returns the full row regardless of the column string, so the suite works before and after 05-05 narrows or widens the select.

**`src/proxy-characterization.test.ts`** — leading docblock: `PRESERVE characterization — src/proxy.ts ring behaviour (REFAC-11)`, Tests cover, WHY THE MOCK SEAM IS @supabase/ssr, Deliberately NOT pinned (the users select column list; response bodies of pass-through). Cases, each passing now and after 05-05:
1. For every path re-derived from the `PROTECTED_ROUTES` literal in `src/proxy.ts` (parse the source like `e2e/fixtures.ts:43`), plus `/profile/edit`: anonymous GET → 307, Location pathname `/`, `signin=required`, `next` equal to the path (assert via `encodeURIComponent` round-trip); `/profiles` anonymous → not redirected.
2. Anonymous GET `/` and GET `/api/events` → no Location header, status 200.
3. Active user on `/my-events` → no Location; exactly one `from("users")` read with eq `("id", <user id>)`.
4. banned_permanent GET `/events?x=1` → 307 Location pathname `/banned` with search `?x=1` preserved; suspended_active likewise to `/banned`.
5. suspension_expired on `/my-events` → no Location.
6. banned_permanent on `/banned` and on `/auth/signout` → no users read, no Location.
7. unonboarded user carrying cookie `needs_onboarding=1` on `/` → 307 `/onboarding`; the same user on `/onboarding`, `/api/events` and `/auth/signout` → no onboarding Location.
8. The 31st POST to `/api/events/x/save` from one IP within the window → 429 and `createServerClient` not constructed for that request (the limiter runs first).

**`src/proxy-defect.test.ts`** — leading docblock: `DEFECT characterization — F-003, F-062, F-088, F-089` with Subject, The defect, What this file is and is not, Registered as … Closes in Phase 5, and `Status: OPEN — assertions move in 05-05's proxy commit`. Each test title begins with its finding id; each asserts today's shape exactly:
a. F-003: `NEXT_PUBLIC_SUPABASE_URL` deleted → `/profile` anonymous is passed through (no Location) and `createServerClient` is never called.
b. F-088: `auth.getUser` rejects → pass-through (no Location, status 200) and console.error called with the `[Middleware] Error:` prefix.
c. F-088: an active-shaped user whose users read returns `{ data: null, error: { code: "XX000" } }` → passed through as not banned.
d. F-088: users read returns `{ data: null, error: { code: "PGRST116" } }` on `/api/events/x/save` POST and on `/my-events` → both passed through; `auth.signOut` not called.
e. F-062: banned_permanent POST `/api/events/x/save` → 307 with Location pathname `/banned` (not JSON).
f. F-089: unonboarded user with NO `needs_onboarding` cookie on `/my-events` → passed through.
g. F-089: active (onboarded) user carrying a stale `needs_onboarding=1` cookie on `/` → 307 `/onboarding`.
Run both suites: `npx jest --ci src/proxy-characterization.test.ts src/proxy-defect.test.ts`, append output to `evidence/slice-3-characterization-ring.txt`. Commit `test(05-02): proxy ring PRESERVE and DEFECT characterization`.
  </action>
  <verify>
    <automated>npx jest --ci src/proxy-characterization.test.ts src/proxy-defect.test.ts src/proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci src/proxy-characterization.test.ts src/proxy-defect.test.ts` exits 0 on the unmodified proxy
    - `node scripts/check-characterization-tags.mjs src/proxy-characterization.test.ts src/proxy-defect.test.ts` exits 0
    - `src/proxy-defect.test.ts` leading docblock contains `F-003`, `F-062`, `F-088`, `F-089` and `Status: OPEN`
    - `src/proxy-characterization.test.ts` reads `src/proxy.ts` to derive PROTECTED_ROUTES rather than restating the eight paths (contains `readFileSync`)
    - `git diff --stat HEAD~1 -- src/proxy.ts src/proxy.test.ts` prints nothing
  </acceptance_criteria>
  <done>The proxy's behaviour, not just its matcher, has a net: what must survive is PRESERVE, what must change is DEFECT with its finding named.</done>
</task>

<task type="auto">
  <name>Task 2: Callback F-077 DEFECT pins, the env-assertion census, and the one-getSession gate</name>
  <files>src/app/auth/callback/route-defect.test.ts, src/lib/__tests__/env-assertions-defect.test.ts, src/server/__tests__/getsession-gate.test.ts</files>
  <read_first>
    - src/app/auth/callback/route.ts (whole file) — line 37 reads next with no shape check, the final redirect builds new URL(next, requestUrl.origin)
    - src/app/auth/callback/route.test.ts lines 1-230 — the three mock seams, loadRoute(), signedInAs(), the beforeEach defaults; copy the scaffolding, do not import from the PRESERVE file and do not edit it
    - .planning/audit/findings.json record F-077 — its validation criterion
    - 05-RESEARCH.md § A "Env non-null assertions" table — the expected census (six files) and § A "getSession() clause"
    - .planning/audit/authz/getsession-register.md — the one server getSession call and why it is non-gating
  </read_first>
  <action>
**`src/app/auth/callback/route-defect.test.ts`** — leading docblock `DEFECT characterization — F-077 (and F-004 once 05-05 moves tests 7 and 8 here)`, with Subject, The defect, `Status: OPEN — assertions move in 05-05's callback commit`. Reproduce the PRESERVE suite's three mocks and helpers locally. For an already-onboarded McGill user (profile `{ onboarding_completed: true, roles: ["user"] }`), measure first with a node probe what `new URL(value, "https://callback.test")` yields for each value, then pin today's redirect: `next=https%3A%2F%2Fevil.test%2Fx` → Location origin `https://evil.test`; `next=%2F%2Fevil.test%2Fx` → Location host `evil.test`; `next=%2F%5Cevil.test%2Fx` (slash-backslash) → Location host `evil.test` (or whatever the probe measured — pin the measurement and record it in the evidence file); control `next=%2Fmy-events` → pathname `/my-events` on `callback.test`.

**`src/lib/__tests__/env-assertions-defect.test.ts`** — docblock `DEFECT characterization — F-003 (config half: process.env non-null assertions)`, `Status: OPEN — the expected set shrinks in 05-04 and empties in 05-05`. The test walks `src/` with `fs`, skips `*.test.ts`, `*.test.tsx` and `__tests__/`, matches the regex for a `process.env.<UPPER_SNAKE>` member access followed by `!`, and asserts the sorted list of `file:VARIABLE` pairs equals an inline expected list. Derive that list from the tree first (`command grep -rnoE 'process\.env\.[A-Z_]+!' src`), expected per research: `src/lib/supabase/server.ts` URL and ANON, `src/lib/supabase/client.ts` URL and ANON, `src/lib/supabase/service.ts` URL and SERVICE_ROLE_KEY, `src/app/auth/callback/route.ts` URL, ANON, URL, SERVICE_ROLE_KEY, `src/app/auth/signout/route.ts` URL and ANON, `src/app/api/auth-debug/route.ts` its three. Pin the measured list, not the research list, and record any difference.

**`src/server/__tests__/getsession-gate.test.ts`** — docblock `PRESERVE characterization gate — REFAC-11 clause 1: getUser() at every authorization decision`. Walk `src/` excluding test files and assert the set of files containing a `getSession(` call is exactly `["src/app/api/health/route.ts"]` (non-gating, REFAC-22's, Phase 6), and that `src/server/context.ts` and `src/proxy.ts` each contain `auth.getUser(`. Passes before and after the whole phase.

Run the three: `npx jest --ci src/app/auth/callback/route-defect.test.ts src/lib/__tests__/env-assertions-defect.test.ts src/server/__tests__/getsession-gate.test.ts` and the PRESERVE callback suite unedited; append to `evidence/slice-3-characterization-ring.txt`. Commit `test(05-02): callback redirect-target, env-assertion and getSession gates`.
  </action>
  <verify>
    <automated>npx jest --ci src/app/auth/callback src/lib/__tests__/env-assertions-defect.test.ts src/server/__tests__/getsession-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three new suites and `src/app/auth/callback/route.test.ts` pass on unmodified source
    - `route-defect.test.ts` contains a case for each of an absolute, a protocol-relative and a slash-backslash `next`, and a control case with `/my-events`
    - `evidence/slice-3-characterization-ring.txt` records the node probe output for the three hostile values
    - The env census suite's expected list was derived from the tree: the evidence file contains the `command grep -rnoE` output next to the pinned list
    - `git diff --stat HEAD~1 -- src/app/auth/callback/route.ts src/app/auth/callback/route.test.ts src/lib/supabase` prints nothing
  </acceptance_criteria>
  <done>The open redirect, the env assertions and the session-accessor rule are each a test that goes red the moment the thing it measures changes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove every new assertion bites, prove the source is unmodified, run the tag gate</name>
  <files>.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-mutation-check-ring.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/slice-3-characterization-ring.txt</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-mutation-check.txt — the cycle format: mutation, command, expected red test, observed, restore proof
    - the five suites written in Tasks 1-2
  </read_first>
  <action>
Run at least these mutation cycles, each as: apply a one-line mutation to the SOURCE (never the test), run the named suite, record the red test names, restore with `git checkout -- <file>`, record `git diff --exit-code -- <file>` exit 0:
1. proxy: delete the `searchParams.set("next", path)` line → PRESERVE case 1 red.
2. proxy: set the banned redirect pathname to `/` → PRESERVE case 4 red.
3. proxy: invert the onboarding `path !== "/onboarding"` exemption → PRESERVE case 7 red.
4. proxy: replace the env pass-through return with a throw → DEFECT a red.
5. proxy: make the catch rethrow → DEFECT b red.
6. callback: replace the next read with the constant "/" → DEFECT F-077 hostile-value cases red.
7. src/lib/supabase/server.ts: remove one `!` → env census red.
8. create `src/tmp-getsession-probe.ts` containing a getSession( call → gate red; delete the file.
Append all cycles to `evidence/slice-3-mutation-check-ring.txt`. Then record the unmodified proof in `evidence/slice-3-characterization-ring.txt`: `git diff --stat <05-01 head> -- src/proxy.ts src/proxy.test.ts src/app src/lib src/server/context.ts` prints nothing, and `node scripts/check-characterization-tags.mjs --all` exits 0 with its final `ok <n> files` line (n = the before-floor count plus the new suites it discovers). Run `npx tsc --noEmit` (test files are type-checked). Commit `docs(05-02): ring characterization mutation evidence`.
  </action>
  <verify>
    <automated>node scripts/check-characterization-tags.mjs --all && npx tsc --noEmit && git diff --exit-code -- src/proxy.ts src/app/auth/callback/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/slice-3-mutation-check-ring.txt` records 8 cycles, each with at least one named red test and a restore line showing exit 0
    - `node scripts/check-characterization-tags.mjs --all` exits 0 and its count exceeds the before-floor's by the number of new tagged suites
    - `npx tsc --noEmit` exits 0
    - `git status --porcelain -- src/` shows no modified tracked production file
  </acceptance_criteria>
  <done>Every pin in the ring net is shown to fail when its behaviour is removed, on a source tree proven untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → source tree | mutation cycles edit production files temporarily; a forgotten restore would ship a mutation |
| characterization → later refactor | a wrong today-pin launders a defect into a PRESERVE contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | a mutation left in src/proxy.ts after a cycle | mitigate | every cycle ends with `git diff --exit-code -- <file>`; Task 3 verify runs it for proxy and callback |
| T-05-02-02 | Elevation of Privilege | an open redirect frozen as PRESERVE (the F-077 history) | mitigate | hostile next values live only in the DEFECT file; the PRESERVE callback suite is untouched |
| T-05-02-03 | Repudiation | a DEFECT suite citing no registered id | mitigate | tag gate run per file and with --all (Task 1, Task 3 acceptance) |
| T-05-02-04 | Information Disclosure | a real key used as a test placeholder | mitigate | placeholders only (`placeholder-project.supabase.co`); `.env.local` never read |
| T-05-02-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci src/proxy-characterization.test.ts src/proxy-defect.test.ts src/app/auth/callback src/lib/__tests__/env-assertions-defect.test.ts src/server/__tests__/getsession-gate.test.ts` exits 0
- `node scripts/check-characterization-tags.mjs --all` exits 0
- No production file under src/ modified
</verification>

<success_criteria>
The auth ring has a characterization net that distinguishes what slice 3 must preserve from what it must fix, every pin proven to bite, before one line of the ring changes.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/proxy-characterization.test.ts` (PRESERVE), `src/proxy-defect.test.ts` (DEFECT F-003/F-062/F-088/F-089), `src/app/auth/callback/route-defect.test.ts` (DEFECT F-077), `src/lib/__tests__/env-assertions-defect.test.ts` (DEFECT F-003), `src/server/__tests__/getsession-gate.test.ts` (PRESERVE gate), `evidence/slice-3-characterization-ring.txt`, `evidence/slice-3-mutation-check-ring.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-02-SUMMARY.md` when done. Any deferred item found is listed under "Deferred items found" in the SUMMARY; 05-08 registers it (this plan does not edit `evidence/deferred-items.md`, so it can share wave 2 with 05-03).
</output>
