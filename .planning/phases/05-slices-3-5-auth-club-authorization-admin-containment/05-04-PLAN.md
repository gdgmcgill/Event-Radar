---
phase: 05-slices-3-5-auth-club-authorization-admin-containment
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - src/lib/env.ts
  - src/lib/env.test.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/service.ts
  - src/app/auth/signout/route.ts
  - src/app/api/auth-debug/route.ts
  - src/lib/__tests__/env-assertions-defect.test.ts
  - src/instrumentation.ts
  - src/instrumentation.test.ts
  - src/server/db/elevated/index.ts
  - src/server/context.ts
  - src/server/__tests__/context.test.ts
  - src/server/authz/requireActiveUser.ts
  - src/server/authz/requireOnboarded.ts
  - src/server/__tests__/requireActiveUser.test.ts
  - src/server/__tests__/requireOnboarded.test.ts
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/env-and-guards.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/build-ci-env.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/boot-check.txt
  - .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-11]

must_haves:
  truths:
    - "No Supabase client factory, the sign-out route or any module reads a required env var with a non-null assertion; each read goes through a validated reader that throws MissingEnvError naming the variable when it is absent or blank"
    - "No module throws on import when env is absent: next build succeeds with CI's placeholder public vars and a blank service key, and src/proxy.test.ts still imports ./proxy with no env"
    - "A server that starts without its required env refuses to serve (register() throws), except in the build phase and the edge runtime"
    - "The seam can decide ban, onboarding and missing-profile outcomes with the exact bytes DEC-34/DEC-35 fix: 401 Unauthorized, 403 Account suspended, 403 Onboarding required, 403 Profile not found"
    - "/api/auth-debug no longer exists (F-027)"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "MissingEnvError, requireEnvValue, supabaseUrl, supabaseAnonKey, isVercelProduction — lazy, literal process.env member reads"
      exports: ["MissingEnvError", "requireEnvValue", "supabaseUrl", "supabaseAnonKey", "isVercelProduction"]
      min_lines: 40
    - path: "src/instrumentation.ts"
      provides: "register() boot completeness check"
      exports: ["register"]
    - path: "src/server/authz/requireActiveUser.ts"
      provides: "401 / 403 Profile not found / 403 Account suspended, composing requireUser and isBanned"
      exports: ["requireActiveUser"]
    - path: "src/server/authz/requireOnboarded.ts"
      provides: "passes anonymous, 403 Profile not found, 403 Onboarding required"
      exports: ["requireOnboarded", "OnboardingGuardResult"]
  key_links:
    - from: "src/lib/supabase/service.ts"
      to: "src/lib/env.ts"
      via: "serviceRoleKey() wraps the literal SUPABASE_SERVICE_ROLE_KEY member read in requireEnvValue; the read stays in the lint home"
      pattern: "requireEnvValue\\(\"SUPABASE_SERVICE_ROLE_KEY\""
    - from: "src/server/authz/requireActiveUser.ts"
      to: "src/lib/ban.ts"
      via: "isBanned is the one ban predicate"
      pattern: "isBanned"
    - from: "src/instrumentation.ts"
      to: "src/server/db/elevated/index.ts"
      via: "assertElevatedConfigured() checks the service key without instrumentation importing the service module"
      pattern: "assertElevatedConfigured"
  prohibitions:
    - "Never push to production: no supabase db push, no --linked flag, no production read or write, no git push"
    - "Never edit an applied migration; fix forward with a new file"
    - "Never widen a route handler's signature to make a test compile"
    - "Never stage with git add -A or git add ."
    - "Never add a site-wide admin bypass to requireClubRole"
    - "No env value is read by computed key; no env read happens at module evaluation"
    - "The service-role key is read only inside src/lib/supabase/service.ts"
    - "No handler adopts the new guards in this plan (05-05..05-07 do); src/proxy.ts and src/app/auth/callback/route.ts are not modified"
---

## Phase Goal

ROADMAP Phase 5 goal, verbatim: "Authorization decisions happen in exactly one fails-closed place, cross-tenant and privilege-escalation paths are denied at both the authz ring and the RLS ring, and the service-role client has exactly one registered door." (Outcome-shaped, not a user story; surfaced, not invented.)

<objective>
Slice 3, refactor part 1: build what the ring will stand on — validated configuration (the `!` half of REFAC-11 and F-003's config half, per DEC-37) and the two new seam guards (DEC-34, DEC-35) — and delete `/api/auth-debug` (F-027, DEC-39).

Purpose: the proxy (05-05) and every write handler (05-06, 05-07) consume these. Research C8/C9 measured the two ways a literal reading of CONTEXT breaks the build and the lint rule; this plan implements the corrected shape and proves it against CI's own environment.

Output: `src/lib/env.ts` and its tests, the four readers rewired, a boot check with a build-phase guard, `requireActiveUser` and `requireOnboarded` with tests, the profile slice widened, auth-debug deleted, and the env census moved one step.
</objective>

<slice_outcome>
Green when the env, instrumentation, context and guard suites pass, `npm run build` with CI's env exits 0, the env-assertion census holds only the callback's four entries, every Phase 4 PRESERVE suite and every 05-02/05-03 suite passes unedited, and `/api/auth-debug` is gone.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-RESEARCH.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-PATTERNS.md
@.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/phase-05-decisions.md
</context>

<phase_locked_constraints>
1. Behaviour preservation; deliberate changes carry INTENTIONAL BEHAVIOUR CHANGE. 2. Characterize first; tags are file-level; PRESERVE files never edited to pass; pins move to `-defect.test.ts` in the fix commit with a ledger row. 3. Production never touched; `.env.local` never read. 4. Migrations fix-forward only. 5. Correct against migrated local and unmigrated production schemas. 6. Jest only; tests type-checked; handlers typed as their own type. 7. Stage explicit paths only. 8. One executor owns Docker and port 3000. 9. Evidence verbatim with exit codes. 10. Registers edited surgically.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lazy validated config, the four readers rewired, auth-debug deleted, env census step one</name>
  <files>src/lib/env.ts, src/lib/env.test.ts, src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/supabase/service.ts, src/app/auth/signout/route.ts, src/app/api/auth-debug/route.ts, src/lib/__tests__/env-assertions-defect.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/env-and-guards.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/build-ci-env.txt, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/defect-ledger.md</files>
  <read_first>
    - src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/supabase/service.ts, src/app/auth/signout/route.ts, src/app/api/auth-debug/route.ts — the six reads being replaced and the route being deleted
    - eslint.config.mjs lines 60-110 — the no-restricted-syntax rule banning the service-key member read outside src/lib/supabase/** and src/server/db/elevated/**
    - scripts/seed/guard.ts lines 1-40 — the fail-closed docblock voice to copy
    - 05-RESEARCH.md § Corrections C8, C9 and § Code Examples "Lazy validated env"
    - .github/workflows/ci.yml lines 20-30 — CI's placeholder public vars and absent service key
    - src/lib/__tests__/env-assertions-defect.test.ts — the census and its expected list
    - src/proxy.test.ts — imports ./proxy with no Supabase env; must keep passing
  </read_first>
  <behavior>
    - Importing src/lib/env.ts with every Supabase and Upstash variable unset does not throw
    - supabaseUrl() and supabaseAnonKey() throw MissingEnvError whose message names the variable when it is unset, and when it is whitespace only
    - each reader returns the value when set
    - isVercelProduction() is true only when VERCEL_ENV is "production"; NODE_ENV "production" alone gives false
    - MissingEnvError instances have name "MissingEnvError" and are instanceof Error
  </behavior>
  <action>
Write `src/lib/env.test.ts` first (RED), then `src/lib/env.ts` (GREEN): export class `MissingEnvError` (name `"MissingEnvError"`, message `Missing required environment variable: <NAME>`), `requireEnvValue(name: string, value: string | undefined): string` (throws when undefined or trimmed-empty), `supabaseUrl()` and `supabaseAnonKey()` each returning `requireEnvValue` over the literal `process.env.NEXT_PUBLIC_SUPABASE_URL` / `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY` member read, and `isVercelProduction()` (`process.env.VERCEL_ENV === "production"`). Nothing is read at module evaluation, nothing by computed key. Docblock in the guard.ts voice: WHAT IT READS, IT FAILS CLOSED AT FIRST READ, why not at import (C8: CI build has no service key; proxy.test.ts imports the proxy with no env), why production is VERCEL_ENV (the harness runs next start under NODE_ENV=production).

Rewire: `src/lib/supabase/server.ts` → `supabaseUrl()`, `supabaseAnonKey()`; `src/lib/supabase/client.ts` keeps literal member reads for build-time inlining, each wrapped: `requireEnvValue("NEXT_PUBLIC_SUPABASE_URL", process.env.NEXT_PUBLIC_SUPABASE_URL)` and the anon equivalent; `src/lib/supabase/service.ts` exports `serviceRoleKey()` returning `requireEnvValue("SUPABASE_SERVICE_ROLE_KEY", process.env.SUPABASE_SERVICE_ROLE_KEY)` and `createServiceClient()` uses `supabaseUrl()` and `serviceRoleKey()`; `src/app/auth/signout/route.ts` → `supabaseUrl()`, `supabaseAnonKey()`.

Delete `src/app/api/auth-debug/route.ts` (F-027) after `command grep -rn "auth-debug" src e2e` shows no caller (record the output). Do not touch `.planning/audit/inventory/endpoints.json` (DEC-55).

Move the env census one step: in `src/lib/__tests__/env-assertions-defect.test.ts` change only the expected list to the callback's four entries, and add a ledger row (F-003, old list, new list, commit). Run the census suite, `npx jest --ci src/lib src/proxy.test.ts src/proxy-characterization.test.ts src/proxy-defect.test.ts src/server`, `npm run lint` and `npx tsc --noEmit`. Prove C8 into `evidence/build-ci-env.txt`: `SUPABASE_SERVICE_ROLE_KEY= NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key npm run build` exits 0 (an empty but defined variable is not overridden by Next's env loader, research C8). Commit `refactor(05-04): validated lazy config replaces env non-null assertions; delete /api/auth-debug (F-003, F-027)`.
  </action>
  <verify>
    <automated>npx jest --ci src/lib/env.test.ts src/lib/__tests__/env-assertions-defect.test.ts src/proxy.test.ts && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/env.ts` contains `export class MissingEnvError`, `export function requireEnvValue(`, `export function supabaseUrl(` (or an exported const), `export function isVercelProduction(`
    - `src/lib/supabase/service.ts` contains `requireEnvValue("SUPABASE_SERVICE_ROLE_KEY", process.env.SUPABASE_SERVICE_ROLE_KEY)` and exports `serviceRoleKey`
    - `test ! -e src/app/api/auth-debug/route.ts` succeeds
    - The env census suite passes with an expected list of exactly the four `src/app/auth/callback/route.ts` entries
    - `evidence/build-ci-env.txt` shows the build command with the blank service key and `exit=0`
    - `npm run lint` exits 0 with 0 errors; `npx tsc --noEmit` exits 0
    - `evidence/defect-ledger.md` has an F-003 row for the census step
  </acceptance_criteria>
  <done>Every required env read outside the callback is validated and lazy, CI's build still passes, and the debug route that echoed identities is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Boot completeness check in src/instrumentation.ts, guarded for build phase and edge runtime</name>
  <files>src/instrumentation.ts, src/instrumentation.test.ts, src/server/db/elevated/index.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/boot-check.txt</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md — register() runs once per server start, must complete before requests; file lives in src/ when a src folder is used
    - src/server/db/elevated/index.ts — the door; it may import the service module
    - src/lib/env.ts (Task 1)
    - 05-RESEARCH.md § Assumptions Log A1 — the build-phase guard is assumed; this task measures it
  </read_first>
  <behavior>
    - register() resolves when NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY and SUPABASE_SERVICE_ROLE_KEY are set
    - register() rejects with MissingEnvError naming the variable when any one is unset or blank
    - register() resolves without checking when NEXT_PHASE is "phase-production-build"
    - register() resolves without checking when NEXT_RUNTIME is not "nodejs"
  </behavior>
  <action>
Add `assertElevatedConfigured(): void` to `src/server/db/elevated/index.ts`, calling `serviceRoleKey()` from `@/lib/supabase/service` (the door may import it; instrumentation may not, by the lint boundary), with a docblock saying it constructs no client. Write `src/instrumentation.test.ts` first, then `src/instrumentation.ts` exporting `async function register()`: return immediately when `process.env.NEXT_RUNTIME !== "nodejs"` or `process.env.NEXT_PHASE === "phase-production-build"`; otherwise call `supabaseUrl()`, `supabaseAnonKey()` and `assertElevatedConfigured()`; on `MissingEnvError` log `console.error("[Config] refusing to start:", err.message)` and rethrow. The Upstash arm is added by 05-18 (leave a one-line note naming DEC-50). Tests set and delete process.env keys per case and restore them afterwards.

Measure into `evidence/boot-check.txt`: (a) the Task 1 CI-env build command again → exit 0 with instrumentation present (closes research A1 by measurement); (b) a start probe: from a scratchpad node script, spawn `npx next start -p 3100` with `SUPABASE_SERVICE_ROLE_KEY=` and placeholder public vars, capture stdout and stderr for 20 seconds or until exit, kill it, and record whether the process exited and whether `[Config] refusing to start` and `MissingEnvError` appear; then `lsof -i :3100` exit 1. Whatever Next does with a throwing register() is recorded as measured; the unit tests are the authority. Commit `feat(05-04): boot completeness check in register() (DEC-37)`.
  </action>
  <verify>
    <automated>npx jest --ci src/instrumentation.test.ts src/server/__tests__/elevated.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/instrumentation.ts` exports `register` and contains `phase-production-build` and `NEXT_RUNTIME`
    - `src/server/db/elevated/index.ts` exports `assertElevatedConfigured`
    - `npx jest --ci src/instrumentation.test.ts` passes with at least 6 cases (all set; each of three unset; build phase; edge runtime)
    - `evidence/boot-check.txt` holds the build exit=0 block, the start-probe output, and `lsof -i :3100` exit=1
    - `node scripts/check-elevated-ratchet.mjs` exits 0 (instrumentation does not enter the census)
  </acceptance_criteria>
  <done>A misconfigured server refuses to start where it serves traffic, and the build and edge runtime are provably unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: requireActiveUser and requireOnboarded, and the profile slice widened to the ban columns</name>
  <files>src/server/context.ts, src/server/__tests__/context.test.ts, src/server/authz/requireActiveUser.ts, src/server/authz/requireOnboarded.ts, src/server/__tests__/requireActiveUser.test.ts, src/server/__tests__/requireOnboarded.test.ts, .planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/evidence/env-and-guards.txt</files>
  <read_first>
    - src/server/authz/requireRole.ts (56 lines) and src/server/authz/requireUser.ts — docblock, imports, the Pick<RequestContext, "user" | "profile"> signature, the AuthGuardResult union, the fail-closed null-profile branch
    - src/server/__tests__/requireRole.test.ts lines 1-80 — makeContext and the deny-arm assertion style
    - src/server/context.ts lines 28-95 — RequestProfile, PROFILE_COLUMNS, the docblock paragraph "THE SEAM PERFORMS NO BAN CHECK" to rewrite
    - src/server/__tests__/context.test.ts lines 80-110 — the three-column assertion
    - src/lib/ban.ts lines 9-13 — isBanned
    - src/server/errors.ts — forbidden(message)
  </read_first>
  <behavior>
    - requireActiveUser: anonymous → 401 {"error":"Unauthorized"}; user with null profile → 403 {"error":"Profile not found"}; banned_at set and ban_expires_at null → 403 {"error":"Account suspended"}; ban_expires_at in the future → 403 Account suspended; ban_expires_at in the past → ok with the user; banned_at null → ok with the user
    - requireOnboarded: no user → { ok: true }; user with null profile → 403 {"error":"Profile not found"}; onboarding_completed false → 403 {"error":"Onboarding required"}; onboarding_completed null → 403 Onboarding required; true → { ok: true }
    - createRequestContext selects exactly id, roles, onboarding_completed, banned_at, ban_expires_at
  </behavior>
  <action>
Tests first. `src/server/authz/requireActiveUser.ts` exports `requireActiveUser(ctx: Pick<RequestContext, "user" | "profile">): AuthGuardResult`: compose `requireUser(ctx)` (its 401), then a null profile returns `forbidden("Profile not found")` (DEC-35), then `isBanned(ctx.profile)` returns `forbidden("Account suspended")`, else the authenticated result. `src/server/authz/requireOnboarded.ts` exports type `OnboardingGuardResult = { ok: true } | { ok: false; response: NextResponse }` and `requireOnboarded(ctx: Pick<RequestContext, "user" | "profile">): OnboardingGuardResult`: no user → ok (authentication is another guard's job, C2); null profile → `forbidden("Profile not found")`; `onboarding_completed !== true` → `forbidden("Onboarding required")`; else ok. Both files carry requireRole.ts-style docblocks naming DEC-34/DEC-35, the order handlers must call them in (requireActiveUser, then requireOnboarded, before any read), and the two onboarding exemptions that live at call sites, not in the guard.

In `src/server/context.ts` add `"banned_at" | "ban_expires_at"` to the RequestProfile Pick and set PROFILE_COLUMNS to `"id, roles, onboarding_completed, banned_at, ban_expires_at"`; rewrite the docblock paragraph to say the ban and onboarding guards now exist, name them, and record DI-35's closure; leave createRequestContext's body unchanged. Update the column-list assertion in `src/server/__tests__/context.test.ts` to the five columns (a seam unit test, not a characterization suite; note the change in `evidence/env-and-guards.txt`).

Run `npx jest --ci` in full: every Phase 4 PRESERVE suite and every 05-02/05-03 suite must pass unedited (no handler uses the guards yet; the wider select is served by the fakes' full rows). Append the counts to `evidence/env-and-guards.txt`. Commit `feat(05-04): requireActiveUser and requireOnboarded seam guards (DEC-34, DEC-35)`.
  </action>
  <verify>
    <automated>npx jest --ci src/server && npx jest --ci</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/authz/requireActiveUser.ts` contains `export function requireActiveUser(` and the strings `Profile not found` and `Account suspended`, and imports `isBanned` from `@/lib/ban`
    - `src/server/authz/requireOnboarded.ts` contains `export function requireOnboarded(`, `export type OnboardingGuardResult`, and `Onboarding required`
    - `src/server/context.ts` PROFILE_COLUMNS equals `"id, roles, onboarding_completed, banned_at, ban_expires_at"`
    - The two guard suites together hold at least 11 cases and assert bodies with `await result.response.json()`
    - `npx jest --ci` exits 0 with 0 failed and the passed count at or above the before-floor plus this plan's new tests; `git diff --stat <05-03 head> -- 'src/__tests__/api/events/*-characterization.test.ts' src/proxy-characterization.test.ts src/__tests__/api/auth-ring/write-handlers-characterization.test.ts` prints nothing
  </acceptance_criteria>
  <done>The seam can now make every ban, onboarding and missing-profile decision with the exact bytes the phase fixed, and nothing that exists today changed behaviour to get there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → server modules | required configuration enters here; an absent value must stop the server, not silently disable a control |
| browser bundle ← NEXT_PUBLIC_* | only literal member reads are inlined; the service key must never reach it |
| seam guard → every write handler | the guard's bytes become forty handlers' bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | a guard that returns ok on a null profile (fail-open) | mitigate | requireActiveUser and requireOnboarded deny null profile with 403 Profile not found; unit cases in acceptance |
| T-05-04-02 | Denial of Service | an import-time env throw breaking next build in CI or every page at runtime | mitigate | readers are lazy; env.test.ts asserts import does not throw; CI-env build exit 0 in evidence |
| T-05-04-03 | Information Disclosure | the service key read outside its lint home or inlined into the browser bundle | mitigate | the read stays in src/lib/supabase/service.ts as a literal member access; `npm run lint` 0 errors; client.ts reads only NEXT_PUBLIC_* |
| T-05-04-04 | Denial of Service | register() failing the production build | mitigate | NEXT_PHASE build guard; CI-env build exit 0 with instrumentation present |
| T-05-04-05 | Information Disclosure | /api/auth-debug echoing id and email under a shared cache (F-027) | mitigate | route deleted; `test ! -e` in acceptance |
| T-05-04-SC | Tampering | package installs | accept | none |
</threat_model>

<verification>
- `npx jest --ci` exits 0, 0 failed
- `npm run lint` 0 errors, `npx tsc --noEmit` exit 0
- CI-env `npm run build` exit 0 recorded
- `node scripts/check-characterization-tags.mjs --all` exits 0
</verification>

<success_criteria>
Validated configuration and the two fail-closed seam guards exist and are proven, the CI build is unaffected, and no existing behaviour changed.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Contributed by this plan:** `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`), `serviceRoleKey` in `src/lib/supabase/service.ts`, `src/instrumentation.ts` (`register`), `assertElevatedConfigured` in the door, `src/server/authz/requireActiveUser.ts`, `src/server/authz/requireOnboarded.ts` (`OnboardingGuardResult`), the widened `RequestProfile`, `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts`; deleted `src/app/api/auth-debug/route.ts`; `evidence/env-and-guards.txt`, `evidence/build-ci-env.txt`, `evidence/boot-check.txt`.

**Phase-wide roll-up (identical in every Phase 5 plan):**
- Modules and symbols: `src/lib/env.ts` (`MissingEnvError`, `requireEnvValue`, `supabaseUrl`, `supabaseAnonKey`, `isVercelProduction`, `upstashConfig`); `serviceRoleKey` in `src/lib/supabase/service.ts`; `src/instrumentation.ts` (`register`); `assertElevatedConfigured` in `src/server/db/elevated/index.ts`; `src/server/authz/requireActiveUser.ts` (`requireActiveUser`); `src/server/authz/requireOnboarded.ts` (`requireOnboarded`, `OnboardingGuardResult`); `CLUB_ROLES` and the narrowed `ClubRole` in `src/server/authz/requireClubRole.ts`; `src/server/ratelimit/types.ts` (`RateLimitStore`, `RateDecision`), `src/server/ratelimit/policy.ts` (`rateLimitPolicy`, `clientIp`, `tooManyRequests`, `ADMIN_BUDGETS`), `src/server/ratelimit/memoryStore.ts` (`MemoryRateLimitStore`), `src/server/ratelimit/upstashStore.ts` (`UpstashRateLimitStore`), `src/server/ratelimit/index.ts` (`applyRateLimit`, `getRateLimitStore`); `src/server/csrf.ts` (`isCrossSiteMutation`, `crossSiteBlocked`)
- Deleted: `src/app/api/auth-debug/route.ts` (05-04), the legacy ban helper in `src/lib/ban.ts` (05-07), `src/lib/admin.ts` (05-13)
- Migrations: `supabase/migrations/20260923120000_events_insert_club_scope.sql` (05-11, F-008), `supabase/migrations/20260923130000_users_grants_audit_log_insert.sql` (05-16, F-006/F-007)
- pgTAP: `supabase/tests/database/060-club-tenant-isolation.test.sql` (05-11), `050-users-privilege-escalation.test.sql` and `055-admin-audit-log-insert.test.sql` (05-16)
- Jest suites: `src/proxy-characterization.test.ts`, `src/proxy-defect.test.ts`, `src/app/auth/callback/route-defect.test.ts`, `src/lib/__tests__/env-assertions-defect.test.ts`, `src/server/__tests__/getsession-gate.test.ts` (05-02); `src/__tests__/api/auth-ring/writeHandlerTable.ts` (helper), `src/__tests__/api/auth-ring/write-handlers-characterization.test.ts`, `src/__tests__/api/auth-ring/write-handlers-ring-defect.test.ts`, `src/__tests__/api/events/anonymous-personalized-defect.test.ts` (05-03); `src/lib/env.test.ts`, `src/instrumentation.test.ts`, `src/server/__tests__/requireActiveUser.test.ts`, `src/server/__tests__/requireOnboarded.test.ts` (05-04); `src/__tests__/api/events/ban-asymmetry-defect.test.ts` (05-06); `src/__tests__/api/clubs/club-gates-characterization.test.ts`, `src/__tests__/api/clubs/club-owner-writes-defect.test.ts` (05-09); `src/__tests__/api/admin/adminArmTable.ts` (helper), `src/__tests__/api/admin/admin-guard-characterization.test.ts`, `src/__tests__/api/admin/admin-guard-defect.test.ts`, `src/__tests__/api/admin/admin-users-patch-defect.test.ts`, `src/__tests__/pages/public-profile-defect.test.tsx`, `src/__tests__/api/events/pending-edits-defect.test.ts` (05-12); `src/__tests__/api/service-role-routing.test.ts` (05-15); `src/server/ratelimit/policy.test.ts`, `src/server/ratelimit/memoryStore.test.ts`, `src/server/__tests__/csrf.test.ts` (05-17); `src/server/ratelimit/upstashStore.test.ts`, `src/server/ratelimit/upstash.contract.test.ts` (05-18, skipped without a store)
- Playwright specs: `e2e/specs/save-and-rsvp.spec.ts` DI-38 fix (05-01); `e2e/specs/ban-and-onboarding-ring.spec.ts`, `e2e/specs/no-profile-row.spec.ts` (05-08); `e2e/specs/club-authorization.spec.ts` (05-09); `e2e/specs/club-invitation-acceptance.spec.ts` (05-11); `e2e/specs/admin-guard.spec.ts`, `e2e/specs/csrf-origin.spec.ts` (05-12); `e2e/specs/admin-audit-row.spec.ts` (05-14); `e2e/specs/admin-write-paths.spec.ts`, `e2e/specs/public-profile-privacy.spec.ts` (05-15)
- REGISTRY.md rows (`src/server/db/elevated/REGISTRY.md`): auth-user deletion and first-sign-in profile sync (05-05); owner club edit and soft-delete, owner member-role change and ownership transfer, club-deletion audit record (05-10); popularity recompute, admin role change, audit-log writer (05-14); notify another user, set another user's roles on approval, ban and unban, club appeal status write, cross-user name reads, create a club for its creator, batch score computation, friend suggestions, public profile of another user (05-15)
- Registers: findings F-086..F-091 and the C16 re-pointing (05-01); decisions DEC-33..DEC-57; deferred DI-41..DI-43; `evidence/defect-ledger.md` rows per fix
- Packages: `@upstash/ratelimit` 2.1.0 and `@upstash/redis` 1.38.4 exact (05-18); `@supabase/supabase-js` 2.116.0 (05-19)
- Evidence (`evidence/`): `floor.before.txt`, `di-38-fix.txt`, `findings-registration.txt`, `phase-05-decisions.md`, `deferred-items.md`, `defect-ledger.md`, `slice-3-characterization-ring.txt`, `slice-3-mutation-check-ring.txt`, `slice-3-characterization-handlers.txt`, `slice-3-mutation-check-handlers.txt`, `env-and-guards.txt`, `build-ci-env.txt`, `boot-check.txt`, `proxy-refactor.txt`, `callback-refactor.txt`, `handler-adoption-events.txt`, `handler-adoption-rest.txt`, `slice-3-fixture-completions.md`, `contract-regen-slice-3.txt`, `floor.slice-3-after.txt`, `playwright.slice-3-after.txt`, `slice-3-close.md`, `slice-4-characterization.txt`, `rls-ring-before.txt`, `slice-4-mutation-check.txt`, `club-guard-adoption.txt`, `schema-push-slice-4.txt`, `floor.slice-4-after.txt`, `playwright.slice-4-after.txt`, `slice-4-close.md`, `slice-5-characterization.txt`, `slice-5-mutation-check.txt`, `admin-guard-adoption.txt`, `admin-behaviours.txt`, `contract-regen-slice-5.txt`, `audit-writer.txt`, `service-role-migration.txt`, `allowlist-shrink.txt`, `rls-privilege-before.txt`, `schema-push-slice-5.txt`, `ratelimit.txt`, `csrf.txt`, `csrf-assessment.md`, `upstash-legitimacy.txt`, `upstash-install.txt`, `di-25-bump.txt`, `floor.phase-after.txt`, `playwright.phase-after.txt`, `slice-5-close.md`, `PHASE-5-COMPLETION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-slices-3-5-auth-club-authorization-admin-containment/05-04-SUMMARY.md` when done.
</output>
