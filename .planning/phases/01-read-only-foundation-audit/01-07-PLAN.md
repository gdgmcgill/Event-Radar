---
phase: 01-read-only-foundation-audit
plan: 07
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .planning/audit/authz/service-role-register.json
  - .planning/audit/authz/service-role-register.md
  - .planning/audit/authz/getsession-register.md
  - .planning/audit/authz/fail-open-register.md
  - .planning/audit/quality/error-observability.md
autonomous: true
requirements: [AUDIT-07, AUDIT-09, AUDIT-10, AUDIT-14]

must_haves:
  truths:
    - "A reviewer can see every place the RLS-bypassing client is constructed, and whether each one actually needs the bypass"
    - "A reviewer can see every session-reading call and whether it gates authorization"
    - "A reviewer can see every authorization check that stops applying when an environment variable is unset, with the route that reaches it"
    - "A reviewer can see how many route handlers have no error handling at all and how unstructured the logging is, as integers"
  artifacts:
    - path: ".planning/audit/authz/service-role-register.json"
      provides: "AUDIT-07 machine-readable register with four justification answers per callsite"
      contains: "rls_bypass_required"
    - path: ".planning/audit/authz/service-role-register.md"
      provides: "Human view of the register with per-callsite rationale"
    - path: ".planning/audit/authz/getsession-register.md"
      provides: "AUDIT-09 classification of every session-reading callsite"
    - path: ".planning/audit/authz/fail-open-register.md"
      provides: "AUDIT-10 register of environment-variable-conditional authorization checks"
      contains: "ADMIN_API_KEY"
    - path: ".planning/audit/quality/error-observability.md"
      provides: "AUDIT-14 quantified error-handling and observability assessment"
  key_links:
    - from: ".planning/audit/authz/service-role-register.json"
      to: ".planning/audit/inventory/endpoints.json"
      via: "row set is derived from the service-client signal rather than re-grepped"
      pattern: "uses_service_client"
    - from: ".planning/audit/authz/fail-open-register.md"
      to: "src/app/api/admin/calculate-popularity/route.ts"
      via: "the primary confirmed fail-open callsite, both handlers, with line numbers"
      pattern: "calculate-popularity"
    - from: ".planning/audit/quality/error-observability.md"
      to: ".planning/audit/inventory/endpoints.json"
      via: "counts are aggregated from the signals already captured per handler"
      pattern: "endpoints\\.json"
---

<objective>
Deliver the static authorization slice end-to-end: three registers that answer, from artifacts alone, where RLS is bypassed, where a session is read instead of a verified user, and where an authorization check silently stops applying — plus the quantified observability assessment that explains why none of it is currently visible in production.

Purpose: Access control is this phase's centre of gravity. These three registers are the inputs to Stage 3's authorization-core slices and to Stage 4's persona matrix. The observability numbers are the baseline REFAC-20 is measured against.
Output: `authz/service-role-register.{json,md}`, `authz/getsession-register.md`, `authz/fail-open-register.md`, `quality/error-observability.md`.
</objective>

## Phase Goal

> The true current state of the codebase is known with captured evidence — every endpoint, page, policy, dependency, and risk inventoried, and every finding recorded with a reproduction and a validation criterion — without a single source, config, dependency, or database change.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md
@.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md
@.planning/phases/01-read-only-foundation-audit/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register every service-role callsite with its four justification answers</name>
  <files>.planning/audit/authz/service-role-register.json, .planning/audit/authz/service-role-register.md</files>
  <read_first>
    - `.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md` § Phase Requirements → the AUDIT-07 row (the verified 25-file breakdown), § Validation Architecture Test Map → the AUDIT-07 row (the exact row-count rule), § Security Domain → V4 Access Control
    - `.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md` § Grep targets → the correct cookie-client-plus-getUser-plus-role-check pattern
    - `src/lib/supabase/service.ts` — the factory itself
    - `src/lib/audit.ts` — the non-route library callsite
    - `src/app/users/[id]/page.tsx` — the single page callsite, and the specific suspect for client-bundle reachability
    - `.planning/audit/inventory/endpoints.json` — the `signals.uses_service_client` set (22 route files)
    - `.planning/PROJECT.md` and `CLAUDE.md` — the stated rule that this client is for admin and cron routes only
  </read_first>
  <action>
    Derive the register's row set from `endpoints.json` rather than re-grepping: every row whose `signals.uses_service_client` is true, which is 22 route files today. Add the three non-route callsites explicitly — the library audit-logging module, the single page component, and the factory module itself — for 25 files total. If the derived count disagrees with `service_client_file_count` in `baseline/versions.txt`, reconcile before proceeding; a disagreement means a callsite is being missed.

    Write `.planning/audit/authz/service-role-register.json` with one object per callsite carrying: `file`, `route` (null for non-route callsites), the `endpoints.json` `id` when one exists, and the four AUDIT-07 justification answers as explicit non-null values — `rls_bypass_required` with a one-sentence rationale, `caller_authenticated_first` stating which check runs before the client is constructed and at which line, `user_input_used_as_filter` naming the parameter when true, and `reachable_from_client_bundle` with the reasoning. Add a `verdict` field taking one of justified, unjustified, or needs-decision, and a `notes` field for the rationale. Every one of the four answers must be non-null on every row — that is the requirement, and the validator enforces it.

    Pay specific attention to three classes. First, the page component: a page that constructs the RLS-bypassing client is the highest-risk callsite in the register because a page compiles toward the client boundary — cross-reference the client-bundle sweep artifact produced by plan 01-03 and record whether the sweep found any evidence of the key reaching the browser bundle. Second, any callsite where the caller is not authenticated before the client is constructed — that combination is an elevation-of-privilege finding candidate. Third, any callsite outside the admin and cron paths, since the project's own documented rule restricts this client to those, and every deviation from the stated rule is a finding.

    Generate `.planning/audit/authz/service-role-register.md` from the JSON as a table so the two cannot drift, with a leading summary line giving the total, the justified count, the unjustified count, and the needs-decision count. Every unjustified or needs-decision row becomes a finding candidate for plan 01-13, carrying the file path and line numbers.
  </action>
  <acceptance_criteria>
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/authz/service-role-register.json','utf8'));process.exit(r.length===25?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/authz/service-role-register.json','utf8'));process.exit(r.every(x=>x.rls_bypass_required!=null&&x.caller_authenticated_first!=null&&x.user_input_used_as_filter!=null&&x.reachable_from_client_bundle!=null)?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/authz/service-role-register.json','utf8'));process.exit(r.every(x=>['justified','unjustified','needs-decision'].includes(x.verdict))?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/authz/service-role-register.json','utf8'));process.exit(r.some(x=>x.file.includes('users/[id]/page.tsx'))?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/authz/service-role-register.json','utf8'));process.exit(r.some(x=>x.file.includes('lib/audit.ts'))?0:1)"` exits 0
    - `.planning/audit/authz/service-role-register.md` contains 25 table rows, verified by counting lines starting with a pipe followed by a source path
    - `node .planning/audit/tools/validate.mjs --check service-role` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check service-role && bash .planning/audit/tools/readonly-guard.sh</automated>
  </verify>
  <done>All 25 service-role callsites are registered with four non-null justification answers and a verdict each, the row set is derived from the endpoint inventory rather than re-grepped, the page and library callsites are present, and the Markdown view is generated from the JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every session-reading call and every environment-variable-conditional authorization check</name>
  <files>.planning/audit/authz/getsession-register.md, .planning/audit/authz/fail-open-register.md</files>
  <read_first>
    - `.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md` § Phase Requirements → the AUDIT-09 and AUDIT-10 rows (the verified callsite counts and the two confirmed fail-open shapes), § Security Domain → "Known Threat Patterns for this stack", § State of the Art → the row correcting the plural-getSession claim
    - `.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md` § Grep targets → the fail-open shape with its generalized detector regex and the two affected handler line ranges
    - `src/app/api/admin/calculate-popularity/route.ts` — both exported handlers
    - `src/app/api/cron/send-reminders/route.ts` and `src/app/api/cron/send-feedback-requests/route.ts` — the contrasting fail-open and fail-closed cron shapes
    - `src/app/api/health/route.ts` — the single session-reading callsite
    - `.planning/audit/inventory/endpoints.json` — `signals.calls_get_session` and `signals.env_vars_referenced`
  </read_first>
  <action>
    Write `.planning/audit/authz/getsession-register.md` covering every session-reading callsite found by `signals.calls_get_session` in the endpoint inventory plus a repository-wide grep over `src/` to catch non-route callsites. There is exactly one today, in the health route, and it is a liveness probe rather than an authorization gate. For each callsite record: file and line number, the enclosing function, whether the result gates access or merely reports state, and — when non-gating — an explicit written reason it is safe. Then record the negative finding prominently: a March-dated concern in the codebase map asserts session-reading is used for authorization checks in the plural, and the evidence shows one non-gating callsite. Name the stale source document so plan 01-13 can file a stale-documentation finding. A negative result with evidence is a deliverable, not an empty section.

    Write `.planning/audit/authz/fail-open-register.md` covering every authorization check that stops applying when an environment variable is unset. Discover candidates with the generalized detector shape — a conditional whose first operand is a bare identifier and whose second operand is an inequality comparison — run across `src/app/api`, then cross-check against `signals.env_vars_referenced` for the admin key, the cron secret, and the admin email list.

    Two findings are already verified and must be banked in this register with their evidence, not rediscovered. First: the admin popularity-calculation route guards both its POST and its GET handler with a conditional that only compares the supplied authorization header when the admin key variable is truthy, so with the variable unset any request is accepted — and the handler runs on the RLS-bypassing client. Record both handlers with their line ranges, note that the route is anonymous-reachable, and mark it a Critical finding candidate with no compensating control. Second: the reminder cron route compares the incoming authorization header against a template string that interpolates the cron secret, so when the secret is unset the comparison is against a fixed, guessable literal rather than rejecting outright. Record it as a guessable-open High finding candidate and contrast it with the feedback-request cron route, which fails closed with a server error when its secret is absent — that contrast is the recommended fix, stated in the register.

    Every row needs: file, line range, the variable the check depends on, the reachable route, the failure mode when the variable is unset, whether the handler runs on the RLS-bypassing client, and a proposed severity with an exposure rationale. Fixing is out of scope — the one-line fixes here are exactly the temptation the phase's own threat model names, and applying one would destroy the baseline Stage 3 must prove behavior against.
  </action>
  <acceptance_criteria>
    - `grep -c 'src/app/api/health/route.ts' .planning/audit/authz/getsession-register.md` is at least 1 with a line number recorded
    - `grep -ciE 'non-gating|does not gate|liveness' .planning/audit/authz/getsession-register.md` is at least 1
    - `grep -ciE 'stale|superseded|CONCERNS' .planning/audit/authz/getsession-register.md` is at least 1 — the stale source is named
    - `grep -c 'calculate-popularity' .planning/audit/authz/fail-open-register.md` is at least 2 — both handlers are recorded
    - `grep -c 'ADMIN_API_KEY' .planning/audit/authz/fail-open-register.md` is at least 1
    - `grep -c 'CRON_SECRET' .planning/audit/authz/fail-open-register.md` is at least 1
    - `grep -c 'send-reminders' .planning/audit/authz/fail-open-register.md` is at least 1 and `grep -c 'send-feedback-requests' .planning/audit/authz/fail-open-register.md` is at least 1
    - `grep -ciE 'critical' .planning/audit/authz/fail-open-register.md` is at least 1
    - `grep -cE '[0-9]+-[0-9]+' .planning/audit/authz/fail-open-register.md` is at least 2 — line ranges are recorded
    - `git diff --exit-code --quiet -- src/` exits 0 — nothing was fixed
    - `node .planning/audit/tools/validate.mjs --check authz-registers` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check authz-registers && bash .planning/audit/tools/readonly-guard.sh && git diff --exit-code --quiet -- src/</automated>
  </verify>
  <done>The session-reading register classifies its one callsite as non-gating with a written reason and records the stale-documentation negative finding; the fail-open register banks both verified findings with line ranges, reachable routes, failure modes, the RLS-bypass flag, and proposed severities; and `src/` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Quantify error handling and observability</name>
  <files>.planning/audit/quality/error-observability.md</files>
  <read_first>
    - `.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md` § Phase Requirements → the AUDIT-14 row (the five verified quantities), § Security Domain → V7 Error Handling & Logging, § Validation Architecture Test Map → the AUDIT-14 row (all five quantities must be integers)
    - `.planning/audit/inventory/endpoints.json` — `signals.has_try_catch`, `catch_any_count`, `console_count`, `as_any_count` per handler
    - `src/middleware.ts` — its outer handler wraps the whole body and continues the request on error
    - `next.config.js` — the security headers that are a positive control worth recording
  </read_first>
  <action>
    Write `.planning/audit/quality/error-observability.md` quantifying the five AUDIT-14 measures as integers, each with the command or the aggregation over `endpoints.json` that produced it, so every number is reproducible.

    First: route handler files with no error handling at all, aggregated from `signals.has_try_catch` being false — 22 of 94 today. List them, because a handler with no error handling is also a handler that returns an unshaped error body.

    Second: handlers that return internal error text to the client. Determine this by inspecting how caught errors are serialised into the response body and record the count plus the specific handlers, since leaking an internal message across the anonymous trust boundary is an information-disclosure finding rather than a style issue.

    Third: the count of catch clauses typed as the permissive any type, aggregated from `signals.catch_any_count` — 5 today.

    Fourth: the unstructured logging call count across the repository and the number of distinct files containing them — 162 calls across 60 files today. Note that the project research summary records a different, older pair of numbers; record both and mark the older one stale.

    Fifth: the absence of request correlation. State that no correlation identifier is generated, propagated, or logged on either runtime, that no error-reporting service is installed, and that this is what makes a production failure uninvestigable today. Name it as the baseline REFAC-20 will be measured against.

    Add two contextual records. The middleware's whole body is wrapped in a handler that continues the request when anything throws, which means the middleware ring fails open — record it here as an observability-adjacent finding candidate and cross-reference it to the page inventory's protection columns. And record the positive control: the security headers already configured in the Next config are present, so the assessment reports what exists as well as what is missing.
  </action>
  <acceptance_criteria>
    - The artifact records all five quantities as integers, verified by `grep -cE '^\| *[A-Za-z].*\| *[0-9]+ *\|' .planning/audit/quality/error-observability.md` being at least 5
    - `grep -c '22' .planning/audit/quality/error-observability.md` is at least 1 — the no-error-handling count
    - `grep -c '162' .planning/audit/quality/error-observability.md` is at least 1 and `grep -c '60' .planning/audit/quality/error-observability.md` is at least 1
    - `grep -ciE 'correlation' .planning/audit/quality/error-observability.md` is at least 1
    - `grep -c 'src/middleware.ts' .planning/audit/quality/error-observability.md` is at least 1
    - `grep -ciE 'reproduc|command' .planning/audit/quality/error-observability.md` is at least 1 — every number carries its derivation
    - `node .planning/audit/tools/validate.mjs --check observability` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check observability && bash .planning/audit/tools/readonly-guard.sh</automated>
  </verify>
  <done>All five AUDIT-14 quantities are recorded as integers with reproducible derivations, the middleware fail-open behavior and the existing security headers are both recorded, and the older stale counts are marked as such.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/audit/authz/service-role-register.json` — 25 rows; fields `file`, `route`, `id`, `rls_bypass_required`, `caller_authenticated_first`, `user_input_used_as_filter`, `reachable_from_client_bundle`, `verdict`, `notes`
- `.planning/audit/authz/service-role-register.md` — generated table view with a summary line
- `.planning/audit/authz/getsession-register.md` — AUDIT-09 classification plus the negative finding
- `.planning/audit/authz/fail-open-register.md` — AUDIT-10 register banking both verified findings
- `.planning/audit/quality/error-observability.md` — AUDIT-14 five quantified measures

No file outside `.planning/` is created or modified. No fix is applied, however small.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous request → admin route | The fail-open shape lets this boundary dissolve when a variable is unset |
| authorization ring → RLS ring | The service-role client removes the second ring entirely |
| server error → client response body | Internal error text crossing here is information disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Elevation of Privilege | `src/app/api/admin/calculate-popularity/route.ts` GET and POST | mitigate | Banked in the fail-open register with line ranges, the reachable route, the RLS-bypass flag, and a Critical severity proposal; the fix is a Stage 3 slice under REFAC-13, never this phase |
| T-01-07-02 | Spoofing | `src/app/api/cron/send-reminders/route.ts` comparing against an interpolated template when the secret is unset | mitigate | Banked as a guessable-open High candidate with the fail-closed sibling route named as the recommended fix |
| T-01-07-03 | Elevation of Privilege | The RLS-bypassing client constructed in a page component | mitigate | Registered with all four justification answers and cross-referenced against the client-bundle sweep from plan 01-03 |
| T-01-07-04 | Elevation of Privilege | Middleware fail-open handler continuing the request on error | mitigate | Recorded in the observability assessment and cross-referenced to the page inventory protection columns; middleware is advisory-only by design in the target architecture |
| T-01-07-05 | Information Disclosure | Handlers serialising internal error text into the response body | mitigate | Counted and listed by handler in the observability assessment as an information-disclosure finding class |
| T-01-07-06 | Tampering | The audit fixing an obvious one-liner and destroying the baseline | mitigate | An acceptance criterion asserts `git diff --exit-code -- src/` is clean; the guard runs in every task verify |
| T-01-07-SC | Tampering | npm/npx package installs | accept | This plan runs only grep, Node one-liners over existing JSON, and file writes under `.planning/`; no package manager is invoked |
</threat_model>

<verification>
- `node .planning/audit/tools/validate.mjs --check service-role` exits 0
- `node .planning/audit/tools/validate.mjs --check authz-registers` exits 0
- `node .planning/audit/tools/validate.mjs --check observability` exits 0
- `git diff --exit-code --quiet -- src/` exits 0
- `bash .planning/audit/tools/readonly-guard.sh` exits 0
</verification>

<success_criteria>
- 25 service-role callsites registered with four non-null justification answers and a verdict each
- The single session-reading callsite is classified with a written safety reason and the stale plural claim is corrected with evidence
- Both verified fail-open findings are banked with line ranges, reachable routes, failure modes, and proposed severities
- All five observability quantities are recorded as reproducible integers
- Nothing in `src/` changed
</success_criteria>

<output>
Create `.planning/phases/01-read-only-foundation-audit/01-07-SUMMARY.md` when done
</output>
