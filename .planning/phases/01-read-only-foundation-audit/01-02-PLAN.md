---
phase: 01-read-only-foundation-audit
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/audit/tools/gen-endpoint-inventory.mjs
  - .planning/audit/tools/gen-endpoints-csv.mjs
  - .planning/audit/inventory/endpoints.json
  - .planning/audit/inventory/endpoints.csv
autonomous: true
requirements: [AUDIT-03]

must_haves:
  truths:
    - "A reviewer can open one machine-readable file and see all 94 route handlers with their methods and derived authorization, cache, validation, and observability signals"
    - "Re-running the generator after a day of hand classification does not discard the classification"
    - "A reviewer can sort the derived CSV to find the 13 route handlers with no authorization signal at all"
  artifacts:
    - path: ".planning/audit/tools/gen-endpoint-inventory.mjs"
      provides: "Zero-dependency generator that merges grep-derived signals into endpoints.json by id"
      min_lines: 45
    - path: ".planning/audit/inventory/endpoints.json"
      provides: "AUDIT-03 keystone artifact; CERT-06's data source"
      contains: "\"signals\""
    - path: ".planning/audit/inventory/endpoints.csv"
      provides: "Derived human-review view; never hand-edited"
  key_links:
    - from: ".planning/audit/tools/gen-endpoint-inventory.mjs"
      to: ".planning/audit/inventory/endpoints.json"
      via: "reads the existing file and merges by id before writing"
      pattern: "existsSync"
    - from: ".planning/audit/tools/gen-endpoints-csv.mjs"
      to: ".planning/audit/inventory/endpoints.json"
      via: "CSV is generated from the JSON, not authored separately"
      pattern: "endpoints\\.json"
    - from: ".planning/audit/inventory/endpoints.json"
      to: ".planning/audit/inventory/endpoints.schema.json"
      via: "validate.mjs --check endpoints-signals"
      pattern: "endpoints-signals"
---

<objective>
Deliver the endpoint inventory slice end-to-end: a merging generator, the 94-row `endpoints.json`, the derived CSV view, and a green validator check. This is the keystone artifact — the cache matrix, the service-role register, every Stage 3 slice plan, and both Stage 4 coverage gates read it.

Purpose: Without a machine-readable denominator, "every endpoint reviewed" in Stage 4 has no meaning, and CERT-06's generated persona-by-endpoint matrix has no data source.
Output: `.planning/audit/inventory/endpoints.json` with all 94 rows carrying machine-derived signals and placeholder human-classification fields that plan 01-07 resolves.
</objective>

## Phase Goal

> The true current state of the codebase is known with captured evidence — every endpoint, page, policy, dependency, and risk inventoried, and every finding recorded with a reproduction and a validation criterion — without a single source, config, dependency, or database change.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md
@.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md
@.planning/phases/01-read-only-foundation-audit/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the 94-row endpoint inventory with merge-by-id semantics</name>
  <files>.planning/audit/tools/gen-endpoint-inventory.mjs, .planning/audit/inventory/endpoints.json</files>
  <read_first>
    - `.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md` § Code Examples 3 (the prototyped generator that emitted 94 rows, with every regex) and § Architecture Patterns → Pattern 1 (canonical row shape and the merge-by-id rule)
    - `.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md` § `gen-endpoint-inventory.mjs` — the header docblock, precondition guard, `main().catch` tail, and the five mandatory deviations from `scripts/platform-analytics.ts`
    - `.planning/audit/inventory/endpoints.schema.json` — the shape the output must satisfy
    - `.planning/audit/baseline/versions.txt` — the authoritative `route_ts_count`
    - `src/lib/admin.ts` and `src/app/api/admin/calculate-popularity/route.ts` — the correct-authorization pattern and the fail-open pattern the signal regexes must detect
  </read_first>
  <action>
    Create `.planning/audit/tools/gen-endpoint-inventory.mjs` as an ESM, zero-dependency Node script importing only `node:fs` and `node:child_process`. Follow the repo's script conventions: a header docblock naming the file and a `Usage: node .planning/audit/tools/gen-endpoint-inventory.mjs` line; a fail-fast precondition guard that exits 1 when the process is not running from the repo root; progress messages to `console.error` so stdout stays clean; and a terminal `main().catch` handler that exits 1. Deviate from `scripts/platform-analytics.ts` in five ways required by this phase: plain `node` not `npx tsx`; ESM so `__dirname` does not exist; zero dependencies (no `dotenv`, no `@supabase/supabase-js`); output written with `writeFileSync` to `.planning/audit/inventory/endpoints.json`; and merge semantics rather than replace.

    Implement exactly the generator from RESEARCH § Code Examples 3. Discover files with `find src/app -name route.ts`, sorted. Derive `route` by stripping the `src/app/` prefix and the trailing `/route.ts`; derive `id` from the route by removing the leading slash, replacing `/` with `.`, and stripping square brackets. Emit the `signals` object with every key listed in Pattern 1: `uses_cookie_client`, `uses_service_client`, `calls_verify_admin`, `calls_get_user`, `calls_get_session`, `calls_check_ban`, `inline_role_check`, `references_club_members`, `parses_body`, `has_zod`, `sets_cache_control`, `cache_control_values`, `env_vars_referenced`, `has_try_catch`, `catch_any_count`, `console_count`, `as_any_count`, `tables_referenced`, `loc`. Use the regexes transcribed in RESEARCH § Code Examples 3 verbatim — they were executed against this repo and produced the aggregates the acceptance criteria assert. Emit `methods` from the exported handler names GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS, and `dynamic_segments` from the bracketed route segments.

    Merge semantics are mandatory: before writing, read the existing `endpoints.json` if present and index it by `id`; for each discovered route, start from the prior row when one exists so that human-classification fields survive, then overwrite only `id`, `file`, `route`, `methods`, `dynamic_segments`, and `signals`. When no prior row exists, seed every human-classification field with the placeholder sentinel defined in `endpoints.schema.json`, seed `service_role_justified` as null, seed `expected_status` as an empty object, and seed `findings` as an empty array. A re-run must never discard classification.

    Run the generator. It must report 94 rows.
  </action>
  <acceptance_criteria>
    - `node -e "process.exit(JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8')).length===94?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8'));process.exit(r.filter(x=>x.signals.uses_service_client).length===22?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8'));process.exit(r.filter(x=>!x.signals.has_try_catch).length===22?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8'));process.exit(r.filter(x=>x.signals.sets_cache_control).length===4?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8'));process.exit(r.filter(x=>!x.signals.calls_get_user&&!x.signals.calls_verify_admin&&!x.signals.calls_get_session).length===13?0:1)"` exits 0
    - `node -e "const r=JSON.parse(require('fs').readFileSync('.planning/audit/inventory/endpoints.json','utf8'));process.exit(new Set(r.flatMap(x=>x.signals.tables_referenced)).size===32?0:1)"` exits 0
    - Running the generator twice in a row leaves `endpoints.json` byte-identical (idempotence), verified by comparing `shasum -a 256` before and after a second run
    - `grep -c 'createServiceClient' .planning/audit/tools/gen-endpoint-inventory.mjs` is at least 1 (the service-client signal regex is present)
    - `node .planning/audit/tools/validate.mjs --check endpoints-signals` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check endpoints-signals && bash .planning/audit/tools/readonly-guard.sh</automated>
  </verify>
  <done>`endpoints.json` holds 94 schema-valid rows whose aggregate signal counts match the values RESEARCH measured against this repo (22 service-client, 22 no-try, 4 cache-control, 13 no-auth-signal, 32 distinct tables), and a second generator run is a no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the CSV review view and surface the no-auth-signal queue</name>
  <files>.planning/audit/tools/gen-endpoints-csv.mjs, .planning/audit/inventory/endpoints.csv</files>
  <read_first>
    - `.planning/phases/01-read-only-foundation-audit/01-RESEARCH.md` § Architecture Patterns → Pattern 1 (the CSV is a derived view, never hand-edited) and § Code Examples 3 (the aggregate table that names the 13-route review queue)
    - `.planning/phases/01-read-only-foundation-audit/01-PATTERNS.md` § No Analog Found → the CSV row explaining that `src/lib/exportUtils.ts` must not be imported by a `.planning/` tool
    - `.planning/audit/inventory/endpoints.json` — the only input
  </read_first>
  <action>
    Create `.planning/audit/tools/gen-endpoints-csv.mjs` as an ESM zero-dependency script that reads `.planning/audit/inventory/endpoints.json` and writes `.planning/audit/inventory/endpoints.csv`. Do not import `src/lib/exportUtils.ts` or any app-layer module — reimplement the quote-and-escape inline: wrap a field in double quotes when it contains a comma, a double quote, or a newline, and double any embedded double quote. Array-valued signals such as `methods`, `cache_control_values`, `env_vars_referenced`, `tables_referenced`, and `dynamic_segments` are joined with a semicolon inside one cell.

    Column order, one row per endpoint: `id`, `route`, `methods`, `file`, `loc`, `auth_requirement`, `role_required`, `rls_reliance`, `personalized`, `input_validation`, `test_present`, `dead_or_duplicate`, `cache_policy_today`, `cache_policy_target`, then the signal columns `uses_cookie_client`, `uses_service_client`, `calls_verify_admin`, `calls_get_user`, `calls_get_session`, `calls_check_ban`, `inline_role_check`, `references_club_members`, `parses_body`, `has_zod`, `sets_cache_control`, `cache_control_values`, `env_vars_referenced`, `has_try_catch`, `catch_any_count`, `console_count`, `as_any_count`, `tables_referenced`. Add a final column `no_auth_signal` computed as true when `calls_get_user`, `calls_verify_admin`, and `calls_get_session` are all false — this column is the hand-review queue plan 01-07 starts from, and it must contain exactly 13 true values today.

    Write a header comment as the first line of the generator (not of the CSV) stating that `endpoints.csv` is generated and that edits belong in `endpoints.json`. Run the generator.
  </action>
  <acceptance_criteria>
    - `.planning/audit/inventory/endpoints.csv` has exactly 95 lines (1 header + 94 rows), verified by `wc -l`
    - The header line contains `no_auth_signal` as its last field
    - `awk -F, 'NR>1 && $NF=="true"' .planning/audit/inventory/endpoints.csv | wc -l` reports 13
    - `grep -c 'exportUtils' .planning/audit/tools/gen-endpoints-csv.mjs` is 0
    - Regenerating the CSV twice produces a byte-identical file
    - `node .planning/audit/tools/validate.mjs --check endpoints-signals` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test "$(wc -l < .planning/audit/inventory/endpoints.csv | tr -d ' ')" = "95" && node .planning/audit/tools/validate.mjs --check endpoints-signals && bash .planning/audit/tools/readonly-guard.sh</automated>
  </verify>
  <done>`endpoints.csv` is generated from the JSON with 94 data rows, carries a `no_auth_signal` column with exactly 13 true values, imports nothing from `src/`, and is reproducible byte-for-byte.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/audit/tools/gen-endpoint-inventory.mjs` — ESM generator. Behavior: `find src/app -name route.ts`, derive `id`/`route`/`methods`/`dynamic_segments`/`signals`, merge by `id` into the existing JSON
- `.planning/audit/tools/gen-endpoints-csv.mjs` — ESM CSV emitter with an inline quote/escape helper; adds the derived `no_auth_signal` column
- `.planning/audit/inventory/endpoints.json` — 94 rows
- `.planning/audit/inventory/endpoints.csv` — 95 lines

No file outside `.planning/` is created or modified. No package is installed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/` source files → audit artifact | Route source is read; nothing is written back |
| audit artifact → git history | `endpoints.json` is committed; `signals.env_vars_referenced` carries variable NAMES |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `src/app/**/route.ts` | mitigate | Generator opens files with `readFileSync` only; `readonly-guard.sh` runs in both task verifies |
| T-01-02-02 | Information Disclosure | `env_vars_referenced` in a committed artifact | mitigate | The regex captures the identifier after `process.env.` — a NAME, never a value; the phase-gate sweep in plan 01-13 greps this path for JWT and secret shapes |
| T-01-02-03 | Repudiation | A regenerated inventory silently discards a day of hand classification | mitigate | Merge-by-`id` is asserted by the idempotence criterion in Task 1; the generator never overwrites human fields |
| T-01-02-04 | Information Disclosure | An incomplete inventory lets an unclassified endpoint reach Stage 4 unnoticed | mitigate | `validate.mjs --check endpoints` (run in plan 01-07) fails while any placeholder sentinel remains; CERT-06 fails on any unclassified endpoint |
| T-01-02-SC | Tampering | npm/npx package installs | accept | This plan invokes no package manager and imports only Node built-ins; no install surface exists |
</threat_model>

<verification>
- `node .planning/audit/tools/validate.mjs --check endpoints-signals` exits 0
- `bash .planning/audit/tools/readonly-guard.sh` exits 0
- Both generators are idempotent across consecutive runs
</verification>

<success_criteria>
- 94 schema-valid endpoint rows exist with machine-derived signals matching this repo's measured aggregates
- The generator merges rather than replaces, proven by idempotence
- A derived CSV exists with a `no_auth_signal` column isolating the 13-route review queue
</success_criteria>

<output>
Create `.planning/phases/01-read-only-foundation-audit/01-02-SUMMARY.md` when done
</output>
