---
phase: 03-notification-system-wiring
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [NUI-01, NUI-02]

must_haves:
  truths:
    - "An authenticated user sees a notification bell icon in the header"
    - "Unauthenticated users do not see the notification bell"
    - "The notification bell shows an unread count badge when unread notifications exist"
    - "GET /api/notifications returns notifications and unread_count for the authenticated user"
    - "PATCH /api/notifications/[id] marks a single notification as read"
    - "POST /api/notifications?action=mark-all-read marks all notifications as read"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "NotificationBell rendered in authenticated branch"
      contains: "NotificationBell"
    - path: "src/components/notifications/NotificationBell.tsx"
      provides: "Polling-based unread count display with badge"
      contains: "unreadCount"
    - path: "src/app/api/notifications/route.ts"
      provides: "GET and POST handlers for notification listing and mark-all-read"
      contains: "unread_count"
    - path: "src/app/api/notifications/[id]/route.ts"
      provides: "PATCH handler for marking single notification as read"
      contains: "read: true"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/components/notifications/NotificationBell.tsx"
      via: "import and conditional render inside isAuthenticated branch"
      pattern: "isAuthenticated.*NotificationBell"
    - from: "src/components/notifications/NotificationBell.tsx"
      to: "/api/notifications"
      via: "fetch in useEffect polling interval"
      pattern: "fetch.*api/notifications"
---

<objective>
Apply the pending Phase 1 migration and verify all existing notification UI and API infrastructure works end-to-end against the real database.

Purpose: NUI-01 (bell in header) and NUI-02 (API routes validated) are already implemented in code but have never been verified against the production database with the UNIQUE dedup index and RLS policies active. The migration from Phase 1 (`20260223000000_notifications_rls_and_dedup.sql`) must be applied before end-to-end verification is meaningful.

Output: Verified production infrastructure — no new code files, only confirmation that existing code works correctly.
</objective>

<execution_context>
@/Users/adyanullah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adyanullah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notification-system-wiring/03-RESEARCH.md
@.planning/phases/03-notification-system-wiring/03-01-SUMMARY.md
@.planning/phases/01-notification-database-foundation/01-01-SUMMARY.md

Key references:
@supabase/migrations/20260223000000_notifications_rls_and_dedup.sql
@src/components/layout/Header.tsx
@src/components/notifications/NotificationBell.tsx
@src/app/api/notifications/route.ts
@src/app/api/notifications/[id]/route.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply Phase 1 migration to Supabase</name>
  <action>
The migration file `supabase/migrations/20260223000000_notifications_rls_and_dedup.sql` needs to be applied to the production Supabase database. This adds:
- RLS policies (SELECT own, UPDATE own) on the notifications table
- The `notifications_dedup_idx` partial UNIQUE index on (user_id, event_id, type) WHERE event_id IS NOT NULL
- Performance indexes (user_id, user unread)

The migration is idempotent (uses IF NOT EXISTS and DO blocks) so it is safe to run multiple times.
  </action>
  <how-to-verify>
Apply the migration using ONE of these methods:

**Option A — Supabase Dashboard (recommended):**
1. Go to supabase.com/dashboard → select project → SQL Editor
2. Paste the contents of `supabase/migrations/20260223000000_notifications_rls_and_dedup.sql`
3. Click "Run"
4. Verify output shows no errors

**Option B — Supabase MCP plugin (if available in current session):**
Run: Apply migration `notifications_rls_and_dedup` using the SQL in `supabase/migrations/20260223000000_notifications_rls_and_dedup.sql`

**Verification query (run in SQL Editor after applying):**
```sql
SELECT indexname FROM pg_indexes
WHERE tablename = 'notifications' AND indexname = 'notifications_dedup_idx';
```
Expected: one row with indexname = `notifications_dedup_idx`.
  </how-to-verify>
  <resume-signal>Type "applied" once the migration has been run successfully, or describe any errors.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify notification system end-to-end with build check</name>
  <files></files>
  <action>
Run the Next.js production build to verify all notification-related files compile correctly together. This validates:

1. **NUI-01**: Header.tsx imports and renders NotificationBell — already confirmed in code but build verifies no import/export errors
2. **NUI-02**: All three API routes (GET, PATCH, POST mark-all-read) compile with correct Supabase client usage
3. **Integration**: NotificationItem (modified in Plan 01) + NotificationBell + NotificationList + notifications page all compile as a unit

Run `npm run build` and confirm zero TypeScript errors and zero build errors. The build does NOT need the dev server running.

If any build errors appear related to notification files, diagnose and fix them. If errors are in unrelated files, note them but do not fix (out of scope).

Additionally, verify the following by reading the source files:
- Header.tsx renders `<NotificationBell />` inside the `isAuthenticated` branch (NUI-01)
- NotificationBell.tsx fetches from `/api/notifications` and displays `unread_count` as a badge (NUI-02)
- All three API routes use `createClient()` from `@/lib/supabase/server` with proper auth checks (NUI-02)
  </action>
  <verify>
    <automated>cd /Users/adyanullah/Documents/GitHub/Event-Radar && npm run build 2>&1 | tail -20</automated>
    <manual>Build succeeds with zero errors; notification files compile correctly</manual>
  </verify>
  <done>Next.js production build passes with zero errors. NUI-01 verified (bell in header for authenticated users). NUI-02 verified (all three notification API routes compile and use correct Supabase client patterns).</done>
</task>

</tasks>

<verification>
1. `notifications_dedup_idx` exists in the production database (confirmed by user in checkpoint)
2. `npm run build` passes with zero errors
3. Header.tsx renders NotificationBell conditionally for authenticated users
4. NotificationBell.tsx polls /api/notifications and shows unread badge
5. All three notification API routes (GET, PATCH, POST) compile and follow correct patterns
</verification>

<success_criteria>
- Phase 1 migration applied to production Supabase database
- Next.js build passes with zero errors
- All NUI-01 and NUI-02 requirements confirmed working in production infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/03-notification-system-wiring/03-02-SUMMARY.md`
</output>
