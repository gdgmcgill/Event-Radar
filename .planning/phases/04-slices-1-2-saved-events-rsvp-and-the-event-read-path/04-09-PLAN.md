---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 09
type: execute
wave: 6
depends_on: ["04-08"]
files_modified:
  - src/lib/eventCursor.ts
  - src/lib/eventCursor.test.ts
  - src/app/api/events/route.ts
  - src/app/api/events/route.test.ts
  - src/__tests__/api/events/pagination-contract-defect.test.ts
  - src/__tests__/api/events/get-events.test.ts
  - e2e/specs/event-read-path.spec.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/pagination-contract.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.pagination.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-10]

must_haves:
  truths:
    - "GET /api/events emits nextCursor when more rows follow and accepts that cursor to return the next page in (start_date, id) order, so the home page's Load More works when a filtered result exceeds its limit"
    - "Page-mode requests (no cursor) return the same rows, totals and range as before; total is the count of all matching events regardless of cursor"
    - "An invalid cursor is rejected with 400 before any database call, and a cursor's values can never break out of the or() filter"
    - "The previously skipped contract suite runs, rewritten against the decided contract; the jest run reports zero skipped suites and zero skipped tests (F-066's first clause)"
    - "Against the real local PostgREST, two seeded events are traversed by cursor, including with a search filter applied"
  artifacts:
    - path: "src/lib/eventCursor.ts"
      provides: "encodeEventCursor, decodeEventCursor, EventCursor"
      exports: ["encodeEventCursor", "decodeEventCursor"]
      min_lines: 30
    - path: "src/app/api/events/route.test.ts"
      provides: "The cursor contract suite, running (no longer skipped), plus a source-derived check that every response field the hook reads is present"
      min_lines: 120
  key_links:
    - from: "src/app/api/events/route.ts"
      to: "src/lib/eventCursor.ts"
      via: "decodeEventCursor validates the cursor parameter; encodeEventCursor builds nextCursor and prevCursor"
      pattern: "decodeEventCursor\\("
    - from: "src/app/api/events/route.ts"
      to: "src/lib/searchFilter.ts"
      via: "the keyset or() values are wrapped with postgrestQuotedValue"
      pattern: "postgrestQuotedValue\\("
    - from: "src/app/api/events/route.test.ts"
      to: "src/hooks/useEvents.ts"
      via: "the suite reads useEvents.ts at test time and asserts every data.<field> it reads is a key of the route's response"
      pattern: "useEvents\\.ts"
  prohibitions:
    - "src/hooks/useEvents.ts and src/app/page.tsx are not modified — the client already speaks this contract"
    - "No index or migration is added for the keyset; a composite index is recorded as a follow-up under F-083 (orchestrator decision 2)"
    - "sort, direction, clubId and before are not implemented (DEC-25); the fuzzy-rank path emits null cursors"
    - "The Cache-Control header is unchanged (Phase 6, REFAC-19)"
    - "The events-list PRESERVE suite and the search PRESERVE tests are not edited"
---

<objective>
Wave 6, alone: implement the cursor contract the client already speaks (F-083) and revive the suite that describes it (F-066's first clause, the last half of DI-24).

Purpose: 04-RESEARCH.md § Q(g) measured that `useEvents` sends a cursor and reads `nextCursor`, the route ignores both, and so `src/app/page.tsx`'s "Load More" never renders — a filtered or searched feed silently stops at 30 results. DEC-25 records the decided contract (research A6, taken on the research's evidence and the project skill's keyset recommendation). The skipped suite is rewritten against that contract rather than revived against current behaviour, per T-01-11-04.

Output: a cursor codec, the keyset route, the running contract suite, moved assertions, a Playwright traversal, and a jest run with nothing skipped.

On seeded data nothing renders differently: two approved events never exceed the feed's limit of 30, so Load More still does not appear. In production it appears for any filtered result larger than 30 — the defect fix, logged.
</objective>

<slice_outcome>
Green when `npx jest --ci` reports 0 skipped suites and 0 skipped tests, `src/app/api/events/route.test.ts` runs green, the events-list PRESERVE suite passes unmodified, and Playwright traverses both seeded approved events by cursor with and without a search term.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@.planning/phases/02-dependency-and-runtime-stabilization/evidence/skipped-suite-disposition.md
@.agents/skills/supabase-postgres-best-practices/references/data-pagination.md
@src/app/api/events/route.ts
@src/app/api/events/route.test.ts
@src/hooks/useEvents.ts
@src/lib/searchFilter.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — page-mode requests are unchanged; the new behaviour is exactly DEC-25's contract.
2. Characterize first — the events-list PRESERVE suite (object matching on current keys, page-mode range) passes unmodified; the F-083 DEFECT suite moves in the fixing commit.
3. No schema change — no index for the keyset.
4. Production is never touched.
5. No visual change on seeded data (two events never trigger Load More).
6. Jest single runner.
7. Stage by explicit path only.
8. Local-stack exclusivity — this plan is wave 6's only plan.
9. Evidence discipline.
10. PostgREST ANDs repeated `or` parameters — measured on the local stack during planning (a search or() plus a keyset or() returned only the row matching both); Task 3's Playwright test re-proves it on every run.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A cursor codec that rejects anything it did not produce</name>
  <files>src/lib/eventCursor.ts, src/lib/eventCursor.test.ts</files>
  <read_first>
    - src/app/api/events/route.test.ts lines 50-70 — the skipped suite's encode and decode helpers: base64 of a JSON `{ sortValue, id }`; the codec must be byte-compatible with them
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-25
    - src/lib/searchFilter.ts — the sibling pure module style
  </read_first>
  <behavior>
    - `encodeEventCursor({ sortValue: "2026-02-02T10:00:00+00:00", id: <uuid> })` equals base64 of `JSON.stringify({ sortValue, id })`, and `decodeEventCursor` of it returns the same object
    - `decodeEventCursor("not-a-valid-cursor")` returns null; so do valid base64 of non-JSON, of a JSON array, of an object missing `id` or `sortValue`, of a `sortValue` that `Date.parse` rejects, and of an `id` that is not a canonical UUID
    - an `id` or `sortValue` containing a comma, parenthesis or double quote is rejected (null)
  </behavior>
  <action>
Write `src/lib/eventCursor.test.ts` first from the behaviour list, run it red, then create `src/lib/eventCursor.ts` with a JSDoc citing F-083 and DEC-25 and exporting the `EventCursor` type, `encodeEventCursor(cursor)` and `decodeEventCursor(raw)`. Decoding uses Node's `Buffer` (route handlers run on the Node runtime), parses in a try/catch, and returns null for every malformed input rather than throwing — the house convention for parse helpers. Validate `id` against a canonical UUID pattern and `sortValue` with `Date.parse`; these checks are defence in depth, because the route also quotes both values. Run it green and commit.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "eventCursor"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "eventCursor"` exits 0 with at least 9 tests
    - `grep -cE "export function (encodeEventCursor|decodeEventCursor)" src/lib/eventCursor.ts` returns 2
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A cursor is an opaque, validated token: the route can trust any cursor the codec accepts and reject the rest before touching the database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the keyset contract in the route, rewrite the skipped suite, and move the F-083 pins</name>
  <files>src/app/api/events/route.ts, src/app/api/events/route.test.ts, src/__tests__/api/events/pagination-contract-defect.test.ts, src/__tests__/api/events/get-events.test.ts, e2e/specs/event-read-path.spec.ts</files>
  <read_first>
    - src/app/api/events/route.ts — the whole GET as left by 04-08: where filters are applied, the two RPC early returns, page-mode `range`, the benign-error branch, the fuzzy re-sort and the response
    - src/app/api/events/route.test.ts — the skipped suite in full; it is rewritten, not revived
    - src/hooks/useEvents.ts lines 53-138 — what the client sends (limit, sort, direction, cursor, filters) and which `data.` fields it reads (`events`, `total`, `nextCursor`, `prevCursor`)
    - src/__tests__/api/events/get-events.test.ts lines 419-470 — the exact-key-set assertion that moves, and the one skipped test whose stated reason ("route no longer uses eq() for status filtering") is false today — the route calls `.eq('status', 'approved')`
    - src/__tests__/api/events/pagination-contract-defect.test.ts — the assertions that move
    - src/__tests__/api/events/events-list-characterization.test.ts — must pass unmodified
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-25
  </read_first>
  <behavior>
    - A `cursor` that `decodeEventCursor` rejects returns 400 `{ error: "Invalid cursor" }` and issues no query
    - Every request orders by start_date ascending, then id ascending
    - Page mode (no cursor): the range, the `total`, `page`, `limit` and `totalPages` values are unchanged; `nextCursor` encodes the last returned row when `(page - 1) * limit + rows.length < total` and is null otherwise; `prevCursor` is null
    - Cursor mode: the main query adds a keyset filter — start_date greater than the cursor's sortValue, or equal to it with id greater than the cursor's id — with both values quoted by `postgrestQuotedValue`; it requests rows 0 through limit-1; `nextCursor` encodes the last row when the main query's count exceeds the rows returned; `prevCursor` encodes the first returned row; `total` comes from a separate head-count query carrying every filter except the keyset
    - When the fuzzy RPC supplied ranked ids, both cursors are null; both early returns and the benign-error branch include `nextCursor: null` and `prevCursor: null`
    - Every `data.<field>` read in `src/hooks/useEvents.ts` is a key of the success response (derived from the hook's source at test time, so the two cannot drift silently)
  </behavior>
  <action>
In the route: validate `cursor` with `decodeEventCursor` immediately after the existing `timeOfDay`/`dayType` validation, returning the 400 on failure. Extract the filter application (status, deleted_at, ids, tags, the search or() from 04-08, the date floor and ceiling, the time-filter id restriction) into one local function applied to a query builder, so the main query and the cursor-mode total query carry identical filters. Add the id tie-break order after the start_date order. Implement page mode and cursor mode exactly per the behaviour list, using `encodeEventCursor` for both cursors and `postgrestQuotedValue` for both keyset values. Add `nextCursor` and `prevCursor` to the success response, the benign-error response and both early returns. Leave the Cache-Control header, the fuzzy re-sort and every error body unchanged.

Rewrite `src/app/api/events/route.test.ts` as a running suite: drop the skip; its leading docblock states that it is the decided cursor contract (DEC-25, F-083), rewritten rather than revived per T-01-11-04, and that the old `before` test was dropped because no client sends that parameter. Use the 04-02 fake (with an explicit `@/lib/timezone` mock) or a mock chain matching the route's real call sequence. Cover: first page with more rows emits nextCursor for the last row; a cursor page emits prevCursor for the first row and a null nextCursor at the end; the keyset or() argument quotes both values; total comes from the unfiltered head count, not the remaining count; an invalid cursor is 400 with no from() call; page mode unchanged; the source-derived hook-field check.

In the same commit: move `pagination-contract-defect.test.ts` to assert the fixed behaviour with a "Status: FIXED in 04-09" paragraph; in `get-events.test.ts`, change the exact-key-set assertion to the seven keys and remove the skip from the one skipped test (it passes against today's route — record that in the commit body); in `e2e/specs/event-read-path.spec.ts`, retitle DEFECT test 5 `FIXED F-083:` asserting `nextCursor` is a non-empty string. Confirm the events-list PRESERVE suite passes unmodified and commit with a message beginning `fix(04-09): implement the cursor contract the client already speaks (F-083, F-066)`.
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci` exits 0 and its summary reports no skipped suites and no skipped tests (`npx jest --ci 2>&1 | grep -E "Tests:.*skipped|Suites:.*skipped"` prints nothing)
    - `grep -c "describe.skip\|it.skip" src/app/api/events/route.test.ts src/__tests__/api/events/get-events.test.ts` reports 0 for both files
    - `grep -c "decodeEventCursor(" src/app/api/events/route.ts` is at least 1 and `grep -c "postgrestQuotedValue(" src/app/api/events/route.ts` is at least 1
    - `git diff --stat HEAD~1 -- src/__tests__/api/events/events-list-characterization.test.ts src/hooks/useEvents.ts src/app/page.tsx` prints nothing
    - `grep -c "useEvents.ts" src/app/api/events/route.test.ts` is at least 1
    - `grep -c "FIXED F-083" e2e/specs/event-read-path.spec.ts` returns 1
    - `node scripts/check-characterization-tags.mjs --all` exits 0
  </acceptance_criteria>
  <done>The route speaks the contract the client was already written for, the contract suite runs instead of being skipped, page-mode callers see no difference, and the hook and the route are held together by a test that reads the hook's source.</done>
</task>

<task type="auto">
  <name>Task 3: Traverse the seeded events by cursor against the real PostgREST, and record the fix</name>
  <files>e2e/specs/event-read-path.spec.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/pagination-contract.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.pagination.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md</files>
  <read_first>
    - e2e/specs/event-read-path.spec.ts — as left by Task 2
    - scripts/seed/personas.ts lines 481-510 — the two approved events: approvedEvent starts earlier than secondApprovedEvent
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
  </read_first>
  <action>
Add three API-level tests to the spec, titled `PRESERVE:` since they pin the contract from here on: (a) `GET /api/events?limit=1` returns approvedEvent, total 2 and a nextCursor; following it returns secondApprovedEvent, total 2, a non-empty prevCursor and a null nextCursor; (b) the same traversal with `search=Seed` added yields the same two pages — the search or() and the keyset or() are both applied (repeated `or` parameters AND in PostgREST); (c) `GET /api/events?cursor=not-a-valid-cursor` returns 400 with `{ error: "Invalid cursor" }`.

Reset and seed, run `npx playwright test` into `evidence/playwright.pagination.txt` (passed = `evidence/playwright.escaping.txt`'s + 3, 0 failed). Write `evidence/pagination-contract.txt` with the metadata line: the contract as implemented, the full `npx jest --ci` summary showing zero skipped suites and tests, and a follow-up note for F-083 recording that keyset efficiency at scale wants a composite index on the approved-status, start_date and id columns, which is a migration and therefore outside Phase 4 (orchestrator decision 2). Append the F-083 and F-066 (first clause) rows to the ledger, including the get-events skip revived and why. Commit the spec and the evidence.
  </action>
  <verify>
    <automated>npx jest --ci && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.pagination.txt` ends with `exit=0`, 0 failed, and passed equal to `evidence/playwright.escaping.txt`'s passed count plus 3
    - `grep -c "not-a-valid-cursor" e2e/specs/event-read-path.spec.ts` is at least 1
    - `evidence/pagination-contract.txt` contains the jest summary line and the words `composite index`
    - `grep -c "F-083" .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md` is at least 1 and `grep -c "F-066" .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md` is at least 2
  </acceptance_criteria>
  <done>The cursor contract is proven against the real parser — including its composition with search — the jest run skips nothing, and the ledger records both halves of F-066 as closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query-string cursor → PostgREST or() filter | A client-supplied token becomes filter values |
| anonymous caller → /api/events | Public, paginated read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | A crafted cursor injecting into the keyset or() | mitigate | `decodeEventCursor` accepts only a canonical UUID and a parseable date and rejects comma, parenthesis and quote; both values are also wrapped with `postgrestQuotedValue`; invalid cursors are 400 before any query |
| T-04-09-02 | Denial of Service | Deep keyset pages scanning without an index | accept | Two seeded events locally; production scale is Phase 8's k6 concern; the composite index is recorded under F-083 as a migration outside Phase 4 |
| T-04-09-03 | Information Disclosure | Cursor pages exposing non-approved events | mitigate | The shared filter function applies the approved-status and deleted_at filters to every query, including the total count; the PRESERVE list suite and Playwright's hidden-status assertions still pass |
| T-04-09-04 | Repudiation | Reviving the skipped suite against current behaviour, laundering the defect into a spec | mitigate | The suite is rewritten against DEC-25 and its header says so; the F-083 DEFECT pin moved in the same commit |
| T-04-09-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `npx jest --ci` exits 0 with 0 skipped suites and 0 skipped tests
- `npx playwright test` exits 0 at 04-08's count + 3
- `npm run lint`, `npx tsc --noEmit` clean
- `git diff --name-only <04-08 head> -- src/hooks src/app/page.tsx supabase/migrations` prints nothing
</verification>

<success_criteria>
"Load More" can work: the route emits and accepts the cursor the client has always sent, page-mode behaviour is unchanged, bad cursors are rejected before they reach the database, the contract is proven against the real PostgREST, and the phase's last skipped suite — and its last skipped test — now run.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New files**
- `src/lib/eventCursor.ts` — `EventCursor`, `encodeEventCursor`, `decodeEventCursor`
- `src/lib/eventCursor.test.ts`
- `evidence/pagination-contract.txt`, `evidence/playwright.pagination.txt`

**Modified:** `src/app/api/events/route.ts` (keyset contract, id tie-break, cursor keys on every response), `src/app/api/events/route.test.ts` (rewritten, running), `pagination-contract-defect.test.ts` and `get-events.test.ts` (moved assertions, skip removed), `e2e/specs/event-read-path.spec.ts` (+3 tests, 1 flipped), `evidence/defect-ledger.md`.
**Findings fixed by code (status flips in 04-11):** F-083, F-066 (both clauses now met). **Deferred items closed:** DI-24.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-09-SUMMARY.md` when done.
</output>
