---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 10
type: execute
wave: 7
depends_on: ["04-07", "04-09"]
files_modified:
  - src/lib/tagMapping.ts
  - src/app/api/events/route.ts
  - src/app/api/events/new/route.ts
  - src/app/api/events/following/route.ts
  - src/app/api/events/happening-now/route.ts
  - src/app/api/events/popular/route.ts
  - src/app/api/calendar/events/route.ts
  - src/app/api/users/saved-events/route.ts
  - src/app/api/events/[id]/route.ts
  - src/__tests__/api/events/club-fabrication-defect.test.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-embed.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-join-latency.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.club-embed.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-10]

must_haves:
  truths:
    - "Every list-shaped event route selects its club through one shared embed that carries the club's real banner and link columns, so the transform no longer blanks them"
    - "/api/users/saved-events returns the real club from a join instead of a club fabricated from the organizer string"
    - "No rendered output changes: the only UI consumer of saved-events reads ids, and no card, modal or detail view renders the five link columns"
    - "The cost of the added join is measured, not asserted (research A4)"
    - "The detail route's false comment is corrected, and its remaining fabrication is annotated as F-080's gated half"
  artifacts:
    - path: "src/lib/tagMapping.ts"
      provides: "EVENT_CLUB_EMBED (and the full event-with-club select literal); the real-club branch reads the five link columns from the embed"
      contains: "EVENT_CLUB_EMBED"
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-join-latency.txt"
      provides: "Median timings of the event read with and without the club embed against local PostgREST"
      min_lines: 15
  key_links:
    - from: "src/app/api/users/saved-events/route.ts"
      to: "src/lib/tagMapping.ts"
      via: "the saved-events event read selects the shared event-with-club literal"
      pattern: "EVENT_WITH_CLUB_SELECT|EVENT_CLUB_EMBED"
    - from: "src/app/api/events/route.ts"
      to: "src/lib/tagMapping.ts"
      via: "the list query selects the shared literal, with count exact kept"
      pattern: "EVENT_WITH_CLUB_SELECT|EVENT_CLUB_EMBED"
  prohibitions:
    - "The organizer fallback in transformEventFromDB is NOT removed and /api/events/[id] does NOT gain the embed in this plan — both change seeded rendered output and are gated behind the 04-11 checkpoint (DEC-27, orchestrator decision 1)"
    - "contact_email is not added to the event embed (DEC-27 data minimisation); it stays null in event payloads"
    - "Routes outside the six list-shaped routes and saved-events are not touched (my-events, clubs/[id]/events, clubs/featured, export, organizer-requests keep their own embeds)"
    - "No PRESERVE suite is edited"
---

<objective>
Wave 7, alone: ship the non-visual half of the club-fabrication fix (F-080, DEC-27) and measure what the join costs.

Purpose: REFAC-10's first clause and ROADMAP criterion 2 — "the events list returns clubs from a real join instead of fabricating club objects". 04-RESEARCH.md § Q(c) found two fabrications: the organizer fallback (a club whose id is a free-text string) and a quieter one on the real branch, where five link columns are hard-coded to null because the embed never asks for them. The second, and the saved-events route's missing join, change no rendered output and ship here. The first changes what every seeded event's detail page says under "Hosted by", which orchestrator decision 1 forbids by default; 04-11 puts it to the owner.

Output: one shared club embed used by the six list routes and saved-events, a transform that reads the real link columns, a latency measurement, and the two F-080 pins that move now.
</objective>

<slice_outcome>
Green when F-080 pins B and C have moved and pins A and D have not, every PRESERVE suite passes unmodified, `npx playwright test` passes at 04-09's count with no UI assertion changed, and `evidence/club-join-latency.txt` records median timings for the event read with and without the embed.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@src/lib/tagMapping.ts
@src/types/index.ts
@src/__tests__/api/events/club-fabrication-defect.test.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — the list routes' responses change only in five club link fields that were always null; saved-events changes only in its `club` field, which no UI renders.
2. Characterize first — the club DEFECT suite's pins B and C move in this plan's commits; pins A and D and every PRESERVE suite stay unchanged.
3. No schema change. 4. Production is never touched.
5. No visual change — proven by the unchanged Playwright UI assertions and by the consumer census in Task 2.
6. Jest single runner.
7. Stage by explicit path only; two commits.
8. Local-stack exclusivity — wave 7's only plan.
9. Evidence discipline.
10. supabase-js infers result types from a literal select string: the shared select must stay a string-literal type (`as const`, composed only from literals), and `npx tsc --noEmit` is the proof.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: One shared club embed carrying the real link columns, adopted by the six list routes (F-080 pin B)</name>
  <files>src/lib/tagMapping.ts, src/app/api/events/route.ts, src/app/api/events/new/route.ts, src/app/api/events/following/route.ts, src/app/api/events/happening-now/route.ts, src/app/api/events/popular/route.ts, src/app/api/calendar/events/route.ts, src/__tests__/api/events/club-fabrication-defect.test.ts</files>
  <read_first>
    - src/lib/tagMapping.ts — `DBClub` and the real-club branch of `transformEventFromDB` with its six hard-coded nulls
    - src/lib/supabase/types.ts, the `clubs` Row — the exact column names: banner_url, website_url, discord_url, twitter_url, linkedin_url, contact_email
    - the six routes' select calls — `src/app/api/events/route.ts` (with `{ count: 'exact' }`), `new`, `following`, `happening-now`, `popular` (which adds a popularity embed on its own line) and `src/app/api/calendar/events/route.ts`; all six list the same ten club columns today
    - src/__tests__/api/events/club-fabrication-defect.test.ts — pin B's describe block
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-27
  </read_first>
  <action>
In `src/lib/tagMapping.ts`, export `EVENT_CLUB_EMBED` as a string literal (`as const`) embedding `clubs` under the alias `club` with fifteen columns: the current ten plus banner_url, website_url, discord_url, twitter_url and linkedin_url — not contact_email, with a comment citing DEC-27 — and export `EVENT_WITH_CLUB_SELECT` as the literal `*, ` followed by that embed. Add the five columns to `DBClub` as optional, and make the real-club branch read each from the embed with a null fallback; `contact_email` stays null with a comment naming the decision. Leave the organizer fallback branch exactly as it is.

In the five routes that select exactly star plus the embed, replace the select string with `EVENT_WITH_CLUB_SELECT`, keeping `{ count: 'exact' }` where it is. In `popular`, build its select from the shared embed plus its popularity embed as one literal-typed string. Change nothing else in any route.

Move F-080 pin B in the DEFECT suite: a row whose embed carries the five link values now transforms to a club carrying those values, and `contact_email` stays null; add "pin B FIXED in 04-10" to the header. Run the full Jest suite — the PRESERVE suites' fixtures carry at most the ten original club columns, so their outputs are unchanged — plus `npx tsc --noEmit` and `npm run lint`. Commit with a message naming F-080 (non-visual half, pin B).
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci` exits 0 with 0 skipped
    - `grep -c "EVENT_WITH_CLUB_SELECT\|EVENT_CLUB_EMBED" <route>` is at least 1 for each of the six routes
    - `node -e 'const s=require("fs").readFileSync("src/lib/tagMapping.ts","utf8");const m=/EVENT_CLUB_EMBED\s*=\s*\n?\s*"([^"]+)"/.exec(s);process.exit(m&&["banner_url","website_url","discord_url","twitter_url","linkedin_url"].every(c=>m[1].includes(c))&&!m[1].includes("contact_email")?0:1)'` exits 0
    - `git diff --stat HEAD~1 -- 'src/__tests__/api/events/*-characterization.test.ts'` prints nothing
    - `grep -c "pin B FIXED in 04-10" src/__tests__/api/events/club-fabrication-defect.test.ts` returns 1
    - The organizer fallback is intact — no removed line mentions it: `git diff -U0 HEAD~1 -- src/lib/tagMapping.ts | grep -E '^-.*organizer'` prints nothing
  </acceptance_criteria>
  <done>The list routes share one club embed that asks for the columns the Club type promises, the transform stops blanking them, and nothing a user sees has changed.</done>
</task>

<task type="auto">
  <name>Task 2: Give saved-events the real club (F-080 pin C), correct the detail route's comment, measure the join, and prove nothing rendered changed</name>
  <files>src/app/api/users/saved-events/route.ts, src/app/api/events/[id]/route.ts, src/__tests__/api/events/club-fabrication-defect.test.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-embed.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-join-latency.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.club-embed.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md</files>
  <read_first>
    - src/app/api/users/saved-events/route.ts — the events read (select star) and the two `any` casts around the transform
    - src/app/api/events/[id]/route.ts lines 76-106 — the false comment on line 79 and the cast on line 105
    - src/app/events/[id]/EventDetailClient.tsx lines 112-128 — the only UI consumer of saved-events reads `savedEventIds` alone
    - src/__tests__/api/events/club-fabrication-defect.test.ts — pins C and D
    - e2e/env.ts — local URL and anon key for the latency measurement
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Q(c) and Assumptions Log A4
  </read_first>
  <action>
**saved-events.** Replace the events read's select with `EVENT_WITH_CLUB_SELECT`; nothing else in the route changes (the UTC floor stays — F-085, Phase 6). Where the embed's literal typing lets the transform accept the rows without the `any` casts, remove them; if removing either needs anything beyond deleting the cast, keep it and say why in the commit body. Move F-080 pin C: the saved-events read now selects the club embed; add "pin C FIXED in 04-10" to the header. Confirm the saved-events PRESERVE suite passes unmodified.

**Detail route.** Replace the false comment on line 79 with one stating that the detail read deliberately still selects no club embed because giving it one changes what every seeded detail page renders under "Hosted by", and that this is F-080's gated half, decided at the 04-11 checkpoint (DEC-27). Change no code in the route; pin D does not move.

**Consumer census.** Record in `evidence/club-embed.txt` the output of a `command grep -rn` over `src/components` and `src/app` for the five link column names read off an event's club, showing no UI renders them from an event payload, plus the saved-events consumer read (ids only).

**Latency (A4).** Against local PostgREST with the anon key, time twenty requests each of the approved-events read selecting star alone and selecting `EVENT_WITH_CLUB_SELECT`, recording each duration and the medians in `evidence/club-join-latency.txt`, with the statement that `events.club_id` is indexed (Phase 3, 03-05) and the measured median difference — never "negligible" without the number.

**Harness.** Reset and seed, run `npx playwright test` into `evidence/playwright.club-embed.txt` (passed equal to `evidence/playwright.pagination.txt`'s, 0 failed, no spec edited in this plan). Append the pin B and pin C rows to the ledger. Commit with a message naming F-080 (non-visual half, pin C) and A4.
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci` exits 0; `git diff --stat HEAD~2 -- 'src/__tests__/api/events/*-characterization.test.ts' e2e/` prints nothing
    - `grep -c "EVENT_WITH_CLUB_SELECT" src/app/api/users/saved-events/route.ts` returns 1
    - `grep -c "Clubs table does not exist" 'src/app/api/events/[id]/route.ts'` returns 0 and `grep -c "04-11" 'src/app/api/events/[id]/route.ts'` is at least 1
    - `git diff -U0 HEAD~1 -- 'src/app/api/events/[id]/route.ts' | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*//'` prints nothing (comment-only change)
    - `evidence/club-join-latency.txt` contains two medians and their difference in milliseconds
    - `evidence/playwright.club-embed.txt` ends with `exit=0`, 0 failed, passed equal to `evidence/playwright.pagination.txt`'s
    - `grep -c "pin C FIXED in 04-10" src/__tests__/api/events/club-fabrication-defect.test.ts` returns 1
  </acceptance_criteria>
  <done>saved-events returns the real club, the detail route tells the truth about why it does not yet, the join's cost is a number, and the harness shows nothing a user sees has moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clubs table → public event payloads | Club columns are embedded into anonymous, cacheable list responses |
| authenticated caller → saved-events | Per-user list, now carrying the real club |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Information Disclosure | Club contact email embedded in every public event payload | mitigate | `contact_email` deliberately excluded from the embed (DEC-27); acceptance asserts it is absent from the literal |
| T-04-10-02 | Information Disclosure | Pending or rejected clubs' data embedded in event payloads | accept | The embed applies the clubs SELECT policy under RLS; F-022 already records that policy ignores status, and six routes already embed; this plan widens columns, not rows. Owner: Phase 5 |
| T-04-10-03 | Denial of Service | Added join latency on saved-events and wider embeds | mitigate | Measured (A4) with medians recorded; `events.club_id` is indexed |
| T-04-10-04 | Tampering | Shipping the visual half of F-080 without the owner's decision | mitigate | Prohibition; pins A and D must not move; the detail route change is comment-only by acceptance |
| T-04-10-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `npx jest --ci` exits 0, 0 skipped; PRESERVE suites unchanged
- `npx tsc --noEmit`, `npm run lint` clean
- `npx playwright test` exits 0 at 04-09's count, no spec edited
- `node scripts/check-characterization-tags.mjs --all` exits 0
</verification>

<success_criteria>
Every club an event payload carries now comes from a real join with the columns its type promises — on the list routes and on saved-events — the cost of that join is measured, and nothing a user sees changed. What remains of F-080 is exactly the part that would change seeded rendering, named in source and waiting for the owner at 04-11.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New exports:** `EVENT_CLUB_EMBED`, `EVENT_WITH_CLUB_SELECT` in `src/lib/tagMapping.ts`.
**Modified routes:** events, events/new, events/following, events/happening-now, events/popular, calendar/events, users/saved-events (shared embed); events/[id] (comment only).
**Moved DEFECT pins:** F-080 pins B and C.
**New evidence:** `evidence/club-embed.txt`, `evidence/club-join-latency.txt`, `evidence/playwright.club-embed.txt`; ledger rows.
**Research assumption closed:** A4.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-10-SUMMARY.md` when done.
</output>
