---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 11
type: execute
wave: 8
depends_on: ["04-10"]
files_modified:
  - src/lib/tagMapping.ts
  - src/lib/eventTags.ts
  - src/lib/eventTags.test.ts
  - src/app/api/events/[id]/route.ts
  - src/__tests__/api/events/club-fabrication-defect.test.ts
  - src/__tests__/lib/tag-coercion-defect.test.ts
  - e2e/specs/event-read-path.spec.ts
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/visual-fix-decision.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/visual-change-log.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.phase-after.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.phase-after.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md
autonomous: false
requirements: [REFAC-09, REFAC-10]

must_haves:
  truths:
    - "The conflict between ROADMAP criterion 2 (real club instead of fabricated) and orchestrator decision 1 (no change to seeded rendered output) is resolved by the phase owner in writing, or by rule to deferral when no owner answer is available — never by the executor's preference"
    - "Whatever was decided is executed exactly: shipped visual fixes land as their own logged commits with their DEFECT pins moved; deferred ones stay pinned with an owner"
    - "After both slices, the full floor is at or above the before-floor, the harness passes from a clean reset, and every Validated workflow in PROJECT.md is re-confirmed by a named test or shown untouched"
    - "Every Phase 4 finding's status in the register matches its evidence, and each requirement is recorded Complete only if every clause is evidenced, otherwise PARTIAL naming the clause"
  artifacts:
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/visual-fix-decision.md"
      provides: "The checkpoint's outcome: option, who decided, when, and what it means for criterion 2 and REFAC-10"
      min_lines: 20
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md"
      provides: "Criteria 1-4 clause by clause with citations, requirement states, Validated-list re-confirmation, findings closed, deferrals, CI observation status"
      min_lines: 120
  key_links:
    - from: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md"
      to: ".planning/audit/findings.json"
      via: "every finding the note calls closed is Fixed in the register with a resolution citing the note"
      pattern: "PHASE-4-COMPLETION"
  prohibitions:
    - "The executor does not choose an option on its own judgement; without an owner answer the rule selects option-defer and the note says so"
    - "No visual change ships outside Task 2, and none ships without a line in evidence/visual-change-log.md"
    - "No push is performed on the executor's own authority; CI observation is recorded as UNOBSERVED if the owner has not pushed"
    - "findings.json edited surgically; FOUNDATION_AUDIT.md only by its generator"
    - "No migration, no production access, no package change"
---

<objective>
Wave 8, alone and the phase's last: put the one conflict the plan set could not resolve to the phase owner, execute the answer, then close the phase with evidence.

Purpose: ROADMAP criterion 2 requires the event read path to stop fabricating clubs; orchestrator decision 1, derived from criterion 4, forbids changing what seeded data renders. Planning found (DEC-27) that they collide: every seeded event carries both a club_id and an organizer label, and `/api/events/[id]` selects no club, so its detail page says "Hosted by" the organizer label today — any real fix changes that text. Criterion 3's tag clause is already met non-visually (04-07), but its underlying six mis-mappings (F-081) are the same kind of change. 04-RESEARCH.md § Q(e) and Open Question 2 recommend exactly this shape: plan the change, and let the owner defer it in writing.

Output: a recorded decision, the decided code change or the recorded deferral, and the Phase 4 completion note with the register reconciled.
</objective>

<slice_outcome>
Green when `evidence/visual-fix-decision.md` names the option and who chose it, the code state matches that option, `npx playwright test` passes from a clean reset, `node .planning/audit/tools/validate.mjs --check findings` passes, and `evidence/PHASE-4-COMPLETION.md` answers all four success criteria clause by clause with every claim citing a committed artifact.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md
@.planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/FOUNDATION-READINESS.md
</context>

<phase_locked_constraints>
1. Behaviour preservation — any behaviour change here is one the owner chose, in its own commit, logged.
2. Characterize first — the F-080 pins A and D and the F-081 suite exist and pass before Task 2; if a fix ships, they move in its commit.
3. No schema change. 4. Production is never touched; no push without the owner.
5. Visual change only by owner decision, logged line by line.
6. Jest single runner. 7. Stage by explicit path only.
8. Local-stack exclusivity — wave 8's only plan.
9. Evidence discipline — the completion note cites a committed path for every claim (the FOUNDATION-READINESS precedent: zero missing citations).
10. A requirement is measured against its own sentence: a met success criterion does not round a requirement up (Phase 3 § 11 precedent).
</phase_locked_constraints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner decision — ship the two data-correction fixes that change seeded rendering, or defer them in writing</name>
  <files>.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-26 and § DEC-27 — the recorded conflict, in the planner's words
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt and e2e/specs/event-read-path.spec.ts DEFECT tests 8 and 9 — the measured seeded deltas to quote to the owner
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/club-embed.txt — what 04-10 already shipped non-visually
  </read_first>
  <action>
Do not change any file in this task. Present the decision below to the phase owner with the measured deltas quoted from the evidence (the organizer label the seeded detail page shows today, read from the 04-04 run), and wait for an explicit selection naming the person deciding. Do not recommend an option: this is the owner's call because it overrides, or honours, a decision the orchestrator fixed. If no owner answer is available, select option-defer by rule and say so in Task 2's decision file.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - The resume signal names exactly one of option-defer, option-ship-club, option-ship-both, and either the deciding owner or "no owner answer"
    - `git status --porcelain -- src/ e2e/` prints nothing at the end of this task
  </acceptance_criteria>
  <done>An option is selected by a named owner, or option-defer is selected by rule, and nothing has changed yet.</done>
  <decision>Whether F-080's visual half (remove the organizer-string club fallback; give /api/events/[id] the real club embed) and F-081's fix (identity mappings for tech, food, volunteer, arts, music, networking) ship in Phase 4 as logged intentional behaviour changes, or are deferred with an owner.</decision>
  <context>
Measured on seeded data by 04-04's Playwright DEFECT tests (evidence/playwright.slice-2-before.txt) and research § Q(e):

- F-080: `/events/<approved event>` shows "Hosted by" the seeded organizer label (one of Seed Organizer A/B/C) while the feed card for the same event shows "Seed Approved Club". Shipping makes the detail page say "Hosted by Seed Approved Club" with the club category. In production, every event created through the app stores the creator's display name as `organizer`, so today its detail page names the creator as host; shipping names the club. Scraped events with no club (organizer only) lose the organizer pill on cards and the "Hosted by" block on detail pages — `event.organizer` stays in the payload.
- F-081: the approved event's card shows Academic + Social; shipping shows Academic + Tech. The music night shows Cultural + Social; shipping shows Music + Social. Filtering by a tag and the badge on the result then agree, which today they do not.

Consequences for the phase: without option-ship-club, ROADMAP criterion 2 and REFAC-10's first clause are recorded PARTIAL at close (the list routes' real embed and saved-events' real club shipped in 04-10; the organizer fallback remains). Criterion 3's tag clause is met either way (04-07). Orchestrator decision 1 ("observable UI on seeded data stays identical") is honoured only by option-defer; the other options override it for the named changes and must be recorded as the owner's override.

Rule if no owner answer is available: select option-defer, and record in evidence/visual-fix-decision.md that the checkpoint was resolved by rule, not by the owner (the 03-08 precedent).
  </context>
  <options>
    <option id="option-defer">
      <name>Defer both (default; honours orchestrator decision 1)</name>
      <pros>No rendered change anywhere; Phase 4 stays inside "no intentional visual change"</pros>
      <cons>Criterion 2 and REFAC-10 clause 1 close PARTIAL; production keeps naming the creator as host; badges keep disagreeing with filters</cons>
    </option>
    <option id="option-ship-club">
      <name>Ship F-080's visual half only</name>
      <pros>Criterion 2 and REFAC-10 clause 1 are met; detail page and card agree about the host</pros>
      <cons>Overrides orchestrator decision 1 for the detail page's host block and scraped events' organizer pill</cons>
    </option>
    <option id="option-ship-both">
      <name>Ship F-080's visual half and F-081's identity mappings</name>
      <pros>Both data-correction defects closed; tag filters and badges agree</pros>
      <cons>Overrides orchestrator decision 1 for host text and for six categories' badges and colours</cons>
    </option>
  </options>
  <resume-signal>Select: option-defer, option-ship-club, or option-ship-both (and name the owner deciding)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the decision — ship each chosen fix as its own logged commit, or record the deferral with an owner</name>
  <files>src/lib/tagMapping.ts, src/lib/eventTags.ts, src/lib/eventTags.test.ts, src/app/api/events/[id]/route.ts, src/__tests__/api/events/club-fabrication-defect.test.ts, src/__tests__/lib/tag-coercion-defect.test.ts, e2e/specs/event-read-path.spec.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/visual-fix-decision.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/visual-change-log.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md</files>
  <read_first>
    - The Task 1 answer
    - src/lib/tagMapping.ts — the organizer fallback branch and `EVENT_WITH_CLUB_SELECT`
    - src/app/api/events/[id]/route.ts lines 71-110 — the detail read, its 04-10 comment, and the cast on the transform call
    - src/lib/eventTags.ts and src/lib/eventTags.test.ts — `TAG_ALIASES`, `KNOWN_NON_ROUNDTRIP_TAGS`, the golden table and the completeness test
    - src/__tests__/api/events/club-fabrication-defect.test.ts pins A and D; src/__tests__/lib/tag-coercion-defect.test.ts
    - e2e/specs/event-read-path.spec.ts DEFECT tests 8 (F-080) and 9 (F-081)
    - src/components/events/EventCard.tsx, DiscoveryCard.tsx, EventDetailView.tsx, EventDetailsModal.tsx and src/app/calendar/page.tsx — every renderer of `event.club`, each already guarded for a missing club
  </read_first>
  <action>
Write `evidence/visual-fix-decision.md` first, with the metadata line, the option selected, the name of the person who selected it or the sentence "resolved by rule — no owner answer was available", the date, and what the option means for criterion 2 and REFAC-10.

**If option-ship-club or option-ship-both — commit A (F-080):** in `transformEventFromDB`, remove the organizer fallback so an event with no club row has no `club` (the `organizer` field still passes through); in `/api/events/[id]`, select `EVENT_WITH_CLUB_SELECT`, remove the cast if the literal typing makes it unnecessary, and replace the 04-10 comment. Move pins A and D, and flip Playwright test 8 to `FIXED F-080:` asserting "Hosted by Seed Approved Club". Every PRESERVE suite must pass unmodified (their fixtures have a null organizer). Commit with a subject beginning `fix(04-11): return the real club instead of fabricating one from the organizer string (F-080)` whose body begins `INTENTIONAL VISUAL CHANGE` and lists the seeded and production deltas from Task 1's context. Add those deltas to `evidence/visual-change-log.md` and the pin moves to the ledger.

**If option-ship-both — commit B (F-081), separately:** in `TAG_ALIASES`, map tech, food, volunteer and arts to themselves and change music and networking to themselves; leave every other alias as it is (coding and technology staying academic is a product call outside F-081). Empty `KNOWN_NON_ROUNDTRIP_TAGS`, keeping the export with a comment that F-081 was fixed here, so the completeness test now requires every member to round-trip. Move the six golden-table rows and the F-081 DEFECT suite's assertions, and flip Playwright test 9 to `FIXED F-081:` asserting the Tech label on the approved event's card. Commit with a subject beginning `fix(04-11): map every EventTag member to itself (F-081)` and a body beginning `INTENTIONAL VISUAL CHANGE`; log the badge deltas and the ledger rows.

**If option-defer (or for whichever fix was not chosen):** change no source. Add to `evidence/deferred-items.md` one DI entry per deferred fix, continuing the DI sequence, stating what is deferred, why (orchestrator decision 1, the owner's choice or the rule), which pins keep it visible (pins A and D; the F-081 suite; Playwright tests 8 and 9), and the owner the decision names — or "the phase owner" when resolved by rule.

After whichever path: run the full Jest suite, `npx tsc --noEmit`, `npm run lint`, and `node scripts/check-characterization-tags.mjs --all`.
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit && npm run lint && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/visual-fix-decision.md` names exactly one of `option-defer`, `option-ship-club`, `option-ship-both` and contains either an owner's name or the phrase `resolved by rule`
    - If a ship option was chosen: `git log --format=%s` shows the F-080 commit (and for ship-both the F-081 commit) as separate commits; each commit's body begins `INTENTIONAL VISUAL CHANGE`; `evidence/visual-change-log.md` has a line per delta; `git diff --stat <04-10 head> -- 'src/__tests__/api/events/*-characterization.test.ts'` prints nothing
    - If option-defer: `git diff --stat <04-10 head> -- src/` prints nothing and `evidence/deferred-items.md` gains one DI entry per deferred fix naming an owner
    - `npx jest --ci` exits 0 with 0 skipped; `npx tsc --noEmit` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>The collision between criterion 2 and orchestrator decision 1 is resolved by an owner or by a stated rule, the code matches the decision exactly, and every visual change that shipped is logged line by line.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 4 — after-floor, harness, register reconciliation, and the completion note</name>
  <files>.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.phase-after.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.phase-after.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/PHASE-4-COMPLETION.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md, .planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 4 — the four success criteria, verbatim
    - .planning/REQUIREMENTS.md lines 74-75 — REFAC-09 and REFAC-10, verbatim
    - .planning/PROJECT.md § Requirements › Validated
    - evidence/floor.before.txt, evidence/floor.slice-1-after.txt, evidence/slice-1-close.md, evidence/defect-ledger.md, evidence/visual-fix-decision.md, evidence/visual-change-log.md (if present), evidence/deferred-items.md, evidence/phase-04-decisions.md
    - .planning/audit/findings.json records F-050, F-066, F-079 through F-085 and F-071
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/FOUNDATION-READINESS.md — the clause-by-clause structure and the citation-count discipline
  </read_first>
  <action>
**After-floor.** Re-run every command from `evidence/floor.before.txt` with a clean `supabase db reset --local`, the unseeded pgTAP run, a seed load, the seeded pgTAP run and `npx playwright test`, into `evidence/floor.phase-after.txt` (line 1 `head=<sha>`) and `evidence/playwright.phase-after.txt`. Require: Jest passed at or above every earlier count with 0 skipped suites and 0 skipped tests; lint 0 errors; tsc clean with tests included; audit at or below the before count; ratchet `committed=25 live=25 delta=0`; filenames unchanged; pgTAP counts equal to the before-floor's (no schema changed); Playwright passed equal to the 04-10 count plus any tests the decision moved, 0 failed; stale-date census 0; client-cast census under `src/app/api/` 0.

**Register.** Surgically set to `Fixed`, each with a `resolution` naming its commits and citing `evidence/PHASE-4-COMPLETION.md`: F-050, F-066 (both clauses), F-082, F-083, and F-080 and F-081 if shipped. If F-080 was not shipped, keep it Open and add a resolution recording the non-visual half fixed in 04-10 and the deferral with its DI id; same for F-081. F-084 and F-085 stay Open at Phase 6. Regenerate `FOUNDATION_AUDIT.md`; run `node .planning/audit/tools/validate.mjs --check findings`.

**Completion note.** Write `evidence/PHASE-4-COMPLETION.md` with the metadata line and:
1. Each of the four success criteria quoted verbatim, then answered clause by clause as MET or PARTIAL, each clause citing committed paths.
2. REFAC-09 and REFAC-10 quoted verbatim, each clause evidenced; a requirement is Complete only when every clause is met — otherwise PARTIAL naming the unmet clause.
3. The Validated-workflow re-confirmation: one row per PROJECT.md Validated bullet, naming the test that re-confirms it after both slices, or showing it untouched by `git diff --name-only <base>`.
4. Every intentional behaviour change the phase shipped, with its finding: special-character search matches literally (F-082); Load More appears in production for filtered results over 30 (F-083); RSVP counts no longer capped (F-079); friends fallback works (F-071); the three dropped auth-error log lines (DEC-29); plus F-080/F-081 if the decision shipped them — and the statement that plain-text search, browse, filter, save and RSVP render identically on seeded data, citing the harness runs.
5. The DI dispositions (DI-24, DI-25, DI-31, DI-34, DI-35 closed or re-owned) and the research assumptions A1-A8 closed or carried.
6. CI observation: if the owner pushed, the run id and the three job conclusions (`ci`, `types`, `e2e`); otherwise the line `CI run UNOBSERVED — no push was authorized in this phase` — never a claim without a run.
7. A citation count: the number of distinct committed paths cited and the number missing (must be 0), computed by a one-line script whose command is in the note.

Update `evidence/deferred-items.md` with the final state of every item it holds.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && npx jest --ci && node scripts/check-characterization-tags.mjs --all && node scripts/check-elevated-ratchet.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.phase-after.txt` ends with `exit=0` and 0 failed
    - `evidence/floor.phase-after.txt` line 1 matches `^head=[0-9a-f]{40}$` and its Jest block reports no skipped suites or tests
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i);process.exit(["F-050","F-066","F-071","F-079","F-082","F-083"].every(i=>g(i).status==="Fixed"&&g(i).resolution)?0:1)'` exits 0
    - `node .planning/audit/tools/validate.mjs --check findings` exits 0
    - `grep -cE "^\*\*(MET|PARTIAL)\*\*|\| (MET|PARTIAL) \|" evidence/PHASE-4-COMPLETION.md` is at least 4
    - `evidence/PHASE-4-COMPLETION.md` contains `REFAC-09` and `REFAC-10` each followed by `Complete` or `PARTIAL`, and a citation-count line reporting 0 missing
    - The Validated re-confirmation table has as many rows as `grep -c '^- ✓' .planning/PROJECT.md` returns
    - `git status --porcelain -- supabase/migrations package.json package-lock.json` prints nothing
  </acceptance_criteria>
  <done>Phase 4 closes with a measured floor at or above where it started, a green harness from a clean database, a register whose statuses match their evidence, and a completion note that answers every criterion and requirement clause by clause — including, in plain words, whichever clause the owner's decision left partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase owner → plan execution | A product-facing change is authorized by a person, not inferred by the executor |
| clubs table → event detail payload | If F-080 ships, the detail route embeds the club under RLS |
| repository → CI | Observation requires a push the executor may not perform on its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Repudiation | A visual change shipped without an owner, or a deferral with no owner | mitigate | Blocking checkpoint; the decision file names the chooser or states the rule; each deferral becomes a DI entry with an owner |
| T-04-11-02 | Information Disclosure | Detail route gaining a club embed | accept | Same embed the list routes already carry under the clubs RLS policy (F-022, Phase 5); contact_email excluded (DEC-27) |
| T-04-11-03 | Repudiation | A completion note claiming what it does not cite | mitigate | Citation count with 0 missing, computed by a committed command; CI stated UNOBSERVED unless a run exists |
| T-04-11-04 | Tampering | findings.json reformatted while flipping statuses | mitigate | Surgical edits; `validate.mjs --check findings` |
| T-04-11-05 | Tampering | A push to trigger CI without authorization | mitigate | Prohibition; UNOBSERVED is the honest default |
| T-04-11-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- The decision is recorded and the code matches it
- `npx jest --ci` 0 skipped; `npx tsc --noEmit`, `npm run lint` clean; ratchet delta 0; tag gate green
- `npx playwright test` green from a clean reset
- `node .planning/audit/tools/validate.mjs --check findings` exits 0
- `evidence/PHASE-4-COMPLETION.md` reports 0 missing citations
</verification>

<success_criteria>
The phase's one genuine conflict is decided by a person or by a written rule, not absorbed silently; the decided changes ship as logged commits or stay pinned with owners; and the completion note answers all four success criteria and both requirements clause by clause against committed evidence, re-confirming every Validated workflow.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:
- `evidence/visual-fix-decision.md`, `evidence/visual-change-log.md` (if anything shipped), `evidence/floor.phase-after.txt`, `evidence/playwright.phase-after.txt`, `evidence/PHASE-4-COMPLETION.md`
- Conditional source changes: organizer fallback removed and detail embed added (F-080 visual half); identity tag mappings (F-081)
- Register: F-050, F-066, F-082, F-083 Fixed; F-080/F-081 Fixed or Open-with-deferral per the decision

Phase-wide roll-up (every plan):
- **New modules:** `src/lib/eventTags.ts` (`TAG_ALIASES`, `partitionTags`, `mapTags`, `KNOWN_NON_ROUNDTRIP_TAGS`), `src/lib/searchFilter.ts` (`escapeLikeLiteral`, `postgrestQuotedValue`, `ilikeContainsFilter`), `src/lib/eventCursor.ts` (`EventCursor`, `encodeEventCursor`, `decodeEventCursor`), `EVENT_CLUB_EMBED` and `EVENT_WITH_CLUB_SELECT` in `src/lib/tagMapping.ts`
- **New scripts:** `scripts/check-characterization-tags.mjs`, `scripts/probes/search-escape-probe.ts`
- **New test helper:** `src/__tests__/helpers/fakeSupabase.ts` (`createFakeSupabase`)
- **New suites:** save, saved-events, calendar-events, rsvp, events-list and events-detail characterization (PRESERVE); rsvp-count (F-079), saved-events-time-floor (F-085), search-escaping (F-082), pagination-contract (F-083), club-fabrication (F-080), tag-coercion (F-081) DEFECT; `eventTags`, `searchFilter`, `eventCursor` unit suites; `src/app/api/events/route.test.ts` rewritten and running
- **New e2e spec:** `e2e/specs/event-read-path.spec.ts`; `e2e/specs/save-and-rsvp.spec.ts` extended
- **New CI steps:** "Elevated-callsite ratchet", "Characterization tag gate"
- **Findings registered:** F-079, F-080, F-081, F-082, F-083, F-084, F-085
- **Decisions:** DEC-23 through DEC-31
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-11-SUMMARY.md` when done.
</output>
