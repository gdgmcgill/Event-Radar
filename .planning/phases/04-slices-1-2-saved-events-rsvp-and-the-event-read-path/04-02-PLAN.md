---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/__tests__/helpers/fakeSupabase.ts
  - src/__tests__/api/events/save-characterization.test.ts
  - src/__tests__/api/events/saved-events-characterization.test.ts
  - src/__tests__/api/events/saved-events-time-floor-defect.test.ts
  - src/__tests__/api/events/calendar-events-characterization.test.ts
  - src/__tests__/api/events/rsvp-characterization.test.ts
  - src/__tests__/api/events/rsvp-count-defect.test.ts
  - e2e/specs/save-and-rsvp.spec.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-characterization.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-mutation-check.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-unmodified.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-1-before.txt
autonomous: true
requirements: [REFAC-09]

must_haves:
  truths:
    - "Save/unsave, the saved-events list, the calendar feed and RSVP create/update/cancel/count are pinned by suites that call the exported handlers against unmodified source and assert on the returned response"
    - "The RSVP count PRESERVE test passes whether counts come from loaded rows or from head-count queries, so it can stay unmodified across the F-079 fix"
    - "The load-all RSVP count (F-079) and the UTC 'upcoming' floor on saved events (F-085) are pinned by DEFECT suites that cite their ids"
    - "The ban check's current asymmetry — POST blocked, DELETE allowed — is pinned so Phase 5 inherits a measured starting point"
    - "The persona harness proves the saved state through GET /api/users/saved-events (not only the profile RSC path) and proves the going count after a reload"
    - "Every PRESERVE and DEFECT suite has been observed going red under a mutation of the behaviour it pins"
  artifacts:
    - path: "src/__tests__/helpers/fakeSupabase.ts"
      provides: "In-memory PostgREST-shaped fake: filtering for eq/neq/is/in/gte/lte/lt/gt, count and head semantics, per-table error injection, a call log for query-shape assertions"
      exports: ["createFakeSupabase"]
      min_lines: 120
    - path: "src/__tests__/api/events/rsvp-characterization.test.ts"
      provides: "PRESERVE — RSVP GET counts and user_rsvp, POST create/update/already, DELETE cancel, ban asymmetry"
      min_lines: 120
    - path: "src/__tests__/api/events/rsvp-count-defect.test.ts"
      provides: "DEFECT F-079 — the rsvps read is a row-returning select with no count option"
      min_lines: 50
    - path: "src/__tests__/api/events/save-characterization.test.ts"
      provides: "PRESERVE — save toggle, unsave, every error body, ban asymmetry"
      min_lines: 120
    - path: "e2e/specs/save-and-rsvp.spec.ts"
      provides: "The Slice 1 end-to-end net, extended with the API-path saved assertion and the reload-time count assertions"
      contains: "going"
  key_links:
    - from: "src/__tests__/api/events/*-characterization.test.ts"
      to: "src/__tests__/helpers/fakeSupabase.ts"
      via: "every new Slice 1 suite injects the fake through a jest.mock of the server factory module"
      pattern: "createFakeSupabase"
    - from: "src/__tests__/api/events/rsvp-count-defect.test.ts"
      to: ".planning/audit/findings.json"
      via: "the DEFECT header cites F-079, which the tag gate resolves"
      pattern: "DEFECT characterization — F-079"
    - from: "e2e/specs/save-and-rsvp.spec.ts"
      to: "src/app/api/users/saved-events/route.ts"
      via: "after a reload the Unsave control is derived from GET /api/users/saved-events in EventDetailClient"
      pattern: "users/saved-events"
  prohibitions:
    - "No file under src/app/, src/lib/ or src/server/ is modified — the subjects stay byte-identical to the base commit recorded in evidence/floor.before.txt"
    - "No new suite imports from the Supabase SDK package or the service-role factory; the fake is structural"
    - "PRESERVE fixtures use only the six tags whose mapping F-081 cannot change (academic, social, sports, career, cultural, wellness), a null organizer, and no fabricated club, so no later F-080 or F-081 commit can move a PRESERVE assertion"
    - "No PRESERVE assertion pins a known defect: the F-059 error-text echo, the F-080 club shape and the F-085 floor value are asserted only in DEFECT suites or not at all"
    - "No Vitest API; the helper uses no jest global so it type-checks inside the main tsconfig program"
    - "The seed is not changed (DEC-30)"
---

<objective>
Wave 2: write the Slice 1 characterization net against unmodified source — Jest suites for every handler Slice 1 refactors, DEFECT pins for the two registered Slice 1 defects, and the Playwright additions that close research Pitfalls 1 and 2 — and prove every assertion bites.

Purpose: REFAC-09's third clause is "characterization tests pass before and after", and ROADMAP criterion 1 requires them tagged PRESERVE or DEFECT against an F-nnn. 04-RESEARCH.md § "Existing test coverage for Slice 1" measured that `rsvp.test.ts` holds 16 error-path tests and no happy path or count assertion, and that `/api/events/[id]/save`, `/api/users/saved-events` and `/api/calendar/events` have no test at all. A refactor proven only by the existing suites would prove the 401s survived and nothing else.

Output: an in-memory Supabase fake, six suites (four PRESERVE, two DEFECT), three Playwright additions, a mutation-cycle record, and a git-ancestry proof that every subject predates its suite.
</objective>

<slice_outcome>
Green when `npx jest --ci --selectProjects node --testPathPatterns "(save|saved-events|calendar-events|rsvp)-(characterization|count-defect|time-floor-defect)"` passes, `node scripts/check-characterization-tags.mjs --all` passes with the six new suites included, `npx playwright test` passes with one more test than the before-floor, and `git diff --stat <base> -- src/app src/lib src/server` is empty.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-PATTERNS.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@src/app/api/events/[id]/save/route.ts
@src/app/api/events/[id]/rsvp/route.ts
@src/app/api/users/saved-events/route.ts
@src/app/api/calendar/events/route.ts
@src/lib/ban.ts
@src/server/context.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — this plan changes no production file.
2. Characterize first — every new suite's leading docblock carries PRESERVE or DEFECT; every DEFECT cites an F-nnn in `.planning/audit/findings.json`; `node scripts/check-characterization-tags.mjs --all` is the gate.
3. No schema change; no seed change (DEC-30).
4. Production is never touched — local stack only; `.env.local` is never read.
5. No intentional visual change — the Playwright additions assert current behaviour only.
6. Jest is the single runner; new `.test.ts` files under `src/__tests__/` route to the `node` project; `--testPathPatterns` is plural; no Vitest API.
7. Stage by explicit path only.
8. Local-stack exclusivity — this plan is wave 2's only stack user (04-03 runs without the stack).
9. Evidence discipline — raw output verbatim with the command above and the exit code below.
10. Test files become type-checked in 04-06 (DI-24): write every new suite so a throwaway tsconfig that extends the repository's, sets `typeRoots` to the repository's `node_modules/@types`, and includes only the new files reports zero errors — typed handler references called with the handler's real argument list, a top-level `import`, no `any` for a handler.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the in-memory Supabase fake, and pin save, saved-events and the calendar feed</name>
  <files>src/__tests__/helpers/fakeSupabase.ts, src/__tests__/api/events/save-characterization.test.ts, src/__tests__/api/events/saved-events-characterization.test.ts, src/__tests__/api/events/saved-events-time-floor-defect.test.ts, src/__tests__/api/events/calendar-events-characterization.test.ts</files>
  <read_first>
    Context budget: ten entries. [FULL] means read the whole file (the four subjects under test are all short); [SECTION] means read only the named lines — do not read a [SECTION] file whole.
    - [FULL, 131 lines] src/app/api/events/[id]/save/route.ts — the whole file; every early return is a branch to pin, and POST calls `checkBanStatus()` before authentication while DELETE does not call it at all
    - [FULL, 148 lines] src/app/api/users/saved-events/route.ts — the whole file; the two-step read (saved rows, then events with in/eq/is and an optional gte floor), the three sort orders, the `include_past` switch, and the per-event `saved_at` merge
    - [FULL, 94 lines] src/app/api/calendar/events/route.ts — the whole file; the union of saved and going/interested ids, the annotation fields, the from/to filters, and the two 500 paths
    - [FULL, 38 lines] src/lib/ban.ts — `checkBanStatus()` builds its own server client, reads the user, then reads `users.banned_at, ban_expires_at` with `.single()`; `isBanned` treats a past expiry as not banned
    - [SECTION] src/server/context.ts lines 56-75 — after 04-05 adopts the seam, every authenticated request also reads `users` with `.single()`; the fake must answer that read sanely so these suites pass unmodified after adoption
    - [SECTION] src/lib/tagMapping.ts lines 102-174 — `transformEventFromDB` runs for real inside these suites; it fabricates a club from `organizer` and coerces six tags, which is why PRESERVE fixtures must avoid both
    - [SECTION] src/__tests__/api/events/rsvp.test.ts lines 1-95 — the repository's existing driver: `jest.mock` of `@/lib/supabase/server`, the `mock`-prefixed module variable the hoisting rule requires, `createRouteContext`
    - [SECTION] src/app/auth/callback/route.test.ts lines 1-54 — the PRESERVE header shape to reproduce
    - [SECTION] src/__tests__/api/events/friends-defect.test.ts lines 1-40 — the DEFECT header shape to reproduce
    - [FULL, 29 lines] src/lib/sanitize.test.ts — the one-describe-per-behaviour, plain `it` + `toBe` style
  </read_first>
  <behavior>
    - save DELETE: anonymous → 401 `{ error: "Unauthorized" }`; an auth error with a null user → 401; success → 200 `{ saved: false }` and the delete is scoped by both user_id and event_id; a delete error → 500 `{ error: "Failed to unsave event" }`; a thrown error → 500 `{ error: "Internal server error" }`
    - save POST: a permanently banned user → 403 `{ error: "Account suspended" }` with no events read; a user whose suspension has expired is not blocked; anonymous → 401; event lookup error → 500 `{ error: "Failed to verify event" }`; missing or soft-deleted event → 404 `{ error: "Event not found" }`; existing-row check error → 500 `{ error: "Failed to check saved event" }`; not yet saved → 200 `{ saved: true }` and exactly one row inserted with the caller's id; already saved → 200 `{ saved: false }` and the row removed; insert error → 500 `{ error: "Failed to save event" }`
    - ban asymmetry (PRESERVE, for Phase 5): the same banned user's DELETE returns 200 `{ saved: false }`
    - saved-events GET: anonymous → 401; an auth error → 401; no saved rows → 200 `{ events: [], savedEventIds: [] }`; default sort orders by saved_at descending; `sort=date` orders by start_date ascending; `sort=title` orders by title; `include_past=true` applies no start_date floor; only approved, non-deleted events are returned; each event carries `saved_at`; `savedEventIds` mirrors the returned order; saved-rows error → 500 `{ error: "Failed to fetch saved events" }`; events error → 500 `{ error: "Failed to fetch event details" }`
    - saved-events floor (DEFECT F-085): with the system clock pinned, the default request applies a start_date floor equal to the true-UTC ISO string of that instant, not the Eastern wall-clock value `getESTNowISO()` would give
    - calendar GET: anonymous → 401; no saved or RSVP'd events → 200 `{ events: [] }`; the result is the union of saved ids and going/interested RSVP ids with `is_saved` and `rsvp_status` annotated per event; a cancelled RSVP contributes nothing; `from`/`to` apply gte/lte on start_date; a query error → status 500; a thrown error → 500 `{ error: "Failed to fetch calendar events" }`
  </behavior>
  <action>
**The fake.** Create `src/__tests__/helpers/fakeSupabase.ts` exporting `createFakeSupabase(init)` where `init` carries `user` (an object with `id` and optional `email`, or null), `authError` (or null), `tables` (a record of table name to row arrays), `errors` (a record of table name to a PostgREST-shaped `{ code, message }` returned by any query on that table), and `rpc` (a record of function name to `{ data, error }`). It returns `{ client, calls, tables }`. `client` has `auth.getUser()` resolving `{ data: { user }, error: authError }`, `from(table)` returning a thenable builder, and `rpc(name, args)` returning the configured result and logging the call. The builder supports `select(columns, options)`, `insert`, `update`, `delete`, `eq`, `neq`, `is`, `in`, `gte`, `lte`, `lt`, `gt`, `or`, `overlaps`, `order`, `range`, `limit`, `maybeSingle`, `single` and `then`. It genuinely evaluates `eq`, `neq`, `is`, `in`, `gte`, `lte`, `lt` and `gt` against the table's rows; records `or` and `overlaps` without evaluating them; applies successive `order` calls cumulatively as sort keys in call order (the first call is the primary key, so a later tie-breaker never re-sorts the whole result), then `range`; returns `{ data: null, count }` when `head: true` was passed and `{ data, count }` when `count: "exact"` was passed without head; makes `single()` return error code `PGRST116` on zero rows and `maybeSingle()` return null data; mutates the in-memory table on insert, update and delete, returning the affected rows when `select()` is chained. Every terminal call appends one entry to `calls` recording table, operation, columns, options, the ordered filter list and any payload. Use no jest global in this file, and import no type from the Supabase SDK package — it is structural, so it type-checks in the main program today.

**The suites.** Each new suite mocks `@/lib/supabase/server` so its `createClient` resolves the current fake's client from a `mock`-prefixed module variable reassigned in `beforeEach`; `@/lib/ban` is NOT mocked, so the real ban check runs against the same fake. Import handlers statically at the top and call them with their real argument lists (`request` and, for `[id]` routes, a context whose `params` is a resolved promise). Pin the system clock with Jest's modern fake timers where a floor value is asserted.

Write `save-characterization.test.ts`, `saved-events-characterization.test.ts` and `calendar-events-characterization.test.ts` as PRESERVE suites whose leading docblock opens `PRESERVE characterization — <route path> (REFAC-09)`, states it is written against the unmodified route at the base commit recorded in `evidence/floor.before.txt` and must pass byte-for-byte after 04-05 adopts the seam, lists "Tests cover:", has a "WHY THE MOCK SEAM IS THE SERVER FACTORY" section (the route imports it today and `createRequestContext()` awaits the same factory after adoption, so the seam does not move), names what is deliberately NOT pinned (the F-059 error-text echo on the calendar 500, the F-080 club shape, the F-085 floor value) and points at `evidence/slice-1-mutation-check.txt`. Write `saved-events-time-floor-defect.test.ts` as a DEFECT suite in the friends-defect header shape citing F-085, stating it passes today and moves when a shared floor function lands in Phase 6.

PRESERVE fixtures: tags only from academic, social, sports, career, cultural and wellness; `organizer` null; no `club` key on saved-events rows; calendar rows may carry a `club` object holding only the ten columns the current embed lists. Assert status, the full JSON body, and — where the behaviour is a write — the fake's resulting table state or call log. Never assert on a value read off the module without invoking the handler.

Run the four suites and capture the run verbatim into `evidence/slice-1-characterization.txt`.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "(save|saved-events|calendar-events)-characterization|saved-events-time-floor-defect"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "(save|saved-events|calendar-events)-characterization|saved-events-time-floor-defect"` exits 0 with 4 suites passed and at least 30 tests passed
    - `node scripts/check-characterization-tags.mjs src/__tests__/api/events/save-characterization.test.ts src/__tests__/api/events/saved-events-characterization.test.ts src/__tests__/api/events/calendar-events-characterization.test.ts src/__tests__/api/events/saved-events-time-floor-defect.test.ts` exits 0
    - `grep -c "DEFECT characterization — F-085" src/__tests__/api/events/saved-events-time-floor-defect.test.ts` returns 1
    - `grep -c "export function createFakeSupabase" src/__tests__/helpers/fakeSupabase.ts` returns 1
    - `npx tsc --noEmit` exits 0 (the helper is inside the main program and must type-check now)
    - `grep -lE "\bjest\." src/__tests__/helpers/fakeSupabase.ts` prints nothing
    - `git diff --stat -- src/app src/lib src/server` prints nothing
    - A throwaway tsconfig outside the repository that extends the repository tsconfig, sets `typeRoots` to the repository's `node_modules/@types` and includes only the four new suites and `next-env.d.ts` makes `npx tsc --noEmit -p <it>` exit 0 — recorded in `evidence/slice-1-characterization.txt`
  </acceptance_criteria>
  <done>Save, the saved-events list and the calendar feed each have a PRESERVE contract driven through a fake that answers row and count reads alike, the UTC floor is pinned as F-085, and none of the subjects has been touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin RSVP behaviour with a count test that survives the count fix, and pin F-079 as a DEFECT</name>
  <files>src/__tests__/api/events/rsvp-characterization.test.ts, src/__tests__/api/events/rsvp-count-defect.test.ts</files>
  <read_first>
    - src/app/api/events/[id]/rsvp/route.ts — the whole file; GET is anonymous-tolerant and reads the user AFTER the counts; POST calls `checkBanStatus()` and compares the body's user_id to the session's; DELETE does not call the ban check and soft-cancels by update
    - src/__tests__/api/events/rsvp.test.ts — the 16 existing error-path tests; do not duplicate them, and do not modify this file (04-06 owns it)
    - src/__tests__/helpers/fakeSupabase.ts — created in Task 1; the count and head semantics are what let the PRESERVE count test be implementation-agnostic
    - src/app/profile/page.tsx lines 38-42 — the head-count form the F-079 fix will use, so the DEFECT header can name what its assertions will move to
    - .planning/audit/quality/phase-04-slice-defects.md § F-079 — the registered wording the DEFECT header must agree with
    - 04-RESEARCH.md § Q(b) and § Code Examples "A DEFECT characterization header" — the draft header
  </read_first>
  <behavior>
    - GET anonymous: with three going, two interested and four cancelled rows on the event, and rows on another event, the body is `{ counts: { going: 3, interested: 2, total: 5 }, user_rsvp: null }` and `total === going + interested`
    - GET authenticated: `user_rsvp` is the caller's non-cancelled row carrying id, status, created_at and updated_at; a caller whose only row is cancelled gets `user_rsvp: null`
    - GET missing or soft-deleted event → 404 `{ error: "Event not found" }`; event lookup error → 500 `{ error: "Failed to verify event" }`; an error on the rsvps read → 500 `{ error: "Failed to fetch RSVPs" }`
    - POST create → 201 `{ success: true, message: "RSVP'd as going", rsvp }`; POST with a different status on an existing row → 200 with message `RSVP updated to interested`; POST with the same status → 200 `{ message: "Already RSVP'd as going", rsvp }`; a banned caller → 403 `{ error: "Account suspended" }`
    - DELETE with an active row → 200 `{ success: true, message: "RSVP cancelled" }` and the row's status becomes cancelled; no active row → 404 `{ error: "No active RSVP found for this event" }`; a banned caller's DELETE is NOT blocked (PRESERVE of the asymmetry Phase 5 inherits)
    - DEFECT F-079: the GET issues exactly one read on rsvps for the counts, with columns `id, status`, no count option and no head option, filtered by event_id and by status not equal to cancelled
  </behavior>
  <action>
Create `rsvp-characterization.test.ts` as a PRESERVE suite (`PRESERVE characterization — /api/events/[id]/rsvp (REFAC-09)`) with the same header sections as Task 1. The count test is the load-bearing one: it seeds the fake's `rsvps` table with rows of all three statuses across two events and asserts only on the response body, so it passes whether the handler loads rows and filters in JavaScript (today) or issues head-count queries (after 04-05). State that property explicitly in the header, because it is the reason this suite can prove "before and after" for F-079 without being edited. Do not assert on the rsvps call log here.

Create `rsvp-count-defect.test.ts` as a DEFECT suite whose header opens `DEFECT characterization — F-079`, following the friends-defect shape and the research's draft: the subject with line numbers (92-105), the defect (a row-returning select counted in JavaScript, capped at `max_rows = 1000` from `supabase/config.toml` line 18), what the file is and is not, why it asserts on the fake's call log rather than a 1001-row fixture (the cap lives in PostgREST, not in the client), and the sentence that its assertions move to two `{ count: "exact", head: true }` reads in 04-05. Assert from `calls` that the rsvps count read has columns `id, status`, no count and no head option, and the two filters named in the behaviour block.

Both suites use the Task 1 fake and the real `@/lib/ban`. Append the run of both to `evidence/slice-1-characterization.txt`.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "rsvp-(characterization|count-defect)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "rsvp-(characterization|count-defect)"` exits 0 with 2 suites passed and at least 14 tests passed
    - `grep -c "DEFECT characterization — F-079" src/__tests__/api/events/rsvp-count-defect.test.ts` returns 1
    - `grep -c "PRESERVE characterization" src/__tests__/api/events/rsvp-characterization.test.ts` returns 1
    - `node scripts/check-characterization-tags.mjs --all` exits 0
    - `git diff --stat -- src/app src/lib src/server src/__tests__/api/events/rsvp.test.ts` prints nothing
  </acceptance_criteria>
  <done>RSVP's happy paths and counts are pinned in a way that is blind to how the counts are computed, while a separate DEFECT suite pins exactly how they are computed today, so 04-05 can change the second without touching the first.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the persona harness for Pitfalls 1 and 2, then prove every assertion bites and every subject predates its suite</name>
  <files>e2e/specs/save-and-rsvp.spec.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-1-before.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-mutation-check.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-unmodified.txt</files>
  <read_first>
    - e2e/specs/save-and-rsvp.spec.ts — the whole file; its header explains why assertions wait on the write response rather than the optimistic label, and that the profile assertion exercises the RSC path
    - src/app/events/[id]/EventDetailClient.tsx lines 72-130 — after a reload, the saved state is re-derived from GET `/api/users/saved-events` (lines 113-127); this is the API path Pitfall 1 says the spec never exercised
    - src/components/events/RsvpButton.tsx lines 25-45 and 125-140 — counts are fetched on mount and rendered as a `strong` number followed by the word "going" / "interested"; the optimistic arithmetic means a count is only trustworthy after a reload (Pitfall 2)
    - scripts/seed/personas.ts lines 554-580 — the seeded RSVPs: club_member is going to approvedEvent; club_member's RSVP on secondApprovedEvent is cancelled
    - e2e/fixtures.ts — `IDS` and `storageStateFor`; navigate by fixed id, never by title search
    - playwright.config.ts lines 80-140 — build-and-start webServer, never reused; one worker
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/callback-mutation-check.txt — the mutation-cycle record format to follow
  </read_first>
  <action>
**Playwright additions** (all PRESERVE; add a paragraph to the spec header naming them and the Phase 4 plan that added them):
1. In the save test, after the save response has landed, reload the event page while waiting for the GET `/api/users/saved-events` response, then assert the Unsave control is visible — the saved state now comes from the API route rather than from React state.
2. In the RSVP test, after the existing reload, assert the going count reads 2: the seeded club_member going row plus this persona's. It is an end-state assertion, so a re-run against an already-RSVP'd event still reads 2.
3. A new test on `/events/${IDS.secondApprovedEvent}` as the same persona asserting, after the counts request completes, that the going count reads 0 — the seeded cancelled row is not counted.

Reset and seed the local stack (`supabase db reset --local`, then `npx tsx scripts/seed/load.ts`), run `npx playwright test`, and capture it into `evidence/playwright.slice-1-before.txt` with the exit code. The passed count must equal the before-floor's Playwright count plus 1.

**Mutation cycles.** For each of the six new suites, perform at least one mutation of the subject route that removes or alters the behaviour the suite pins, run only that suite, record the red with the failing test's name, restore the file with `git checkout -- <route>`, confirm `git diff --quiet -- <route>` exits 0, and re-run the suite green. Mutations to use at minimum: in the save route change the saved-true literal to false; in the saved-events route invert the recent sort comparator; in the calendar route stop annotating `is_saved`; in the rsvp route compute `total` from the going count alone (rsvp-characterization) and add `{ count: "exact" }` to the rsvps count select (rsvp-count-defect); in the saved-events route replace the true-UTC floor with a fixed ISO string (saved-events-time-floor-defect). Record each cycle — mutation, command, red output with test name, restore command, clean-diff check, green output — in `evidence/slice-1-mutation-check.txt`. A suite that stays green under its mutation must be strengthened before this task closes.

**Unmodified proof.** In `evidence/slice-1-unmodified.txt` record, for each of `src/app/api/events/[id]/save/route.ts`, `src/app/api/events/[id]/rsvp/route.ts`, `src/app/api/users/saved-events/route.ts`, `src/app/api/calendar/events/route.ts`: `git diff --stat <base> -- <file>` (empty), `git log -1 --format='%H %cI' -- <file>`, and `git merge-base --is-ancestor <that commit> HEAD` with its exit code 0 — the subject predates the suite as a git-ancestry fact.
  </action>
  <verify>
    <automated>npx jest --ci && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.slice-1-before.txt` ends with `exit=0` and its passed count equals the Playwright number in `evidence/floor.before.txt` plus 1, with 0 failed
    - `grep -c "users/saved-events" e2e/specs/save-and-rsvp.spec.ts` is at least 1
    - `grep -c "secondApprovedEvent" e2e/specs/save-and-rsvp.spec.ts` is at least 1
    - `evidence/slice-1-mutation-check.txt` records at least 6 cycles, each with a red whose output names a test, a `git diff --quiet` exit of 0 after restore, and a green re-run
    - `evidence/slice-1-unmodified.txt` records exit code 0 for all four `git merge-base --is-ancestor` checks and four empty diffs against the base
    - `npx jest --ci` exits 0 with the before-floor's passed count plus at least 44 and the same skipped count
    - `git diff --stat -- src/app src/lib src/server` prints nothing
  </acceptance_criteria>
  <done>The harness now proves saved state through the API route and RSVP counts after a reload, every new suite has been watched turning red under a mutation of what it pins, and every subject is provably older than its suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → production source | A characterization plan creates pressure to edit the subject so an assertion passes |
| Playwright persona session → local app | Storage states carry real local sessions for seeded personas; they are gitignored |
| laptop → local Supabase stack | reset and seed run against 127.0.0.1 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Editing a route so a PRESERVE assertion passes, turning a contract into a transcription | mitigate | Prohibition by name; mutations restored with `git checkout --` and a `git diff --quiet` check per cycle; the ancestry proof records the subjects unchanged since the base |
| T-04-02-02 | Repudiation | A PRESERVE suite whose assertions cannot fail when the behaviour is removed (02-REVIEW WR-04) | mitigate | One recorded red per suite under a mutation of its pinned behaviour |
| T-04-02-03 | Elevation of Privilege | A banned user reaching a mutation (the save/rsvp DELETE asymmetry) | accept | Pinned, not changed: ban enforcement is REFAC-11 in Phase 5 (DEC-24). The PRESERVE test gives Phase 5 a measured starting point |
| T-04-02-04 | Information Disclosure | Cross-user read of RSVPs or saved events via a body-supplied user id | mitigate | The POST/DELETE 403 on a body user_id that differs from the session is already covered by rsvp.test.ts and is re-asserted as a PRESERVE behaviour here so the seam adoption cannot drop it |
| T-04-02-05 | Tampering | reset or seed aimed at a non-local target | mitigate | `--local` on every CLI call; the seed loader's guard refuses non-local URLs before any client exists |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No package is installed; Jest, Playwright and testing-library are already present |
</threat_model>

<verification>
- `npx jest --ci` exits 0: passed count = before-floor + new tests, skipped unchanged
- `node scripts/check-characterization-tags.mjs --all` exits 0 and lists the six new suites
- `npx playwright test` exits 0 at before-floor + 1 passed
- `npm run lint` exits 0 with 0 errors; `npx tsc --noEmit` exits 0
- `git diff --name-only <base> -- src/app src/lib src/server supabase scripts` prints nothing
</verification>

<success_criteria>
Every Slice 1 handler has a contract written against unmodified source and proven to bite; the RSVP count contract is deliberately blind to implementation so it can stand unchanged across the F-079 fix while a separate DEFECT suite pins the implementation; F-079 and F-085 are pinned under their registered ids; and the persona harness now observes the two paths research Pitfalls 1 and 2 said it could not.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New files**
- `src/__tests__/helpers/fakeSupabase.ts` — `createFakeSupabase(init)` → `{ client, calls, tables }`
- `src/__tests__/api/events/save-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/saved-events-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/saved-events-time-floor-defect.test.ts` (DEFECT F-085)
- `src/__tests__/api/events/calendar-events-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/rsvp-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/rsvp-count-defect.test.ts` (DEFECT F-079)
- `evidence/slice-1-characterization.txt`, `evidence/slice-1-mutation-check.txt`, `evidence/slice-1-unmodified.txt`, `evidence/playwright.slice-1-before.txt`

**Modified:** `e2e/specs/save-and-rsvp.spec.ts` (+2 assertions, +1 test).
**New packages / npm scripts / CI steps:** none.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-02-SUMMARY.md` when done.
</output>
