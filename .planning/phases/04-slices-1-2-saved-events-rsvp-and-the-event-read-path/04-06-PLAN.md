---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 06
type: execute
wave: 4
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - src/__tests__/api/clubs/analytics.test.ts
  - src/__tests__/api/events/analytics.test.ts
  - supabase/functions/events-webhook/index.ts
  - tsconfig.json
  - src/__tests__/api/events/rsvp.test.ts
  - src/__tests__/api/events/reviews.test.ts
  - src/__tests__/api/events/get-events.test.ts
  - src/__tests__/api/events/date-validation.test.ts
  - src/__tests__/moderation/audit-shape.test.ts
  - CLAUDE.md
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tsc-tests-included.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.slice-1-after.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-1-after.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
autonomous: true
requirements: [REFAC-09, REFAC-10]

must_haves:
  truths:
    - "Test files are type-checked by npx tsc --noEmit, and the tree type-checks clean with them included (F-066's second clause, DI-24)"
    - "No route handler signature was widened to make a test type-check; every fix is inside a test file"
    - "The analytics fixtures use the authoritative start_date column, the webhook comment no longer claims club_id and status are absent, and the three analytics suites still pass"
    - "After Slice 1, the full floor is at or above the before-floor, the Playwright harness passes from a clean reset, and every Validated workflow Slice 1 touches is re-confirmed by a named test"
    - "F-079 and F-071 are Fixed in the register with a resolution naming their commits and evidence"
  artifacts:
    - path: "tsconfig.json"
      provides: "No test-file glob in exclude"
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md"
      provides: "Slice 1 close-out: floor before/after table, Playwright before/after, Validated-workflow re-confirmation, findings closed, no-visual-change statement"
      min_lines: 60
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tsc-tests-included.txt"
      provides: "The error census with the exclusion removed, per file and code, before and after the fixes"
      min_lines: 20
  key_links:
    - from: "tsconfig.json"
      to: "src/**/*.test.ts"
      via: "the include glob now reaches test files because exclude no longer removes them"
      pattern: "\"include\""
    - from: ".planning/audit/findings.json"
      to: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md"
      via: "F-079 and F-071 resolution fields cite the close-out note"
      pattern: "slice-1-close"
  prohibitions:
    - "No file under src/app/, src/lib/ or src/server/ is modified — a type error that can only be fixed in source stops the task with a written explanation"
    - "No `any` is introduced into a test to silence an error; the Next.js 16 two-argument handler signature is called correctly instead"
    - "findings.json is edited surgically; FOUNDATION_AUDIT.md only by its generator"
    - "The edge function's code is not changed — only its stale comment"
---

<objective>
Wave 4, alone: close Slice 1. Correct the stale event-date fixtures (F-050), take the test-file exclusion out of the type-check (DI-24, F-066's second clause), then capture the Slice 1 after-evidence and flip the Slice 1 findings.

Purpose: 04-RESEARCH.md routes the tsconfig change with Slice 1 because 18 of its errors are in `rsvp.test.ts` and 9 in the events analytics suite. Phase 2 warned — and this plan's prohibitions repeat — that the obvious fix for the 17 `TS2554` and 15 `TS2352` errors is to widen handler signatures under `src/app/**`, which is a behaviour change disguised as a type fix. ROADMAP criterion 4 then requires the harness to pass and the Validated list to be re-confirmed after the slice, not only at phase end.

Output: corrected fixtures and comment, a type-checked test tree, the Slice 1 after-floor and Playwright run, a close-out note, and F-079/F-071 marked Fixed.
</objective>

<slice_outcome>
Green when `npx tsc --noEmit` exits 0 with test files included, `npx jest --ci` and `npm run build` pass, `npx playwright test` passes from a clean reset at the 04-04 count, and `node .planning/audit/tools/validate.mjs --check findings` passes with F-079 and F-071 Fixed.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md
@.planning/audit/schema/events-date-columns.md
@tsconfig.json
</context>

<phase_locked_constraints>
1. Behaviour preservation — no production file changes in this plan apart from one comment in the edge function.
2. Characterize first — the analytics fixture correction must leave the three analytics suites passing (F-050's own validation criterion).
3. No schema change. 4. Production is never touched.
5. No visual change — Slice 1 changed no rendered output; the close-out states how that was established.
6. Jest single runner; `--testPathPatterns` plural.
7. Stage by explicit path only.
8. Local-stack exclusivity — this plan is wave 4's only plan and uses the stack in Task 3.
9. Evidence discipline.
10. "This is NOT the Next.js you know" — read the route-handler section of `node_modules/next/dist/docs/` before typing handler references in tests; Next.js 16 handlers take a request and a context whose `params` is a promise.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Correct the stale event-date fixtures and the edge-function comment (F-050)</name>
  <files>src/__tests__/api/clubs/analytics.test.ts, src/__tests__/api/events/analytics.test.ts, supabase/functions/events-webhook/index.ts</files>
  <read_first>
    - src/__tests__/api/clubs/analytics.test.ts lines 150-200 and src/__tests__/api/events/analytics.test.ts lines 85-150 — the six fixture rows keyed on the legacy date column, and the assertions around them
    - src/app/api/clubs/[id]/analytics/route.ts and src/app/api/events/[id]/analytics/route.ts — confirm each route selects `start_date`, so the corrected fixtures supply what the route actually reads
    - .planning/audit/schema/events-date-columns.md — AUDIT-19's verdict: `start_date` and `end_date` are authoritative, the older pair does not exist
    - supabase/functions/events-webhook/index.ts lines 95-125 — the comment claiming club_id and status are absent from events; src/lib/supabase/types.ts shows both present
    - .planning/audit/findings.json record F-050 — its validation criterion
  </read_first>
  <action>
<!-- planner-discipline-allow: event_date -->
<!-- planner-discipline-allow: event_time -->
In both analytics suites, rename the fixture key `event_date` to `start_date` on each of the six rows, keeping each value. If an assertion in either suite was passing only because the date was undefined, adjust that assertion to the value the route actually derives from `start_date` and record the change and its reason in this task's commit body — the research observed that the assertions "look elsewhere", so the expectation is that none needs to change. Run the three analytics suites (`npx jest --ci --selectProjects node --testPathPatterns "analytics"`) and require them green.

In the edge function, rewrite the comment on line 99 (and its sibling on line 118 if it makes the same claim) so it states the columns the function writes without claiming that `club_id` or `status` are absent from `events` and without naming the legacy date pair. Change no code in the function.

Then run the stale-date census exactly as the before-floor did and record its count; the two remaining hits are expected to be `src/lib/tagMapping.ts` line 98 (fixed in 04-07) — anything else means this task is incomplete. Commit with a message naming F-050.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "analytics"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "analytics"` exits 0 with the same test count as the before-floor for those suites
    - `command grep -rn "event_date\|event_time" src/__tests__ supabase/functions | wc -l` returns 0
    - `command grep -rn "event_date\|event_time" src/ supabase/ | command grep -v _archive_pre_baseline` prints only lines from `src/lib/tagMapping.ts`
    - `git diff --stat -- supabase/functions/events-webhook/index.ts` shows only comment lines changed (`git diff -U0 -- supabase/functions/events-webhook/index.ts | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*(\*|//|/\*)'` prints nothing)
  </acceptance_criteria>
  <done>The analytics fixtures describe the schema that exists, the suites still pass, and the edge function no longer documents a schema that does not.</done>
</task>

<task type="auto">
  <name>Task 2: Type-check the test files (DI-24, F-066 second clause) by fixing the tests, never the routes</name>
  <files>tsconfig.json, src/__tests__/api/events/rsvp.test.ts, src/__tests__/api/events/reviews.test.ts, src/__tests__/api/events/get-events.test.ts, src/__tests__/api/events/date-validation.test.ts, src/__tests__/moderation/audit-shape.test.ts, CLAUDE.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tsc-tests-included.txt</files>
  <read_first>
    - tsconfig.json — the `exclude` array and its two test-file glob entries
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Q(g) "Measured cost of removing the tsconfig exclusion" — the 77-error breakdown by code and by file, the three genuine fixture lies, and the warning against widening signatures
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/type-fixes-note.md § 3.2 — Phase 3's measurement and deferral
    - each of the five test files in `<files>` — in full, since each has a different error mix (redeclared block-scoped variables across script-style files; handler references typed as one-argument functions; the email field on a `{ id: string }` fixture in date-validation)
    - the route handlers those tests import — only to read their signatures, never to change them
    - CLAUDE.md line 126 — the sentence stating test files are excluded from the main tsconfig
  </read_first>
  <action>
Remove the two test-file glob entries from the `exclude` array in `tsconfig.json`, leaving every other entry in place. Run `npx tsc --noEmit` and record the full error census into `evidence/tsc-tests-included.txt`, grouped by file and by error code — this is the measured count for this tree, which will differ from the research's 77 because 04-02, 04-04 and 04-05 added and fixed test files.

Fix every error inside test files:
- Redeclared block-scoped variables and duplicate implementations: the file is a script, not a module — add an empty `export {}` at the top of each such file.
- Wrong argument count and unsafe conversions on handler calls: type the handler reference as the handler's real type (for example via `typeof import("<route>").GET`), and call it with a request and a context whose `params` is a resolved promise, exactly as the handler is declared.
- The three genuine fixture lies: give the date-validation user fixture the type its code path actually receives, and correct any other excess-property or missing-member error by fixing the fixture or the mock type, not by widening to `any`.

If an error can only be fixed by changing a file under `src/app/`, `src/lib/` or `src/server/`, stop, record the error and why in the evidence file, and do not change the source — that is a behaviour change this plan is not permitted to make. If `tsc` reports an error in a test file not listed in `<files>`, fix it in that test file only if the fix is test-local, record the file in the evidence and the SUMMARY, and include it in the commit.

Record the after census (0 errors) and confirm `npx jest --ci` still passes with the same counts, `npm run lint` is clean, and `npm run build` succeeds (Next's build type-check now covers test files too). Update `CLAUDE.md` line 126 to say test files are type-checked by the main tsconfig and run by Jest's two projects. Commit with a message naming DI-24 and F-066.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --ci && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node -e 'const c=JSON.parse(require("fs").readFileSync("tsconfig.json","utf8"));process.exit(c.exclude.some(e=>/test/.test(e))?1:0)'` exits 0
    - `npx tsc --noEmit` exits 0
    - `npx jest --ci` exits 0 with passed and skipped counts identical to the run before this task
    - `npm run build` exits 0
    - `git diff --stat -- src/app src/lib src/server` prints nothing
    - No line added by this task to a test file introduces an `any`: `git diff -U0 HEAD~1 -- 'src/*.test.ts' | grep -E '^\+.*\bany\b'` prints nothing (run after the commit)
    - `evidence/tsc-tests-included.txt` holds a before census with a total error count and an after census reading 0
    - `grep -c "type-checked" CLAUDE.md` is at least 1
  </acceptance_criteria>
  <done>Test files are inside the type-check, the tree is clean with them there, and every fix was made in the tests — no handler signature moved.</done>
</task>

<task type="auto">
  <name>Task 3: Close Slice 1 — after-floor, harness from a clean reset, Validated-workflow re-confirmation, and the register</name>
  <files>.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.slice-1-after.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-1-after.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-close.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md, .planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt — the command list and the numbers every after value is compared with
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt — the most recent harness count (04-04), which the after run must equal
    - .planning/PROJECT.md § Requirements › Validated — the sixteen workflows to re-confirm
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/defect-ledger.md — F-079 and F-071 rows
    - .planning/audit/findings.json records F-079, F-071 and F-066, and the `resolution` field's description in findings.schema.json
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/FOUNDATION-READINESS.md — the clause-by-clause, cite-every-claim style for the close-out note
  </read_first>
  <action>
**After-floor.** Re-run every command from `evidence/floor.before.txt` in the same order, with a clean `supabase db reset --local` and a seed load before the seeded pgTAP run and Playwright, into `evidence/floor.slice-1-after.txt` (line 1 `head=<sha>`) and `evidence/playwright.slice-1-after.txt`. Every number must be at or above the before value, except the stale-date census and the cast census, which must be lower; the ratchet must read `committed=25 live=25 delta=0` (04-03's one-time widening) and `validate.mjs --quick` must keep its two by-design failures only.

**Close-out note.** Write `evidence/slice-1-close.md` with the metadata line and: (1) a before/after table for every floor command; (2) the Playwright before (04-02) and after counts; (3) the Validated-workflow re-confirmation — one row per PROJECT.md Validated bullet, stating either the test that re-confirms it after Slice 1 (for "save/unsave events and RSVP" name the three save-and-rsvp tests and the RSVP, save, saved-events and calendar PRESERVE suites; for sign-in, browse, onboarding and the rest name the existing spec) or "not touched by Slice 1" with the `git diff --name-only <base>` evidence that none of its files changed; (4) findings closed (F-079, F-071) and partially closed (F-066's type-check clause; its skipped-suite clause is 04-09's); (5) the statement of how "no intentional visual change" was established — the Playwright UI assertions are unchanged and pass, count values are identical below 1000 rows, and the friends fallback does not run on any deployed schema; (6) the DEC-29 log-line difference, stated.

**Register.** In `findings.json`, surgically set F-079 and F-071 to `Fixed` and add a `resolution` to each naming the fixing commit, the moved DEFECT suite, and `evidence/slice-1-close.md`; add a `resolution` to F-066 recording that the type-check clause is met by this plan's Task 2 commit and the skipped-suite clause remains (status stays Open). Regenerate `FOUNDATION_AUDIT.md` and run `node .planning/audit/tools/validate.mjs --check findings`. Append an F-066 row to the ledger noting the tsconfig change (no DEFECT suite moved; the clause is closed by a configuration change).
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && node scripts/check-characterization-tags.mjs --all && npx jest --ci</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.slice-1-after.txt` ends with `exit=0`, 0 failed, passed equal to `evidence/playwright.slice-2-before.txt`'s passed count
    - `evidence/floor.slice-1-after.txt` line 1 matches `^head=[0-9a-f]{40}$` and contains the string `committed=25 live=25 delta=0`
    - `node -e 'const f=require("./.planning/audit/findings.json");const g=i=>f.find(r=>r.id===i);process.exit(g("F-079").status==="Fixed"&&g("F-071").status==="Fixed"&&g("F-066").status==="Open"&&g("F-079").resolution&&g("F-071").resolution&&g("F-066").resolution?0:1)'` exits 0
    - `node .planning/audit/tools/validate.mjs --check findings` exits 0
    - `evidence/slice-1-close.md` has one re-confirmation row per PROJECT.md Validated bullet — its row count in that table equals `grep -c '^- ✓' .planning/PROJECT.md`
    - `git diff --numstat -- .planning/audit/findings.json` shows at most 3 deleted lines (the two status lines and at most one neighbouring line)
  </acceptance_criteria>
  <done>Slice 1 is closed with evidence: the floor did not regress, the harness passes from a clean database, every Validated workflow is either re-confirmed by a named test or shown untouched, and the register records exactly which Slice 1 findings closed and how.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type-checker → test fixtures | Excluded tests let fixture shapes drift from the schema unseen (F-050's root cause) |
| build pipeline → route handlers | A "type fix" that widens a handler signature changes the handler contract |
| laptop → local stack | reset, seed, pgTAP and Playwright run against 127.0.0.1 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Widening a route signature to silence a test's TS2554 | mitigate | Prohibition; acceptance asserts no diff under src/app, src/lib, src/server |
| T-04-06-02 | Repudiation | Silencing errors with `any`, restoring the blind spot the change exists to close | mitigate | Acceptance greps added lines in test files for `any` |
| T-04-06-03 | Repudiation | Claiming Slice 1 preserved every workflow without evidence | mitigate | Per-bullet re-confirmation table with a named test or a diff-based "untouched" proof |
| T-04-06-04 | Tampering | findings.json reformatted while flipping statuses | mitigate | Surgical edits; deletion count bounded in acceptance |
| T-04-06-05 | Tampering | Stack commands aimed at a non-local target | mitigate | `--local`; loader guard |
| T-04-06-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 with test files included
- `npx jest --ci`, `npm run lint`, `npm run build` exit 0
- `npx playwright test` exits 0 from a clean reset
- `node .planning/audit/tools/validate.mjs --check findings` exits 0; F-079 and F-071 Fixed
- `git diff --name-only <base> -- src/app src/lib src/server` lists only the five 04-05 route files
</verification>

<success_criteria>
Slice 1 ends with the test tree inside the type-check, the stale date residue gone from tests and the edge function, the floor at or above where it started, the harness green from a clean reset, every Validated workflow accounted for, and the register saying — with commits and evidence — that F-079 and F-071 are fixed and F-066 is half-closed.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**Modified configuration/docs:** `tsconfig.json` (test globs removed from exclude), `CLAUDE.md` (test files are type-checked).
**Modified tests (types and fixtures only):** clubs and events analytics suites, rsvp, reviews, get-events, date-validation, audit-shape.
**Modified comment:** `supabase/functions/events-webhook/index.ts`.
**New evidence:** `evidence/tsc-tests-included.txt`, `evidence/floor.slice-1-after.txt`, `evidence/playwright.slice-1-after.txt`, `evidence/slice-1-close.md`.
**Register:** F-079 Fixed, F-071 Fixed, F-066 resolution (type-check clause met), F-050 test/edge residue removed (status flips in 04-11 after the tagMapping comment).
**Deferred items closed:** DI-24 type-check half.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-06-SUMMARY.md` when done.
</output>
