---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/findings.json
  - .planning/audit/FOUNDATION_AUDIT.md
  - .planning/audit/quality/phase-04-slice-defects.md
  - scripts/check-characterization-tags.mjs
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/findings-registration.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/di-25-enumeration.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md
autonomous: true
requirements: [REFAC-09, REFAC-10]

must_haves:
  truths:
    - "Every defect Phase 4 fixes or pins has a registered F-nnn in .planning/audit/findings.json (F-079 through F-085) carrying severity, rationale, reproduction and a validation criterion, before any DEFECT test cites it"
    - "F-050's owner is Phase 4, so the stale event-date residue is closed by this phase rather than left ownerless"
    - "A machine check rejects any characterization suite that is untagged, or that cites an F-nnn the register does not hold"
    - "The before-floor is re-measured on the phase's base commit, including pgTAP and the Playwright harness, so no floor number used later is a citation"
    - "Every inherited deferred item (DI-24, DI-25, DI-31, DI-34, DI-35) and every research assumption (A1-A8) has a written disposition with an owner"
    - "The plan set's treatment of seeded-visible fixes follows one written rule: F-080 and F-081 go to the 04-11 owner checkpoint, and F-082's special-character search change is exempted from it by DEC-32, whose argument (ROADMAP criterion 3 mandates the escaping verbatim, no delivery of it leaves seeded output unchanged, it is an injection-class query defect, plain-text search is proven unchanged) is recorded before any plan executes"
  artifacts:
    - path: ".planning/audit/quality/phase-04-slice-defects.md"
      provides: "The evidence document every new finding's evidence field resolves to, one anchored section per finding, reproductions pasted from 04-RESEARCH.md"
      min_lines: 120
    - path: "scripts/check-characterization-tags.mjs"
      provides: "The PRESERVE/DEFECT tag gate: untagged suite, DEFECT without an F-nnn, or a cited F-nnn absent from findings.json all exit 1"
      min_lines: 60
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt"
      provides: "The before-floor on the base commit: jest, lint, tsc, audit, ratchet, filenames, validate --quick, pgTAP unseeded and seeded, Playwright"
      min_lines: 60
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md"
      provides: "DEC-23 onward: the design calls every later plan executes against"
      min_lines: 80
    - path: ".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md"
      provides: "Phase 4's deferred-item register, opened with the inherited DI dispositions and the A1-A8 assumption dispositions"
      min_lines: 60
  key_links:
    - from: ".planning/audit/findings.json"
      to: ".planning/audit/quality/phase-04-slice-defects.md"
      via: "each new record's evidence field is a path relative to .planning/audit/ with an anchor, which validate.mjs --check findings resolves"
      pattern: "quality/phase-04-slice-defects.md#f-0(79|8[0-5])"
    - from: "scripts/check-characterization-tags.mjs"
      to: ".planning/audit/findings.json"
      via: "reads the register's ids and rejects any cited F-nnn not in it"
      pattern: "findings\\.json"
  prohibitions:
    - "findings.json is edited by per-record insertion and one per-field edit on F-050 — never parsed and re-serialized (T-03-08-08 precedent: a 912/231 whole-file reformat was reverted in 03-08)"
    - "FOUNDATION_AUDIT.md is never hand-edited; it is regenerated by .planning/audit/tools/gen-foundation-audit.mjs"
    - "No file under src/, e2e/, supabase/ or the repository root configuration is modified"
    - "No production read or write of any kind; no --linked flag; .env.local is never opened"
    - "No package is installed or version-moved in the main working tree; the DI-25 enumeration happens in a throwaway worktree outside the repository and leaves nothing behind"
---

<objective>
Wave 1, alone: establish the ground every later Phase 4 plan stands on — a re-measured floor, a finding register that holds an id for every defect this phase touches, a mechanical gate for the PRESERVE/DEFECT tagging discipline, and a written decision record.

Purpose: ROADMAP success criterion 1 requires characterization tests "tagged PRESERVE or DEFECT (referencing their F-nnn)". 04-RESEARCH.md § "The finding-register gap" measured that only F-066 and F-071 exist for this phase's defects; the RSVP count cap, the club fabrication, the tag coercion, the search interpolation and the pagination divergence have no id. A DEFECT header cannot cite an id that does not exist, so registration strictly precedes every characterization plan (04-02, 04-04). The floor is re-measured rather than cited because A8 in the research is explicitly an assumption (pgTAP 86 and Playwright 27 were cited, not re-run).

Output: seven new findings plus one owner correction, their evidence document, a tag-checking script, the before-floor, the Phase 4 decision record, and the Phase 4 deferred-item register.

**No CONTEXT.md exists for this phase.** The binding inputs are the three orchestrator decisions (no intentional visual change on seeded data; no schema change; production never touched) and 04-RESEARCH.md. Where the research recommends and nothing overrides it, this plan records the recommendation as a DEC so later plans execute against a written decision rather than a paragraph.
</objective>

<slice_outcome>
Green when `node .planning/audit/tools/validate.mjs --check findings` passes 8/8 at 85 findings, `node scripts/check-characterization-tags.mjs --all` exits 0 on the base tree (five pre-existing tagged suites: the auth callback, middlewareRateLimit and proxy PRESERVE suites, and the friends-defect and audit-shape DEFECT suites) and exits 1 on a planted bad fixture, and `evidence/floor.before.txt` holds every floor command's verbatim output with exit codes, measured on the recorded base commit.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-VALIDATION.md
@.planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md
@.planning/audit/findings.schema.json
@.planning/audit/SEVERITY_SLA.md
</context>

<phase_locked_constraints>
1. Behaviour preservation — wire bytes of every Validated workflow stay identical except where a registered F-nnn fix is a commit's stated purpose, and each such change lands in its own commit naming the id.
2. Characterize first — every suite is tagged PRESERVE or DEFECT in its leading docblock; every DEFECT cites an F-nnn present in `.planning/audit/findings.json`.
3. No schema change (orchestrator decision 2) — nothing is added under `supabase/migrations/`; `events.rsvp_count` is neither adopted nor dropped. A plan that believes a migration is unavoidable stops at a checkpoint.
4. Production is never touched (orchestrator decision 3, DI-23) — local stack only; no `supabase db push`, no `--linked`; `.env.local` is never read or modified.
5. No intentional visual change on seeded data (orchestrator decision 1) — a change to rendered output is either proven absent by the harness, gated behind the 04-11 decision checkpoint, or — for F-082 alone — exempted by DEC-32, whose argument Task 3 writes down. No other exemption exists.
6. Jest is the single runner with two projects routed by directory; the selector flag is `--testPathPatterns` (plural). No Vitest API.
7. Stage by explicit path, never with an add-everything form (04-RESEARCH.md Pitfall 9). `.agents/`, `docs/product-master-plan.md`, `skills-lock.json` and `.mcp.json` stay untracked.
8. Local-stack exclusivity — one plan per wave uses the Docker stack and port 3000; the Playwright webServer never reuses a server. This plan is the only wave-1 plan, so it may use the stack.
9. Evidence discipline — raw output verbatim, the invoking command above it, the exit code below it; decision notes carry `**Plan:** · **Phase:** · **Recorded:**`.
10. findings.json is edited surgically; FOUNDATION_AUDIT.md only through its generator.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the before-floor on the base commit, including pgTAP and the persona harness</name>
  <files>.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Validation Architecture, "The floor — re-measured in this working tree" table — every command and its recorded number, which this task re-measures rather than trusts
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Common Pitfalls, Pitfall 4 — `supabase test db --local` emits 21 SKIPs unseeded and executes them seeded; the capture must say which run is which
    - .github/workflows/ci.yml — the `e2e` job's step order (reset, seed load, `supabase test db --local`, `npx playwright test`) is the order this task reproduces locally
    - playwright.config.ts lines 80-140 — the webServer builds and starts the app itself and never reuses a server; nothing may be listening on port 3000 before the run
    - scripts/seed/load.ts lines 1-40 — the loader's method contract: it refuses any non-local target and never reads a credential from `.env.local`
  </read_first>
  <action>
Record the base commit first: the output of `git rev-parse HEAD` goes on the first line of `evidence/floor.before.txt` as `base=<sha>`, and every later Phase 4 "unmodified source" proof refers to this value.

Then run each floor command in this order, appending to the same file a block per command consisting of the exact command line, the verbatim tail of its output that carries the numbers (the whole output for short commands), and a line `exit=<code>`:

1. `npx jest --ci` — record passed, skipped, total tests and passed, skipped, total suites.
2. `npx jest --ci src/__tests__/api/events src/hooks/useEvents.test.ts` — the phase-relevant subset.
3. `npm run lint` — errors and warnings.
4. `npx tsc --noEmit`.
5. `npm audit --audit-level=high --omit=dev` — if the network is unavailable, record the failure verbatim and mark the row UNMEASURED rather than copying the research's number.
6. `node scripts/check-elevated-ratchet.mjs`.
7. `node scripts/check-migration-filenames.mjs`.
8. `node .planning/audit/tools/validate.mjs --quick` — the two AUDIT-01 staging/local snapshot failures are failing by design; name them.
9. `supabase db reset --local`, then `supabase test db --local` — label this block UNSEEDED and record the assertion count and the SKIP count.
10. `npx tsx scripts/seed/load.ts`, then `supabase test db --local` again — label this block SEEDED.
11. `npx playwright test` — record passed, failed and skipped.
12. Two census greps recorded with their counts: the stale event-date residue as 04-RESEARCH.md § Q(d) defines it (`command grep -rn` over `src/` and `supabase/` excluding the archive directory), and the client-cast census as `command grep -rn` for the parenthesised supabase-as-any form over `src/`.

At the top of the file, under the base line, write a summary table with one row per command: the command, the number measured now, the number 04-RESEARCH.md recorded, and the delta. Where they differ, the measured value is the floor for every later plan and the delta is stated, never rounded away (the Phase 2 bundle-size precedent). Do not modify any file other than the evidence file; if any command fails for an environmental reason (Docker down, port 3000 occupied), fix the environment and re-run — never record a partial floor as complete.
  </action>
  <verify>
    <automated>test -s .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt && head -1 .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/floor.before.txt | grep -E '^base=[0-9a-f]{40}$'</automated>
  </verify>
  <acceptance_criteria>
    - Line 1 of `evidence/floor.before.txt` matches `^base=[0-9a-f]{40}$` and equals the `git rev-parse HEAD` value at the start of this plan
    - The file contains a line `exit=` after each of the 13 command blocks (jest full, jest subset, lint, tsc, audit, ratchet, filenames, validate, reset, pgTAP unseeded, seed load, pgTAP seeded, playwright) — `grep -c '^exit=' evidence/floor.before.txt` is at least 13
    - The file contains the strings `UNSEEDED` and `SEEDED` labelling the two pgTAP runs
    - The summary table has a row for Playwright whose "measured now" column is a number read from this run's output, not the cited 27
    - `git status --porcelain -- src/ e2e/ supabase/ scripts/ package.json package-lock.json tsconfig.json` prints nothing
  </acceptance_criteria>
  <done>The floor every later plan compares against is a measurement on a named commit, the pgTAP count is split into its unseeded and seeded runs, and research assumption A8 is closed by measurement.</done>
</task>

<task type="auto">
  <name>Task 2: Register F-079 through F-085, move F-050 to Phase 4, and add the PRESERVE/DEFECT tag gate</name>
  <files>.planning/audit/findings.json, .planning/audit/FOUNDATION_AUDIT.md, .planning/audit/quality/phase-04-slice-defects.md, scripts/check-characterization-tags.mjs, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/findings-registration.txt</files>
  <read_first>
    Context budget: this list names about twenty files, most of them only for a line range. [FULL] means read the whole file; [SECTION] means read only the named lines or section — locate it with grep first, then read with an offset and limit. Do not read a [SECTION] file whole.
    - [FULL, 108 lines] .planning/audit/findings.schema.json — every required field, the category and severity enums, the rule that `evidence` is a path relative to `.planning/audit/` that must resolve, and the rule that Critical and High carry line numbers
    - [FULL, 51 lines] .planning/audit/SEVERITY_SLA.md — the exposure-adjusted severity rules the rationales must argue from
    - [SECTION — 1,868 lines; locate records with `grep -n '"id": "F-0'`] .planning/audit/findings.json — the F-071 and F-078 records as the house style to copy field by field, and the F-050 record whose `closes_in_phase` this task changes; also the last record, after which the new ones are inserted
    - [SECTION — the metadata line and one finding section] .planning/audit/quality/cast-removal-defects.md — the shape of an existing evidence document with anchored per-finding sections
    - [SECTION — the named subsections only] .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Q(b), Q(c), Q(d), Q(e), Q(f), Q(g), Pitfall 6, the Runtime State Inventory row on `events.rsvp_count`, and § "The finding-register gap" — the reproductions to paste
    - [SECTION — the cited line ranges only] src/app/api/events/[id]/rsvp/route.ts lines 92-105, src/lib/tagMapping.ts lines 10-50 and 102-174, src/app/api/events/[id]/route.ts lines 79-106, src/app/api/users/saved-events/route.ts lines 89-118, src/app/api/events/route.ts lines 214-243 and 280-307, src/hooks/useEvents.ts lines 53-138, src/app/page.tsx lines 505-520, src/app/api/events/create/route.ts lines 85-95 and 170-180, scripts/seed/load.ts lines 320-352 — the line numbers each record's affected_paths must cite, re-read rather than transcribed
    - [SECTION — gen-foundation-audit.mjs's header comment; validate.mjs lines 760-805 only] .planning/audit/tools/gen-foundation-audit.mjs and .planning/audit/tools/validate.mjs lines 760-805 — how the report is regenerated and what `--check findings` asserts
    - [SECTION — the cited header lines only] src/app/auth/callback/route.test.ts lines 1-12, src/__tests__/api/events/friends-defect.test.ts lines 1-40, src/__tests__/moderation/audit-shape.test.ts lines 1-10 — the three existing tagged headers the gate must accept
  </read_first>
  <action>
**The evidence document.** Create `.planning/audit/quality/phase-04-slice-defects.md` with the metadata line `**Plan:** 04-01 · **Phase:** 04 · **Recorded:** <date>` and one section per finding whose heading slugifies to the anchor the finding's `evidence` field cites (`## F-079 — rsvp counts load every row` and so on). Each section holds the measured reproduction from 04-RESEARCH.md, re-run where it is a code read, and the exact file:line pointers.

**The seven records**, inserted after the current last record (F-078) by editing the array's tail — a per-record insertion, never a parse-and-stringify. Every record has every schema field; `status` is `Open`, `risk_acceptance` is null. Severities below are the planned values; read SEVERITY_SLA.md and, if the policy argues otherwise, change the value and say why in `severity_rationale` — never leave the rationale generic.

- **F-079** — title: RSVP counts are computed by loading every non-cancelled row and filtering in JavaScript, so they are silently capped at PostgREST max_rows. Low, `performance`. Paths: `src/app/api/events/[id]/rsvp/route.ts` 92-105, `supabase/config.toml` 18. Rationale states that production's `max_rows` is unverified (research A1; Phase 4 does not read production) and that below 1000 RSVPs per event the counts are correct, which is why this is Low. Recommended fix: two `{ count: "exact", head: true }` queries, prior art `src/app/profile/page.tsx` 38-42. Validation criterion: `src/__tests__/api/events/rsvp-count-defect.test.ts` assertions move from a row-returning select with no count option to two head-count queries, and `src/__tests__/api/events/rsvp-characterization.test.ts` passes unmodified before and after. closes_in_phase `"04"`, related `["F-011"]`.
- **F-080** — title: Event responses fabricate a club object from the free-text organizer string, with a non-UUID id, and blank five real club fields. Medium, `validation`. Paths: `src/lib/tagMapping.ts` 102-144, `src/app/api/events/[id]/route.ts` 79-86, `src/app/api/users/saved-events/route.ts` 92-97, `src/components/events/EventDetailView.tsx` 219-260. Rationale: every event created through `/api/events/create` stores `organizer` as the creator's display name (create route 88 and 176), so its detail page reads "Hosted by" the creator rather than the club, while the list card shows the real club — two surfaces disagree about the same row; no data is exposed that F-022 does not already expose. Validation criterion: `src/__tests__/api/events/club-fabrication-defect.test.ts` assertions move; `transformEventFromDB` returns no club when there is no club row; `/api/events/[id]` and `/api/users/saved-events` select the club embed. closes_in_phase `"04"`, related `["F-050", "F-022"]`.
- **F-081** — title: Six of twelve EventTag members do not survive the read-path tag mapping, and any unmapped tag is silently rendered as Social. Medium, `validation`. Paths: `src/lib/tagMapping.ts` 10-50, `src/lib/constants.ts` 7-143, `src/lib/classifier.ts` 553-584, and the `TAG_HIERARCHY` block in `src/lib/constants.ts` (which maps hackathon and workshop to tech and fitness to sports while the read path maps them to academic and wellness). Reproduction includes the research's per-member probe table and the seeded rows `["academic","tech"]` and `["music","social"]` rendering as academic+social and cultural+social. Validation criterion: `src/lib/eventTags.test.ts` iterates `Object.values(EventTag)`; `src/__tests__/lib/tag-coercion-defect.test.ts` assertions move when identity mappings land; an unmapped tag produces a server-side warning naming the event id. closes_in_phase `"04"`.
- **F-082** — title: Search input is interpolated raw into a PostgREST or() filter, so % and _ act as wildcards and a comma produces a 400 the handler returns as a 500 carrying the internal filter text. Medium, `injection`. Paths: `src/app/api/events/route.ts` 214-243 and 280-307. Reproduction: the research's measured table plus the live PGRST100 body. related `["F-078", "F-059"]`. Validation criterion: `src/lib/searchFilter.test.ts` asserts one escape per character for percent, underscore, comma, both parentheses, double quote and backslash; `src/__tests__/api/events/search-escaping-defect.test.ts` moves; `scripts/probes/search-escape-probe.ts` shows literal matching against local PostgREST; `GET /api/events?search=a,b` returns 200. closes_in_phase `"04"`.
- **F-083** — title: The events list client pages by cursor and the handler pages by offset, so nextCursor is never emitted and Load More never renders. Medium, `validation`. Paths: `src/hooks/useEvents.ts` 53-138, `src/app/api/events/route.ts` 158-339, `src/app/page.tsx` 510-518, `src/app/api/events/route.test.ts` 19. related `["F-066"]`. Validation criterion: `src/app/api/events/route.test.ts` runs un-skipped and green; `src/__tests__/api/events/pagination-contract-defect.test.ts` moves; a Playwright API spec traverses the two seeded approved events by cursor. closes_in_phase `"04"`.
- **F-084** — title: events.rsvp_count is a denormalized counter that no migration, trigger or route writes. Low, `dead-code`. Paths: the column's line in `supabase/migrations/20260915214553_baseline.sql` (grep it) and `src/lib/tagMapping.ts` 161. Recommended fix: drop it or maintain it by trigger, each with a pgTAP test — a schema change, so not in Phase 4 (orchestrator decision 2). closes_in_phase `"06"`, with the rationale that a migration reaches production only through the DI-23 repair.
- **F-085** — title: /api/users/saved-events floors "upcoming" on true UTC while /api/events floors on Eastern wall-clock, so an event can be upcoming on one and past on the other for up to five hours a day. Low, `validation`. Paths: `src/app/api/users/saved-events/route.ts` 99-101, `src/app/api/events/route.ts` 245-246, `src/lib/timezone.ts` 1-25. Recommended fix: one shared floor function; not in Phase 4 because unifying them is a timezone-dependent behaviour change (04-RESEARCH.md Pitfall 6). Validation criterion: `src/__tests__/api/events/saved-events-time-floor-defect.test.ts` assertions move. closes_in_phase `"06"`.

**F-050's owner.** Change its `closes_in_phase` from null to `"04"` with a single-field edit, nothing else in the record.

**Regenerate and validate.** Run `node .planning/audit/tools/gen-foundation-audit.mjs`, then `node .planning/audit/tools/validate.mjs --check findings`, capturing both plus `git diff --stat -- .planning/audit/findings.json` into `evidence/findings-registration.txt`. The diffstat must show insertions only apart from the one F-050 line.

**The tag gate.** Create `scripts/check-characterization-tags.mjs`, Node built-ins only, with a header docblock in the style of `scripts/check-elevated-ratchet.mjs` (PURPOSE, REQUIREMENT, INVOCATION, DEPENDENCIES). Two invocations: explicit file paths as arguments, or `--all`, which discovers every `src/**/*.test.ts` and `src/**/*.test.tsx` whose filename ends in `-characterization.test.ts(x)` or `-defect.test.ts(x)`, or whose leading `/** … */` docblock contains the word "characterization" case-insensitively. For each file it reads only the leading docblock and fails when: it contains neither the word PRESERVE nor the word DEFECT; it contains DEFECT but cites no `F-` id of three digits; or it cites an `F-` id that is not an `id` in `.planning/audit/findings.json`. It prints one line per file and a final `ok <n> files` and exits 0, or prints each failure and exits 1. Prove it both ways into `evidence/findings-registration.txt`: `--all` on this tree (expect exactly five files — src/app/auth/callback/route.test.ts, src/middlewareRateLimit.test.ts, src/proxy.test.ts, src/__tests__/api/events/friends-defect.test.ts, src/__tests__/moderation/audit-shape.test.ts — all passing, exit 0; the callback suite cites F-004 and F-040, which must resolve), and an explicit path to a throwaway file created outside the repository whose docblock says DEFECT and cites F-999 (expect exit 1). Delete the throwaway afterwards.
  </action>
  <verify>
    <automated>node .planning/audit/tools/validate.mjs --check findings && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/audit/tools/validate.mjs --check findings` exits 0 with 8 passed and prints `findings.json has 85`
    - `node -e 'const f=require("./.planning/audit/findings.json");const ids=["F-079","F-080","F-081","F-082","F-083","F-084","F-085"];process.exit(ids.every(i=>f.some(r=>r.id===i&&r.status==="Open"))?0:1)'` exits 0
    - `node -e 'const f=require("./.planning/audit/findings.json");process.exit(f.find(r=>r.id==="F-050").closes_in_phase==="04"?0:1)'` exits 0
    - `git diff --numstat -- .planning/audit/findings.json` shows a deletion count of at most 2 (only the F-050 field line and, if the array tail required it, the closing line)
    - `grep -c '^## F-0' .planning/audit/quality/phase-04-slice-defects.md` is at least 7
    - `node scripts/check-characterization-tags.mjs --all` exits 0 and its final line reads `ok 5 files`
    - `evidence/findings-registration.txt` records a run of the gate against a DEFECT fixture citing F-999 that exited 1
    - `git status --porcelain -- src/ e2e/ supabase/` prints nothing
  </acceptance_criteria>
  <done>Every defect Phase 4 will cite has a schema-valid register entry with an evidence anchor that resolves, F-050 has an owner, the human-readable register is regenerated from the JSON, and a script exists that fails a suite whose tag or finding reference is wrong.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Phase 4 decision record, enumerate DI-25's blocking sites, and open the Phase 4 deferred register</name>
  <files>.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/di-25-enumeration.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/deferred-items.md</files>
  <read_first>
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Inherited Deferred Items, § Q(b), Q(e), Q(g), Q(h), Q(i), Q(j), § Assumptions Log, § Open Questions — the dispositions this task writes down
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md § "The two D- sequences", DI-24, DI-25, DI-31, DI-34, DI-35 — the register format and the DI-/DEC- prefix rule
    - .planning/STATE.md § Decisions — confirm the highest DEC number in use (DEC-22) before numbering from DEC-23
    - scripts/seed/personas.ts lines 554-605 — the seeded RSVPS (one going, one cancelled) that falsify research Pitfall 3's "no seeded rsvps" premise
    - scripts/seed/load.ts lines 320-352 — every seeded event carries both a club_id and a PRNG-picked organizer label, which is why the club fix is visible on seeded data
    - src/components/events/EventDetailView.tsx lines 219-260 and src/app/events/[id]/EventDetailClient.tsx lines 72-110 — the detail page renders the club block from `/api/events/[id]`
    - .planning/phases/02-dependency-and-runtime-stabilization/evidence/supabase-js-decision.md — Phase 2's record of the six TS2345 sites, if it lists them
  </read_first>
  <action>
**The DI-25 enumeration (research assumption A5).** In a throwaway directory outside the repository, create a detached worktree of the base commit, run `npm ci` there, then install `@supabase/supabase-js@2.116.0` there with the no-save form, run `npx tsc --noEmit`, and capture every `TS2345` line with its file path into `evidence/di-25-enumeration.txt`. Remove the worktree with the force form of `git worktree remove` and record `git worktree list` afterwards showing only the main working tree. The package was checked OK in 04-RESEARCH.md § Package Legitimacy Audit and nothing from this install reaches the repository. Classify each site as inside or outside Phase 4's thirteen handlers (the Slice 1 and Slice 2 inventories in 04-RESEARCH.md). If the network or install fails, record the failure verbatim and dispose DI-25 on the unenumerated premise, saying so.

**The decision record.** Create `evidence/phase-04-decisions.md` with the metadata line and one section per decision, numbered from DEC-23 (confirm against STATE.md), each stating the decision, the evidence, the alternative rejected, and which plan executes it:

- DEC-23 — RSVP counts use two parallel `{ count: "exact", head: true }` queries, not a DB function (research § Q(b) option A; orchestrator decision 2 forbids the migration option B would need).
- DEC-24 — DI-35: `RequestProfile` narrows to the three columns something reads (`id`, `roles`, `onboarding_completed`); no ban guard is added to the seam; `checkBanStatus()` stays exactly where it is called today (save POST and rsvp POST) and its asymmetry is pinned by PRESERVE tests. Ban enforcement is REFAC-11, Phase 5. Executed by 04-03.
- DEC-25 — the pagination contract (research A6, taken on the research's evidence because no discuss-phase ran; the phase owner may override before 04-09 executes): keyset cursor on `(start_date, id)` ascending, cursor encoded as base64 of a JSON `{sortValue, id}` exactly as the skipped suite encodes it; `page`/`limit` stay accepted; response adds `nextCursor` and `prevCursor`; `total` is the count of all matching rows regardless of cursor; an invalid cursor is a 400 `{ error: "Invalid cursor" }`; the `before` parameter, `sort`, `direction` and `clubId` are not honoured because no live caller sends a non-default value (only `src/app/page.tsx` calls `useEvents`); when the fuzzy-rank path is active (dead today, F-078, Phase 5) both cursors are null. Executed by 04-09.
- DEC-26 — tag surfacing is non-visual (orchestrator decision 1): one centralized mapping module, a completeness test over every `EventTag` member, and a server-side warning naming the unmapped tag and the event id; the six identity mappings that would change seeded badges are gated behind the 04-11 decision checkpoint with deferral as the default. Executed by 04-07 and 04-11.
- DEC-27 — club fabrication: the non-visual half ships in 04-10 (a shared club embed with the five real URL columns, `/api/users/saved-events` gains the real embed — its only consumer reads ids, so nothing renders it); `contact_email` is deliberately not embedded in event payloads (data minimisation — it is fetched with the club); the visual half — removing the organizer fallback and giving `/api/events/[id]` the embed, which changes "Hosted by" on every seeded event's detail page from the organizer label to "Seed Approved Club" — is gated behind the 04-11 checkpoint because it conflicts with orchestrator decision 1 while ROADMAP criterion 2 requires it. State that conflict in those words.
- DEC-28 — DI-25 disposition from the enumeration: if no blocking site is in a Phase 4 handler, the minor re-defers to Phase 5 with the site list and the `@supabase/ssr` major to Phase 5 at the earliest; if any site is in a Phase 4 handler, record it and flag it in the plan SUMMARY as an open owner decision — no dependency moves in Phase 4 either way.
- DEC-29 — seam adoption drops three server-side warning log lines that today fire on an auth error before a 401 (rsvp POST, rsvp DELETE, saved-events GET), because `createRequestContext()` does not surface the auth error; the response bytes are identical; auth-failure logging belongs to Phase 6's structured logging.
- DEC-30 — no seed change in Phase 4: RSVP rows already exist in the seed (personas.ts 566-579 — research Pitfall 3's premise is corrected here), and adding an organizer-only event would itself create a seeded visual delta the moment F-080 lands. The sha256 determinism proof is therefore untouched and research A3 is moot.
- DEC-31 — `events.rsvp_count` is registered (F-084) and neither adopted as the count source nor dropped.
- DEC-32 — F-082's escaping ships unconditionally in 04-08 and is not routed to the 04-11 owner checkpoint: a bounded exception to orchestrator decision 1, argued here so that the different treatment of F-080/F-081 and F-082 is a stated rule rather than an unexplained asymmetry. Write the section in this order. **(a) The visible delta, stated first:** on seeded data a search for `%` or `_` returns both approved events today and none afterwards, because no seeded title contains either character; a search for `a,b` changes from an API 500 to a 200 with an empty list, while the feed renders the same "No events found" state both times (`src/app/page.tsx` ignores the hook's error — 04-04 Task 3 pins this). **(b) The rule:** a seeded-visible fix goes to the owner checkpoint when the ROADMAP clause that motivates it can be delivered, fully or in part, without that visible change; it ships under a numbered decision only when no delivery of the mandated clause leaves seeded output unchanged. F-081 goes to the checkpoint because ROADMAP success criterion 3's tag clause ("centralized with unknown tags surfaced") is met fully and non-visually by 04-07 and the identity mappings go beyond its letter (DEC-26). F-080 goes to the checkpoint because ROADMAP success criterion 2's real join is delivered in part non-visually by 04-10, so the owner has a real choice between PARTIAL and an override (DEC-27). F-082 does not: ROADMAP success criterion 3 says "`%` and `_` are escaped in search input" in so many words, and every implementation of that sentence changes what a `%` search returns — there is no non-visual partial delivery, so putting F-082 on the checkpoint would put criterion 3 itself on the checkpoint. **(c) Criterion 4 is not overridden but scoped:** its "search behaves exactly as before" is read as governing every search term the defect does not mishandle, because criterion 3 names these characters and the specific mandate governs the specific inputs — read any other way, criteria 3 and 4 cannot both be met. The terms whose results change are exactly those containing one of `%`, `_`, backslash, comma, either parenthesis or a double quote; every other term is proven to return identical results by 04-04's events-list PRESERVE suite, which passes unmodified through 04-08, and by the existing "Music Night" UI search spec. **(d) It is a query-semantics defect, not a presentation choice:** F-082 is registered Medium in the `injection` category (the ASVS 4.0.3 § V5.3 injection-prevention class): today a user-typed `%` rewrites the query into match-everything, and a comma produces a PostgREST parse error that the handler returns to an anonymous caller as a 500 carrying the internal filter text (the F-059 echo). The checkpoint's no-answer rule defaults to deferral, so routing F-082 there would let an open injection-class finding stay open by rule rather than by anyone's decision, against the program's core value that every workflow is correct and secure. **(e) Consequences:** 04-08 cites DEC-32 in its objective and in its fix commit body; 04-11's checkpoint lists F-082 as already decided by DEC-32 and not up for deferral, quoting rule (b) to the owner; 04-11's completion note records F-082 as shipped under DEC-32. Like DEC-25, this decision is taken by the planner because no discuss-phase ran: the phase owner may override it before 04-08 executes by appending an owner-signed paragraph to this section, and 04-08 reads this section and stops before changing any file if such a paragraph exists. Executed by 04-08.

**The deferred register.** Create `evidence/deferred-items.md` for Phase 4 in the Phase 3 register's format, continuing the DI- sequence after DI-35, opened with: a table disposing each inherited item — DI-20, DI-28, DI-32 CLOSED before Phase 4 and not re-planned; DI-24 to 04-06 (type-checked tests) and 04-09 (zero skipped suites); DI-25 per DEC-28; DI-31 to 04-03 with "no shrink available in Phase 4, by measurement — none of the 13 slice handlers is in the 24-entry allow-list"; DI-34 to 04-03; DI-35 to 04-03 per DEC-24; DI-21, DI-22, DI-23, DI-26, DI-27, DI-29, DI-33 untouched with their existing owners — and a second table disposing research assumptions A1 through A8: A1 stays assumed (production not read), A2 closes by the 04-08 probe, A3 moot per DEC-30, A4 closes by the 04-10 latency measurement, A5 closed here, A6 decided by DEC-25, A7 goes to the 04-11 checkpoint, A8 closed by Task 1.
  </action>
  <verify>
    <automated>grep -c '^## DEC-' .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DEC-' evidence/phase-04-decisions.md` returns at least 10, the first is DEC-23 and the last is DEC-32
    - `evidence/phase-04-decisions.md` contains the metadata line beginning `**Plan:** 04-01`
    - `evidence/di-25-enumeration.txt` either lists each TS2345 line with an "inside Phase 4" or "outside Phase 4" label, or records the install/network failure verbatim; it also records a `git worktree list` output with exactly one line
    - `git worktree list | wc -l` returns 1
    - `git status --porcelain -- package.json package-lock.json node_modules/.package-lock.json` prints nothing
    - `evidence/deferred-items.md` contains a row for each of DI-24, DI-25, DI-31, DI-34, DI-35 and a row for each of A1 through A8 — `grep -cE '^\| \*\*(DI-(24|25|31|34|35)|A[1-8])\*\*' evidence/deferred-items.md` is at least 13
    - DEC-27's section contains the phrase `orchestrator decision 1` and names ROADMAP success criterion 2
    - DEC-32's section (the file's last section) states the rule and contrasts F-082 with both F-080 and F-081: `node -e 'const s=require("fs").readFileSync(".planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md","utf8");const d=s.slice(s.indexOf("## DEC-32"));process.exit(s.includes("## DEC-32")&&["ROADMAP success criterion 3","orchestrator decision 1","F-080","F-081","F-082","04-11","injection","PRESERVE"].every(t=>d.includes(t))?0:1)'` exits 0
  </acceptance_criteria>
  <done>Every design call later plans need is written as a numbered decision with its evidence, the SDK bump's blocking sites are enumerated instead of assumed, and every inherited item and assumption has an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laptop → local Supabase stack (Docker) | Every DB command in this plan targets 127.0.0.1; the seed loader's guard refuses anything else |
| laptop `.env.local` → production Postgres | The file holds production credentials; nothing in this plan opens it |
| throwaway worktree → npm registry | The DI-25 enumeration downloads one package version into a directory outside the repository |
| planning register → later plans | findings.json ids become citations in test headers; a wrong or missing id breaks the characterize-first discipline silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | findings.json whole-file reserialization hiding record changes in a reformat | mitigate | Per-record insertion only; acceptance asserts a deletion count of at most 2 in `git diff --numstat` |
| T-04-01-02 | Repudiation | A DEFECT test citing an id that does not exist, so a "known defect" is untraceable | mitigate | `scripts/check-characterization-tags.mjs` rejects unknown ids; proven red on an F-999 fixture |
| T-04-01-03 | Information Disclosure | A production credential reaching an evidence file | mitigate | No command reads `.env.local`; the seed loader prints no keys (its own contract); local keys come from `supabase status` and are never echoed into evidence |
| T-04-01-04 | Tampering | `supabase db reset` or the seed loader aimed at production | mitigate | `--local` on every CLI call; the loader's `assertSeedTargetAllowed` refuses non-local targets before a client exists |
| T-04-01-05 | Tampering | A floor number copied from research instead of measured, making "no regression" unprovable later | mitigate | The summary table requires measured and cited columns side by side, with the delta stated |
| T-04-01-SC | Tampering | npm install of `@supabase/supabase-js@2.116.0` | accept | Installed only into a throwaway worktree outside the repository for type-checking, never committed; the package is verified OK in 04-RESEARCH.md § Package Legitimacy Audit; acceptance asserts the main tree's manifests are untouched and the worktree removed |
</threat_model>

<verification>
- `node .planning/audit/tools/validate.mjs --check findings` exits 0, 85 findings
- `node scripts/check-characterization-tags.mjs --all` exits 0 with `ok 5 files`
- `evidence/floor.before.txt` line 1 is `base=<sha>` and holds 13 exit-coded blocks
- `git diff --name-only <base> -- src/ e2e/ supabase/ package.json package-lock.json tsconfig.json` prints nothing
- `node .planning/audit/tools/validate.mjs --quick` shows the same pass count as the before-floor plus the findings rows
</verification>

<success_criteria>
The phase has a measured floor on a named commit, a register entry for every defect it will pin or fix, a gate that makes the PRESERVE/DEFECT tagging discipline mechanical rather than aspirational, and a numbered decision for every design call the research left open — including the two places where the roadmap's success criteria and orchestrator decision 1 pull against each other, stated as a conflict rather than resolved by omission, and the one place (F-082, DEC-32) where a mandated security fix is exempted from the owner checkpoint by a written rule rather than by silence.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New files**
- `scripts/check-characterization-tags.mjs` — the PRESERVE/DEFECT tag gate (`--all` or explicit paths)
- `.planning/audit/quality/phase-04-slice-defects.md` — evidence for F-079 through F-085
- `evidence/floor.before.txt`, `evidence/findings-registration.txt`, `evidence/di-25-enumeration.txt`
- `evidence/phase-04-decisions.md` — DEC-23 through DEC-32
- `evidence/deferred-items.md` — the Phase 4 deferred register

**New finding ids:** F-079, F-080, F-081, F-082, F-083, F-084, F-085. **Changed:** F-050 `closes_in_phase` null → "04".
**New packages / npm scripts / CI steps:** none (the tag gate is wired into CI by 04-03).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-01-SUMMARY.md` when done. The SUMMARY carries a section headed "Planner decisions the phase owner may override" listing DEC-25 (before 04-09 executes) and DEC-32 (before 04-08 executes), each with one sentence on what an override would change — so the owner sees both before the plans that execute them run.
</output>
