---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 07
type: execute
wave: 5
depends_on: ["04-06"]
files_modified:
  - src/lib/eventTags.ts
  - src/lib/eventTags.test.ts
  - src/lib/tagMapping.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tag-centralization.txt
autonomous: true
requirements: [REFAC-10]

must_haves:
  truths:
    - "The DB-tag-to-EventTag mapping is defined in exactly one module, src/lib/eventTags.ts, and every read path reaches it through transformEventFromDB"
    - "Centralization changed no mapped output: the F-081 DEFECT suite and a golden table over every alias key, every enum member and an unknown tag pass unmodified across the move"
    - "An unmapped tag is surfaced without any visual change: a server-side warning names the event id and the unmapped tags, and a completeness test fails if an EventTag member is added without a mapping"
    - "The six known non-round-tripping members are listed once, citing F-081, so their fix is a one-place change gated by the 04-11 decision"
  artifacts:
    - path: "src/lib/eventTags.ts"
      provides: "TAG_ALIASES, partitionTags, mapTags, KNOWN_NON_ROUNDTRIP_TAGS"
      exports: ["TAG_ALIASES", "partitionTags", "mapTags", "KNOWN_NON_ROUNDTRIP_TAGS"]
      min_lines: 50
    - path: "src/lib/eventTags.test.ts"
      provides: "Golden table, completeness over Object.values(EventTag), partition behaviour, warning behaviour"
      min_lines: 60
  key_links:
    - from: "src/lib/tagMapping.ts"
      to: "src/lib/eventTags.ts"
      via: "transformEventFromDB calls partitionTags and re-exports mapTags so existing importers and the F-081 DEFECT suite keep working"
      pattern: "from \"@/lib/eventTags\""
    - from: "src/lib/eventTags.test.ts"
      to: "src/types/index.ts"
      via: "the completeness test iterates Object.values(EventTag)"
      pattern: "Object\\.values\\(EventTag\\)"
  prohibitions:
    - "No mapped output changes in this plan — the six identity mappings are the 04-11 checkpoint's decision (DEC-26, orchestrator decision 1)"
    - "src/lib/constants.ts (EVENT_CATEGORIES, TAG_HIERARCHY) and src/lib/classifier.ts are not modified — TAG_HIERARCHY serves the recommendation engine's affinity scoring, a different purpose, and the scoring formula is out of scope"
    - "tag-coercion-defect.test.ts is not edited"
    - "No route file is modified"
    - "This plan does not use the local stack (04-08 owns it in wave 5)"
---

<objective>
Wave 5, Jest-only, beside 04-08: centralize the tag mapping and surface unknown tags without changing a single rendered badge.

Purpose: REFAC-10's third clause and ROADMAP criterion 3 — "tag mapping is centralized with unknown tags surfaced rather than silently coerced to SOCIAL". Orchestrator decision 1 fixes how "surfaced" is delivered: non-visually (a log and a test), exactly as 04-RESEARCH.md § Q(e) recommends, because the six identity mappings that would fix F-081 change seeded badges. So this plan is two commits: a pure move proven byte-identical, then the surfacing, with mapped output unchanged in both.

Output: `src/lib/eventTags.ts` and its tests, and a `tagMapping.ts` that consults it.
</objective>

<slice_outcome>
Green when `npx jest --ci --selectProjects node --testPathPatterns "eventTags|tag-coercion-defect"` passes with `src/__tests__/lib/tag-coercion-defect.test.ts` unmodified, `src/lib/tagMapping.ts` holds no alias table of its own, and the full `npx jest --ci`, `npm run lint` and `npx tsc --noEmit` are green.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@src/lib/tagMapping.ts
@src/types/index.ts
@src/__tests__/lib/tag-coercion-defect.test.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — mapped tags are identical before and after both commits.
2. Characterize first — the F-081 DEFECT suite (04-04) and the golden table (written first, against the current function) are the before-and-after proof.
3. No schema change. 4. Production is never touched.
5. No visual change — by construction: the only new behaviour is a server-side warning.
6. Jest single runner; `src/lib/*.test.ts` routes to the node project.
7. Stage by explicit path only; two commits.
8. No local stack.
9. Evidence discipline.
10. Use the `EventTag` enum, never raw strings, for mapped values (CLAUDE.md § Key Patterns).
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the mapping into src/lib/eventTags.ts, proven byte-identical by a golden table written first</name>
  <files>src/lib/eventTags.ts, src/lib/eventTags.test.ts, src/lib/tagMapping.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tag-centralization.txt</files>
  <read_first>
    - src/lib/tagMapping.ts — the whole file: the 22-entry alias table (lines 10-35), `mapTags` with its lower-case, trim, Social default and de-duplication (lines 42-50), the stale comment on line 98, and `transformEventFromDB`'s tags line
    - src/types/index.ts lines 19-32 — the twelve EventTag members
    - src/__tests__/lib/tag-coercion-defect.test.ts — imports `mapTags` from `@/lib/tagMapping`; it must pass unmodified, so that export must survive
    - src/lib/sanitize.test.ts — the one-describe-per-export unit-test style
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md § Q(e) — the measured per-member table
  </read_first>
  <behavior>
    - Golden table, written and run green against the CURRENT `mapTags` before anything moves: each of the 22 alias keys maps to its current value; each of the 12 enum members maps to its current value (six to themselves, tech/food/volunteer/arts to social, music to cultural, networking to social); `quidditch` maps to social; `" Music "` (mixed case, padded) maps to cultural; `["academic", "Academic", "coding"]` de-duplicates to `[academic]`; `null` and `[]` map to `[]`
    - After the move, the same golden table passes with the subject imported from `@/lib/eventTags`, and also through the `@/lib/tagMapping` re-export
  </behavior>
  <action>
Write `src/lib/eventTags.test.ts` first with the golden table importing `mapTags` from `@/lib/tagMapping`, run it green, and record that run in `evidence/tag-centralization.txt` as the before proof.

<!-- planner-discipline-allow: Record<string, EventTag> -->
Create `src/lib/eventTags.ts` with a file-level JSDoc naming it the single definition of the database-tag to `EventTag` mapping, REFAC-10, F-081, and the known disagreement with `TAG_HIERARCHY` in `src/lib/constants.ts` (which maps hackathon and workshop to tech and fitness to sports for affinity scoring — a different purpose, deliberately not unified here). Export `TAG_ALIASES` (a `Record<string, EventTag>` with exactly the current 22 entries and values, in the current order) and `mapTags(dbTags)` with exactly the current semantics.

In `src/lib/tagMapping.ts`, delete its own alias table and `mapTags` body, import `mapTags` from `@/lib/eventTags`, and re-export it so `@/lib/tagMapping`'s public surface keeps `mapTags`; `transformEventFromDB` keeps calling it. Reword the stale comment on line 98 to say the transform passes the authoritative `start_date` and `end_date` columns through — this closes F-050's last source hit.
<!-- planner-discipline-allow: event_date -->
<!-- planner-discipline-allow: event_time -->
The rewording must not name the legacy event_date/event_time pair.

Switch the golden table's import to `@/lib/eventTags` and add one test asserting the `@/lib/tagMapping` re-export is the same function. Run the golden table, the F-081 DEFECT suite (unmodified), and the full Jest run; append to the evidence. Commit alone with a message stating "no mapped output changed".
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "eventTags|tag-coercion-defect" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "eventTags|tag-coercion-defect"` exits 0
    - `git diff --stat HEAD~1 -- src/__tests__/lib/tag-coercion-defect.test.ts` prints nothing
    - `grep -c "Record<string, EventTag>" src/lib/tagMapping.ts` returns 0 and `grep -c "Record<string, EventTag>" src/lib/eventTags.ts` returns 1
    - `grep -c 'from "@/lib/eventTags"' src/lib/tagMapping.ts` returns 1
    - `command grep -rn "event_date\|event_time" src/ supabase/ | command grep -v _archive_pre_baseline | wc -l` returns 0
    - `evidence/tag-centralization.txt` holds the golden table's green run against the pre-move function, captured before `src/lib/eventTags.ts` existed
  </acceptance_criteria>
  <done>The mapping has one home, the golden table and the unmodified DEFECT suite prove the move changed no output, and F-050's last stale reference is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface unmapped tags non-visually and make completeness a test (DEC-26)</name>
  <files>src/lib/eventTags.ts, src/lib/eventTags.test.ts, src/lib/tagMapping.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/tag-centralization.txt</files>
  <read_first>
    - src/lib/eventTags.ts and src/lib/eventTags.test.ts — as left by Task 1
    - src/lib/tagMapping.ts `transformEventFromDB` — where the event id is available to name in the warning
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-26
    - .claude/CLAUDE.md § Logging — the bracketed-prefix convention for server log lines
  </read_first>
  <behavior>
    - `partitionTags(["academic", "tech", "quidditch"])` returns `mapped` equal to today's `mapTags` output for the same input and `unmapped` equal to `["tech", "quidditch"]` (normalized, de-duplicated); `mapTags(x)` equals `partitionTags(x).mapped` for every golden-table input
    - Completeness: for every member of `Object.values(EventTag)`, either `mapTags([member])` equals `[member]`, or the member is in `KNOWN_NON_ROUNDTRIP_TAGS` and does not round-trip; `KNOWN_NON_ROUNDTRIP_TAGS` has exactly six entries (tech, food, volunteer, arts, music, networking); a synthetic thirteenth member neither mapped nor listed would fail this test (prove it with a mutation cycle)
    - `transformEventFromDB` on a row tagged `["academic", "tech"]` returns tags `[academic, social]` (unchanged) and logs exactly one warning with the `[tags]` prefix whose payload names the row's id and `["tech"]`; a row whose tags all map logs nothing
  </behavior>
  <action>
In `src/lib/eventTags.ts`, add `partitionTags(dbTags)` returning `{ mapped, unmapped }`, where `mapped` is exactly today's output (unmapped strings still become Social, because the display must not change) and `unmapped` is the normalized strings that hit the default; re-implement `mapTags` as `partitionTags(dbTags).mapped`. Add `KNOWN_NON_ROUNDTRIP_TAGS`, a readonly array of the six `EventTag` members, with a comment citing F-081, orchestrator decision 1 and the 04-11 checkpoint as the one place their fix lands.

In `transformEventFromDB`, call `partitionTags` and, when `unmapped` is non-empty, emit one `console.warn` with the `[tags]` prefix, the message that unmapped tags were rendered as Social, and a payload object carrying `eventId` and `unmapped`. This is the server-side surfacing; Phase 6 replaces `console` with structured logging. Note in the function's JSDoc that the warning fires per transformed event and that its volume is accepted until Phase 6.

Add the partition, completeness and warning tests to `src/lib/eventTags.test.ts`, with the warning tests spying on and restoring `console.warn`. Record one mutation cycle proving the completeness test bites: temporarily remove one member from `KNOWN_NON_ROUNDTRIP_TAGS`, observe the red, restore with `git checkout --`, confirm green. Run the Slice 2 and full Jest suites — the list, detail and calendar PRESERVE suites use only stable tags and must not warn or change. Commit with a message naming REFAC-10, F-081 and "no rendered output changed".
  </action>
  <verify>
    <automated>npx jest --ci && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci` exits 0 with the skipped count unchanged
    - `grep -c "export function partitionTags" src/lib/eventTags.ts` returns 1 and `grep -c "KNOWN_NON_ROUNDTRIP_TAGS" src/lib/eventTags.ts` is at least 1
    - `grep -c "Object.values(EventTag)" src/lib/eventTags.test.ts` is at least 1
    - `grep -c "\[tags\]" src/lib/tagMapping.ts` returns 1
    - `git diff --stat HEAD~2 -- src/__tests__/lib/tag-coercion-defect.test.ts src/__tests__/api/events/events-list-characterization.test.ts src/__tests__/api/events/events-detail-characterization.test.ts` prints nothing
    - `evidence/tag-centralization.txt` records the completeness mutation cycle with its red and green
    - `git diff --stat HEAD~2 -- src/app src/components src/lib/constants.ts src/lib/classifier.ts` prints nothing
  </acceptance_criteria>
  <done>Unknown tags are no longer silently coerced — every one is logged with its event — and an enum member added without a mapping fails a test, while every badge on every page renders exactly as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingestion (scraper, classifier, organizers) → events.tags | Free-text tags enter the database from several writers |
| events.tags → rendered badges | The read mapping decides what category a user sees |
| transform → server logs | The new warning writes tag strings and event ids to logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | A "refactor" that silently changes mapped output | mitigate | Golden table written and run against the pre-move function; the F-081 DEFECT suite passes unmodified |
| T-04-07-02 | Information Disclosure | Tag strings and event ids in logs | accept | Tags are public event metadata and ids are public URLs; no user data is logged |
| T-04-07-03 | Denial of Service | Log volume from a warning per transformed event | accept | Bounded by page size (≤100 per list request); Phase 6 structured logging owns levels and sampling; recorded in the JSDoc |
| T-04-07-04 | Repudiation | A new enum member added with no mapping, silently rendered Social | mitigate | Completeness test over Object.values(EventTag), proven to bite by a mutation cycle |
| T-04-07-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `npx jest --ci` exits 0; skipped count unchanged
- `npx tsc --noEmit`, `npm run lint` clean
- `node scripts/check-characterization-tags.mjs --all` exits 0
- Two commits: the byte-identical move, then the surfacing
</verification>

<success_criteria>
The tag mapping is centralized in one module, unknown tags are surfaced by a server warning and guarded by a completeness test, the six known mis-mappings are named in one place with their finding id, and no rendered output changed — which is exactly the non-visual delivery orchestrator decision 1 requires of ROADMAP criterion 3's tag clause.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New files**
- `src/lib/eventTags.ts` — exports `TAG_ALIASES`, `partitionTags`, `mapTags`, `KNOWN_NON_ROUNDTRIP_TAGS`
- `src/lib/eventTags.test.ts` — golden table, completeness, partition and warning tests
- `evidence/tag-centralization.txt`

**Modified:** `src/lib/tagMapping.ts` — consults `@/lib/eventTags`, re-exports `mapTags`, warns on unmapped tags, stale comment corrected.
**Register effect:** F-050's last source hit removed (status flips in 04-11); F-081 stays Open pending the 04-11 decision.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-07-SUMMARY.md` when done.
</output>
