---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/__tests__/api/events/events-list-characterization.test.ts
  - src/__tests__/api/events/search-escaping-defect.test.ts
  - src/__tests__/api/events/pagination-contract-defect.test.ts
  - src/__tests__/api/events/events-detail-characterization.test.ts
  - src/__tests__/api/events/club-fabrication-defect.test.ts
  - src/__tests__/lib/tag-coercion-defect.test.ts
  - src/__tests__/helpers/fakeSupabase.ts
  - e2e/specs/event-read-path.spec.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-characterization.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-mutation-check.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-unmodified.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt
autonomous: true
requirements: [REFAC-10]

must_haves:
  truths:
    - "The event list's filtering, paging-by-page, time-of-day validation, fuzzy-rank ordering and cache header are pinned by a PRESERVE suite that none of the Slice 2 fixes can move"
    - "The event detail route's 404 and pending_edits visibility rules are pinned by a PRESERVE suite"
    - "Each Slice 2 defect — club fabrication (F-080), tag coercion (F-081), raw search interpolation (F-082) and the missing cursor contract (F-083) — is pinned by a DEFECT suite citing its id, passing today"
    - "The persona harness pins, against the real local PostgREST, that a comma in search is a 500 today, that % matches everything today, that no nextCursor is emitted today, and what the detail page and the feed card render today"
    - "Every new suite has been observed going red under a mutation of what it pins, and every subject is provably older than its suite"
  artifacts:
    - path: "src/__tests__/api/events/events-list-characterization.test.ts"
      provides: "PRESERVE — /api/events filtering, page-mode range, validation 400s, early returns, fuzzy-rank ordering, Cache-Control"
      min_lines: 150
    - path: "src/__tests__/api/events/search-escaping-defect.test.ts"
      provides: "DEFECT F-082 (and the F-059 echo it triggers)"
      min_lines: 50
    - path: "src/__tests__/api/events/pagination-contract-defect.test.ts"
      provides: "DEFECT F-083 — cursor ignored, no nextCursor, invalid cursor accepted"
      min_lines: 40
    - path: "src/__tests__/api/events/club-fabrication-defect.test.ts"
      provides: "DEFECT F-080 — four separately-moving pins: organizer fallback, blanked URL fields, saved-events select, detail select"
      min_lines: 60
    - path: "src/__tests__/lib/tag-coercion-defect.test.ts"
      provides: "DEFECT F-081 — six non-round-tripping members and the silent Social default"
      min_lines: 40
    - path: "e2e/specs/event-read-path.spec.ts"
      provides: "Slice 2 end-to-end net: 4 PRESERVE and 5 DEFECT tests against the seeded local stack"
      min_lines: 80
  key_links:
    - from: "src/__tests__/lib/tag-coercion-defect.test.ts"
      to: "src/lib/tagMapping.ts"
      via: "imports mapTags from @/lib/tagMapping, so 04-07's move must keep that export and this suite proves the move byte-identical"
      pattern: "from \"@/lib/tagMapping\""
    - from: "e2e/specs/event-read-path.spec.ts"
      to: "src/app/api/events/route.ts"
      via: "API-level requests through Playwright's request fixture exercise the real PostgREST parser, which no in-process mock can"
      pattern: "request\\.get\\("
  prohibitions:
    - "No file under src/app/, src/lib/ or src/server/ is modified"
    - "The PRESERVE list suite never pins: the or() argument string, the exact response key set, the number of order() calls, the select column string, the club shape, any tag outside the six stable members, or the text of a 500 body"
    - "No suite imports from the Supabase SDK package or the service-role factory"
    - "The seed is not changed (DEC-30)"
    - "No Vitest API"
---

<objective>
Wave 3 (the wave's only stack user, running beside 04-05): write the Slice 2 characterization net against the unmodified read path — a PRESERVE contract for the list and the detail route that no Slice 2 fix is allowed to move, a DEFECT pin for each of the four registered read-path defects, and a Playwright spec that pins the same facts against the real PostgREST parser.

Purpose: REFAC-10 changes four behaviours of the highest-traffic read in the product. 04-RESEARCH.md § Validation Architecture lists every Slice 2 test as missing. Two of the four defects (the `.or()` parse failure and the cursor contract) are properties of the real PostgREST grammar that an in-process mock can describe but cannot prove, which is why this plan adds API-level Playwright tests through the request fixture as well as Jest suites.

Output: six suites (two PRESERVE, four DEFECT), one new Playwright spec, a mutation-cycle record, an ancestry proof, and the before-run of the harness.
</objective>

<slice_outcome>
Green when the six new suites pass, `node scripts/check-characterization-tags.mjs --all` exits 0 with them included, `npx playwright test` passes with nine more tests than 04-02's run, and `git diff --stat <base> -- src/app src/lib src/server` is empty in this plan's worktree.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-PATTERNS.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@.planning/audit/quality/phase-04-slice-defects.md
@src/app/api/events/route.ts
@src/app/api/events/[id]/route.ts
@src/lib/tagMapping.ts
@src/__tests__/helpers/fakeSupabase.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — this plan changes no production file.
2. Characterize first — PRESERVE or DEFECT in every leading docblock; DEFECT cites a registered F-nnn; `node scripts/check-characterization-tags.mjs --all` is the gate.
3. No schema change; no seed change (DEC-30).
4. Production is never touched; `.env.local` is never read.
5. No intentional visual change — the Playwright tests assert what renders today.
6. Jest single runner; `src/__tests__/**` routes to the node project; `--testPathPatterns` plural; no Vitest API.
7. Stage by explicit path only.
8. Local-stack exclusivity — this plan is wave 3's only stack user; 04-05 runs Jest only.
9. Evidence discipline — raw output verbatim, command above, exit code below.
10. Write every new suite type-clean for the DI-24 un-exclusion in 04-06 (typed handler references, top-level imports), proven with the throwaway-tsconfig recipe 04-02 used.
</phase_locked_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the event list — a PRESERVE contract no Slice 2 fix may move, plus the F-082 and F-083 DEFECT pins</name>
  <files>src/__tests__/api/events/events-list-characterization.test.ts, src/__tests__/api/events/search-escaping-defect.test.ts, src/__tests__/api/events/pagination-contract-defect.test.ts, src/__tests__/helpers/fakeSupabase.ts</files>
  <read_first>
    - src/app/api/events/route.ts — the whole GET: parameter parsing (158-172), the two allow-lists and their 400 bodies (174-190), the base query (192-198), ids and tags (200-212), the fuzzy RPC and its three outcomes (214-243), the date floor via `getESTNowISO()` (245-250), the time-of-day RPC and its early return (252-270), page-mode `range` (272-275), the benign-error and 500 branches (280-307), the fuzzy re-sort (312-320), and the response with its Cache-Control header (326-339)
    - src/__tests__/api/events/get-events.test.ts — the existing 30-odd tests over the same route; do not duplicate them and do not modify this file (04-06, 04-08 and 04-09 own it)
    - src/app/api/events/route.test.ts — the skipped cursor suite; read it to know the contract the F-083 DEFECT describes the absence of, and do not modify it (04-09 rewrites it)
    - src/__tests__/helpers/fakeSupabase.ts — its rpc support, its count semantics, its cumulative order semantics, and its call log
    - .planning/audit/quality/phase-04-slice-defects.md § F-082 and § F-083 — the registered wording the DEFECT headers must agree with
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-25 — the decided contract, so the pagination DEFECT names what its assertions move to
  </read_first>
  <behavior>
    - PRESERVE list: only approved, non-deleted events are queried; with no `dateFrom` the start_date floor equals the mocked `getESTNowISO()` value, and an explicit `dateFrom` replaces it; `dateTo` adds an upper bound; `tags=academic, social` filters by overlap with the trimmed array; `ids=a,b` restricts by id; an invalid `timeOfDay` or `dayType` returns 400 with the exact current message and issues no query; an empty time-filter RPC result returns 200 whose body matches `{ events: [], total: 0, page: 1, limit: 50, totalPages: 0 }`; page 2 with limit 10 and a count of 25 requests rows 10 through 19 and returns `total: 25, totalPages: 3`; a successful fuzzy RPC restricts to its ids and returns events in rank order, not start_date order; a fuzzy RPC with no matches returns 200 with an empty list and total 0; when the fuzzy RPC errors, one or() filter is applied whose argument contains `title.ilike.`, `description.ilike.` and the search term; a benign range error (PGRST103) returns 200 with an empty list; a non-benign query error returns status 500; the success response carries `Cache-Control: public, s-maxage=30, stale-while-revalidate=60`
    - DEFECT F-082: for the terms `a,b`, `%` and `_` (RPC erroring), the or() argument is exactly the raw interpolation `title.ilike.%<term>%,description.ilike.%<term>%`; and when the events query fails with a PGRST100 error the response is 500 whose `error` equals the PostgREST message (the F-059 echo the malformed filter triggers)
    - DEFECT F-083: a request carrying a well-formed base64 cursor still requests rows 0 through limit-1 and the body has neither a `nextCursor` nor a `prevCursor` property; with a count greater than the limit no next cursor is emitted; `cursor=not-a-valid-cursor` returns 200, not 400
  </behavior>
  <action>
Mock `@/lib/supabase/server` to resolve the 04-02 fake (same `mock`-prefixed variable pattern), and mock `@/lib/timezone` so `getESTNowISO` returns a fixed ISO string. `@/lib/tagMapping` is NOT mocked: fixtures use tags only from the six stable members, a null organizer and no `club` key, so the real transform is harmless and later F-080/F-081 commits cannot move these assertions. If the fake needs an extension to run this route (for example rpc argument capture or `overlaps` recording), extend it additively and keep 04-02's suites green; record the extension in the evidence file.

Create `events-list-characterization.test.ts` as PRESERVE (`PRESERVE characterization — GET /api/events (REFAC-10)`) with the house header sections. Its header must list, in a "DELIBERATELY NOT PINNED" block, each item from this plan's prohibitions and the Slice 2 commit that is allowed to change it (F-082 → the or() string; F-083 → the key set, the id tie-break order call and the cursor keys on early returns; F-080 → the select string and club shape; F-081 → non-stable tags; F-059 → 500 body text). Assert early-return and success bodies with object matching on the five current keys rather than an exact key list, so DEC-25's added keys do not move them.

Create `search-escaping-defect.test.ts` (`DEFECT characterization — F-082`, also naming F-059 for the echo) and `pagination-contract-defect.test.ts` (`DEFECT characterization — F-083`) in the friends-defect header shape: subject with line numbers, the defect, what the file is and is not, why the assertion is on the call log, and exactly which assertions move in which plan (04-08 for F-082's or() strings — the PGRST100-to-500 echo assertion stays, because the echo branch is F-059's and Phase 5's; 04-09 for F-083).

Capture the three suites' run into `evidence/slice-2-characterization.txt`.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "events-list-characterization|search-escaping-defect|pagination-contract-defect"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "events-list-characterization|search-escaping-defect|pagination-contract-defect"` exits 0 with 3 suites and at least 25 tests passed
    - `grep -c "DEFECT characterization — F-082" src/__tests__/api/events/search-escaping-defect.test.ts` returns 1 and `grep -c "DEFECT characterization — F-083" src/__tests__/api/events/pagination-contract-defect.test.ts` returns 1
    - `grep -c "DELIBERATELY NOT PINNED" src/__tests__/api/events/events-list-characterization.test.ts` returns 1
    - `node scripts/check-characterization-tags.mjs --all` exits 0
    - `npx jest --ci --selectProjects node --testPathPatterns "characterization|defect"` exits 0 (04-02's suites still pass with any fake extension)
    - `git diff --stat -- src/app src/lib src/server src/__tests__/api/events/get-events.test.ts src/app/api/events/route.test.ts` prints nothing
  </acceptance_criteria>
  <done>The list route has a contract that survives every planned Slice 2 change, and the two list defects are pinned under their ids with the plan that moves each assertion named in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the detail route, the club fabrication and the tag coercion</name>
  <files>src/__tests__/api/events/events-detail-characterization.test.ts, src/__tests__/api/events/club-fabrication-defect.test.ts, src/__tests__/lib/tag-coercion-defect.test.ts</files>
  <read_first>
    - src/app/api/events/[id]/route.ts lines 71-134 — the GET: `select("*")` with the false comment on line 79, the PGRST116-to-404 mapping, the 500 echo, the transform, and the pending_edits stripping for anyone who is neither the creator nor an admin
    - src/lib/tagMapping.ts — the whole file: the 22-entry alias table, `mapTags` and its silent Social default, and `transformEventFromDB`'s two club branches (real embed with six hard-coded nulls; organizer fallback with the organizer string as id and name)
    - src/app/api/users/saved-events/route.ts lines 89-118 — its events read uses `select("*")`, so a row with an organizer gets a fabricated club
    - src/types/index.ts lines 19-32 — the twelve EventTag members
    - .planning/audit/quality/phase-04-slice-defects.md § F-080 and § F-081
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-26 and § DEC-27 — which F-080 pins move in 04-10 (non-visual) and which only move if the 04-11 checkpoint ships the visual fix
  </read_first>
  <behavior>
    - PRESERVE detail: a PGRST116 error returns 404 `{ error: "Event not found" }`; any other query error returns status 500; an anonymous caller gets `{ event }` with no `pending_edits` key; the creator gets `pending_edits`; a caller whose users row has the admin role gets `pending_edits`; a signed-in non-creator non-admin does not
    - DEFECT F-080 pin A (moves only if 04-11 ships the visual fix): a DB row with no club and organizer `Seed Organizer A` transforms to a club whose id and name are both `Seed Organizer A` and whose status is approved
    - DEFECT F-080 pin B (moves in 04-10): a DB row whose club embed carries `website_url`, `banner_url`, `discord_url`, `twitter_url` and `linkedin_url` values transforms to a club with all five null
    - DEFECT F-080 pin C (moves in 04-10): `/api/users/saved-events` reads events with columns `*` and no club embed
    - DEFECT F-080 pin D (moves only if 04-11 ships): `/api/events/[id]` reads the event with columns `*` and no club embed
    - DEFECT F-081: mapTags maps tech, food, volunteer and arts to [social], music to [cultural], networking to [social], and an unknown tag such as quidditch to [social], with no signal of any kind
  </behavior>
  <action>
Create `events-detail-characterization.test.ts` as PRESERVE (`PRESERVE characterization — GET /api/events/[id] (REFAC-10)`), using the fake and stable-tag, null-organizer, no-club fixtures; its "DELIBERATELY NOT PINNED" block names the club shape and select string (F-080) and the 500 body text (F-059).

Create `club-fabrication-defect.test.ts` (`DEFECT characterization — F-080`, also naming F-050 for the false comment) with the four pins as four separately named `describe` blocks, each block's name stating which plan moves it: "moves in 04-10" for B and C, "moves only if the 04-11 decision ships the visual fix" for A and D. Pin A and B call `transformEventFromDB` directly; pins C and D invoke the handlers and assert on the fake's call log. The header must state the seeded consequence of pin A in plain words: every seeded event has both a club_id and an organizer label, so the detail page today says "Hosted by" the organizer label while the feed card shows the real club.

Create `src/__tests__/lib/tag-coercion-defect.test.ts` (`DEFECT characterization — F-081`), importing `mapTags` from `@/lib/tagMapping` — deliberately that path, because 04-07 moves the mapping and must keep this export, and this suite passing unmodified is the proof the move changed nothing. Its header states that the six-member coercion is visible on seeded data (the approved event renders academic and social; the music night renders cultural and social) and that its fix is gated behind the 04-11 checkpoint under orchestrator decision 1.

Append the three suites' run to `evidence/slice-2-characterization.txt`.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "events-detail-characterization|club-fabrication-defect|tag-coercion-defect"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "events-detail-characterization|club-fabrication-defect|tag-coercion-defect"` exits 0 with 3 suites and at least 18 tests passed
    - `grep -c "DEFECT characterization — F-080" src/__tests__/api/events/club-fabrication-defect.test.ts` returns 1 and `grep -c "DEFECT characterization — F-081" src/__tests__/lib/tag-coercion-defect.test.ts` returns 1
    - `grep -c "moves in 04-10" src/__tests__/api/events/club-fabrication-defect.test.ts` is at least 2 and `grep -c "04-11" src/__tests__/api/events/club-fabrication-defect.test.ts` is at least 2
    - `grep -c 'from "@/lib/tagMapping"' src/__tests__/lib/tag-coercion-defect.test.ts` returns 1
    - `node scripts/check-characterization-tags.mjs --all` exits 0
    - `git diff --stat -- src/app src/lib src/server` prints nothing
  </acceptance_criteria>
  <done>The detail route's visibility rules are a contract, and the club and tag defects are pinned in pieces that each move in exactly one named later plan — so a reviewer can see which pin moved in which commit and why.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the read path against the real PostgREST in the persona harness, and prove every new assertion bites</name>
  <files>e2e/specs/event-read-path.spec.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/playwright.slice-2-before.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-mutation-check.txt, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-2-unmodified.txt</files>
  <read_first>
    Context budget: eight entries. [FULL] means read the whole file; [SECTION] means read only the named lines — do not read a [SECTION] file whole (page.tsx and constants.ts are long).
    - [FULL, 71 lines] e2e/specs/anonymous-browse.spec.ts — the anonymous storage-state declaration, the feed wait, and the search placeholder text `Search events, clubs, categories...`
    - [FULL, 78 lines] e2e/fixtures.ts — `IDS`; navigate and request by fixed id
    - [SECTION] src/app/page.tsx lines 244-300 and 470-520 — the filtered feed ignores the hook's error, so a 500 renders the same "No events found" state as an empty result; Load More renders only when nextCursor is set
    - [SECTION] src/components/events/EventDetailView.tsx lines 219-260 — the "Hosted by" block renders `event.club.name`
    - [SECTION] src/components/events/EventCard.tsx lines 285-330 — the card renders the club name pill and one label per tag from EVENT_CATEGORIES
    - [SECTION] src/lib/constants.ts lines 22-143 — the exact labels (Social, Tech, Academic, Cultural, Music) the card renders
    - [SECTION] scripts/seed/load.ts lines 320-352 — the organizer label is PRNG-picked from three fixed strings; read it from the API rather than hard-coding it
    - [SECTION — the first cycle's block only, as the format] .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/slice-1-mutation-check.txt — the cycle format to reuse
  </read_first>
  <action>
Create `e2e/specs/event-read-path.spec.ts` with an anonymous storage state and a header in the style of the existing specs naming Phase 4 plan 04-04, the Validated workflows it re-confirms ("Anonymous visitors can browse…", "User can browse, search, and filter events…"), and — per test — whether it is PRESERVE or DEFECT with its F-nnn. Prefix each test title with `PRESERVE:` or `DEFECT F-0nn:`. Use Playwright's `request` fixture for API-level tests so the real PostgREST grammar is exercised.

PRESERVE tests: (1) `GET /api/events?limit=1` returns 200, first event id `IDS.approvedEvent`, total 2; (2) `GET /api/events?search=Music%20Night` returns exactly the second approved event; (3) on `/`, typing `a,b` into the search box shows the "No events found" state — true today because the page ignores the 500, and still true after the fix; (4) `/events/${IDS.approvedEvent}` renders the title "Seed Approved Event".

DEFECT tests: (5, F-083) the body of `GET /api/events?limit=1` has no `nextCursor` property; (6, F-082) `GET /api/events?search=a,b` returns 500 and its `error` contains `failed to parse logic tree`; (7, F-082) `GET /api/events?search=%25` returns total 2 — a percent sign matches every event; (8, F-080) read `event.organizer` from `GET /api/events/${IDS.approvedEvent}`, then assert the detail page's "Hosted by" block shows that label and not `Seed Approved Club`; (9, F-081) within the feed card scoped to the text "Seed Approved Event", the `Social` label is visible and the `Tech` label is absent — scope to the card, because the filter chips also render every label.

Reset and seed the local stack, run `npx playwright test`, and capture into `evidence/playwright.slice-2-before.txt`: passed must equal the count in `evidence/playwright.slice-1-before.txt` plus 9.

Mutation cycles, recorded in `evidence/slice-2-mutation-check.txt` in the 04-02 format (mutate, run only that suite, red naming a test, `git checkout --` restore, `git diff --quiet` exit 0, green): list PRESERVE — change the approved status filter to pending; search DEFECT — wrap the interpolated or() values in double quotes; pagination DEFECT — add a null `nextCursor` key to the success body; detail PRESERVE — remove the pending_edits stripping branch; club DEFECT — make the organizer fallback set the id to null; tag DEFECT — change the Social default to Academic. Six cycles minimum.

Unmodified proof in `evidence/slice-2-unmodified.txt` for `src/app/api/events/route.ts`, `src/app/api/events/[id]/route.ts`, `src/lib/tagMapping.ts` and `src/app/api/users/saved-events/route.ts`: empty diff against the base and a `git merge-base --is-ancestor` exit of 0 for each file's last commit.
  </action>
  <verify>
    <automated>npx jest --ci && node scripts/check-characterization-tags.mjs --all</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/playwright.slice-2-before.txt` ends with `exit=0`, 0 failed, and a passed count equal to `evidence/playwright.slice-1-before.txt`'s plus 9
    - `grep -cE 'test\("(PRESERVE|DEFECT F-0[0-9]{2}):' e2e/specs/event-read-path.spec.ts` returns 9 (test titles written as double-quoted strings)
    - `evidence/slice-2-mutation-check.txt` records at least 6 cycles, each with a red naming a test, a clean-diff check and a green re-run
    - `evidence/slice-2-unmodified.txt` records four empty diffs and four ancestry exits of 0
    - `npx jest --ci` exits 0 with the skipped count unchanged from the before-floor
    - `git diff --stat -- src/app src/lib src/server` prints nothing
  </acceptance_criteria>
  <done>The four read-path defects are pinned in-process and against the real PostgREST, the rendered consequences of two of them on seeded data are measured rather than described, and every new assertion has been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser search box → PostgREST logic tree | Untrusted text is interpolated into an or() filter today (F-082) |
| anonymous caller → /api/events | Public, cacheable read; this plan only observes it |
| test process → production source | Pressure to edit a subject so an assertion passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | A comma in search yields a 500 carrying the internal filter (F-082/F-059) | accept (this plan) | Pinned, not fixed; 04-08 removes the trigger. The e2e DEFECT test proves the leak against the real parser so the fix is measured |
| T-04-04-02 | Tampering | Editing a route so an assertion passes | mitigate | Prohibition by name; `git checkout --` restore and `git diff --quiet` per mutation cycle; ancestry proof |
| T-04-04-03 | Repudiation | PRESERVE assertions that a Slice 2 fix would silently move, blurring "preserved" and "fixed" | mitigate | The "DELIBERATELY NOT PINNED" header block and object matching on current keys; each DEFECT pin names the plan that moves it |
| T-04-04-04 | Tampering | Stack reset or seed aimed at a non-local target | mitigate | `--local` on every CLI call; loader guard |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | No package installed |
</threat_model>

<verification>
- `npx jest --ci` exits 0; skipped unchanged; passed = previous + new
- `node scripts/check-characterization-tags.mjs --all` exits 0
- `npx playwright test` exits 0 at 04-02's count + 9
- `npm run lint` 0 errors; `npx tsc --noEmit` exits 0
- `git diff --name-only <base> -- src/app src/lib src/server` prints nothing in this plan's worktree
</verification>

<success_criteria>
Slice 2 has a before-picture precise enough that every later commit can be classified as "preserved" or "fixed F-nnn" by which assertion it moved: the list and detail contracts that must not move, and four DEFECT pins — in Jest and against the real PostgREST — each labelled with the plan allowed to move it.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**New files**
- `src/__tests__/api/events/events-list-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/events-detail-characterization.test.ts` (PRESERVE)
- `src/__tests__/api/events/search-escaping-defect.test.ts` (DEFECT F-082, names F-059)
- `src/__tests__/api/events/pagination-contract-defect.test.ts` (DEFECT F-083)
- `src/__tests__/api/events/club-fabrication-defect.test.ts` (DEFECT F-080, names F-050)
- `src/__tests__/lib/tag-coercion-defect.test.ts` (DEFECT F-081)
- `e2e/specs/event-read-path.spec.ts` — 4 PRESERVE + 5 DEFECT tests
- `evidence/slice-2-characterization.txt`, `evidence/slice-2-mutation-check.txt`, `evidence/slice-2-unmodified.txt`, `evidence/playwright.slice-2-before.txt`

**Possibly extended (additively):** `src/__tests__/helpers/fakeSupabase.ts`.
**New packages / npm scripts / CI steps:** none.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-04-SUMMARY.md` when done.
</output>
