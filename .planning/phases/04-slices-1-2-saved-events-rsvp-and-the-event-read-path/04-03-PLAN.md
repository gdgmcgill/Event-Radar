---
phase: 04-slices-1-2-saved-events-rsvp-and-the-event-read-path
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/check-elevated-ratchet.mjs
  - eslint.config.mjs
  - eslint.elevated-allowlist.mjs
  - src/server/db/elevated/REGISTRY.md
  - .github/workflows/ci.yml
  - src/server/context.ts
  - src/server/__tests__/context.test.ts
  - src/server/__tests__/requireRole.test.ts
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/boundary-widening.txt
  - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/di-35-narrowing.txt
autonomous: true
requirements: [REFAC-09]

must_haves:
  truths:
    - "A new file anywhere under src/ that reaches the service-role client — directly, by dynamic import, or by reading the service-role key — fails lint, except inside the credential's two sanctioned homes"
    - "The ratchet census and the ESLint boundary read the same thing: value imports, including dynamic ones, across src/, ignoring type-only imports and the two sanctioned homes"
    - "src/lib/audit.ts, the indirect elevated caller no control could see, is counted by both controls, and it is the only entry the one-time widening adds"
    - "CI runs the elevated ratchet and the characterization tag gate on every pull request"
    - "The seam's per-request profile read selects only columns something consumes, and its docblock no longer implies a ban check the seam does not perform"
  artifacts:
    - path: "scripts/check-elevated-ratchet.mjs"
      provides: "Census over src/ with sanctioned-home exemptions, type-only import skip, dynamic-import detection"
      contains: "src/server/db/elevated/"
    - path: "eslint.elevated-allowlist.mjs"
      provides: "25 entries: the 24 legacy callsites plus src/lib/audit.ts"
      contains: "src/lib/audit.ts"
    - path: ".github/workflows/ci.yml"
      provides: "Two structural gate steps in the ci job"
      contains: "check-elevated-ratchet.mjs"
    - path: "src/server/context.ts"
      provides: "RequestProfile narrowed to id, roles, onboarding_completed (DEC-24)"
      contains: "PROFILE_COLUMNS"
  key_links:
    - from: "eslint.config.mjs"
      to: "eslint.elevated-allowlist.mjs"
      via: "the allow-list block turns off every boundary rule for the legacy entries, including the new companion rule"
      pattern: "LEGACY_ELEVATED_CALLSITES"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-characterization-tags.mjs"
      via: "a ci-job step runs the tag gate with --all"
      pattern: "check-characterization-tags.mjs --all"
  prohibitions:
    - "The allow-list is regenerated exactly once, and the only entry-level change is the addition of src/lib/audit.ts; any other entry-level difference stops the task for a written explanation"
    - "No rule is downgraded to warn and no inline disable comment is added"
    - "No application route is modified; no route is migrated to the elevated door (no shrink is available in Phase 4 — DEC in 04-01)"
    - "No ban guard is added to src/server/authz/ (DEC-24; REFAC-11 is Phase 5's)"
    - "The workflow's top-level placeholder env block is not touched (DI-32's fix depends on it staying as it is)"
    - "This plan does not use the local Supabase stack or port 3000 (04-02 owns the stack in wave 2)"
---

<objective>
Wave 2, in parallel with 04-02 and without the local stack: close the three seam-kit control gaps Phase 3 handed to Phase 4 — DI-34 (the boundary sees only `src/app/**`, so `src/lib/audit.ts` reaches the service role invisibly), DI-31 (the dynamic-import and bare-key evasions, and the ratchet's missing CI wiring) and DI-35 (the seam reads ban columns nothing consumes, under a docblock that implies otherwise).

Purpose: REFAC-09's first clause is "handlers use the seam kit". A seam whose elevated door can be bypassed by adding one indirection is not yet a seam; 04-RESEARCH.md § Q(h) and Q(i) and `src/server/db/elevated/REGISTRY.md` both say so in prose. This plan turns the prose into two controls that agree and one CI job that enforces them. It deliberately does not shrink the allow-list: none of Phase 4's thirteen handlers is in it, and manufacturing a shrink would break the slice boundary (research § Q(i)).

Output: a widened census and lint boundary with a red/green proof on an indirect import, a companion rule for the two evasions with its own red/green proof, CI steps for the ratchet and the tag gate, and the narrowed request profile.
</objective>

<slice_outcome>
Green when `npm run lint` exits 0 with 0 errors, `node scripts/check-elevated-ratchet.mjs` prints `committed=25 live=25 delta=0`, both red fixtures were observed failing and are gone, `.github/workflows/ci.yml` carries the two new steps, and `npx jest --ci --selectProjects node --testPathPatterns "src/server"` passes with the narrowed profile.
</slice_outcome>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-RESEARCH.md
@.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md
@.planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md
@scripts/check-elevated-ratchet.mjs
@eslint.config.mjs
@src/server/db/elevated/REGISTRY.md
@src/server/context.ts
</context>

<phase_locked_constraints>
1. Behaviour preservation — no application route changes behaviour; the only runtime change is one fewer pair of columns in the seam's profile read, which no guard consumes.
2. Characterize first — the seam's own suites under `src/server/__tests__/` are the characterization of `context.ts`; they change only in the assertions that name the dropped columns.
3. No schema change. 4. Production is never touched. 5. No visual change.
6. Jest is the single runner; `--testPathPatterns` plural.
7. Stage by explicit path only.
8. No local stack in this plan.
9. Evidence discipline — raw output verbatim with command and exit code.
10. The allow-list may only shrink, with one sanctioned exception recorded here: the one-time widening that absorbs the pre-existing indirect caller (DI-34). It is regenerated once, inside this plan, with its before/after diff captured — never inside a slice diff.
</phase_locked_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Widen the elevated census and the lint boundary to src/, prove it bites on an indirect import, and absorb src/lib/audit.ts once</name>
  <files>scripts/check-elevated-ratchet.mjs, eslint.config.mjs, eslint.elevated-allowlist.mjs, src/server/db/elevated/REGISTRY.md, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/boundary-widening.txt</files>
  <read_first>
    - scripts/check-elevated-ratchet.mjs — the whole file: the substring census over `src/app`, the test-file skip and why it must stay, the bracket escaping, the generated header text in `renderAllowlist`
    - eslint.config.mjs — the boundary block (patterns only, never paths, because paths double-reports) and the allow-list block that turns the rule off for legacy entries
    - eslint.elevated-allowlist.mjs — the 24 entries and the header that forbids regeneration to make a check pass
    - src/server/db/elevated/REGISTRY.md — the "One known elevated caller that neither control can see" section that this task converts from a note into a counted control
    - src/lib/audit.ts line 1, src/lib/supabase/service.ts line 1, src/server/db/elevated/index.ts, src/server/context.ts line 20, src/server/authz/requireUser.ts line 13 — the five files outside `src/app` that match today's markers: one real indirect caller, two sanctioned homes, two type-only imports
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md § DI-34 and 04-RESEARCH.md § Q(h) — the carve-out design and why a naive widening adds five rows to a shrink-only list
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/eslint-boundary-fixture.red.txt and .green.txt — the red/green fixture record format
  </read_first>
  <action>
Capture the before state into `evidence/boundary-widening.txt`: `node scripts/check-elevated-ratchet.mjs` (expect `committed=24 live=24 delta=0`), `npm run lint` (0 errors), and `shasum -a 256 eslint.elevated-allowlist.mjs`.

**The census.** In `scripts/check-elevated-ratchet.mjs`, scan `src/` instead of `src/app`. Exempt two path prefixes as the credential's sanctioned homes, `src/lib/supabase/` and `src/server/db/elevated/`, with a header comment saying why this is not a loosening. Keep the test-file skip and its comment exactly. Replace the substring match with an import-statement census: a file counts when it has a value import or value re-export whose specifier is the service module (the `@/lib/supabase/service` alias or any relative path ending in `lib/supabase/service`) or the Supabase SDK package, or a dynamic `import(...)` of the service module. A type-only import or type-only re-export does not count, because it cannot construct a client. Update the PURPOSE and INVOCATION sections of the header (it is now wired into CI by Task 2) and the generated allow-list header text in `renderAllowlist` (it now describes `src/**` with the two exemptions).

**The lint boundary.** In `eslint.config.mjs`, widen the boundary block's `files` from `src/app/**` to all TypeScript under `src/`, with `ignores` for the two sanctioned homes. The widened rule must ignore type-only imports: run `npx eslint --print-config src/server/context.ts` and check whether eslint-config-next registers the typescript-eslint plugin; if it does, move the boundary to that plugin's `no-restricted-imports` with `allowTypeImports: true` and the same two pattern groups, and make the allow-list block turn that rule off; if it does not, record the output and use the core rule with an explicit, commented `ignores` entry for exactly the two type-only importers, `src/server/context.ts` and `src/server/authz/requireUser.ts`. Record which branch was taken and why in the evidence file.

**The red/green fixture.** With the widened controls in place and the allow-list still at 24, create a throwaway `src/lib/probe-elevated.ts` that imports `createServiceClient` from the service module and a throwaway route `src/app/api/probe-elevated/route.ts` that imports only from `@/lib/probe-elevated`. Run `npm run lint` — it must report an error on `src/lib/probe-elevated.ts` and none on the route (the route's reach is indirect; the file that reaches is the one that fails). Run the ratchet — it must FAIL listing both `src/lib/probe-elevated.ts` and `src/lib/audit.ts`. Delete both throwaway files, confirm `git status --porcelain -- src/` shows none of them, and capture everything as the RED section of the evidence file.

**The one-time regeneration.** Run `node scripts/check-elevated-ratchet.mjs --write`. Capture `git diff -- eslint.elevated-allowlist.mjs`: the entry-level change must be exactly one added line for `src/lib/audit.ts`; header prose may change. If any legacy entry disappears or any other entry appears, stop and write the explanation into the evidence file before continuing — an entry vanishing because the census became stricter is a measurement change, not a shrink. Then re-run the ratchet (expect `committed=25 live=25 delta=0`) and `npm run lint` (0 errors, warnings no higher than the before-floor), capturing both as the GREEN section.

**The register.** In `src/server/db/elevated/REGISTRY.md`, replace the "neither control can see" section with a short section recording that since plan 04-03 both controls count `src/lib/audit.ts` as a pre-existing elevated caller, still a note and not a register row because it does not go through `getElevatedClient()`, and that migrating it is Phase 5's (REFAC-13).
  </action>
  <verify>
    <automated>node scripts/check-elevated-ratchet.mjs && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-elevated-ratchet.mjs` exits 0 and prints `committed=25 live=25 delta=0`
    - `npm run lint` exits 0 with 0 errors and a warning count no higher than the one recorded in `evidence/floor.before.txt`
    - `grep -c "src/lib/audit.ts" eslint.elevated-allowlist.mjs` returns 1
    - `git diff -U0 -- eslint.elevated-allowlist.mjs | grep -E '^[+-]  "' ` prints exactly one line, and it begins with `+` and names `src/lib/audit.ts`
    - `evidence/boundary-widening.txt` has a RED section in which lint reports an error on the probe library file and the ratchet exits 1 naming it, and a GREEN section with the regenerated census
    - `git status --porcelain -- src/lib/probe-elevated.ts src/app/api/probe-elevated` prints nothing
    - `grep -c "src/server/db/elevated/" scripts/check-elevated-ratchet.mjs` is at least 1
  </acceptance_criteria>
  <done>The boundary and the census both cover all of src/, agree on what counts, have been watched failing on exactly the indirect-import move DI-34 described, and hold one more entry — the caller that was already there.</done>
</task>

<task type="auto">
  <name>Task 2: Close the dynamic-import and bare-key evasions, and wire the ratchet and the tag gate into CI</name>
  <files>eslint.config.mjs, .github/workflows/ci.yml, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/boundary-widening.txt</files>
  <read_first>
    - eslint.config.mjs — as left by Task 1
    - .planning/phases/03-refactor-foundations-schema-truth-and-the-seam-kit/evidence/deferred-items.md § DI-31 — the two evasions and why the original broad property ban was not shipped (12 legitimate public env reads across 5 files)
    - src/app/api/admin/calculate-popularity/route.ts lines 1-25 and src/app/auth/callback/route.ts lines 1-25 — the two legacy files that read the service-role key inline; both are allow-listed and must stay lint-clean
    - .github/workflows/ci.yml — the `ci` job's step order; the new steps go after "Migration filename parse check" and before "Run tests"; the workflow-level env block must not be touched
    - scripts/check-characterization-tags.mjs — created by 04-01; its `--all` invocation is what CI runs
  </read_first>
  <action>
**The companion rule.** Add a `no-restricted-syntax` rule over the same widened `files` and `ignores` as the boundary block, with two selectors and a message pointing at `src/server/db/elevated/` and its REGISTRY.md: one matching a member access on `process.env` whose property is the identifier or string literal `SUPABASE_SERVICE_ROLE_KEY` (dot or bracket form), and one matching a dynamic import expression whose source string ends in `lib/supabase/service`. Name the key specifically — the broad form that bans all environment access is what DI-31 recorded as unshippable. Add `no-restricted-syntax` to the rules the allow-list block turns off, so the two allow-listed inline readers stay clean, and exempt test files from this rule only (`src/**/*.test.ts` and `src/**/*.test.tsx` set the variable for their own process). Before adding it, run `npx eslint --print-config` on a route file and confirm no existing `no-restricted-syntax` configuration would be overwritten; if one exists, merge the selectors rather than replacing it.

**Red/green.** Create throwaway `src/lib/probe-key.ts` reading the key from `process.env` and throwaway `src/lib/probe-dynamic.ts` dynamically importing the service module; `npm run lint` must report one error in each. Delete both, re-run lint to 0 errors, and append RED and GREEN sections to `evidence/boundary-widening.txt`.

**CI.** In `.github/workflows/ci.yml`'s `ci` job, after the "Migration filename parse check" step, add a step named "Elevated-callsite ratchet" running `node scripts/check-elevated-ratchet.mjs` and a step named "Characterization tag gate" running `node scripts/check-characterization-tags.mjs --all`. Touch nothing else in the file. Run both commands locally and append their output. CI observation needs a push, which this plan does not perform; record "CI run UNOBSERVED — no push in this plan" in the evidence, per the Phase 2 STAB-14 precedent.
  </action>
  <verify>
    <automated>npm run lint && node scripts/check-characterization-tags.mjs --all && grep -c "check-elevated-ratchet.mjs" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with 0 errors
    - `evidence/boundary-widening.txt` records lint errors for both `src/lib/probe-key.ts` and `src/lib/probe-dynamic.ts` before their deletion
    - `git status --porcelain -- src/lib/probe-key.ts src/lib/probe-dynamic.ts` prints nothing
    - `grep -c "SUPABASE_SERVICE_ROLE_KEY" eslint.config.mjs` is at least 1
    - `grep -c "node scripts/check-elevated-ratchet.mjs" .github/workflows/ci.yml` returns 1 and `grep -c "node scripts/check-characterization-tags.mjs --all" .github/workflows/ci.yml` returns 1
    - `git diff -- .github/workflows/ci.yml` shows only added lines (no deleted lines)
  </acceptance_criteria>
  <done>The two evasions DI-31 named now fail lint, the rule names only the service-role key so public configuration reads are untouched, and CI runs both structural gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Narrow the seam's request profile to the columns something reads (DI-35, DEC-24)</name>
  <files>src/server/context.ts, src/server/__tests__/context.test.ts, src/server/__tests__/requireRole.test.ts, .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/di-35-narrowing.txt</files>
  <read_first>
    - src/server/context.ts — the whole file; `RequestProfile`, `PROFILE_COLUMNS`, and the docblock above them that claims the five columns are what the ban check needs
    - src/server/__tests__/context.test.ts — lines 35-110; the fixture profile and the assertion on the selected column list
    - src/server/__tests__/requireRole.test.ts lines 20-40 — the profile fixture carrying the two ban fields
    - src/server/authz/requireRole.ts and src/server/authz/requireClubRole.ts — confirm which profile fields they read, so the narrowing removes only unread columns
    - .planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/evidence/phase-04-decisions.md § DEC-24
    - src/proxy.ts lines 85-145 and src/lib/ban.ts — the two ban checks that exist, so the new docblock describes them correctly
  </read_first>
  <behavior>
    - `createRequestContext()` for an authenticated caller selects exactly `id, roles, onboarding_completed` from users, scoped by the caller's id
    - an anonymous caller still issues no profile read
    - `RequestProfile` carries exactly those three keys; `requireRole` and `requireClubRole` still pass their suites
  </behavior>
  <action>
Record in `evidence/di-35-narrowing.txt` the before census: the command `command grep -rn` for the two ban column names over `src/server/`, and which files read which profile fields (from the authz guard files).

Change `RequestProfile` to pick `id`, `roles` and `onboarding_completed` from the generated users row, and set `PROFILE_COLUMNS` to the string `id, roles, onboarding_completed`. Rewrite the docblock above them: the three columns are what the role guards read and what the onboarding guard will read; the seam performs no ban check; ban enforcement today is the proxy ring (fail-open, F-003/F-062) plus `checkBanStatus()` on the save and rsvp POST handlers; a seam ban guard is REFAC-11 in Phase 5 and will re-add the two columns knowingly (DEC-24). Update `context.test.ts` so its column assertion names the three columns and its fixture drops the two ban fields, and drop the two ban fields from `requireRole.test.ts`'s fixture. Change nothing else in either test file; if any other assertion needs to change, stop and record why — that would mean the narrowing is not the pure removal it claims to be.

Record the after census and the passing runs of `npx jest --ci --selectProjects node --testPathPatterns "src/server"` and `npx tsc --noEmit` in the evidence file.
  </action>
  <verify>
    <automated>npx jest --ci --selectProjects node --testPathPatterns "src/server" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --ci --selectProjects node --testPathPatterns "src/server"` exits 0 with the same number of tests as before this task
    - `npx tsc --noEmit` exits 0
    - `node -e 'const s=require("fs").readFileSync("src/server/context.ts","utf8");process.exit(/PROFILE_COLUMNS\s*=\s*\n?\s*"id, roles, onboarding_completed"/.test(s)?0:1)'` exits 0
    - `grep -c "REFAC-11" src/server/context.ts` is at least 1
    - `git diff --stat -- src/server/authz/` prints nothing
    - `evidence/di-35-narrowing.txt` holds the before census, the after census and both passing runs
  </acceptance_criteria>
  <done>The seam reads three columns instead of five, its docblock describes the ban checks that actually exist instead of one it does not perform, and Phase 5 inherits a written reason to re-add the columns alongside the guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → service-role credential | The RLS-bypassing key reaches request paths only through files the controls can count |
| lint/ratchet controls → CI | A control that is correct but not enforced protects nothing on a pull request |
| request → per-request profile read | The seam reads the caller's own users row once per request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | A new `src/lib/*.ts` constructing a service-role client, imported by a route (the DI-34 one-move bypass) | mitigate | Boundary and census widened to src/; the red fixture proves lint fails on the library file and the ratchet names it |
| T-04-03-02 | Elevation of Privilege | Dynamic import of the service module, or a bare read of the service-role key (DI-31 evasions) | mitigate | `no-restricted-syntax` selectors for both; red fixtures prove each fails lint |
| T-04-03-03 | Tampering | Regenerating the allow-list to make a check pass, quietly resetting the ratchet | mitigate | One regeneration inside a dedicated plan; acceptance asserts the entry-level diff is exactly `+src/lib/audit.ts`; any other change stops the task |
| T-04-03-04 | Repudiation | Controls pass locally but are never run on a pull request | mitigate | Two CI steps; the CI run itself is recorded UNOBSERVED until a push, not claimed |
| T-04-03-05 | Elevation of Privilege | A handler adopting `requireUser` assumes ban enforcement the seam does not perform (DI-35) | mitigate | Docblock rewritten to name the actual ban checks and their owner (REFAC-11, Phase 5); unread columns removed so the type no longer implies otherwise |
| T-04-03-06 | Denial of Service | The widened lint rule breaking the build on legitimate public-config reads | mitigate | The selector names `SUPABASE_SERVICE_ROLE_KEY` only; lint must stay at 0 errors |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No package installed; if the typescript-eslint rule is used it is the plugin eslint-config-next already registers |
</threat_model>

<verification>
- `npm run lint` exits 0, 0 errors
- `node scripts/check-elevated-ratchet.mjs` → `committed=25 live=25 delta=0`
- `node scripts/check-characterization-tags.mjs --all` exits 0
- `npx jest --ci` exits 0 at the before-floor counts (this plan adds no tests)
- `npx tsc --noEmit` exits 0
- `git diff --name-only <base> -- src/app src/lib` prints nothing — no route or library file is changed and the throwaway probes are gone
</verification>

<success_criteria>
The elevated boundary is a boundary rather than a fence with a gap: both controls cover src/, agree on what counts, catch the indirect, dynamic and bare-key reaches that were documented as open, and run in CI. The only list growth is the caller that already existed. The seam's profile read and its documentation now say the same thing.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Contributed by this plan:

**Modified controls:** `scripts/check-elevated-ratchet.mjs` (src/ census, sanctioned-home exemptions, type-only skip, dynamic-import detection), `eslint.config.mjs` (widened boundary, `no-restricted-syntax` companion rule), `eslint.elevated-allowlist.mjs` (24 → 25 entries: `+src/lib/audit.ts`).
**New CI steps:** "Elevated-callsite ratchet", "Characterization tag gate" in the `ci` job.
**Modified seam:** `src/server/context.ts` — `RequestProfile` and `PROFILE_COLUMNS` narrowed to three columns.
**Modified docs:** `src/server/db/elevated/REGISTRY.md`.
**New evidence:** `evidence/boundary-widening.txt`, `evidence/di-35-narrowing.txt`.
**Deferred items closed:** DI-34, DI-31 (CI wiring and companion rule; the shrink is recorded as unavailable in Phase 4), DI-35.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-slices-1-2-saved-events-rsvp-and-the-event-read-path/04-03-SUMMARY.md` when done.
</output>
