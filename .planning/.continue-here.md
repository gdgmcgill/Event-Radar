---
phase: milestone-init
task: research-complete
status: in_progress
last_updated: 2026-02-26T01:18:39.207Z
---

<current_state>
Milestone v1.1 "Club Organizer UX Overhaul" initialization is in progress.
Codebase mapping (refreshed) and domain research (4 parallel researchers + synthesis) are COMPLETE.
Next step: Define requirements from research, then create roadmap with phase breakdown.

The GSD new-milestone workflow is at Step 9 (Define Requirements) — research just finished.
</current_state>

<completed_work>

- Codebase mapping: Refreshed all 7 documents in `.planning/codebase/` (committed 10c8661)
- PROJECT.md: Updated with v1.1 milestone goals, active requirements, context, constraints, key decisions (committed 9525ccf)
- STATE.md: Reset for new milestone (committed 9525ccf)
- Research — Stack (`.planning/research/STACK.md`): Only 4 npm packages needed (react-hook-form, zod, @hookform/resolvers, resend); 6 missing shadcn/ui components; no new packages for event editing
- Research — Features (`.planning/research/FEATURES.md`): Owner/organizer two-role model; invitation = trust event (no admin); 4 features need no backend work; 3 CTA states on public club page are table stakes
- Research — Architecture (`.planning/research/ARCHITECTURE.md`): Dead link `/my-clubs/[id]` is central deliverable; existing APIs cover events tab; auto-grant is a one-line role change; invite can ship without email (copy-link)
- Research — Pitfalls (`.planning/research/PITFALLS.md`): `is_club_owner()` SECURITY DEFINER function is critical prerequisite; clubs UPDATE locked by security audit; PostgREST role escalation risk on invites; RLS infinite recursion without helper function
- Research — Synthesis (`.planning/research/SUMMARY.md`): 4 suggested phases; database-first approach; committed 30f04f4
</completed_work>

<remaining_work>

- Step 9: Define requirements — Read FEATURES.md, present by category, scope with user, generate REQUIREMENTS.md with REQ-IDs
- Step 10: Create roadmap — Spawn gsd-roadmapper to create ROADMAP.md with phases continuing from phase 5 (phases 1-4 were v1.0)
- Step 11: Present roadmap for approval, commit all artifacts
</remaining_work>

<decisions_made>

- Auto-grant organizer (owner) on club approval — confirmed by user
- Owner/organizer role distinction (not just generic "organizer") — two roles only, no deeper hierarchy
- Direct invitations from owners — no admin approval needed for co-organizer invites
- No event deletion for organizers — edit only, admin deletes
- No club deletion for organizers — admin-only destructive action
- Copy-link invites for v1.1, Resend email for v1.2
- Database-first approach: `is_club_owner()` + role CHECK constraint must land before any UI
- Research enabled: workflow.research = true in config.json
</decisions_made>

<blockers>
None. Research is complete and clean. Ready for requirements definition.
</blockers>

<context>
The user identified 7 distinct UX failures in the current club organizer flow during deep codebase exploration at the start of this session. The core problems: club creator != organizer, dead-end dashboard, no club management, no event creation from club context, no event management UI, no member management, disconnected public page.

Research confirmed the approach is mostly UI work on existing infrastructure — the APIs for event listing, event editing, and event creation with club_id already exist. The critical new work is the database migration (role constraint + RLS helper function), the dashboard page shell, the members/invite system, and the club settings form.

Suggested 4-phase roadmap from synthesis:
1. Database Foundation (migration, is_club_owner(), role constraint, invites table)
2. Dashboard Shell + Read-Only Tabs (Overview, Events using existing APIs)
3. Members Tab + Invite Flow (member list, remove, copy-link invite)
4. Settings Tab + Surface Fixes (club editing, context-aware public page, create-event selector)

Phase numbering should continue from 5 (v1.0 used phases 1-4).
</context>

<next_action>
Run `/gsd:resume-work` in a fresh context window. It will pick up from here.

The immediate next step is GSD new-milestone Step 9: Define Requirements.
1. Read `.planning/research/FEATURES.md` for feature categories
2. Present features by category with table stakes / differentiators
3. Scope each category with user via AskUserQuestion
4. Generate REQUIREMENTS.md with REQ-IDs
5. Then spawn gsd-roadmapper for ROADMAP.md (phases starting at 5)
</next_action>
